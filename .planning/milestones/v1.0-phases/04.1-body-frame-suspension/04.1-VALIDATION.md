---
phase: 4.1
slug: body-frame-suspension
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-06-01
updated: 2026-06-02
---

# Phase 4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js assertion scripts (`.mjs`) — no test runner; scripts exit 0/1 |
| **Config file** | none |
| **Quick run command** | `node test/assert-m4-02-asymmetric-bump.mjs` |
| **Full suite command** | `node test/assert-m4-02-asymmetric-bump.mjs && node test/assert-m4-04-static-vs-braking.mjs && node test/assert-m4-05-wheel-lift-ramp.mjs && node test/assert-m4-06-bump-response.mjs && node test/assert-m4-07-ramp-slide.mjs` |
| **Estimated runtime** | ~5 seconds (log-replay, no browser) |

---

## Sampling Rate

- **After every task commit:** Run quick command (at minimum m4-02)
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | Status |
|---------|------|------|-------------|-----------|-------------------|--------|
| audit-existing-scripts | 01 | 1 | D-18 | parse-check + grep | `grep -c "hubY\|hubVy" test/assert-m4-*.mjs && node --check` on each script | ⬜ pending |
| m4-07-scenario | 01 | 1 | D-17 | schema-check | `node -e` JSON schema gate | ⬜ pending |
| m4-07-assert-script | 01 | 1 | D-17 | parse-check | `node test/assert-m4-07-ramp-slide.mjs` runs without ReferenceError | ⬜ pending |
| state-rename | 02 | 2 | D-01/D-08/D-11 | grep gate | `grep -E ... data/ranger.js` returns 6 new params | ⬜ pending |
| strut-axis-ode | 02 | 2 | D-04/D-05/D-08/D-09 | grep gate | `grep -cE "from 'three'" src/suspension.js` returns 0 | ⬜ pending |
| normal-xz-routing | 02 | 2 | D-06/D-06a + flat-ground regression | grep + assertion | `grep -c "_hubNormalXZ" src/physics.js && node test/assert-m4-02-asymmetric-bump.mjs <log>` | ⬜ pending |
| full-regression | 02 | 2 | D-17 | human-gated full suite | all 5 m4-* assertions exit 0 | ⬜ pending |
| wheel-visual + wheelDebug.strutComp | 03 | 3 | D-07/D-12 | grep gate | `grep -c "applyQuaternion\|hubLocal\|body_down_mesh" src/main.js` | ⬜ pending |
| debug-sliders + travel-bars | 03 | 3 | D-13/D-14 | grep gate | slider grep + `grep -c "updateTravelBars\|travelCanvas" src/debug.js` | ⬜ pending |
| logger-fields + glossary | 03 | 3 | D-12 housekeeping | grep gate | `grep -cE "'fl_sc'..." src/logger.js >= 4` | ⬜ pending |
| phase-close | 03 | 3 | D-17 | human-gated full suite | all 5 m4-* assertions exit 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements (Plan 01)

- [x] `test/assert-m4-07-ramp-slide.mjs` — NEW gate test for D-06 X/Z routing fix; Plan 01 Task 3 creates it before Plan 02 executes
- [x] `scenarios/m4-07-ramp-slide.json` — NEW scenario placing car at rest on 10° ramp; Plan 01 Task 2 creates it
- [x] D-18 audit of existing m4-02/04/05/06 scripts — Plan 01 Task 1 confirms no field-rename collisions and parse-cleanliness of all four scripts

*Plan 01 (wave 1) is the Wave 0 deliverable per the GSD convention: it produces failing gate tests + scenarios that Plan 02 (wave 2) turns green. Existing m4-02/04/05/06 scripts require no migration (scenarios are flat-ground; dot(normal, body_up)=1 preserved per D-15).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Wheel visual stays on ramp face | D-07 | Requires browser rendering | Drive up ramp slowly; confirm front wheels don't visually lift from surface due to pitch projection |
| Travel bars in debug panel | D-13 | UI element | Open debug panel; confirm 4 corner bars move as suspension compresses/extends |
| Bump stop stiffens on hard landing | D-09 | Requires observational tuning | Drop car from height; confirm sharp deceleration at bottom of travel without instability |
| Body offset slider shifts ride height | D-08/D-14 | UI element + visual | Adjust Front Body Offset to +0.05 m; confirm car visibly rises and wheel meshes track mount shift (no desync) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (Plan 01 audit/schema/parse; Plan 02 grep + m4-02 regression; Plan 03 grep gates; full-suite at both human checkpoints)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (each task in Plans 01/02/03 has an `<automated>` block; human checkpoints follow automated tasks, not other checkpoints)
- [x] Wave 0 covers all MISSING references (Plan 01 creates `test/assert-m4-07-ramp-slide.mjs` + `scenarios/m4-07-ramp-slide.json`; D-18 audit covered)
- [x] No watch-mode flags (all commands are one-shot Node script invocations or grep)
- [x] Feedback latency < 5s (log-replay assertions average ~1s each; grep is sub-second)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-06-02 (post-checker-revision iteration 1)
