---
phase: 03-tire-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/ranger.js
  - src/tire.js
autonomous: true
requirements: [M3-03, M3-04, M3-10]
tags: [tire-model, pacejka]

must_haves:
  truths:
    - "data/ranger.js exports Pacejka lateral coefficients (pacejkaB, pacejkaC, pacejkaD, pacejkaE), longitudinal coefficients (pacejkaBx, pacejkaCx, pacejkaDx, pacejkaEx), wheelInertia, and maxHandbrakeTorque"
    - "computeLateralForce body is the Pacejka Magic Formula with no internal friction cap and no sign negation"
    - "computeLongitudinalForce body is the Pacejka Magic Formula with no internal friction cap"
    - "Both Pacejka functions hard-clamp C to [1.0, 1.99] internally"
  artifacts:
    - path: "data/ranger.js"
      provides: "Pacejka coefficients + wheelInertia + maxHandbrakeTorque on RANGER_PARAMS"
      contains: "pacejkaB"
    - path: "src/tire.js"
      provides: "Pacejka lateral and longitudinal force functions"
      contains: "Math.atan(B"
  key_links:
    - from: "src/tire.js"
      to: "data/ranger.js"
      via: "params.pacejkaB/C/D/E and params.pacejkaBx/Cx/Dx/Ex read inside function bodies"
      pattern: "params\\.pacejka"
---

<objective>
Replace the Phase 1 linear placeholder bodies in `src/tire.js` with the Pacejka Magic Formula for both lateral and longitudinal forces, and add the Pacejka coefficients, wheel inertia, and handbrake torque constants to `data/ranger.js` (RANGER_PARAMS).

Purpose: Lay the pure-math foundation that all downstream plans depend on. Tire functions become physics-honest while their locked signatures stay untouched.

Output:
- `data/ranger.js` augmented with `pacejkaB/C/D/E`, `pacejkaBx/Cx/Dx/Ex`, `wheelInertia`, `maxHandbrakeTorque`.
- `src/tire.js` `computeLateralForce` and `computeLongitudinalForce` bodies replaced with Pacejka. No internal friction caps. No sign negation. C clamped internally to `[1.0, 1.99]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tire-model/03-CONTEXT.md
@.planning/phases/03-tire-model/03-RESEARCH.md
@.planning/phases/03-tire-model/03-PATTERNS.md
@src/tire.js
@data/ranger.js
@docs/GLOSSARY.md

<interfaces>
Locked signatures in `src/tire.js` (do not change):

  export function computeLateralForce(slipAngle, Fz, params)
  export function computeLongitudinalForce(slipRatio, Fz, params)

Both return a Number (Newtons). Callers in `src/physics.js` pass `Fn` for `Fz` and the live `params` (RANGER_PARAMS object) — Phase 3 does not change those call sites.

`data/ranger.js` exports `RANGER_PARAMS` as a plain (non-frozen) object. New fields are added inline; existing fields are not reordered.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Pacejka coefficients, wheelInertia, and maxHandbrakeTorque to data/ranger.js</name>
  <files>data/ranger.js</files>
  <read_first>
    - data/ranger.js (current RANGER_PARAMS structure and section comment style)
    - .planning/phases/03-tire-model/03-PATTERNS.md (data/ranger.js section, lines describing additions)
    - .planning/phases/03-tire-model/03-RESEARCH.md (Pacejka Starting Coefficients section for exact starting values)
  </read_first>
  <action>
    Add the following fields to the `RANGER_PARAMS` object exported from `data/ranger.js`. Do NOT reorder or remove any existing field. Do NOT call `Object.freeze`.

    Per D-10, append `maxHandbrakeTorque: 2000` to the existing drivetrain section (immediately after `maxReverseTorque`).

    Per D-07 and D-02, add a new section after the existing friction placeholders block, using the file's existing section comment style `// ── Section Name ──────────────────────────────────────────────────────────`. The section is titled `Phase 3 Pacejka Tire Model (D-07)` and contains exactly these fields with these starting values:

    - `pacejkaB:  10.0`  (lateral stiffness factor)
    - `pacejkaC:   1.9`  (lateral shape factor; hard-clamped in tire.js per M3-03)
    - `pacejkaD:   1.0`  (lateral peak factor)
    - `pacejkaE:  0.97`  (lateral curvature)
    - `pacejkaBx: 10.0`  (longitudinal stiffness)
    - `pacejkaCx:  1.9`  (longitudinal shape)
    - `pacejkaDx:  1.0`  (longitudinal peak)
    - `pacejkaEx: 0.97`  (longitudinal curvature)
    - `wheelInertia: 1.22`  (kg·m²; 0.5 × 18 kg × 0.368²)

    Field names MUST match exactly — they are read by name from `params` in `src/tire.js` (Plan 01 Task 2), `src/physics.js`, and `src/debug.js` (Plan 03). Do NOT remove `corneringStiffness` or `lateralDampingCoeff` from `data/ranger.js` — they are removed from the debug slider list in Plan 03 but kept in params until later cleanup per RESEARCH.md "State of the Art" notes.
  </action>
  <verify>
    <automated>grep -E "pacejkaB|pacejkaC|pacejkaD|pacejkaE|pacejkaBx|pacejkaCx|pacejkaDx|pacejkaEx|wheelInertia|maxHandbrakeTorque" data/ranger.js | grep -v "^[[:space:]]*//" | wc -l | awk '{ if ($1 < 10) { print "FAIL: expected >=10 non-comment lines, got " $1; exit 1 } else { print "OK" } }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pacejkaB:" data/ranger.js` returns 1 (lateral B field present, distinct from `pacejkaBx`)
    - `grep -c "pacejkaBx:" data/ranger.js` returns 1
    - `grep -c "pacejkaC:" data/ranger.js` returns 1
    - `grep -c "pacejkaCx:" data/ranger.js` returns 1
    - `grep -c "pacejkaD:" data/ranger.js` returns 1
    - `grep -c "pacejkaDx:" data/ranger.js` returns 1
    - `grep -c "pacejkaE:" data/ranger.js` returns 1
    - `grep -c "pacejkaEx:" data/ranger.js` returns 1
    - `grep -c "wheelInertia:" data/ranger.js` returns 1
    - `grep -c "maxHandbrakeTorque:" data/ranger.js` returns 1
    - Starting values match exactly: `grep "pacejkaB:" data/ranger.js` includes `10.0`; `pacejkaC:` includes `1.9`; `pacejkaD:` includes `1.0`; `pacejkaE:` includes `0.97`; `wheelInertia:` includes `1.22`; `maxHandbrakeTorque:` includes `2000`
    - `node --check data/ranger.js` exits 0 (file parses as valid JS; `data/ranger.js` is an ES module — if `node --check` rejects ESM syntax, fall back to a Node import smoke: `node -e "import('./data/ranger.js').then(m => console.log(m.RANGER_PARAMS.pacejkaB))"` prints `10`)
    - Existing fields `corneringStiffness` and `lateralDampingCoeff` are still present in `data/ranger.js` (`grep -c corneringStiffness data/ranger.js` returns >= 1)
  </acceptance_criteria>
  <done>RANGER_PARAMS exports all 10 new fields with the exact names and starting values listed above; file still parses as valid ES module; no existing field removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace tire.js bodies with Pacejka Magic Formula (no caps, no negation, C clamped)</name>
  <files>src/tire.js</files>
  <read_first>
    - src/tire.js (current Phase 1 bodies and locked signatures)
    - data/ranger.js (verifies the Pacejka field names this task reads from `params`)
    - .planning/phases/03-tire-model/03-RESEARCH.md (Pattern 1; Pitfall 1; Pitfall 4; Pitfall 5)
    - .planning/phases/03-tire-model/03-PATTERNS.md (src/tire.js section — exact replacement bodies)
  </read_first>
  <action>
    In `src/tire.js`, leave the two exported function signatures EXACTLY as they are:

      export function computeLateralForce(slipAngle, Fz, params) { ... }
      export function computeLongitudinalForce(slipRatio, Fz, params) { ... }

    Replace ONLY the bodies. Do not add imports. Do not import Three.js (the file header explicitly forbids it). Do not add helper functions outside these two exports unless they are local consts inside each function body.

    For `computeLateralForce(slipAngle, Fz, params)`, the new body must:
    1. Read `params.pacejkaB` into local `const B`.
    2. Read `params.pacejkaC` and immediately clamp it via `Math.max(1.0, Math.min(1.99, params.pacejkaC))` into local `const C` (per M3-03 and Pitfall 5).
    3. Read `params.pacejkaD` into local `const D`.
    4. Read `params.pacejkaE` into local `const E`.
    5. Assign `const x = slipAngle`.
    6. Return exactly `Fz * D * Math.sin(C * Math.atan(B * x - E * (B * x - Math.atan(B * x))))`.

    For `computeLongitudinalForce(slipRatio, Fz, params)`, the new body mirrors the same structure but reads `params.pacejkaBx`, `params.pacejkaCx` (clamped to [1.0, 1.99] identically), `params.pacejkaDx`, `params.pacejkaEx`, with `const x = slipRatio`. The return expression is the same Pacejka formula with these longitudinal coefficients.

    REMOVE the following Phase 1 constructs that currently exist in the file:
    - The lateral function's early-return speed gate (`if (Math.sqrt(latVel*latVel + longVel*longVel) < 0.2) return 0`)
    - The `-params.corneringStiffness * slipAngle` linear formula
    - The lateral `Math.max(-maxFlat, Math.min(maxFlat, raw))` clamp
    - The longitudinal `rollingDrag + (params._driveForce || 0)` expression
    - The longitudinal `Math.max(-maxFlong, Math.min(maxFlong, raw))` clamp

    The new bodies must NOT clamp the output to `params.frictionCoeff * Fz` — the friction-circle cap lives in `src/physics.js` (Plan 02). Double-clamping is forbidden per Pitfall 4. The new lateral body must NOT negate its return value — Pacejka inherits sign from `slipAngle` per Pitfall 1.

    Update the file's existing header comment to reference Phase 3 and the Pacejka Magic Formula. The directive `// Do NOT import Three.js — this module is pure math.` must remain in the header.
  </action>
  <verify>
    <automated>node -e "import('./src/tire.js').then(m => { const p = { pacejkaB: 10, pacejkaC: 1.9, pacejkaD: 1.0, pacejkaE: 0.97, pacejkaBx: 10, pacejkaCx: 1.9, pacejkaDx: 1.0, pacejkaEx: 0.97 }; const Fy0 = m.computeLateralForce(0, 1000, p); const Fy_pos = m.computeLateralForce(0.1, 1000, p); const Fx_zero = m.computeLongitudinalForce(0, 1000, p); const Fx_pos = m.computeLongitudinalForce(0.2, 1000, p); if (Math.abs(Fy0) > 1e-6) { console.error('FAIL: Fy(0) != 0, got ' + Fy0); process.exit(1) } if (Fy_pos <= 0) { console.error('FAIL: Fy(+slipAngle) must be positive (no negation), got ' + Fy_pos); process.exit(1) } if (Math.abs(Fx_zero) > 1e-6) { console.error('FAIL: Fx(0) != 0, got ' + Fx_zero); process.exit(1) } if (Fx_pos <= 0) { console.error('FAIL: Fx(+slipRatio) must be positive, got ' + Fx_pos); process.exit(1) } console.log('OK Fy(+)=' + Fy_pos.toFixed(1) + ' Fx(+)=' + Fx_pos.toFixed(1)) }).catch(e => { console.error(e); process.exit(1) })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Math.atan" src/tire.js` returns 4 (two nested atan calls per function, two functions)
    - `grep -c "Math.sin" src/tire.js` returns 2 (one per function)
    - `grep -c "Math.max(1.0, Math.min(1.99" src/tire.js` returns 2 (C clamp present in both functions)
    - `grep -c "corneringStiffness" src/tire.js` returns 0 (Phase 1 lateral formula gone)
    - `grep -c "rollingResistanceCoeff" src/tire.js` returns 0 (Phase 1 longitudinal expression gone)
    - `grep -c "_driveForce" src/tire.js` returns 0 (Phase 1 longitudinal expression gone)
    - `grep -c "frictionCoeff" src/tire.js` returns 0 (no internal friction cap remains)
    - `grep -E "return\s+-" src/tire.js` produces no match (no negated return — Pitfall 1)
    - Locked signatures unchanged: `grep -c "export function computeLateralForce(slipAngle, Fz, params)" src/tire.js` returns 1; `grep -c "export function computeLongitudinalForce(slipRatio, Fz, params)" src/tire.js` returns 1
    - Header comment still contains the string `Do NOT import Three.js`
    - Behavior assertion (covered by `<verify>` command): with the canonical params dict, `computeLateralForce(0, 1000, p) === 0`; `computeLateralForce(0.1, 1000, p) > 0` (positive slip angle → positive lateral force); `computeLongitudinalForce(0, 1000, p) === 0`; `computeLongitudinalForce(0.2, 1000, p) > 0`
    - Spot-check at C clamp boundary: `computeLateralForce(0.1, 1000, { ...p, pacejkaC: 2.5 })` returns the same value as with `pacejkaC: 1.99` (clamp applied)
  </acceptance_criteria>
  <done>Both function bodies are pure Pacejka; signatures untouched; no internal caps; no negation; C clamped inside each function; behavioral assertions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 3 adds no network, no storage, no input ingestion — all changes are in-process math reading from `RANGER_PARAMS` (already trusted) and writing to in-memory vehicle state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `params.pacejkaC` slider value reaching 2.0 causes formula collapse (`sin(π)` ≈ 0) | mitigate | Hard-clamp `C = Math.max(1.0, Math.min(1.99, params.pacejkaC))` inside both tire functions; slider range is `[1.0, 1.99]` in Plan 03 — defense in depth |
| T-03-02 | Denial of Service | Numerical NaN propagation from divide-by-zero or out-of-domain math at rest | mitigate | Pacejka formula domain is all of ℝ (atan and sin are total functions); zero-input returns zero — verified in Task 2 acceptance criteria |
</threat_model>

<verification>
- `node --check data/ranger.js` (or import smoke) succeeds
- `node -e "import('./src/tire.js')..."` behavior assertions in Task 2 `<verify>` pass
- All `grep` acceptance criteria above match
- Open `index.html` via local HTTP server; car still renders and W/A/S/D inputs still register (steering will feel wrong until Plan 02 is also executed — this is expected; the friction circle and slip ratio plumbing are still Phase 1 here)
</verification>

<success_criteria>
- `data/ranger.js` exports all 10 new fields with exact names and starting values
- `src/tire.js` bodies are Pacejka with C clamped, no internal caps, no negation
- Locked signatures preserved
- No existing field removed from `data/ranger.js`
</success_criteria>

<output>
After completion, create `.planning/phases/03-tire-model/03-01-SUMMARY.md` following the standard summary template. Note: the lateral function now produces sign-correct output without negation — downstream `wheelRight * Flat` accumulation in `physics.js` will produce the correct steering direction once Plan 02 wires the new params through.
</output>
