---
phase: 02-scenario-system-debug-menu
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/debug.js
autonomous: true
requirements:
  - M2-05
  - M2-06

must_haves:
  truths:
    - "User presses backtick, opens debug menu, and sees sliders for mass, corneringStiffness (labeled Phase 2 placeholder), frictionCoeff, maxDriveTorque, maxBrakeTorque, bodyContactStiffness, bodyContactDamping"
    - "User moves the frictionCoeff slider from 0.9 to 0.4 while driving — the car slides visibly more on the same input"
    - "User moves the mass slider from 1360 to 2000 while driving — the car accelerates more slowly"
    - "Debug menu shows a disabled read-only label '\\  to record' in the Logger section"
    - "lateralDampingCoeff slider remains present, labeled with '(unused)'"
    - "corneringStiffness slider is labeled to indicate it is a Phase 2 placeholder replaced by Pacejka in Phase 3"
  artifacts:
    - path: "src/debug.js"
      provides: "Expanded lil-gui panel with all D-08 sliders and Logger hint"
      contains: "corneringStiffness"
  key_links:
    - from: "src/debug.js gui.add(params, 'frictionCoeff')"
      to: "RANGER_PARAMS.frictionCoeff"
      via: "params reference passed in from main.js — mutation is live"
      pattern: "gui.add\\(params, 'frictionCoeff'"
    - from: "src/debug.js gui.add(params, 'mass')"
      to: "RANGER_PARAMS.mass"
      via: "params reference — mass slider affects physics.js next step"
      pattern: "gui.add\\(params, 'mass'"
---

<objective>
Expand the existing lil-gui debug panel in src/debug.js with all D-08 tunable physics sliders and a disabled read-only Logger hint label. Also remove the rollingResistanceCoeff slider per D-09 (it is a fixed field — no slider). All slider mutations take effect immediately because RANGER_PARAMS is passed by reference (M2-06 already established for existing sliders).

Purpose: Gives the user live physics tuning — drag a slider while driving, feel the change immediately.

Output: src/debug.js (modified — removes rollingResistanceCoeff slider, adds 7 new sliders, relabels lateralDampingCoeff, adds Logger hint)
</objective>

<execution_context>
@/Users/ledogen/CodeShit/CarGame/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ledogen/CodeShit/CarGame/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ledogen/CodeShit/CarGame/.planning/PROJECT.md
@/Users/ledogen/CodeShit/CarGame/.planning/ROADMAP.md
@/Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-CONTEXT.md
@/Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-PATTERNS.md
</context>

<interfaces>
<!-- Key signatures the executor needs. No codebase exploration required. -->

Current src/debug.js (full file — 39 lines):
  Exports: initDebug(params) — creates GUI, adds 4 sliders, registers backtick listener, returns gui.
  Existing sliders:
    gui.add(params, 'lateralDampingCoeff', 500, 10000, 100).name('Lateral Damping (N/m·s)')
    gui.add(params, 'rollingResistanceCoeff', 10, 1000, 10).name('Rolling Resistance (N/m·s)')  ← REMOVE per D-09
    gui.add(params, 'tireStiffness', 50000, 400000, 5000).name('Tire Stiffness (N/m)')
    gui.add(params, 'tireDamping', 500, 20000, 500).name('Tire Damping (N·s/m)')

  The rollingResistanceCoeff slider is currently at line 26 of src/debug.js and MUST be deleted.
  D-09 classifies rollingResistanceCoeff as a fixed field — no slider. Removing it brings the kept
  count from 4 to 3: lateralDampingCoeff, tireStiffness, tireDamping.

RANGER_PARAMS fields for D-08 sliders (all confirmed present in data/ranger.js):
  mass: 1360                    — range 500–3000, step 10
  corneringStiffness: 50000     — range 5000–200000, step 1000; label indicates Phase 2 placeholder
  frictionCoeff: 0.9            — range 0.1–1.5, step 0.05
  maxDriveTorque: 800           — range 100–2000, step 50
  maxBrakeTorque: 3000          — range 500–8000, step 100
  bodyContactStiffness: 200000  — range 50000–500000, step 10000
  bodyContactDamping: 8000      — range 1000–30000, step 500

Per D-09/D-10 — do NOT add sliders for: rollingResistanceCoeff (fixed — must be removed if present),
  steerRate, steerDecayRate, maxSteerAngle, speedSteerRef, geometry fields, weightFront, weightRear,
  inertiaRoll, inertiaPitch, inertiaYaw.

Per D-11: lateralDampingCoeff slider stays; change its .name() label to 'Lateral Damping (unused)'
Per D-12: corneringStiffness label: 'Cornering Stiffness (Phase 2 placeholder — Phase 3: Pacejka)'

lil-gui read-only label pattern (from 02-PATTERNS.md):
  const hint = { hint: '\\ to record' }
  gui.add(hint, 'hint').name('Logger').disable()
  The disable() call makes the field non-interactive (read-only display).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add D-08 sliders and Logger hint to src/debug.js; remove rollingResistanceCoeff</name>
  <files>src/debug.js</files>
  <read_first>
    /Users/ledogen/CodeShit/CarGame/src/debug.js
    /Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-CONTEXT.md
    /Users/ledogen/CodeShit/CarGame/data/ranger.js
  </read_first>
  <action>
    Modify src/debug.js. The file is 39 lines. Read it fully before editing.

    Changes to make inside initDebug(params):

    0. REMOVE the rollingResistanceCoeff slider (D-09 — fixed field, no slider):
       Delete the line at approximately line 26:
         gui.add(params, 'rollingResistanceCoeff', 10, 1000, 10).name('Rolling Resistance (N/m·s)')
       This line must not appear anywhere in the final file.

    1. Update the lateralDampingCoeff slider label from 'Lateral Damping (N/m·s)' to
       'Lateral Damping (unused)' per D-11.

    2. Add a folder or separator comment before the new sliders for readability — use a comment
       line, not a lil-gui folder (folders add nesting; keep the panel flat per existing style).

    3. Add the following sliders in this order (copy gui.add(params, ...) pattern exactly),
       after the existing kept sliders (lateralDampingCoeff, tireStiffness, tireDamping):
       a. gui.add(params, 'mass', 500, 3000, 10).name('Mass (kg)')
       b. gui.add(params, 'frictionCoeff', 0.1, 1.5, 0.05).name('Friction Coeff')
       c. gui.add(params, 'maxDriveTorque', 100, 2000, 50).name('Max Drive Torque (N·m)')
       d. gui.add(params, 'maxBrakeTorque', 500, 8000, 100).name('Max Brake Torque (N·m)')
       e. gui.add(params, 'bodyContactStiffness', 50000, 500000, 10000).name('Body Contact Stiffness (N/m)')
       f. gui.add(params, 'bodyContactDamping', 1000, 30000, 500).name('Body Contact Damping (N·s/m)')
       g. gui.add(params, 'corneringStiffness', 5000, 200000, 1000).name('Cornering Stiffness (Phase 2 placeholder — Phase 3: Pacejka)')

    4. After the new sliders, add the Logger hint label:
       const _loggerHint = { hint: '\\ to record' }
       gui.add(_loggerHint, 'hint').name('Logger').disable()
       (Use _loggerHint as variable name to avoid polluting any outer scope, though it is inside
       initDebug's function scope.)

    5. Update the JSDoc comment at the top of the file to note Phase 2 additions and the removal
       of rollingResistanceCoeff per D-09.

    Do NOT add a \ keydown listener here — that is handled in main.js (Plan 01).
    Do NOT change the backtick listener or the return gui statement.
    Do NOT add a lil-gui folder/group — keep all controls at the root level.
  </action>
  <verify>
    <automated>grep -c "gui.add(params, 'mass'" /Users/ledogen/CodeShit/CarGame/src/debug.js && grep -c "corneringStiffness" /Users/ledogen/CodeShit/CarGame/src/debug.js && grep -c "disable()" /Users/ledogen/CodeShit/CarGame/src/debug.js && grep -c "unused" /Users/ledogen/CodeShit/CarGame/src/debug.js && grep -c "rollingResistanceCoeff" /Users/ledogen/CodeShit/CarGame/src/debug.js</automated>
  </verify>
  <acceptance_criteria>
    - rollingResistanceCoeff slider is ABSENT from src/debug.js (grep -c "rollingResistanceCoeff" returns 0)
    - src/debug.js contains gui.add(params, 'mass', 500, 3000, 10) with .name('Mass (kg)')
    - src/debug.js contains gui.add(params, 'frictionCoeff', 0.1, 1.5, 0.05)
    - src/debug.js contains gui.add(params, 'maxDriveTorque', 100, 2000, 50)
    - src/debug.js contains gui.add(params, 'maxBrakeTorque', 500, 8000, 100)
    - src/debug.js contains gui.add(params, 'bodyContactStiffness', 50000, 500000, 10000)
    - src/debug.js contains gui.add(params, 'bodyContactDamping', 1000, 30000, 500)
    - src/debug.js contains gui.add(params, 'corneringStiffness', 5000, 200000, 1000) with label containing 'Phase 2 placeholder'
    - lateralDampingCoeff slider label contains '(unused)'
    - .disable() is called on the Logger hint controller
    - Hint text is '\\ to record' (backslash escape)
    - tireStiffness and tireDamping sliders are still present (not removed)
    - initDebug still returns gui
    - backtick toggle listener unchanged
    - Total slider count is 10: 3 kept (lateralDampingCoeff, tireStiffness, tireDamping) + 7 new D-08 sliders
  </acceptance_criteria>
  <done>src/debug.js has 10 total sliders (3 kept from Phase 1 + 7 new from D-08; rollingResistanceCoeff removed per D-09), lateralDampingCoeff relabeled '(unused)', corneringStiffness labeled 'Phase 2 placeholder — Phase 3: Pacejka', and a disabled Logger hint. All slider mutations take effect immediately (RANGER_PARAMS by reference, existing behavior).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → lil-gui sliders | Slider values clamped by lil-gui min/max; written directly to RANGER_PARAMS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | lil-gui slider → RANGER_PARAMS live mutation | accept | Ranges are physically reasonable (e.g. mass 500–3000 kg); no security boundary crossed; this is the intended debug workflow |
</threat_model>

<verification>
Manual check after task completes:
1. Open browser, load game, press backtick
2. Confirm debug panel shows: Mass (kg), Friction Coeff, Max Drive Torque, Max Brake Torque, Body Contact Stiffness, Body Contact Damping, Cornering Stiffness sliders
3. Confirm 'Cornering Stiffness' slider label contains 'Phase 2 placeholder'
4. Confirm 'Lateral Damping' slider label contains '(unused)'
5. Confirm 'Logger' field shows '\\ to record' and is not interactive (disabled/greyed out)
6. Confirm 'Rolling Resistance' slider is NOT present in the panel (removed per D-09)
7. Count sliders in the panel — confirm exactly 10 total
8. While driving, move Friction Coeff slider to 0.1 — car slides out visibly
9. Move Friction Coeff back to 0.9 — grip restored immediately, no restart needed
</verification>

<success_criteria>
- 10 sliders visible in debug panel (3 kept from Phase 1 + 7 new from D-08)
- rollingResistanceCoeff slider absent (removed per D-09)
- corneringStiffness slider labeled to indicate Phase 2 placeholder
- lateralDampingCoeff slider labeled '(unused)'
- Disabled Logger hint visible showing '\\ to record'
- All slider changes take effect immediately while driving (M2-06)
- No console errors on load
</success_criteria>

<output>
After completion, create /Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-02-SUMMARY.md
</output>
