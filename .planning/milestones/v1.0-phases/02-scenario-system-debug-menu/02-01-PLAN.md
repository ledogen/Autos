---
phase: 02-scenario-system-debug-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logger.js
  - src/physics.js
  - src/main.js
autonomous: true
requirements:
  - M2-01
  - M2-02
  - M2-03
  - M2-04

must_haves:
  truths:
    - "User presses \\ while driving, drives further, presses \\ again — browser auto-downloads a .json file named rangersim-log-{timestamp}.json"
    - "Downloaded JSON has a 'fields' array (t, px, py, pz, vx..., fl_fn, fl_fy, fl_sa, fl_c, fr_*, rl_*, rr_*) and a 'frames' array of scalar arrays"
    - "Each frame row has exactly 33 values matching the FIELDS order (t + 3 pos + 3 vel + 4 quat + 3 angvel + steer + thr + brk + 4×4 wheel)"
    - "User loads a JSON initial condition file via a button or key press — car position, velocity, quaternion, and angularVelocity are set immediately from the file"
    - "Malformed or non-JSON initial condition files log an error to console and do not crash the sim"
    - "Downloaded log file contains non-zero per-wheel fn/fy/sa/c columns when car is driven on ground — confirmed by opening the JSON and inspecting the frames array"
  artifacts:
    - path: "src/logger.js"
      provides: "Frame logger module — toggleRecording, captureFrame, openInitialCondition"
      exports:
        - toggleRecording
        - captureFrame
        - openInitialCondition
    - path: "src/physics.js"
      provides: "wheelDebug write — writes fn, fy, sa, c to vehicleState.wheelDebug[i] each step"
      contains: "vehicleState.wheelDebug"
    - path: "src/main.js"
      provides: "Game loop hooks — simTime counter, captureFrame call after stepPhysics, IC loader trigger"
      contains: "captureFrame"
  key_links:
    - from: "src/main.js (game loop while block)"
      to: "src/logger.js captureFrame"
      via: "called after stepPhysics each fixed step"
      pattern: "captureFrame\\(simTime"
    - from: "src/physics.js stepPhysics (per-wheel loop)"
      to: "vehicleState.wheelDebug[i]"
      via: "direct write inside wheel loop"
      pattern: "wheelDebug\\[i\\]"
    - from: "src/main.js IC loader trigger"
      to: "src/logger.js openInitialCondition"
      via: "button click or key press passes vehicleState reference"
      pattern: "openInitialCondition\\(vehicleState"
---

<objective>
Create the frame logger module (src/logger.js) and wire it into the fixed-timestep game loop so the user can toggle a per-frame physics recording with the \ key, auto-downloading a columnar JSON log on stop. Also add the initial condition loader so the user can set car state from a JSON file.

Purpose: Enables bug reporting — user reproduces a physics issue, captures a log, and shares the compact JSON with Claude alongside a description.

Output: src/logger.js (new), src/physics.js (wheelDebug write added), src/main.js (simTime counter + captureFrame hook + IC loader wiring)
</objective>

## Phase Goal

**As a** developer debugging physics, **I want to** capture a per-frame log of all physics state with one key press and reproduce any starting condition from a JSON file, **so that** I can share exact reproduction steps with Claude when something breaks.

<execution_context>
@/Users/ledogen/CodeShit/CarGame/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ledogen/CodeShit/CarGame/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ledogen/CodeShit/CarGame/.planning/PROJECT.md
@/Users/ledogen/CodeShit/CarGame/.planning/ROADMAP.md
@/Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-CONTEXT.md
@/Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-PATTERNS.md
</context>

<interfaces>
<!-- Key signatures the executor needs. No codebase exploration required. -->

From src/main.js (existing game loop — lines 363–383):
  The while block calls updateVehicle, then stepPhysics(vehicleState, RANGER_PARAMS, FIXED_DT, queryContacts).
  simTime variable does not yet exist — must be added as a module-level let, incremented by FIXED_DT each step.

vehicleState shape (lines 40–50, src/main.js):
  position: THREE.Vector3
  velocity: THREE.Vector3
  quaternion: THREE.Quaternion
  angularVelocity: THREE.Vector3
  steerAngle: number (rad)
  throttle: number
  brake: number
  wheelAngles: [0,0,0,0]
  wheelSteerAngles: [0,0,0,0]
  wheelDebug: NOT YET PRESENT — must be added to vehicleState in main.js (see key_code_facts)

vehicleState.wheelDebug shape to add (from key_code_facts):
  [
    { fn: 0, fy: 0, sa: 0, c: 0 },  // FL
    { fn: 0, fy: 0, sa: 0, c: 0 },  // FR
    { fn: 0, fy: 0, sa: 0, c: 0 },  // RL
    { fn: 0, fy: 0, sa: 0, c: 0 },  // RR
  ]

From src/physics.js stepPhysics per-wheel loop (lines 92–150):
  params._lateralVelocity = hubVel.dot(wheelRight)         — set before computeLateralForce
  params._longitudinalVelocity = hubVel.dot(wheelFwd)      — set before computeLongitudinalForce
  params._compression = depth                               — set inside contacts loop
  Fn = computeNormalForce(...)
  Flat = computeLateralForce(0, Fn, params)                 — lateral force result
  Flong = computeLongitudinalForce(0, Fn, params)           — not needed for debug write

  After computing Flat and Fn inside the contacts loop, write to vehicleState.wheelDebug[i]:
    fn: Fn
    fy: Flat
    sa: Math.atan2(params._lateralVelocity, Math.abs(params._longitudinalVelocity || 1e-6))
    c: params._compression (depth)

  NOTE: vehicleState.wheelDebug[i] should be zeroed at the top of the per-wheel loop (before
  contacts inner loop) so wheels with no contacts show zeros, not stale values.

From 02-PATTERNS.md — logger.js core patterns:
  FIELDS constant (33 entries): ['t','px','py','pz','vx','vy','vz','qx','qy','qz','qw','wx','wy','wz','steer','thr','brk','fl_fn','fl_fy','fl_sa','fl_c','fr_fn','fr_fy','fr_sa','fr_c','rl_fn','rl_fy','rl_sa','rl_c','rr_fn','rr_fy','rr_sa','rr_c']
  captureFrame(simTime, vehicleState, contactData) — contactData = vehicleState.wheelDebug
  toggleRecording() — flips _recording bool; on stop calls _downloadLog() and clears _frames
  openInitialCondition(vehicleState) — creates <input type="file">, reads JSON, applies to vehicleState
  _downloadLog() — Blob + URL.createObjectURL + anchor click pattern
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create src/logger.js — frame logger module</name>
  <files>src/logger.js</files>
  <read_first>
    /Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-PATTERNS.md
    /Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-CONTEXT.md
    /Users/ledogen/CodeShit/CarGame/src/vehicle.js
  </read_first>
  <action>
    Create src/logger.js as a new ES6 module. No imports needed.

    Module-private state (per vehicle.js pattern — private vars, named exports only):
      let _recording = false
      const _frames = []

    FIELDS constant (D-06/D-07 — 33 entries, exact order matters for columnar format):
      ['t','px','py','pz','vx','vy','vz','qx','qy','qz','qw','wx','wy','wz',
       'steer','thr','brk',
       'fl_fn','fl_fy','fl_sa','fl_c',
       'fr_fn','fr_fy','fr_sa','fr_c',
       'rl_fn','rl_fy','rl_sa','rl_c',
       'rr_fn','rr_fy','rr_sa','rr_c']

    Export toggleRecording():
      If _recording is true: set _recording = false, call _downloadLog(), set _frames.length = 0.
      If _recording is false: set _recording = true, set _frames.length = 0.

    Export captureFrame(simTime, vehicleState, wheelDebug):
      Guard: if (!_recording) return immediately.
      Push one array to _frames. Order must match FIELDS exactly:
        simTime,
        vehicleState.position.x, .y, .z,
        vehicleState.velocity.x, .y, .z,
        vehicleState.quaternion.x, .y, .z, .w,
        vehicleState.angularVelocity.x, .y, .z,
        vehicleState.steerAngle, vehicleState.throttle, vehicleState.brake,
        then for each of [wheelDebug[0], wheelDebug[1], wheelDebug[2], wheelDebug[3]]:
          w.fn ?? 0, w.fy ?? 0, w.sa ?? 0, w.c ?? 0

    Private _downloadLog():
      JSON.stringify({ fields: FIELDS, frames: _frames }) into a Blob (type: 'application/json').
      URL.createObjectURL(blob) → anchor element with download = 'rangersim-log-' + Date.now() + '.json'.
      Append anchor to document.body, call a.click(), remove anchor from body, URL.revokeObjectURL(url).

    Export openInitialCondition(vehicleState):
      Create <input type="file" accept=".json"> element. On change, take e.target.files[0].
      If no file, return. Use FileReader.readAsText. In reader.onload, JSON.parse(ev.target.result).
      Apply fields present in parsed IC: if ic.position exists, vehicleState.position.set(x,y,z).
      Same for ic.velocity → vehicleState.velocity.set, ic.quaternion → vehicleState.quaternion.set(x,y,z,w),
      ic.angularVelocity → vehicleState.angularVelocity.set. Wrap entire parse+apply in try/catch;
      on error: console.error('[logger] Failed to parse IC file:', err) and return without crashing.
      After constructing the input element, call input.click() to open the browser file picker.

    Add a JSDoc block at the top of the file summarizing the module's purpose, exports, and the
    columnar log format (fields header + frames arrays). No fenced code blocks inside action.
  </action>
  <verify>
    <automated>grep -c "export function toggleRecording" /Users/ledogen/CodeShit/CarGame/src/logger.js && grep -c "export function captureFrame" /Users/ledogen/CodeShit/CarGame/src/logger.js && grep -c "export function openInitialCondition" /Users/ledogen/CodeShit/CarGame/src/logger.js && grep -c "fl_fn" /Users/ledogen/CodeShit/CarGame/src/logger.js && grep -c "rr_c" /Users/ledogen/CodeShit/CarGame/src/logger.js</automated>
  </verify>
  <acceptance_criteria>
    - src/logger.js exists and is a valid ES6 module (no syntax errors — browser loads without import error)
    - File exports exactly three functions: toggleRecording, captureFrame, openInitialCondition
    - FIELDS constant contains 33 entries; first is 't', last is 'rr_c'
    - captureFrame returns immediately (no-op) when _recording is false
    - toggleRecording flips _recording state; when transitioning from true→false it calls _downloadLog() and resets _frames
    - _downloadLog uses URL.createObjectURL + anchor.click pattern (not window.open or fetch)
    - openInitialCondition wraps JSON.parse in try/catch; errors go to console.error without throwing
  </acceptance_criteria>
  <done>src/logger.js created with all three exports, correct 33-field FIELDS constant, download pattern, and IC loader with error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Expose wheelDebug in physics.js and wire logger into main.js</name>
  <files>src/physics.js, src/main.js</files>
  <read_first>
    /Users/ledogen/CodeShit/CarGame/src/physics.js
    /Users/ledogen/CodeShit/CarGame/src/main.js
    /Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-CONTEXT.md
  </read_first>
  <action>
    PHYSICS.JS — add wheelDebug write inside stepPhysics per-wheel loop (lines ~92–150):

    At the top of the `for (let i = 0; i < 4; i++)` loop body (before the contacts inner loop),
    zero the debug entry for this wheel:
      vehicleState.wheelDebug[i] = { fn: 0, fy: 0, sa: 0, c: 0 }
    Guard: only write if vehicleState.wheelDebug is an array (if (!vehicleState.wheelDebug) skip).

    Inside the contacts inner loop (after Fn is computed, after Flat = computeLateralForce),
    overwrite vehicleState.wheelDebug[i] with the last-contact values (last contact wins — acceptable
    for debug logging since most steps have exactly one contact):
      vehicleState.wheelDebug[i].fn = Fn
      vehicleState.wheelDebug[i].fy = Flat
      vehicleState.wheelDebug[i].sa = Math.atan2(params._lateralVelocity, Math.abs(params._longitudinalVelocity || 1e-6))
      vehicleState.wheelDebug[i].c  = params._compression

    Do NOT change the stepPhysics function signature. Do NOT change any existing force computation.

    MAIN.JS — changes:

    1. Add import at the top of the existing imports block (after existing imports, before the
       console.log line). Use this single definitive import statement:
         import { captureFrame, toggleRecording, openInitialCondition } from './logger.js'

    2. Add wheelDebug to vehicleState initializer (the const vehicleState = { ... } block at lines 40–50).
       Append as the last property:
         wheelDebug: [ {fn:0,fy:0,sa:0,c:0}, {fn:0,fy:0,sa:0,c:0}, {fn:0,fy:0,sa:0,c:0}, {fn:0,fy:0,sa:0,c:0} ]

    3. Add a module-level simTime counter and captureFrame call:
       After the const declarations for FIXED_DT and MAX_FRAME_TIME, add:
         let simTime = 0
       Inside the while (accumulator >= FIXED_DT) block, after the stepPhysics call and before
       accumulator -= FIXED_DT, add:
         simTime += FIXED_DT
         captureFrame(simTime, vehicleState, vehicleState.wheelDebug)

    4. Add the \ key listener and IC loader button:
       The \ key listener should be registered once (not in the game loop). Add it near the existing
       initDebug call (line ~346). Key listener:
         document.addEventListener('keydown', e => {
           if (e.key === '\\') toggleRecording()
           if (e.key === 'i' && e.ctrlKey) openInitialCondition(vehicleState)
         })
       Use Ctrl+I as the IC loader trigger per D-02 (file picker, no specific key specified in CONTEXT.md).

    Also update the vehicleState reset block (lines ~368–379, inside `if (resetRequested)`) to
    also reset wheelDebug to zeros:
      vehicleState.wheelDebug = [ {fn:0,fy:0,sa:0,c:0}, {fn:0,fy:0,sa:0,c:0}, {fn:0,fy:0,sa:0,c:0}, {fn:0,fy:0,sa:0,c:0} ]
  </action>
  <verify>
    <automated>grep -c "captureFrame" /Users/ledogen/CodeShit/CarGame/src/main.js && grep -c "wheelDebug" /Users/ledogen/CodeShit/CarGame/src/main.js && grep -c "wheelDebug" /Users/ledogen/CodeShit/CarGame/src/physics.js && grep -c "toggleRecording" /Users/ledogen/CodeShit/CarGame/src/main.js</automated>
  </verify>
  <acceptance_criteria>
    - src/main.js imports captureFrame, toggleRecording, openInitialCondition from './logger.js'
    - vehicleState in main.js contains wheelDebug: [{fn,fy,sa,c} × 4] as initial value
    - simTime is declared as a module-level let and incremented by FIXED_DT inside the while block
    - captureFrame(simTime, vehicleState, vehicleState.wheelDebug) is called after stepPhysics inside the while block
    - \ keydown listener calls toggleRecording()
    - Ctrl+I keydown listener calls openInitialCondition(vehicleState)
    - src/physics.js zeroes vehicleState.wheelDebug[i] at the top of each wheel loop iteration
    - src/physics.js writes fn, fy, sa, c to vehicleState.wheelDebug[i] inside the contacts loop
    - stepPhysics signature is unchanged (still takes vehicleState, params, dt, queryContacts)
  </acceptance_criteria>
  <done>physics.js writes wheelDebug per wheel per step; main.js has simTime counter, captureFrame call after stepPhysics, \ toggle and Ctrl+I IC loader wired; sim loads in browser without console errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User filesystem → vehicleState | IC file loaded via FileReader; content is untrusted JSON |
| vehicleState → Blob download | Logger serializes vehicleState fields to JSON for download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | openInitialCondition JSON parse | mitigate | try/catch around JSON.parse; unknown keys ignored (only named fields applied); no eval |
| T-02-02 | Denial of Service | Frame logger _frames array unbounded growth | accept | recording is user-toggled; session length bounded by user action; no auto-start |
| T-02-03 | Information Disclosure | Downloaded log contains vehicleState | accept | local download only; user explicitly triggers; no server involved |
</threat_model>

<verification>
Manual check after both tasks complete:
1. Open browser, load game from local HTTP server (npx serve . or python3 -m http.server)
2. Drive for 3-5 seconds, press \ — no visible crash, no console error
3. Drive 3 more seconds, press \ again — browser auto-downloads rangersim-log-{timestamp}.json
4. Open downloaded file: confirm it has "fields" array (33 entries, starts with "t", ends with "rr_c") and "frames" array of arrays
5. Open the frames array and confirm the fl_fn, fl_fy, fl_sa, fl_c columns contain non-zero values for frames recorded while driving on ground
6. Press Ctrl+I — OS file picker opens. Select a valid .json file with {"position":{"x":0,"y":0.55,"z":-20}} — car teleports to that position
7. Press Ctrl+I, select a non-JSON file — console.error appears, sim continues
</verification>

<success_criteria>
- src/logger.js exists with three exports: toggleRecording, captureFrame, openInitialCondition
- Downloaded log file contains non-zero per-wheel fn/fy/sa/c values for frames recorded while driving on ground
- Pressing \ starts/stops recording; stopping triggers auto-download of columnar JSON
- Downloaded JSON fields array has exactly 33 entries in the specified order
- Ctrl+I opens file picker; valid JSON applies position/velocity/quaternion/angularVelocity to vehicleState
- Invalid JSON files produce console.error and do not crash the sim
- stepPhysics signature unchanged
</success_criteria>

<output>
After completion, create /Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-01-SUMMARY.md
</output>
