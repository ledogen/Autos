---
phase: 06-procedural-terrain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/terrain.js
  - src/terrain-worker.js
autonomous: true
requirements:
  - TERR-01
  - TERR-02
  - TERR-03
  - TERR-06

must_haves:
  truths:
    - "TerrainSystem class is importable from src/terrain.js with no errors"
    - "A chunk for position (0,0) is generated by the worker and stored in the chunk map"
    - "sampleHeight(0, 0) returns 0 (simplex lattice point — safe spawn)"
    - "sampleHeight on a loaded chunk returns a float, not 0 and not NaN"
    - "sampleNormal returns a unit vector with y > 0 (upward-biased) on flat/loaded terrain"
    - "Chunks outside the 5×5 ring are disposed when the ring updates to a new center"
    - "At most 2 chunk geometries are built per frame (frame-spread queue)"
  artifacts:
    - path: "src/terrain.js"
      provides: "TerrainSystem class: constructor, update(), sampleHeight(), sampleNormal()"
      exports: ["TerrainSystem", "CHUNK_SIZE", "GRID_SAMPLES"]
    - path: "src/terrain-worker.js"
      provides: "Classic Blob worker source string embedded in terrain.js; simplex noise inlined"
  key_links:
    - from: "src/terrain.js TerrainSystem constructor"
      to: "Blob URL classic worker"
      via: "WORKER_SOURCE string literal containing simplex noise + onmessage handler"
      pattern: "new Blob.*application/javascript"
    - from: "TerrainSystem.update()"
      to: "_chunkMap"
      via: "updateChunkRing: dispose out-of-ring chunks, request new ones via worker postMessage"
      pattern: "chunkMap\\.delete|terrainWorker\\.postMessage"
    - from: "worker onmessage reply"
      to: "TerrainSystem._pendingQueue"
      via: "push {key,cx,cz,heights} into a FIFO queue for frame-spread geometry build"
      pattern: "_pendingQueue\\.push"
    - from: "TerrainSystem._flushPendingQueue()"
      to: "scene (Three.js)"
      via: "Build PlaneGeometry, set Y from heights, computeVertexNormals, scene.add(mesh)"
      pattern: "PlaneGeometry.*64.*64.*64.*64"
---

<objective>
Build the TerrainSystem module: a self-contained chunk-streaming heightmap engine that generates
simplex noise terrain via a Blob Web Worker and exposes O(1) bilinear height/normal queries for
the physics pipeline.

Purpose: All terrain physics and rendering live in terrain.js. main.js will integrate by calling
terrainSystem.update() and terrainSystem.sampleHeight/sampleNormal() in Plan 02. This plan
establishes the complete service before any wiring.

Output: src/terrain.js (TerrainSystem class), src/terrain-worker.js (worker source file — exists
for readability but its content is also embedded as a string in terrain.js for the Blob spawn).
</objective>

<execution_context>
@/Users/ledogen/CodeShit/CarGame/.planning/phases/06-procedural-terrain/06-RESEARCH.md
@/Users/ledogen/CodeShit/CarGame/.planning/phases/06-procedural-terrain/06-PATTERNS.md
</execution_context>

<context>
@/Users/ledogen/CodeShit/CarGame/.planning/PROJECT.md
@/Users/ledogen/CodeShit/CarGame/.planning/ROADMAP.md

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. -->

From src/main.js (scene construction pattern):
```javascript
const scene = new THREE.Scene()
// All meshes added via: scene.add(mesh)
// All meshes removed via: scene.remove(mesh); mesh.geometry.dispose()
```

From src/main.js (module-scope service init pattern):
```javascript
initDebug(RANGER_PARAMS)  // called at module scope after scene exists
// terrain.js follows same: const terrainSystem = new TerrainSystem(scene, RANGER_PARAMS)
```

From src/main.js (queryContacts — the consumer of sampleHeight/sampleNormal):
```javascript
function queryContacts (cx, cy, cz, r) {
  // cx, cy, cz = wheel hub world-space coordinates; r = wheelRadius
  // returns Array of { normal: THREE.Vector3, depth: number, contactPoint: THREE.Vector3 }
  const gd = r - cy  // current flat-ground logic — Plan 02 replaces this
  // ...
}
```

Contact hit shape (physics.js reads these fields exactly):
```javascript
{
  normal:       new THREE.Vector3(nx, ny, nz),  // unit vector pointing away from surface
  depth:        gd,                              // positive penetration depth in metres
  contactPoint: new THREE.Vector3(cx, hy, cz)   // world-space point on the surface
}
```

From RESEARCH.md Pattern 3 — Blob classic worker spawn:
```javascript
const blob    = new Blob([WORKER_SOURCE], { type: 'application/javascript' })
const blobURL = URL.createObjectURL(blob)
const worker  = new Worker(blobURL)
// Worker receives: { type: 'generate', cx, cz, key }
// Worker posts:    { key, cx, cz, heights }  with [heights.buffer] as transferable
```

From RESEARCH.md Pattern 4 — bilinear sampleHeight:
```javascript
// CHUNK_SIZE = 64, GRID_SAMPLES = 65, CELL_SIZE = 1.0 m
// cx = Math.floor(wx / CHUNK_SIZE), key = `${cx},${cz}`
// chunk = chunkMap.get(key)
// if (!chunk || !chunk.heights) return 0  (safe fallback = flat ground)
// bilinear on chunk.heights[zi * N + xi]
// multiply bilinear result by this._params.terrainAmplitude before returning
```

From RESEARCH.md noise FBM formula:
```javascript
const h =
  noise2D(wx * 0.02, wz * 0.02) * 4.0 +
  noise2D(wx * 0.06, wz * 0.06) * 1.5 +
  noise2D(wx * 0.15, wz * 0.15) * 0.5
// noise2D(0,0) = 0 — safe spawn at world origin (standard simplex lattice point)
```

From PATTERNS.md shared patterns:
```javascript
// Geometry disposal: geometry.dispose() but NOT material.dispose() (shared material)
// THREE.MeshPhongMaterial for terrain chunks (matches existing ground + ramp)
// Named exports only (no default exports)
// File-top module docblock with responsibilities
// JSDoc on all exported methods
// Underscore prefix for module-private scratch (_chunkMap, _pendingWorker, _pendingQueue)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/terrain-worker.js — simplex noise worker source</name>
  <files>src/terrain-worker.js</files>
  <read_first>
    /Users/ledogen/CodeShit/CarGame/.planning/phases/06-procedural-terrain/06-RESEARCH.md (Pattern 3 blob worker, noise FBM formula, anti-patterns)
    /Users/ledogen/CodeShit/CarGame/.planning/phases/06-procedural-terrain/06-PATTERNS.md (terrain-worker section, message protocol, Float32Array transferable)
  </read_first>
  <behavior>
    - File is NOT an ES6 module: zero import/export statements
    - Begins with a block comment: "// src/terrain-worker.js — Classic Blob worker source for TerrainSystem" plus responsibilities
    - Inlines a minimal simplex noise 2D implementation (NOT a CDN import — worker has no importmap)
    - Uses createNoise2D seeded with a deterministic function so chunk edges are seamless: seed function = () => 0.5 produces a valid but fixed permutation (RESEARCH §Pitfall 3 + §A6)
    - Defines constants N=65 (GRID_SAMPLES), S=64 (CHUNK_SIZE), cell = S/(N-1) = 1.0
    - self.onmessage handler: receives {type, cx, cz, key}; ignores non-'generate' types
    - Allocates Float32Array(N*N), fills with 3-octave FBM: noise2D(wx*0.02,wz*0.02)*4.0 + noise2D(wx*0.06,wz*0.06)*1.5 + noise2D(wx*0.15,wz*0.15)*0.5
    - Posts {key, cx, cz, heights} with [heights.buffer] as the second postMessage arg (transferable)
    - Comments reference RESEARCH.md Pattern 3 as authoritative source
  </behavior>
  <action>
    Create src/terrain-worker.js as a plain JS file (not an ES module). The file will be read as text
    by terrain.js and embedded in a Blob string — it is never loaded via a script tag or import.

    Inline a minimal simplex 2D noise implementation extracted from simplex-noise@4.0.3 (MIT, Jonas
    Wagner). The minimal implementation needs: buildPermutationTable(), createNoise2D(random), and
    the noise2D callable returned by createNoise2D. The full source is available at
    https://cdn.jsdelivr.net/npm/simplex-noise@4.0.3/dist/esm/simplex-noise.js — fetch it to extract
    the minimal needed subset. Credit the original author and license in a comment at the top.

    Constants at top of worker body:
      const GRID_SAMPLES = 65
      const CHUNK_SIZE   = 64
      const CELL_SIZE    = CHUNK_SIZE / (GRID_SAMPLES - 1)  // 1.0 m

    Seed the noise with a fixed deterministic function to guarantee identical heights at chunk
    boundaries: const noise2D = createNoise2D(() => 0.5)

    Verify that noise2D(0,0) is NOT NaN after construction (log to console once on worker startup).

    The FBM formula per RESEARCH.md:
      const h = noise2D(wx*0.02, wz*0.02) * 4.0
              + noise2D(wx*0.06, wz*0.06) * 1.5
              + noise2D(wx*0.15, wz*0.15) * 0.5

    Post heights Float32Array using transferable: self.postMessage({key,cx,cz,heights}, [heights.buffer])
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const src = fs.readFileSync('src/terrain-worker.js', 'utf8');
// Must not have import/export (not an ES module)
if (/^import |^export /m.test(src)) { console.error('FAIL: has import/export'); process.exit(1); }
// Must contain self.onmessage
if (!src.includes('self.onmessage')) { console.error('FAIL: missing self.onmessage'); process.exit(1); }
// Must use transferable postMessage with [heights.buffer]
if (!src.includes('heights.buffer')) { console.error('FAIL: missing heights.buffer transferable'); process.exit(1); }
// Must contain FBM formula markers
if (!src.includes('0.02') || !src.includes('0.06') || !src.includes('0.15')) { console.error('FAIL: missing FBM octave frequencies'); process.exit(1); }
// Must declare GRID_SAMPLES = 65 and CHUNK_SIZE = 64
if (!src.includes('GRID_SAMPLES') || !src.includes('CHUNK_SIZE')) { console.error('FAIL: missing constants'); process.exit(1); }
console.log('PASS: terrain-worker.js structure ok');
" 2>&1
    </automated>
  </verify>
  <done>
    src/terrain-worker.js exists with no import/export statements, contains self.onmessage handler,
    uses Float32Array transferable with [heights.buffer], declares GRID_SAMPLES=65 CHUNK_SIZE=64,
    implements 3-octave FBM noise with the exact frequencies 0.02/0.06/0.15.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create src/terrain.js — TerrainSystem class</name>
  <files>src/terrain.js</files>
  <read_first>
    /Users/ledogen/CodeShit/CarGame/.planning/phases/06-procedural-terrain/06-RESEARCH.md (Pattern 1 chunk coordinates, Pattern 2 ring buffer, Pattern 4 bilinear query, Pattern 5 queryContacts extension sketch, Pattern 6 geometry build, anti-patterns)
    /Users/ledogen/CodeShit/CarGame/.planning/phases/06-procedural-terrain/06-PATTERNS.md (terrain.js section: imports, exports, shared patterns, critical integration notes)
    /Users/ledogen/CodeShit/CarGame/src/terrain-worker.js (just created — embed its content as WORKER_SOURCE string)
  </read_first>
  <behavior>
    - Exports: TerrainSystem (class), CHUNK_SIZE (64), GRID_SAMPLES (65)
    - Constructor(scene, params): stores scene ref and params ref as this._params, reads params.terrainAmplitude (default 1.0 scale multiplier, applied to heights during build — allows live tuning), spawns Blob worker from WORKER_SOURCE string literal, registers worker.onmessage to push into _pendingQueue
    - Private state: _chunkMap (Map), _pendingWorker (Set of pending keys), _pendingQueue (Array FIFO)
    - update(carPos): calls updateChunkRing(carPos) — dispose out-of-ring chunks with geometry.dispose() (NOT material.dispose()), request new chunks via worker postMessage; then calls _flushPendingQueue() limited to MAX_BUILDS_PER_FRAME=2
    - _flushPendingQueue(): pops up to 2 items, builds PlaneGeometry(S, S, N-1, N-1), rotateX(-PI/2), overwrites Y from heights Float32Array (multiplied by params.terrainAmplitude), calls computeVertexNormals() for rendering, scene.add(mesh), chunkMap.set(key, {mesh, heights})
    - sampleHeight(wx, wz): bilinear interpolation on chunk.heights (raw worker values); multiplies the bilinear result by this._params.terrainAmplitude before returning, so the physics contact surface matches the visual terrain surface at any amplitude setting; returns 0 if chunk not loaded (safe fallback)
    - sampleNormal(wx, wz): central-difference finite-difference; EPS=0.5m; calls sampleHeight 4× at ±EPS in X and Z; returns {x,y,z} plain object (NOT THREE.Vector3 — physics callers construct Vector3 themselves per PATTERNS contact hit shape)
    - Material: single shared MeshPhongMaterial({color: 0x336622}) created in constructor, reused across all chunks; do NOT dispose per-chunk
    - Flat ground plane removal: TerrainSystem does NOT remove the flat ground — that is wired by main.js in Plan 02 (terrain.js has no reference to the ground variable)
    - FBM amplitude is applied in TWO places: (1) _flushPendingQueue multiplies heights[i] by params.terrainAmplitude for visual geometry; (2) sampleHeight multiplies its bilinear result by params.terrainAmplitude for physics queries — both must use the same scale so contact surface and visual surface are identical
    - WORKER_SOURCE: embed the content of src/terrain-worker.js as a template-literal string constant at the top of terrain.js, preceding the class declaration
  </behavior>
  <action>
    Create src/terrain.js as an ES6 module starting with:
      import * as THREE from 'three'

    Export constants at module scope:
      export const CHUNK_SIZE   = 64
      export const GRID_SAMPLES = 65
      const RING_RADIUS         = 2
      const MAX_BUILDS_PER_FRAME = 2

    Read src/terrain-worker.js and embed its full text verbatim as:
      const WORKER_SOURCE = `...content of terrain-worker.js...`

    Class structure:
      export class TerrainSystem {
        constructor(scene, params) { ... }
        update(carPos) { ... }           // call outside physics accumulator, render rate
        sampleHeight(wx, wz) { ... }     // O(1), returns number (0 = fallback); MULTIPLIES by params.terrainAmplitude
        sampleNormal(wx, wz) { ... }     // returns {x, y, z} plain object, always upward-biased
        _flushPendingQueue() { ... }     // builds up to MAX_BUILDS_PER_FRAME chunk geometries
        _updateChunkRing(ccx, ccz) { ... }  // dispose + request
        _chunkKey(cx, cz) { return `${cx},${cz}` }
        _worldToChunk(wx, wz) { return { cx: Math.floor(wx/CHUNK_SIZE), cz: Math.floor(wz/CHUNK_SIZE) } }
      }

    sampleHeight implementation — CRITICAL: multiply by terrainAmplitude:
      const chunk = this._chunkMap.get(this._chunkKey(cx, cz))
      if (!chunk || !chunk.heights) return 0
      // ... bilinear interpolation on chunk.heights (raw values from worker) ...
      return bilinearResult * (this._params.terrainAmplitude ?? 1.0)
    This ensures the physics contact surface always matches the visual geometry
    (which also multiplies by terrainAmplitude in _flushPendingQueue).

    sampleNormal implementation per RESEARCH.md Pattern 4:
      EPS = 0.5
      hL = sampleHeight(wx-EPS, wz),  hR = sampleHeight(wx+EPS, wz)
      hD = sampleHeight(wx, wz-EPS),  hU = sampleHeight(wx, wz+EPS)
      nx = -(hR-hL)/(2*EPS),  ny = 1,  nz = -(hU-hD)/(2*EPS)
      len = sqrt(nx*nx + ny*ny + nz*nz)
      return {x: nx/len, y: ny/len, z: nz/len}

    Chunk disposal in _updateChunkRing: for evicted chunks call chunk.mesh.geometry.dispose()
    and scene.remove(chunk.mesh) before chunkMap.delete(key). Do NOT call material.dispose().

    Geometry build in _flushPendingQueue: PlaneGeometry vertices are in X-Y plane before rotation.
    After rotateX(-PI/2) the geometry is in X-Z plane. The heights Float32Array from the worker
    uses row-major order heights[zi * N + xi]. After rotation, position.setY(i, heights[i] * params.terrainAmplitude).
    Call pos.needsUpdate = true and geom.computeVertexNormals() after setting Y values.

    Mesh position after rotation: mesh.position.set(cx * CHUNK_SIZE + CHUNK_SIZE/2, 0, cz * CHUNK_SIZE + CHUNK_SIZE/2)

    File-top module docblock:
      /**
       * src/terrain.js — TerrainSystem for RangerSim
       *
       * Responsibilities:
       *  - Chunk ring management (5×5, RING_RADIUS=2, 64 m tiles)
       *  - Heightmap generation via Blob classic Web Worker (simplex noise inlined)
       *  - Frame-spread geometry build (MAX_BUILDS_PER_FRAME=2 per frame)
       *  - O(1) bilinear height query + central-difference normal for physics pipeline
       *
       * Physics contract: sampleHeight returns 0 when chunk not loaded (safe = flat ground fallback).
       *   sampleHeight multiplies by params.terrainAmplitude so physics contact surface matches
       *   visual geometry at all amplitude settings.
       * Rendering contract: chunk meshes use shared MeshPhongMaterial — do NOT dispose per-chunk.
       *
       * Conventions: see docs/GLOSSARY.md
       * Anti-patterns: do NOT use Raycaster for height queries (O(N²)); do NOT call
       *                computeVertexNormals from physics (rendering only).
       */
  </action>
  <verify>
    <automated>node --input-type=module &lt;&lt;'EOF'
import { TerrainSystem, CHUNK_SIZE, GRID_SAMPLES } from './src/terrain.js'
// Verify exports exist
if (typeof TerrainSystem !== 'function') throw new Error('TerrainSystem not exported')
if (CHUNK_SIZE !== 64) throw new Error('CHUNK_SIZE wrong: ' + CHUNK_SIZE)
if (GRID_SAMPLES !== 65) throw new Error('GRID_SAMPLES wrong: ' + GRID_SAMPLES)
// Verify sampleHeight returns 0 when no chunks loaded (default empty map)
const { JSDOM } = await import('jsdom').catch(() => null)
// Skip DOM-dependent test in Node; rely on structure checks below
console.log('PASS: TerrainSystem exports ok, CHUNK_SIZE=64, GRID_SAMPLES=65')
EOF
    </automated>
  </verify>
  <done>
    src/terrain.js exports TerrainSystem class, CHUNK_SIZE=64, GRID_SAMPLES=65.
    Class has constructor, update, sampleHeight, sampleNormal, _flushPendingQueue,
    _updateChunkRing methods. WORKER_SOURCE string is embedded. No import of Three.js inside
    the worker source string (blob context has no importmap).
    sampleHeight multiplies its bilinear result by this._params.terrainAmplitude (matching
    the scale applied in _flushPendingQueue) so physics and visual surfaces are always aligned.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → Main thread | Worker posts Float32Array heights data; main thread builds geometry from it |
| CDN → importmap | simplex-noise not needed in importmap for this plan (inlined in worker); no CDN trust boundary this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Denial of Service | _pendingQueue unbounded | mitigate | MAX_BUILDS_PER_FRAME=2 caps main-thread geometry build cost per frame; worker requests capped by ring size (25 max) |
| T-06-02 | Information Disclosure | Worker Blob URL | accept | Blob URL is same-origin; no sensitive data in worker source string |
| T-06-03 | Denial of Service | geometry.dispose() omitted on chunk eviction | mitigate | Explicit geometry.dispose() in _updateChunkRing before chunkMap.delete; documented in file-top docblock |
</threat_model>

<verification>
After both tasks complete:
1. node --input-type=module -e "import { TerrainSystem, CHUNK_SIZE, GRID_SAMPLES } from './src/terrain.js'; console.log(CHUNK_SIZE, GRID_SAMPLES)" prints "64 65"
2. grep -c "import\|export" src/terrain-worker.js returns 0 (no ES module syntax in worker file)
3. grep "WORKER_SOURCE" src/terrain.js shows the embedded string constant
4. grep "geometry.dispose" src/terrain.js shows disposal call in _updateChunkRing
5. grep "heights.buffer" src/terrain-worker.js shows transferable pattern
6. grep "terrainAmplitude" src/terrain.js shows amplitude applied in both sampleHeight and _flushPendingQueue
</verification>

<success_criteria>
- src/terrain.js and src/terrain-worker.js both exist and pass structural checks
- TerrainSystem is importable as an ES6 named export
- Worker source contains no import/export statements
- Worker source embeds 3-octave simplex FBM with frequencies 0.02/0.06/0.15
- sampleHeight returns 0 when chunk map is empty (safe fallback confirmed by code inspection)
- sampleHeight multiplies bilinear result by params.terrainAmplitude (physics/visual alignment confirmed by code inspection)
- Chunk eviction calls geometry.dispose() (confirmed by code inspection)
- Material is shared (one instance), not disposed per chunk (confirmed by code inspection)
</success_criteria>

<output>
After completion, create /Users/ledogen/CodeShit/CarGame/.planning/phases/06-procedural-terrain/06-01-SUMMARY.md
</output>
