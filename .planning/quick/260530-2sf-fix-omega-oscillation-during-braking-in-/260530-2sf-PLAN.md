---
phase: 260530-2sf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/physics.js]
autonomous: true
requirements: [QUICK-260530-2sf]
must_haves:
  truths:
    - "Braking to a stop no longer produces omega oscillation between 0 and ~18 rad/s"
    - "Brake torque continues to act when omega is exactly 0 (preventing road-reaction kick)"
    - "Zero-crossing clamp still prevents brake from reversing wheel spin direction"
  artifacts:
    - path: "src/physics.js"
      provides: "Omega integrator block with spinSign fix at lines 247-257"
      contains: "spinSign"
  key_links:
    - from: "src/physics.js omega integrator"
      to: "brakeSigned application"
      via: "spinSign = omega0 >= 0 ? 1 : -1"
      pattern: "spinSign"
---

<objective>
Fix omega (wheel angular velocity) oscillation during braking caused by `Math.sign(0) = 0` disabling brake torque exactly when the zero-crossing clamp lands omega at 0. The next frame the road-reaction torque (from slip=-1) kicks omega up to ~18 rad/s, then the clamp slams it back to 0 — a stable 0↔18 oscillation every frame.

Purpose: Make full braking-to-rest stable instead of oscillating.
Output: Patched omega integrator block in `src/physics.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/physics.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Math.sign(omega0) with spinSign in omega integrator</name>
  <files>src/physics.js</files>
  <action>
    In `src/physics.js`, replace the omega integrator block at lines 247-256 (the `else` branch of the free-rolling clamp). Apply exactly:

    1. Compute `const spinSign = omega0 >= 0 ? 1 : -1` so that omega=0 still receives brake torque (Math.sign(0)=0 would zero out `brakeSigned` and let the negative road-reaction torque kick omega positive).
    2. Use `const brakeSigned = brakeTorque * spinSign`.
    3. Keep `newOmega = omega0 + (driveTorque - roadReactionTorque - brakeSigned) / wheelInertia * dt`.
    4. Update the zero-crossing clamp condition to drop the `omega0 !== 0` guard and compare against `spinSign` instead of `Math.sign(omega0)`: `if (brakeTorque > 0 && Math.sign(newOmega) !== spinSign) { vehicleState.wheelOmega[i] = 0 } else { vehicleState.wheelOmega[i] = newOmega }`.

    Preserve the surrounding comments' intent — update the inline comment for the spinSign line to note that `>= 0` is used because `Math.sign(0) = 0` would disable brake torque at exactly omega=0. Do not touch any other code in this file.
  </action>
  <verify>
    <automated>grep -n "spinSign" src/physics.js | grep -v '^#' | wc -l | awk '{ if ($1 >= 3) print "PASS"; else { print "FAIL: expected >=3 spinSign occurrences, got " $1; exit 1 } }' && ! grep -n "Math.sign(omega0)" src/physics.js && ! grep -n "omega0 !== 0" src/physics.js && node -e "import('./src/physics.js').then(()=>console.log('PARSE OK')).catch(e=>{console.error(e);process.exit(1)})" --input-type=module 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `src/physics.js` contains `spinSign = omega0 >= 0 ? 1 : -1`, `brakeTorque * spinSign`, and `Math.sign(newOmega) !== spinSign` in the omega integrator.
    - No remaining `Math.sign(omega0)` reference in that block.
    - No remaining `omega0 !== 0` guard in the brake-clamp condition.
    - File parses as valid ES module.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Brake-to-rest omega oscillation fix in `src/physics.js`.</what-built>
  <how-to-verify>
    1. Open the sim in the browser (Live Server or `npx serve .`).
    2. Drive forward, then apply full brake until the vehicle stops.
    3. Watch the wheel omega HUD/debug values as the car comes to rest. Expected: omega smoothly approaches 0 and stays at 0. Failure mode (regression): omega oscillates between 0 and ~18 rad/s every frame.
    4. Confirm the car still stops (brake torque actually acts) and that the vehicle does not creep backward (clamp still prevents reverse-spin from brake).
  </how-to-verify>
  <resume-signal>Type "approved" if stable at rest, or describe observed oscillation/regression.</resume-signal>
</task>

</tasks>

<verification>
- omega no longer oscillates at rest under brake
- braking still brings the wheel to a clean stop
- no reverse-spin from brake torque
</verification>

<success_criteria>
- Patched block matches the "After" snippet from the task brief.
- Manual brake-to-stop check passes (no 0↔18 rad/s oscillation).
</success_criteria>

<output>
After completion, create `.planning/quick/260530-2sf-fix-omega-oscillation-during-braking-in-/260530-2sf-SUMMARY.md`.
</output>
