---
context: default
phase: null
task: null
status: in_progress
last_updated: 2026-06-04T07:28:09Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Discovered through actual failure across 3 debug iterations this session.

- [ ] CONSTRAINT: **sLat-only fixes don't kill the slosh** — The surviving yaw oscillation is driven by sLong at the rear wheels from yaw-induced longitudinal hub velocity. Do not spend more effort on sLat.
- [ ] CONSTRAINT: **fl_sa = Math.hypot(sLong, sLat)** — A large rl_sa does NOT mean sLat is large. Break down the sLong vs sLat components before diagnosing.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Mis-attributing rl_sa to sLat | rl_sa=1.46 was sLong from wheel omega, not sLat — sLat was already fixed | blocking | Print sLong and sLat separately before diagnosing any wheel |
| sLat tunnel vision | Three iterations on sLat while sLong/wheel-omega feedback went unexamined | blocking | When fix has no effect, check whether the dominant force term changed |

<current_state>
Debugging BUG-03: yaw slosh after a lateral slide stops. Three debug iterations this session.

WHAT WORKS:
- SLIP_EPSILON = 3.0 (prevents vCon→0 at rest)
- sLat sign-clamp + SS overshoot cap: sLat now bounded to latVelCur*L/vCon
- fl_sa at FL dropped from 0.54m to 0.04m — sLat IS capped at front ✓
- HUD slip vector diagram: vLong now shows omegaCur-longVelCur

WHAT'S STILL BROKEN:
- rl_sa = 1.46m and rr_sa = 1.29m — dominated by sLong, not sLat
- rl_omega swings +4.83 to -7.36 rad/s during a spin (wheel omega oscillation)
- wY oscillates: 2.0 → -0.9 → +0.6 → slowly decaying, still very visible

ROOT CAUSE:
Positive feedback loop at rear wheels:
1. wY > 0 → RL hub forward longvel from spin: wY * halftrack = 2 * 0.77 = 1.54 m/s
2. rl_omega small → omegaCur < longVelCur → sLong negative
3. Negative sLong → Flong_RL backward on car
4. RL at r_x = -0.77m → T_yaw = r_x * Flong = -0.77 * (negative) = POSITIVE
5. Positive T_yaw amplifies wY → loop
</current_state>

<completed_work>
- Sky: 0x87ceeb blue, neutral white lighting (was Mojave desert)
- sLat sign-clamp: reset to SS when sLat sign conflicts with latVelCur
- SLIP_EPSILON: 0.1 → 3.0 (bounded sLat_ss at low speed)
- sLat SS overshoot cap: sLat cannot exceed current-velocity steady state
- HUD vLong: stores omegaCur-longVelCur (actual longitudinal slip velocity)
- Identified sLong/wheel-omega positive feedback as surviving oscillation root cause
</completed_work>

<remaining_work>
BUG-03: Fix sLong/wheel-omega positive feedback at rear wheels.

Recommended approach order:
1. LOW-SPEED YAW DAMPING (simplest first): Add `totalTorque.y -= C * angularVelocity.y * max(0, 1 - carSpeed/3)` near physics.js line 548. C=5000 N·m·s/rad. Physical basis: tire scrub drilling moment, absent from point-contact model. Test with a spin log, check if rl_om oscillation is damped.
2. DECOUPLE YAW-ROTATION FROM sLong (targeted): Separate translational vs rotational components of hubVel. For sLong/_longitudinalVelocity, use only translational part. The yaw component of longVelCur at RL (wY*halftrack) should not drive sLong.
3. T_YAW CLAMP (structural): After totalTorque accumulated, clamp d_omega_yaw to not exceed current omega_yaw magnitude (prevents yaw reversal in single step from tire forces).

Other open bugs:
- BUG-04: Physics instability at high tireDamping slider
- BUG-05: Body probe spheres overlap wheel hubs at low ride height
</remaining_work>

<decisions_made>
- SLIP_EPSILON 0.1→3.0: prevents sLat_ss going to meters at near-zero speed
- sLat SS cap: sLat = min(sLat, sLatSS) — removes spring energy overshoot
- sLat sign-clamp: resets sLat to SS when sign conflicts with latVelCur direction
- HUD: wheelDebug.vLong = omegaCur - longVelCur (was just longVelCur)
- Sky: 0x87ceeb, white lighting (user requested)
</decisions_made>

<blockers>
- BUG-03 still present: yaw oscillation driven by sLong/wheel-omega feedback
- src/physics.js and src/main.js modified, committed as WIP (39cf0f0)
</blockers>

## Required Reading
1. `src/physics.js` lines 296-460 — slip computation, clamps, Newton loop
2. `Logs/rangersim-log-1780557674779.json` — latest log showing rl_om oscillation
3. `data/ranger.js` lines 24-30 — inertia values (Izz=3700), wheelRadius=0.36m

## Infrastructure State
- Dev server: `npx serve .` from project root, http://localhost:3000
- No build step — ES modules loaded directly
- Logs in `Logs/` directory, toggle in lil-gui debug panel

<context>
Three debug iterations all targeting sLat. All legitimate — each narrowed the problem. But the surviving oscillation is now confirmed to be sLong-driven from wheel omega instability at rear wheels during yaw. The sLat fixes were correct and should be kept. The next fix is for yaw→sLong→T_yaw feedback.

Simplest next step: low-speed angular damping on yaw in physics.js totalTorque. Try C=5000, fade above 3 m/s. If insufficient, decouple yaw-rotation from longVelCur computation.
</context>

<next_action>
Add low-speed yaw damping to physics.js near line 548 (after tire forces accumulated, before angular velocity integration):
  const carSpeed = vehicleState.velocity.length()
  const yawDampScale = Math.max(0, 1 - carSpeed / 3.0)
  totalTorque.y -= 5000 * yawDampScale * vehicleState.angularVelocity.y

Test with a spin log. Check rl_om amplitude — if it still swings ±5+ rad/s, increase C or move to option 2 (decouple yaw-rotation from sLong).
</next_action>
