---
context: default
phase: none (freeform tuning session)
status: paused
last_updated: 2026-06-03T06:41:19Z
---

# No Blocking Constraints

_No structural failures encountered this session._

<current_state>
Phase 4.1 UAT is pending (all code committed as of 2e9dc71). A full freeform tuning/polish session was completed on top of that. All changes are uncommitted working tree modifications. No active phase plan.
</current_state>

<completed_work>

This session (all uncommitted, on top of Phase 4.1):

- **Camera stutter fix**: `src/camera.js` — replaced `LERP_FACTOR=0.08` constant lerp with framerate-independent `1 - exp(-CHASE_STIFFNESS * dt)`. `updateCamera` now takes `dt` param; `main.js` passes `frameTime`.
- **ranger.js tuning**: travel 0.25m both axles, damping→2500→3000, rear ARB→0, body width→1.66m, rear body offset→0.035m, brake→1700, tire damping sign bug fixed (was positive feedback, now `-strutCompVelI`).
- **Body collision overhaul**: Replaced 4-sphere spring-damper with 14-sphere impulse solver. Sphere probes: 2 front bumper, 2 rear bumper, 4 mid-axle undercarriage, 2 center sill, 4 roof. Impulse solver has Coulomb friction (μ=0.6), Baumgarte position correction. `queryVertexContacts` added to main.js (face-normal queries for future use, not yet wired to body contacts — body still uses sphere `queryContacts`).
- **Wheel spin fix**: `syncMeshesToState` was wiping spin by calling `quaternion.copy(steerQ)` after `rotation.x = angle`. Fixed: combined spin+steer as single quaternion multiply.
- **Debug overhaul**: slip-vector 2x2 canvas added (vLong/vLat per wheel from wheelDebug), travel bars resized 2x2, all 3 canvases moved to left side under HUD, backgrounds → `rgba(0,0,0,0.5)`, arrow scale halved, Fz readouts added to slip canvas.
- **Debug spheres**: 14 translucent orange spheres showing body contact probe positions in 3D scene (backtick toggle).
- **CG sliders**: cgHeight and weightFront (fwd/back) added to top of debug panel; weightFront onChange syncs weightRear.
- **Vehicle presets**: `data/vehicles.js` created with Ranger and 240sx presets. Dropdown in debug panel top; switching copies all non-underscore keys and refreshes all controllers.

</completed_work>

<remaining_work>

- Phase 4.1 UAT still needs to be run (was pending at session start):
  - `node test/assert-m4-02/04/05/06/07.mjs` must all exit 0
  - Browser: car spawns settled, slides on ramp, debug panel sliders + travel bars work
  - Logger: 4 `*_sc` fields at end of FIELDS
  - GLOSSARY: 7 new terms
- Commit all current working tree changes (they are all freeform, not part of the Phase 4.1 plan)
- Decide whether to close Phase 4.1 or keep open

</remaining_work>

<decisions_made>

- Body collision impulse solver: BODY_RESTITUTION=0.05, BODY_FRICTION_MU=0.6, BAUMGARTE_BETA=0.25, SLOP=5mm
- Tire damping sign was inverted (bug): `compressionVel = strutCompVelI` → `-strutCompVelI`
- Top body colliders aligned to visual box top (+0.4m in body space, not parameterized bodyHeight)
- Vehicle presets spread from RANGER_PARAMS at import time (clean snapshot before physics mutates internal keys)
- 240sx: 1190kg, wheelbase 2.474m, cgHeight 0.50m, arbRear=0, maxSteerAngle=0.65rad, stronger handbrake

</decisions_made>

<blockers>
- None
</blockers>

## Required Reading
1. `src/physics.js` lines ~466–530 — body contact impulse solver (new this session)
2. `src/suspension.js` lines ~315–325 — tire damping sign fix
3. `data/vehicles.js` — new file, vehicle presets

## Infrastructure State
- Dev server: not running (use `npx serve .` from project root)
- All changes uncommitted

<context>
Freeform polish session after Phase 4.1 code was complete. No plan was being followed. Changes were incremental tuning and feature additions driven by user requests.
</context>

<next_action>
Commit the working tree changes with a descriptive message, then run Phase 4.1 UAT (`node test/assert-m4-*.mjs`) to close the phase.
</next_action>
