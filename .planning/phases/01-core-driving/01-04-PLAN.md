---
phase: 01-core-driving
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01-PLAN.md
  - 01-02-PLAN.md
  - 01-03-PLAN.md
files_modified:
  - src/camera.js
  - src/debug.js
  - src/main.js
autonomous: false
requirements:
  - M1-10
  - M1-11
tags:
  - camera
  - debug
  - hud
  - lil-gui

must_haves:
  truths:
    - "Camera smoothly follows the car from behind and above; it does not snap but springs toward the chase goal position each frame"
    - "Pressing C toggles between chase mode and cockpit mode; cockpit mode places the viewpoint inside the cabin looking along the car's forward direction"
    - "HUD shows live speed in km/h updated every frame (speed readout wired in Plan 03 is confirmed live here)"
    - "Pressing backtick (`) opens/closes the lil-gui debug panel; the panel exposes lateralDampingCoeff and rollingResistanceCoeff sliders that take effect immediately"
    - "stats.js FPS counter remains visible (not hidden by debug panel toggle)"
  artifacts:
    - path: "src/camera.js"
      provides: "Spring-follow chase camera and cockpit mode; exports updateCamera"
      exports: ["updateCamera"]
    - path: "src/debug.js"
      provides: "lil-gui panel with backtick toggle; lateralDampingCoeff and rollingResistanceCoeff sliders; exports initDebug"
      exports: ["initDebug"]
    - path: "src/main.js"
      provides: "Imports updateCamera + initDebug; camera update called each render frame; C-key toggle wired; backtick key handled by debug.js"
      exports: []
  key_links:
    - from: "src/main.js"
      to: "src/camera.js"
      via: "import { updateCamera } from './camera.js'; called in render loop after syncMeshesToState"
      pattern: "import.*updateCamera.*from.*camera"
    - from: "src/main.js"
      to: "src/debug.js"
      via: "import { initDebug } from './debug.js'; called once at startup with camera and RANGER_PARAMS"
      pattern: "import.*initDebug.*from.*debug"
    - from: "src/debug.js"
      to: "lil-gui (bundled)"
      via: "import { GUI } from 'three/addons/libs/lil-gui.module.min.js'"
      pattern: "lil-gui.module.min.js"

user_setup: []
---

<objective>
Complete Phase 1 by delivering the camera system and debug panel. Create src/camera.js (spring-follow chase camera + cockpit toggle on C), create src/debug.js (lil-gui panel with backtick toggle exposing the two D-10 friction sliders), and update src/main.js to call both. After this plan, every Phase 1 success criterion is green.

Purpose: The car is already drivable from Plan 03. This plan adds the view experience and tuning surface. The debug panel is minimal (two sliders only — full debug menu is Phase 2/M2-05), but the backtick toggle and lil-gui wiring are established now so Phase 2 only adds sliders, not new infrastructure.

Output: User drives the car with spring-follow camera, toggles to cockpit with C, presses backtick to adjust friction feel in real time. Phase 1 is complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-driving/01-CONTEXT.md
@.planning/phases/01-core-driving/01-RESEARCH.md
@.planning/phases/01-core-driving/01-01-SUMMARY.md
@.planning/phases/01-core-driving/01-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts from Plans 01–03 that camera.js and debug.js consume. -->

From src/main.js (Plans 01+03):
  vehicleState.position: THREE.Vector3
  vehicleState.quaternion: THREE.Quaternion
  vehicleState.velocity: THREE.Vector3
  camera: THREE.PerspectiveCamera — currently static (Plan 01); Plan 04 replaces with spring-follow
  RANGER_PARAMS: imported from data/ranger.js — lateralDampingCoeff and rollingResistanceCoeff are mutable fields

From data/ranger.js (Plan 01):
  RANGER_PARAMS.lateralDampingCoeff = 4000      (N/(m/s)) — debug slider target (D-10)
  RANGER_PARAMS.rollingResistanceCoeff = 200     (N/(m/s)) — debug slider target (D-10)

From three/addons (importmap already set in index.html, Plan 01):
  import { GUI } from 'three/addons/libs/lil-gui.module.min.js'
  import Stats from 'three/addons/libs/stats.module.js'  (already in main.js; debug.js may read stats.dom reference)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/camera.js — spring-follow chase camera + cockpit toggle</name>
  <files>src/camera.js</files>
  <read_first>
    - .planning/phases/01-core-driving/01-RESEARCH.md §Pattern 6 (spring-follow chase camera — exact lerp formula, CHASE_OFFSET_LOCAL, LERP_FACTOR, cockpit offset) and §Pitfall 4 (frame-rate-independent lerp)
    - .planning/REQUIREMENTS.md M1-10 (chase + cockpit; C key toggle)
    - docs/GLOSSARY.md §Named Vectors (forward = (0,0,-1) rotated by vehicleState.quaternion)
    - .planning/phases/01-core-driving/01-CONTEXT.md (Claude's discretion: camera spring constants)
    - CLAUDE.md §What NOT to Use (no camera parenting to car mesh — lerp-to-goal only)
  </read_first>
  <action>
    Create src/camera.js exporting a single function updateCamera and a module-level cameraMode string ('chase' | 'cockpit') accessible via a getCameraMode getter.

    File header comment: "Camera module for RangerSim. Spring-follow chase mode and fixed-offset cockpit mode. C key toggles between modes. Does NOT parent camera to car mesh — parenting propagates jitter. Uses lerp-to-goal per RESEARCH.md §Pattern 6. See GLOSSARY.md §Named Vectors for quaternion-derived forward direction."

    Imports:
      import * as THREE from 'three'

    Module-level camera state:
      let cameraMode = 'chase'          // 'chase' | 'cockpit'

      // Chase mode constants (Claude's discretion for tuning — see CONTEXT.md)
      const CHASE_OFFSET_LOCAL = new THREE.Vector3(0, 2.5, 6.0)  // body-space: behind (+Z) and above (+Y)
      const LERP_FACTOR = 0.08   // ~8% per frame at 60fps; see Pitfall 4 note below

    Register C-key listener at module load:
      document.addEventListener('keydown', e => {
        if (e.key.toLowerCase() === 'c') {
          cameraMode = cameraMode === 'chase' ? 'cockpit' : 'chase'
        }
      })

    Export — updateCamera(camera, vehicleState):
    JSDoc:
      @param {THREE.PerspectiveCamera} camera — Three.js camera; mutated in-place (position and quaternion/lookAt)
      @param {object} vehicleState — {position: THREE.Vector3, quaternion: THREE.Quaternion, velocity: THREE.Vector3}
      @returns {void}

    Implementation (RESEARCH.md §Pattern 6):

    Chase mode:
      // Goal position: CHASE_OFFSET_LOCAL rotated to world space by vehicle quaternion, then offset from vehicle CG
      const goalOffset = CHASE_OFFSET_LOCAL.clone().applyQuaternion(vehicleState.quaternion)
      const goalPos = vehicleState.position.clone().add(goalOffset)
      camera.position.lerp(goalPos, LERP_FACTOR)
      camera.lookAt(vehicleState.position)
      Comment: "Pitfall 4: LERP_FACTOR is frame-rate dependent. At target 60fps the feel is intentional. A frame-rate-independent version would be: 1 - Math.exp(-5 * dt). Claude's discretion (CONTEXT.md) — 0.08 is the default; expose as debug constant if needed."

    Cockpit mode:
      // Fixed offset inside cabin (body space → world space)
      // RESEARCH.md §Open Questions #2: offset ~(0, 0.8, 0.3) in body space — slightly above and forward of CG
      const COCKPIT_OFFSET_LOCAL = new THREE.Vector3(0, 0.8, 0.3)  // body-space
      const cockpitOffset = COCKPIT_OFFSET_LOCAL.clone().applyQuaternion(vehicleState.quaternion)
      camera.position.copy(vehicleState.position).add(cockpitOffset)
      // Look along forward direction (body -Z in world)
      const forward = new THREE.Vector3(0, 0, -1).applyQuaternion(vehicleState.quaternion)
      const lookTarget = vehicleState.position.clone().add(cockpitOffset).add(forward)
      camera.lookAt(lookTarget)

    Export — getCameraMode():
      return cameraMode
    (Exported for Plan 02+ debug panel if needed; not required by Phase 1 success criteria.)
  </action>
  <verify>
    <automated>test -f src/camera.js &amp;&amp; grep -q "export function updateCamera" src/camera.js &amp;&amp; grep -q "lerp" src/camera.js &amp;&amp; grep -q "applyQuaternion" src/camera.js &amp;&amp; grep -q "lookAt" src/camera.js &amp;&amp; grep -q "CHASE_OFFSET_LOCAL" src/camera.js &amp;&amp; grep -q "cockpit" src/camera.js &amp;&amp; ! grep -q "backup1" src/camera.js &amp;&amp; echo "OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - src/camera.js exists and exports updateCamera.
    - File contains CHASE_OFFSET_LOCAL offset vector (behind and above the car body space).
    - lerp call present: camera.position.lerp(goalPos, LERP_FACTOR).
    - lookAt call present for both chase and cockpit modes.
    - applyQuaternion used to rotate body-space offsets to world space (not Euler/matrix math).
    - C-key listener toggles cameraMode between 'chase' and 'cockpit'.
    - File does NOT contain "backup1" (D-03).
    - File does NOT parent camera to a mesh or use THREE.Object3D.add(camera) on the car body.
  </acceptance_criteria>
  <done>
    Camera module complete. Plan 04 Task 3 imports it into main.js and removes the static camera position. Pressing C in the running app toggles views.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/debug.js — lil-gui panel with backtick toggle, D-10 friction sliders</name>
  <files>src/debug.js</files>
  <read_first>
    - .planning/phases/01-core-driving/01-CONTEXT.md (D-10: lateralDampingCoeff and rollingResistanceCoeff sliders; Claude's discretion on slider ranges)
    - .planning/phases/01-core-driving/01-RESEARCH.md §Standard Stack §Supporting (lil-gui import path: 'three/addons/libs/lil-gui.module.min.js')
    - .planning/REQUIREMENTS.md M2-05 (full debug menu is Phase 2 — Phase 1 just needs the two D-10 sliders and backtick toggle)
    - CLAUDE.md §What NOT to Use (no dat.GUI — lil-gui only)
  </read_first>
  <action>
    Create src/debug.js exporting initDebug. This module owns the lil-gui panel, the backtick toggle key, and the two D-10 friction sliders. Phase 2 expands this file with additional sliders (M2-05) — the toggle infrastructure is established now.

    File header comment: "Debug panel for RangerSim. Phase 1: backtick (`) toggle, lateralDampingCoeff and rollingResistanceCoeff sliders (D-10). Phase 2 adds full physics parameter panel (M2-05, M2-06). Uses lil-gui (bundled in three/addons — NOT dat.GUI per CLAUDE.md)."

    Import:
      import { GUI } from 'three/addons/libs/lil-gui.module.min.js'

    Export — initDebug(params):
    JSDoc:
      @param {object} params — RANGER_PARAMS reference (NOT a copy). Slider mutations write directly to this object, which is the same object physics.js reads each step — changes take effect immediately (M2-06 behavior established in Phase 1 for these two sliders).
      @returns {GUI} the lil-gui GUI instance (for use by Phase 2 when adding more controllers)

    Implementation:

    1. Create GUI instance:
       const gui = new GUI({ title: 'RangerSim Debug' })
       gui.domElement.style.display = 'none'   // hidden by default; backtick reveals it

    2. Add Phase 1 friction sliders (D-10). Ranges are Claude's discretion — use ranges that allow meaningful feel exploration:
       gui.add(params, 'lateralDampingCoeff', 500, 10000, 100).name('Lateral Damping (N/m·s)')
       gui.add(params, 'rollingResistanceCoeff', 10, 1000, 10).name('Rolling Resistance (N/m·s)')
       Comment above: "D-10: Phase 1 friction tuning. Range (500–10000) and (10–1000) chosen for feel; expose as Claude's discretion per CONTEXT.md. Both fields live in data/ranger.js, mutated live here."

    3. Backtick toggle listener:
       document.addEventListener('keydown', e => {
         if (e.key === '`') {
           const hidden = gui.domElement.style.display === 'none'
           gui.domElement.style.display = hidden ? '' : 'none'
         }
       })

    4. Return the GUI instance so Phase 2 can call gui.addFolder(...) on the returned value without re-importing or re-creating:
       return gui
  </action>
  <verify>
    <automated>test -f src/debug.js &amp;&amp; grep -q "export function initDebug" src/debug.js &amp;&amp; grep -q "lil-gui.module.min.js" src/debug.js &amp;&amp; grep -q "lateralDampingCoeff" src/debug.js &amp;&amp; grep -q "rollingResistanceCoeff" src/debug.js &amp;&amp; grep -q "backtick\|'\`'" src/debug.js &amp;&amp; ! grep -q "dat.GUI\|dat\.gui" src/debug.js &amp;&amp; ! grep -q "backup1" src/debug.js &amp;&amp; echo "OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - src/debug.js exists and exports initDebug.
    - Import uses 'three/addons/libs/lil-gui.module.min.js' (NOT dat.GUI).
    - initDebug(params) adds two controllers: params.lateralDampingCoeff and params.rollingResistanceCoeff.
    - Both sliders use gui.add(params, 'fieldName', min, max, step) — mutations go directly to the params object passed in.
    - gui.domElement.style.display is 'none' by default (panel hidden at startup).
    - Backtick listener toggles display between 'none' and '' (visible).
    - initDebug returns the GUI instance.
    - File does NOT contain "dat.GUI" or "dat.gui" (CLAUDE.md).
    - File does NOT contain "backup1" (D-03).
  </acceptance_criteria>
  <done>
    Debug panel wired. Phase 2 imports initDebug and calls gui.addFolder() on the returned instance to expand the panel without changing the toggle or slider infrastructure.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (Checkpoint): Wire camera + debug into main.js and verify full Phase 1 experience</name>

  <what_automated>
    Before this checkpoint, edit src/main.js to:

    1. Add two imports after the existing vehicle.js/physics.js imports:
       import { updateCamera } from './camera.js'
       import { initDebug } from './debug.js'

    2. Replace the static camera setup from Plan 01 (camera.position.set(0, 4, 10); camera.lookAt(...)) by removing those two lines. The spring-follow camera.js will position the camera dynamically each frame.

    3. After the scene bootstrap block (before the game loop), call:
       initDebug(RANGER_PARAMS)
       Comment: "D-10: passes mutable RANGER_PARAMS ref so sliders write directly to the object physics.js reads."

    4. In the render loop, after the syncMeshesToState call and the HUD speed update, add:
       updateCamera(camera, vehicleState)

    5. Verify the main.js file now contains imports for all four src/ modules:
       - import { stepPhysics } from './physics.js'
       - import { updateVehicle, SPAWN_STATE } from './vehicle.js'
       - import { updateCamera } from './camera.js'
       - import { initDebug } from './debug.js'

    Automated check (run after edits):
    grep -q "import.*updateCamera" src/main.js &amp;&amp; grep -q "import.*initDebug" src/main.js &amp;&amp; grep -q "updateCamera(camera, vehicleState)" src/main.js &amp;&amp; grep -q "initDebug(RANGER_PARAMS)" src/main.js &amp;&amp; echo "WIRED"
  </what_automated>

  <how_to_verify>
    1. Start a local HTTP server: python3 -m http.server 8000 (from the project root)
    2. Open http://localhost:8000/index.html in a browser (Chrome or Firefox)
    3. Check the browser console — it must show no errors and must show "THREE.REVISION 184"

    4. Scene check (Phase 1 criterion 1):
       - Dark 3D scene visible with blue box car body, four black wheel cylinders, lit grid
       - stats.js FPS panel in top-left corner

    5. Drive check (Phase 1 criterion 2):
       - Press W — car moves forward, HUD speed number increases
       - Hold W and press A — car curves left
       - Release A — steering decays back toward center
       - Wheels visually spin while moving

    6. Camera check (Phase 1 criterion 5):
       - Camera follows the car from behind (spring-follow — not snapping)
       - Press C — view switches to cockpit (inside the car, looking forward)
       - Press C again — back to chase mode

    7. Reset check (Phase 1 criterion 3):
       - Drive the car away from origin
       - Press R — car snaps back to spawn; camera reorients to follow from behind

    8. HUD check (Phase 1 criterion 4):
       - Speed value (bottom-left or top-left green text) updates live while driving
       - At rest: shows 0.0; while moving: shows a positive number in km/h

    9. Debug panel check:
       - Press ` (backtick) — lil-gui panel appears in top-right corner
       - Adjust the "Lateral Damping" slider — car feel changes immediately (more or less lateral grip)
       - Adjust "Rolling Resistance" slider — car decelerates faster or slower on release
       - Press ` again — panel hides

    10. No-gimbal-lock check:
        - Drive the car and intentionally try to roll it (ramp not available yet, but heavy turning should not produce Euler-related rotation artifacts)
        - Console: no NaN in vehicleState fields after driving (type vehicleState.position in console)
  </how_to_verify>

  <resume_signal>Type "approved" if all 10 verification steps pass, or describe any failures to fix.</resume_signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard input → cameraMode | 'c' and '`' key events handled; only string comparison, no eval, no user data |
| lil-gui sliders → RANGER_PARAMS | Direct object mutation; RANGER_PARAMS is a module-level constant but not frozen (Plan 01 intentionally did not freeze it — D-10 requires mutability). No bounds overflow possible: slider min/max enforced by lil-gui. |
| Browser ↔ CDN | lil-gui loaded from same three@0.184.0 CDN as Three.js; same trust as T-01-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | lil-gui lateralDampingCoeff set to 0 (zero lateral damping → no lateral grip → car spins endlessly) | accept | Slider minimum is 500, not 0. lil-gui enforces the min clamp. Developer may still set 0 via console, which is acceptable for a physics sandbox. |
| T-04-02 | Tampering | RANGER_PARAMS mutated by lil-gui and not restored on R-key reset | accept | By design: debug slider changes persist across resets. The reset only restores vehicleState (position/velocity/orientation), not params. User expectation: slider values are a "session setting". |
| T-04-03 | Denial of Service | Camera lerp produces NaN if vehicleState.position is NaN (physics exploded) | mitigate | Physics ground constraint prevents NaN propagation for typical driving. If NaN occurs, console will show it; the lerp will produce NaN camera but page won't crash. Acceptable for Phase 1; Phase 2 scenario runner adds NaN detection. |
| T-04-04 | Information Disclosure | lil-gui panel exposes RANGER_PARAMS field names/values in browser | accept | All code is open-source client-side; no PII, no secrets. |
</threat_model>

<verification>
Phase 1 gate (all five criteria from ROADMAP.md must be green after this plan):

1. User can open index.html and see a 3D scene with car and wheels on a grid — COVERED (Plan 01)
2. User can drive W/S/A/D; orientation tracks correctly through 360° without gimbal lock — COVERED (Plan 03 physics, verified in checkpoint)
3. User can press R to reset the car to spawn position — COVERED (Plan 03, verified in checkpoint)
4. User sees live speed readout (km/h) in HUD and stable FPS counter in debug mode — COVERED (Plan 03 HUD + Plan 01 stats.js, verified in checkpoint)
5. User can toggle chase and cockpit camera modes with C — COVERED (this plan, verified in checkpoint)

Static file checks (before checkpoint):
- test -f src/camera.js src/debug.js
- grep -q "export function updateCamera" src/camera.js
- grep -q "export function initDebug" src/debug.js
- grep -q "lil-gui.module.min.js" src/debug.js
- grep -q "import.*updateCamera" src/main.js
- grep -q "import.*initDebug" src/main.js
- grep -q "updateCamera(camera, vehicleState)" src/main.js
- grep -q "initDebug(RANGER_PARAMS)" src/main.js
</verification>

<success_criteria>
- src/camera.js and src/debug.js committed; src/main.js updated with all four src/ module imports.
- Spring-follow chase camera visible in browser; C key toggles cockpit mode.
- Backtick opens/closes lil-gui panel with two friction sliders that affect feel immediately.
- Phase 1 human checkpoint passes (all 10 steps verified by user).
- Phase 1 complete: all M1-01 through M1-15 requirements covered across Plans 01–04.
</success_criteria>

<output>
After completion, create .planning/phases/01-core-driving/01-04-SUMMARY.md capturing:
- Camera spring constants used (CHASE_OFFSET_LOCAL, LERP_FACTOR) and feel notes from checkpoint.
- Debug slider ranges chosen and rationale.
- Confirmation that all five Phase 1 success criteria passed the human checkpoint.
- Any adjustments made during the checkpoint (e.g., steer feel, camera spring tuning).
- Phase 1 completion status: all FOUND-* and M1-* requirements satisfied.
</output>
