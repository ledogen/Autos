---
phase: 04.1-body-frame-suspension
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/main.js
  - src/debug.js
  - src/logger.js
  - src/physics.js
  - docs/GLOSSARY.md
autonomous: false
requirements:
  - D-07
  - D-12
  - D-13
  - D-14
tags:
  - visual
  - ui
  - logger
  - housekeeping
must_haves:
  truths:
    - "Wheel meshes track the derived hub world position via inverse-quaternion transform; pitched body does not exaggerate wheel motion via axle-Z projection"
    - "Debug panel exposes the five new sliders: Front Travel, Rear Travel, Front Body Offset, Rear Body Offset, Bump Stop Stiffness"
    - "Debug panel shows a 4-corner travel-bar visualization with fill proportional to strutComp position in the travel window"
    - "Logger captures per-corner strutComp as appended FIELDS entries (fl_sc, fr_sc, rl_sc, rr_sc); existing log fields unchanged"
    - "wheelDebug[i].strutComp is populated each step so the logger captureFrame call sees a real value"
    - "docs/GLOSSARY.md documents strut axis, strutComp, bump stop, droop stop, body offset terms"
  artifacts:
    - path: "src/main.js syncMeshesToState"
      provides: "Wheel mesh world target derived from D-02; inverse-transformed into carGroup local space"
      contains: "applyQuaternion"
    - path: "src/debug.js Suspension folder"
      provides: "Five new sliders + travel-bar canvas + backtick toggle integration"
      contains: "bumpStopStiffness"
    - path: "src/logger.js"
      provides: "fl_sc/fr_sc/rl_sc/rr_sc appended to FIELDS and captureFrame row"
      contains: "fl_sc"
    - path: "src/physics.js"
      provides: "wheelDebug[i].strutComp populated each step"
      contains: "strutComp"
    - path: "docs/GLOSSARY.md"
      provides: "Phase 4.1 terminology definitions"
      contains: "strut axis"
  key_links:
    - from: "src/main.js syncMeshesToState"
      to: "vehicleState.strutComp[i]"
      via: "D-02 world-target derivation + inverse-rotation by carGroup.quaternion"
      pattern: "applyQuaternion"
    - from: "src/debug.js updateTravelBar"
      to: "vehicleState.strutComp / params.suspensionTravel*"
      via: "drawn per frame from inside the main render loop"
      pattern: "strutComp"
    - from: "src/logger.js captureFrame"
      to: "wheelDebug[i].strutComp"
      via: "appended at end of frame row"
      pattern: "strutComp"
---

## Phase Goal

**As a** developer tuning the new suspension parameters and watching the car climb a ramp, **I want to** see wheels visually stuck to the ramp face (no pitch-projection ghosting), tune travel and bump stop stiffness via debug sliders, watch 4-corner travel bars while driving, and capture strutComp in scenario logs, **so that** Phase 4.1 closes with both the physics fix and the user-facing instrumentation in place.

<objective>
Wave 3 polish slice — wires the user-facing surfaces to the Plan 02 physics fix. Wheel mesh D-07 visual binding, debug panel D-13/D-14 sliders and travel bars, logger D-12 strutComp fields, and the per-phase housekeeping audit (GLOSSARY, slider audit, HUD verify).

Purpose: After Plan 02 the physics is correct but invisible. Plan 03 makes the new state and parameters tunable and observable, completes the per-phase housekeeping convention from Phase 2 D-13, and adds the glossary entries needed for future LLM sessions to understand the strut-axis vocabulary.

Output: src/main.js (wheel mesh visual fix only), src/debug.js (sliders + travel bar), src/logger.js (FIELDS + captureFrame append), src/physics.js (wheelDebug populated), docs/GLOSSARY.md (new terminology).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-body-frame-suspension/04.1-CONTEXT.md
@.planning/phases/04.1-body-frame-suspension/04.1-RESEARCH.md
@.planning/phases/04.1-body-frame-suspension/04.1-PATTERNS.md
@.planning/phases/04.1-body-frame-suspension/04.1-VALIDATION.md
@.planning/phases/04.1-body-frame-suspension/04.1-02-SUMMARY.md

@src/main.js
@src/debug.js
@src/logger.js
@src/physics.js
@docs/GLOSSARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix syncMeshesToState wheel binding in src/main.js (D-07) + populate wheelDebug.strutComp in src/physics.js</name>
  <files>src/main.js, src/physics.js</files>
  <read_first>
    - src/main.js (lines 195-260 — wheelLocalOffsets at 198+, hubYRest at 211, wheelMeshes creation at 213, syncMeshesToState at ~254-259)
    - src/physics.js (Step 3 loop where wheelDebug is populated — grep for "wheelDebug[i]" or "wheelDebug =")
    - .planning/phases/04.1-body-frame-suspension/04.1-PATTERNS.md (src/main.js Touch point 4 in full)
    - .planning/phases/04.1-body-frame-suspension/04.1-RESEARCH.md (Pitfall 7 — wheel mesh X/Z must also be derived; Code Examples — wheel_mesh_fix)
    - .planning/phases/04.1-body-frame-suspension/04.1-CONTEXT.md (D-07, D-12)
  </read_first>
  <action>
src/main.js syncMeshesToState (lines ~254-259): replace the existing wheelMeshes[i].position.y = wheelLocalOffsets[i].y + (state.hubY[i] - hubYRest[i]) block. Per D-07 the wheel mesh world target IS the derived hub world position from D-02. Per Pitfall 7 the X and Z must also come from this derivation (not from the hardcoded wheelLocalOffsets[i].x/z) so that suspensionBodyOffsetFront/Rear changes don't desync wheels from hubs.

Specific replacement:
  - isFrontMesh = i < 2
  - L_S_mesh = isFrontMesh ? RANGER_PARAMS.suspensionRestLengthFront : RANGER_PARAMS.suspensionRestLengthRear
  - strutLen_i = L_S_mesh - (state.strutComp?.[i] ?? 0)
  - carQ = state.quaternion (already THREE.Quaternion per existing main.js line 93)
  - body_down_mesh = new THREE.Vector3(0,-1,0).applyQuaternion(carQ)
  - mountLocal = wheelLocalOffsets[i].clone(); then per D-08 inject the body offset into the Y component so the visual mount tracks the physics mount: mountLocal.y = -(RANGER_PARAMS.cgHeight - RANGER_PARAMS.wheelRadius) + (isFrontMesh ? (RANGER_PARAMS.suspensionBodyOffsetFront || 0) : (RANGER_PARAMS.suspensionBodyOffsetRear || 0)). This MUST match the localY formula in suspension.js Plan 02 Task 2 (C) exactly — otherwise the wheel visual drifts off the physics hub when the offset slider is non-zero
  - rMount_mesh = mountLocal.clone().applyQuaternion(carQ)
  - mountWorld = state.position.clone().add(rMount_mesh)
  - hubWorld = mountWorld.clone().addScaledVector(body_down_mesh, strutLen_i)
  - hubLocal = hubWorld.clone().sub(state.position).applyQuaternion(carQ.clone().invert())
  - wheelMeshes[i].position.copy(hubLocal)

Remove the now-unused hubYRest constant declaration at line 211 (it referenced _spawnEq.hubY which no longer exists). Keep wheelLocalOffsets — still used by the wheel mesh CREATION block at line 213 (initial mesh placement). The spin and steer quaternions applied to each wheel mesh elsewhere in syncMeshesToState are unchanged.

src/physics.js wheelDebug population — find the existing Step 3 loop or wherever wheelDebug[i].fz is written (grep for "wheelDebug[i].fz"). In the same block where fz, fy, sa, omega, etc. are set, add:
  vehicleState.wheelDebug[i].strutComp = vehicleState.strutComp[i]
This is the value captureFrame reads for the logger fields. Use the post-substep strutComp (the current value after stepSuspensionSubsteps returns) so the logged value reflects the displayed state, not the pre-substep value.
  </action>
  <verify>
    <automated>grep -c "applyQuaternion\|hubLocal\|body_down_mesh" src/main.js | grep -v "^0$"</automated>
  </verify>
  <done>src/main.js syncMeshesToState builds hubWorld from D-02 derivation, inverse-rotates into body-local space via carQ.clone().invert(), and copies result to wheelMeshes[i].position. The old hubYRest constant is removed. src/physics.js Step 3 wheelDebug block sets wheelDebug[i].strutComp = vehicleState.strutComp[i] for all four corners. No reference to state.hubY or hubYRest remains in src/main.js.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add D-14 sliders + D-13 travel bar visualization to src/debug.js</name>
  <files>src/debug.js</files>
  <read_first>
    - src/debug.js (full file — Suspension folder at lines ~75-83, Pacejka canvas creation at ~91-96, backtick toggle at ~100-106, slider audit comments)
    - .planning/phases/04.1-body-frame-suspension/04.1-PATTERNS.md (src/debug.js section in full — slider pattern + travel bar canvas pattern + backtick toggle extension)
    - .planning/phases/04.1-body-frame-suspension/04.1-CONTEXT.md (D-13 — travel bar formula; D-14 — slider list)
  </read_first>
  <action>
Sliders (D-14): inside the existing suspFolder, after the existing arbStiffnessRear slider, add five new sliders following the existing add().name() pattern. Use the lil-gui (min, max, step) convention:

  suspFolder.add(params, 'suspensionTravelFront',     0.05,  0.40,   0.01).name('Front Travel (m)')
  suspFolder.add(params, 'suspensionTravelRear',      0.05,  0.40,   0.01).name('Rear Travel (m)')
  suspFolder.add(params, 'suspensionBodyOffsetFront', -0.10, 0.10,   0.005).name('Front Body Offset (m)')
  suspFolder.add(params, 'suspensionBodyOffsetRear',  -0.10, 0.10,   0.005).name('Rear Body Offset (m)')
  suspFolder.add(params, 'bumpStopStiffness',         10000, 500000, 5000).name('Bump Stop Stiffness (N/m)')

Travel bar canvas (D-13): create a new canvas alongside plotCanvas. Suggested layout per Claude's Discretion: 4 vertical bars (one per corner FL, FR, RL, RR) rendered to a small canvas (e.g. 160x140 px) positioned at top:240, right:320, with display:none initially (synced with backtick toggle). Append to document.body. Expose the canvas reference at module scope so it can be drawn each frame.

Add a new drawing function updateTravelBars(vehicleState, params) called from initDebug's hookless return or from main.js animate(). Bar fill formula per D-13:
  fill[i] = (strutComp[i] + droopRange) / (travel[i] + droopRange)
where droopRange is the strut compression at full droop. Simplification per RESEARCH.md: droop stop engages at strutComp <= 0, so droopRange = 0 and the formula reduces to fill = max(0, strutComp[i] / travel[i]) clamped to [0, 1.05]. Render each bar with a background rectangle (full height), a filled rectangle proportional to fill, and a corner label (FL/FR/RL/RR). Color hint per Claude's Discretion: green at fill < 0.7, yellow at 0.7..0.95, red at fill >= 1.0 (bump stop engaged). Use plain canvas 2D context — no new libraries.

Backtick toggle (line ~100-106): extend the existing keydown handler so the new travelCanvas.style.display tracks gui.domElement.style.display alongside plotCanvas.

Slider audit per Phase 2 D-13 housekeeping convention: scan the existing suspFolder for any param key in data/ranger.js that lacks a slider, and confirm the five new sliders cover all D-08 params. The DROOP_STOP_STIFFNESS constant is deliberately NOT exposed (Claude's Discretion per CONTEXT.md). Note this in a comment above the slider block.

Export updateTravelBars from src/debug.js so main.js can call it inside the render loop. If initDebug currently returns nothing, refactor to return an object that includes update functions (consistent with existing pattern if any).
  </action>
  <verify>
    <automated>grep -cE "suspensionTravelFront|suspensionTravelRear|suspensionBodyOffsetFront|suspensionBodyOffsetRear|bumpStopStiffness" src/debug.js && grep -c "updateTravelBars\|travelCanvas" src/debug.js</automated>
  </verify>
  <done>src/debug.js add() calls for all five D-14 sliders are present (grep returns >= 5). A travel-bar canvas is created, appended to document.body, and its display toggles with the backtick handler alongside the existing plotCanvas. updateTravelBars draws four corner bars proportional to strutComp/travel per D-13.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append strutComp fields to src/logger.js (D-12) + main.js animate loop calls updateTravelBars + docs/GLOSSARY.md entries</name>
  <files>src/logger.js, src/main.js, docs/GLOSSARY.md</files>
  <read_first>
    - src/logger.js (FIELDS array lines 33-49, captureFrame at lines 130-160)
    - src/main.js (animate loop — find the per-frame block that calls syncMeshesToState or stats.update; the updateTravelBars call lives there)
    - docs/GLOSSARY.md (read existing entries to match formatting; look for "hub" or "suspension" sections)
    - .planning/phases/04.1-body-frame-suspension/04.1-PATTERNS.md (src/logger.js section — append-only convention)
    - .planning/phases/04.1-body-frame-suspension/04.1-CONTEXT.md (D-12 — strutComp logged; D-13 housekeeping; "Add entries: strut axis, body-frame strut compression, bump stop, droop stop, body offset")
  </read_first>
  <action>
src/logger.js FIELDS array: append four new string entries at the END of the array (NEVER reorder existing entries — Phase 2 D-13 constraint): 'fl_sc', 'fr_sc', 'rl_sc', 'rr_sc'. Prepend a comment line:
  // Phase 4.1 additions — per-wheel strut compression (D-12), 2026-06-01

src/logger.js captureFrame: in the _frames.push([...]) call, append four new values at the end of the row in the same order as the FIELDS entries:
  fl.strutComp ?? 0, fr.strutComp ?? 0, rl.strutComp ?? 0, rr.strutComp ?? 0
(fl/fr/rl/rr are the per-wheel debug shorthand already used in captureFrame for fz; reuse those bindings.) Prepend the same comment line as in FIELDS for traceability.

src/main.js animate loop: find the per-frame block that calls syncMeshesToState or stats.update or renderer.render. Add a call to updateTravelBars(vehicleState, RANGER_PARAMS) imported from './debug.js'. Place it after the physics step and before renderer.render so the bars reflect the most recent strut state.

docs/GLOSSARY.md: append a new section (or new entries inside an existing Suspension section) defining:
  - strut axis — the body-local up/down axis along which the strut translates. Body-fixed; rotates with the body quaternion.
  - strutComp — strut compression from rest length [m]; positive = compressed below rest. State per corner.
  - strutCompVel — rate of strut compression [m/s]; positive = compressing.
  - bump stop — penalty spring engaging when strutComp >= suspensionTravel; zero force at engagement boundary, linear past it. Stiffness exposed as slider.
  - droop stop — penalty spring engaging when strutComp <= 0 (fully extended); fixed constant DROOP_STOP_STIFFNESS, no slider.
  - body offset (suspensionBodyOffsetFront/Rear) — Y shift of the mount point in body space; controls ride height.
  - hub slider — conceptual model: the hub is a rigid 1D slider in body-Y. The only DOF is strutComp; lateral forces on the hub transmit instantly to the body through the rail constraint.

Match the existing GLOSSARY.md style for definitions (term — definition, one line each, or term: definition depending on existing style).
  </action>
  <verify>
    <automated>grep -cE "'fl_sc'|'fr_sc'|'rl_sc'|'rr_sc'" src/logger.js</automated>
  </verify>
  <done>src/logger.js FIELDS contains all four new entries (grep returns >= 4); captureFrame appends fl.strutComp through rr.strutComp at the end of the row. src/main.js animate loop calls updateTravelBars per frame. docs/GLOSSARY.md contains the seven new term definitions in formatting consistent with existing entries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual + UI verification + final D-17 phase-close regression</name>
  <what-built>
    Phase 4.1 user-facing surface complete:
    - Wheel meshes track derived hub world position (D-07)
    - Debug panel has 5 new sliders and 4-corner travel bars (D-13, D-14)
    - Logger captures strutComp per corner (D-12)
    - docs/GLOSSARY.md documents new terminology
  </what-built>
  <how-to-verify>
    1. Browser sanity checks:
       a) Open sim. Car spawns settled, no console errors.
       b) Drive over flat ground — wheels rotate correctly, no visible wheel/body desync.
       c) Drive slowly up the ramp (forward into z = -15 region). Confirm each wheel visually stays in contact with the ramp face — no phantom vertical motion from body pitch. Compare against a pre-Plan-03 screenshot if available: front wheels used to lift ~27 cm above the ramp face at 10 degrees pitch.
       d) Park the car on the ramp under no input. It should slide downhill (per Plan 02 fix). The wheels should remain visually attached during the slide.
       e) Lift one wheel off the ground (drive aggressively into a corner). Confirm the airborne wheel stops at droop stop position (no infinite extension visible).
       f) Hit a curb hard. Confirm sharp bump-stop deceleration without instability (no NaN, no explosion).

    2. Debug panel verification:
       a) Press backtick. Debug panel opens. Suspension folder contains: Front Stiffness, Rear Stiffness, Front Damping, Rear Damping, Front Rest Length, Rear Rest Length, Front ARB, Rear ARB, Front Travel, Rear Travel, Front Body Offset, Rear Body Offset, Bump Stop Stiffness.
       b) 4-corner travel bar canvas is visible. Drive the car around. Confirm bars move as suspension compresses/extends. Bar colors change to red when bump stop engages.
       c) Adjust Bump Stop Stiffness slider while driving. Confirm the suspension feel changes immediately (no restart).
       d) Adjust Front Body Offset to +0.05 m. Confirm the front of the car visibly rises (ride height changes).

    3. Logger verification:
       a) Press backslash to start recording. Drive briefly. Press backslash again to stop and download.
       b) Open the downloaded JSON. Confirm "fl_sc", "fr_sc", "rl_sc", "rr_sc" appear as the LAST four entries in the fields array.
       c) Confirm the frame data rows have four extra numeric values at the end (matching strutComp values).
       d) Confirm existing fields (fl_fz etc.) are unchanged in position and value (regression).

    4. D-17 phase closure — run the full assertion suite one more time end-to-end:
       node test/assert-m4-02-asymmetric-bump.mjs Logs/&lt;m4-02-log&gt;.json
       node test/assert-m4-04-static-vs-braking.mjs Logs/&lt;m4-04-log&gt;.json
       node test/assert-m4-05-wheel-lift-ramp.mjs   Logs/&lt;m4-05-log&gt;.json
       node test/assert-m4-06-bump-response.mjs    Logs/&lt;m4-06-log&gt;.json
       node test/assert-m4-07-ramp-slide.mjs       Logs/&lt;m4-07-log&gt;.json
       All five MUST exit 0.

    5. GLOSSARY check: open docs/GLOSSARY.md and confirm the seven new terms are present and formatted consistently with the existing entries.

    If any check fails, describe which step and paste the relevant output.
  </how-to-verify>
  <read_first>
    - .planning/phases/04.1-body-frame-suspension/04.1-CONTEXT.md (Success Criteria from ROADMAP — 8 truths)
    - .planning/phases/04.1-body-frame-suspension/04.1-VALIDATION.md (Manual-Only Verifications section)
  </read_first>
  <acceptance_criteria>
    - Wheels visually track ramp face during slow ramp climb (no phantom lift)
    - 4-corner travel bars in debug panel respond to driving in real time
    - All 5 new sliders functional and tune the physics live
    - Logger downloaded JSON has fl_sc/fr_sc/rl_sc/rr_sc as the last four field entries
    - All 5 m4-* assertion scripts exit 0
    - docs/GLOSSARY.md has 7 new term entries (strut axis, strutComp, strutCompVel, bump stop, droop stop, body offset, hub slider)
    - No console errors during any scenario
  </acceptance_criteria>
  <resume-signal>Type "approved" to close Phase 4.1; otherwise describe failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lil-gui slider input -> RANGER_PARAMS | User numeric input bound to physics params |
| Browser canvas 2D -> document.body | New DOM node injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-06 | Tampering | New D-14 sliders | accept | Slider min/max bounds enforced by lil-gui; physics stability verified in RESEARCH.md A3 at upper bound |
| T-04.1-07 | Denial of Service | Travel bar canvas redraw per frame | accept | 160x140 canvas redraw is < 0.1 ms; well within 60fps budget |
</threat_model>

<verification>
- Manual browser checks per Task 4 step 1.
- Debug panel functional per Task 4 step 2.
- Logger schema regression per Task 4 step 3.
- Full m4-* suite green per Task 4 step 4 (D-17 closure).
- GLOSSARY entries present per Task 4 step 5.
</verification>

<success_criteria>
- D-07 wheel visual fix observable in browser on ramp.
- D-13 4-corner travel bars functional and responsive.
- D-14 all 5 sliders present, named correctly, and live-tunable.
- D-12 strutComp logged per corner; FIELDS append-only convention honored.
- D-17 full m4-* suite green.
- docs/GLOSSARY.md captures the new vocabulary for future LLM sessions.
- No remaining references to hubY, hubVy, or hubYRest anywhere in src/.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-body-frame-suspension/04.1-03-SUMMARY.md`.
</output>
