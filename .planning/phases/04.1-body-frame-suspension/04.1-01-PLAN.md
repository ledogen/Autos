---
phase: 04.1-body-frame-suspension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/assert-m4-07-ramp-slide.mjs
  - scenarios/m4-07-ramp-slide.json
autonomous: true
requirements:
  - D-17
  - D-06
tags:
  - validation
  - wave-0
  - ramp-slide
must_haves:
  truths:
    - "Running m4-07 assertion script against the existing (pre-fix) Phase 4 codebase fails (car does NOT slide on ramp — gate is sensitive)"
    - "Scenario file places the car at rest on the existing 10-degree ramp midpoint at z≈-17.5, y chosen so wheels are in contact with ramp face"
    - "Assertion script exits 0 when fed a log where vz grows by >0.5 m/s during the 3 s record window and total Fz never exceeds 3× static weight"
  artifacts:
    - path: "test/assert-m4-07-ramp-slide.mjs"
      provides: "D-06 ramp X/Z routing gate test"
      contains: "assertNoNaN"
    - path: "scenarios/m4-07-ramp-slide.json"
      provides: "Car-at-rest-on-10deg-ramp initial condition"
      contains: "recordDuration"
  key_links:
    - from: "test/assert-m4-07-ramp-slide.mjs"
      to: "test/lib-log.mjs"
      via: "import { run, assertNoNaN, totalFz } from './lib-log.mjs'"
      pattern: "from './lib-log.mjs'"
    - from: "scenarios/m4-07-ramp-slide.json"
      to: "src/logger.js openInitialCondition loader"
      via: "JSON IC schema matches existing m4-* scenarios"
      pattern: "recordDuration"
---

## Phase Goal

**As a** developer validating the Phase 4.1 contact-normal routing fix, **I want to** run a single assertion command that proves the car slides downhill on a 10-degree ramp under gravity alone, **so that** D-06 / D-06a (the strut-axis vs X/Z residual split) has a deterministic gate that can run after every commit during Wave 1 and gate phase completion per D-17.

<objective>
Wave 0 deliverable: the failing gate test that Plan 02 will turn green.

Create the new ramp-slide scenario (10-degree ramp, car at rest, no input, 3 s record) and its assertion script. The assertion must currently FAIL against the unfixed codebase (because `physics.js` line 270 drops the horizontal contact normal) and must PASS after Plan 02 routes the X/Z residual through `_hubNormalXZ` into the body.

Purpose: D-06 is the highest-risk change in Phase 4.1 because it touches the force-accumulation contract between `suspension.js` and `physics.js`. Without a deterministic, isolated test for the ramp-slide behavior, Plan 02 has no rapid feedback signal.
Output: One `.mjs` assertion script + one `.json` scenario. No source-code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-body-frame-suspension/04.1-CONTEXT.md
@.planning/phases/04.1-body-frame-suspension/04.1-RESEARCH.md
@.planning/phases/04.1-body-frame-suspension/04.1-PATTERNS.md
@.planning/phases/04.1-body-frame-suspension/04.1-VALIDATION.md

# Canonical analog for assertion structure (D-18: re-read before writing new script)
@test/assert-m4-06-bump-response.mjs
@test/assert-m4-02-asymmetric-bump.mjs
@test/lib-log.mjs

# Closest scenario IC analog (ramp + specific pose)
@scenarios/m4-05-wheel-lift-ramp.json

# Ramp geometry reference — read to confirm RAMP_START_Z, RAMP_LENGTH, RAMP_ANGLE constants
@src/main.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit existing m4-02/04/05/06 assertion scripts (D-18)</name>
  <files>(read-only; no file output — findings recorded in commit message)</files>
  <read_first>
    - test/assert-m4-02-asymmetric-bump.mjs
    - test/assert-m4-04-static-vs-braking.mjs
    - test/assert-m4-05-wheel-lift-ramp.mjs
    - test/assert-m4-06-bump-response.mjs
    - test/lib-log.mjs
    - .planning/phases/04.1-body-frame-suspension/04.1-CONTEXT.md (D-18 section)
    - .planning/phases/04.1-body-frame-suspension/04.1-RESEARCH.md (Phase Requirements -> Test Map and "Pitfall 2: D-06 Double-Count on Flat Ground")
  </read_first>
  <action>Per D-18 confirm that every field probed by the 4 existing assertion scripts retains valid meaning under the Phase 4.1 architecture. Specifically verify: (a) fl_fz/fr_fz/rl_fz/rr_fz still represent tire-spring force at the contact (strut-axis projection per D-06b) on flat ground where dot(contact_normal, body_up) = 1.0, so the sum fl_fz+fr_fz+rl_fz+rr_fz remains comparable to m*g; (b) no assertion reads hubY or hubVy (those fields are being renamed); (c) m4-06 settling assertion relies on the spring/damper energy decay which is unchanged in magnitude (only direction changes from world-Y to body-up). Record findings as four bullet points in the commit message body. If any script DOES probe a field that the Phase 4.1 rename breaks, raise it as a blocker before writing m4-07.</action>
  <verify>
    <automated>grep -c "hubY\|hubVy" test/assert-m4-*.mjs && for f in test/assert-m4-02-asymmetric-bump.mjs test/assert-m4-04-static-vs-braking.mjs test/assert-m4-05-wheel-lift-ramp.mjs test/assert-m4-06-bump-response.mjs; do node --check "$f" || exit 1; done</automated>
  </verify>
  <done>Grep returns 0 for all four scripts (no probes of renamed fields). node --check exits 0 for each of the four scripts (parse-clean, no syntax errors). Audit findings noted in commit message. If either gate fails, STOP and report to user before continuing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create scenarios/m4-07-ramp-slide.json</name>
  <files>scenarios/m4-07-ramp-slide.json</files>
  <read_first>
    - scenarios/m4-05-wheel-lift-ramp.json (closest IC schema analog — copy field shape exactly)
    - scenarios/m4-02-asymmetric-bump.json (alternate IC analog)
    - src/main.js (search for RAMP_START_Z, RAMP_LENGTH, RAMP_ANGLE, RAMP_MAX_H — needed to compute ramp midpoint y/z)
    - .planning/phases/04.1-body-frame-suspension/04.1-RESEARCH.md (Code Examples — ramp_slide_scenario; Assumption A1)
    - .planning/phases/04.1-body-frame-suspension/04.1-PATTERNS.md (scenarios/m4-07-ramp-slide.json section — exact field set and reference values)
  </read_first>
  <action>
Create the JSON scenario with the exact field set used by other m4-* scenarios: description, position, velocity, quaternion, angularVelocity, recordDuration. Place the car at rest on the existing ramp face at ramp midpoint. Use main.js ramp constants (RAMP_START_Z = -15, RAMP_LENGTH = 5, RAMP_ANGLE = 10 degrees) to compute: midpoint z = RAMP_START_Z - RAMP_LENGTH/2 = -17.5; ramp surface height at midpoint = (RAMP_LENGTH/2) * tan(10 deg) ~= 0.4408 m; spawn y = ramp_height + cgHeight from data/ranger.js (0.55) minus wheelRadius (~0.37) — i.e. position.y ~= 0.99 (refine empirically if logs show the car spawning above the ramp face). quaternion uses half-angle pitch of 10 deg around X axis: x = sin(5 deg) = 0.08716, y = 0, z = 0, w = cos(5 deg) = 0.99619. recordDuration: 3.0. velocity and angularVelocity are all zero. description must mention "M4-07 ramp-slide D-06 gate" and "10 degree ramp" so future readers know the purpose. Use the exact key names ("position", "velocity", "quaternion", "angularVelocity", "recordDuration") as in m4-05-wheel-lift-ramp.json — no extra fields.
  </action>
  <verify>
    <automated>node -e "const s = JSON.parse(require('fs').readFileSync('scenarios/m4-07-ramp-slide.json', 'utf8')); if (!s.position || !s.quaternion || s.recordDuration !== 3.0) { console.error('schema mismatch'); process.exit(1); } if (Math.abs(s.quaternion.x - 0.08716) > 0.001 || Math.abs(s.quaternion.w - 0.99619) > 0.001) { console.error('quaternion not 10deg pitch'); process.exit(1); } console.log('OK')"</automated>
  </verify>
  <done>scenarios/m4-07-ramp-slide.json exists; loads as JSON; quaternion is a 10 degree pitch around X; recordDuration is 3.0; field shape matches m4-05-wheel-lift-ramp.json exactly (no extra keys).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create test/assert-m4-07-ramp-slide.mjs</name>
  <files>test/assert-m4-07-ramp-slide.mjs</files>
  <read_first>
    - test/assert-m4-06-bump-response.mjs (canonical template — copy import/run/fail/pass pattern verbatim)
    - test/assert-m4-02-asymmetric-bump.mjs (totalFz usage reference)
    - test/lib-log.mjs (confirm run, assertNoNaN, totalFz, col are exported)
    - .planning/phases/04.1-body-frame-suspension/04.1-PATTERNS.md (test/assert-m4-07-ramp-slide.mjs section)
    - .planning/phases/04.1-body-frame-suspension/04.1-CONTEXT.md (D-17 — exact gate criteria)
    - data/ranger.js (mass value — needed for static Fz constant)
  </read_first>
  <action>
Create the assertion script following the exact structural template of assert-m4-06-bump-response.mjs. Use ES module imports from './lib-log.mjs' (named: run, assertNoNaN, totalFz). Define STATIC_TOTAL_FZ as mass * 9.81 using the mass constant from data/ranger.js (1360 kg in current data — read and use the actual value, do not hardcode 1360 if the file shows otherwise). Inside run() body, perform three assertions in this order:

  (1) assertNoNaN(log) — fail with the returned reason if any frame contains NaN/Inf in any column;
  (2) Total Fz bound check — compute totals[i] = totalFz(log, i) for every frame, find max; fail if max > 3 * STATIC_TOTAL_FZ with message naming the peak value and the "possible double-count" hypothesis; pass with the peak value otherwise;
  (3) Downhill slide gate — using log.col('vz'), compare last vs first frame; fail if lastVz - firstVz <= 0.5 with message "Car did not slide downhill: vz start=X.XXX, end=Y.YYY. X/Z routing broken?"; pass with the actual start/end values otherwise.

The 0.5 m/s growth threshold accounts for the gravity component along the ramp at 10 degrees over 3 seconds (g*sin(10 deg) = 1.7 m/s^2; ~5 m/s in 3 s in the absence of any friction — 0.5 m/s is a generous lower bound that still distinguishes "slides" from "static stuck"). Do not import from any other test file. Do not introduce new helpers in lib-log.mjs — col() already exists for the vz read.
  </action>
  <verify>
    <automated>node test/assert-m4-07-ramp-slide.mjs 2>&1 | grep -E "fail|pass|cannot find|Error" | head -20</automated>
  </verify>
  <done>Script file exists; node executes it without ReferenceError; when run with no log argument the run() helper produces the same "missing argv" behavior as assert-m4-06-bump-response.mjs (i.e. exits cleanly with a usage message — does NOT crash with an uncaught exception). Script structure matches the canonical template (imports only from lib-log.mjs; uses run/assertNoNaN/totalFz; three pass/fail blocks in the specified order).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Log file -> assertion script | Untrusted JSON read by Node.js |
| Scenario JSON -> browser IC loader | Untrusted JSON read by openInitialCondition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | scenarios/m4-07-ramp-slide.json | accept | Local-only file under git; tampering surfaces in code review; no PII or auth context |
| T-04.1-02 | Denial of Service | test/assert-m4-07-ramp-slide.mjs | accept | Script reads finite log file; bounded loop over frames; no network or unbounded recursion |
</threat_model>

<verification>
- Both new files exist and load without error.
- `node test/assert-m4-07-ramp-slide.mjs Logs/<recent-log>.json` exits 1 against an old Phase 4 log (no rolled-over ramp slide), confirming the gate is sensitive.
- All four existing scripts still exit 0 against their existing logs (D-18 audit holds — no field shape changed).
</verification>

<success_criteria>
- `scenarios/m4-07-ramp-slide.json` parses as JSON and matches the schema of the other four m4-* scenarios exactly.
- `test/assert-m4-07-ramp-slide.mjs` runs under Node.js with no syntax errors and follows the assert-m4-06 template.
- The gate is sensitive: it currently fails when run against any Phase 4 log because no Phase 4 log contains the m4-07 scenario.
- D-18 audit completed: no existing assertion script probes `hubY` or `hubVy`.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-body-frame-suspension/04.1-01-SUMMARY.md`.
</output>
