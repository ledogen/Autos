---
phase: 07-free-cam-seeded-layered-terrain
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/seed.js
  - tests/seed-test.html
autonomous: true
requirements: [SEED-01, SEED-02, SEED-03, SEED-05]
user_setup: []

must_haves:
  truths:
    - "parseWorldSeed accepts a string (e.g. 'lone-pine') or an integer and returns a deterministic unsigned 32-bit integer"
    - "seedFor(worldSeed, domainTag, ...coords) returns the same 32-bit integer for the same inputs across calls and page reloads (P7-1)"
    - "seedFor with different domain tags ('coarse' vs 'fine' vs 'regional') yields different sub-seeds from the same worldSeed (independent streams)"
    - "Every function in seed.js is pure math with no DOM, no import, no THREE — copyable verbatim into the Worker source"
  artifacts:
    - path: "src/seed.js"
      provides: "djb2, parseWorldSeed, seedFor, mulberry32 — pure seed utilities"
      exports: ["parseWorldSeed", "seedFor", "mulberry32"]
    - path: "tests/seed-test.html"
      provides: "P7-1 determinism + independence + parse-equivalence assertions (browser console)"
      contains: "P7-1"
  key_links:
    - from: "tests/seed-test.html"
      to: "src/seed.js"
      via: "ES module import of parseWorldSeed/seedFor/mulberry32"
      pattern: "from '\\.\\./src/seed\\.js'"
---

<objective>
Create `src/seed.js`, the world-seed foundation: a `djb2` string→32-bit hash, `parseWorldSeed(input)` (accepts string or int), `seedFor(worldSeed, domainTag, ...coords)` for independent domain-tagged sub-seed streams, and a `mulberry32(seed)` PRNG used to build seeded noise permutation tables. All functions are pure math (no DOM, no import, no THREE) so the identical bodies can later be pasted verbatim into the Web Worker source (Plan 03). Create `tests/seed-test.html` implementing the P7-1 exit-gate assertions: determinism, domain independence, and string/int parse equivalence.

Purpose: P7-1 is a hard exit gate that MUST pass before any generator consumes the seed (STATE.md Pending Todo #1). This plan delivers the seed module and its gate test in isolation so Plan 03's terrain rewrite builds on a verified-deterministic foundation.
Output: `src/seed.js`, `tests/seed-test.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-CONTEXT.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-VALIDATION.md

<interfaces>
<!-- Required function signatures (RESEARCH §Seed System; PATTERNS §src/seed.js) -->
- `parseWorldSeed(input)` → unsigned 32-bit int. If `typeof input === 'number'` return `(input|0)>>>0`; else `djb2(String(input))`.
- `seedFor(worldSeed, domainTag, ...coords)` → unsigned 32-bit int. Pure; no mutable module state.
- `mulberry32(seed)` → `() => [0,1)` closure. Used as the `random` arg to `createNoise2D`.
- `djb2(str)` → unsigned 32-bit int (helper; may be internal or exported).
Domain tags used this milestone: "coarse", "fine", "regional", "spawn".
Constants: hash-combine `0x9e3779b9`, coord-mix `0x85ebca6b`; mulberry32 increment `0x6D2B79F5`.

<!-- index.html importmap (for the test page head) -->
three → https://cdn.jsdelivr.net/npm/three@0.184.0/build/three.module.js
(seed.js needs no THREE; importmap only required if a future test imports terrain.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/seed-test.html (P7-1 gate) THEN src/seed.js to make it pass</name>
  <files>src/seed.js, tests/seed-test.html</files>
  <read_first>
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md §Seed System Architecture (djb2, seedFor, mulberry32 bodies) and §Validation Architecture (P7-1 assertion block)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md §src/seed.js (no-import pattern, exact function bodies, domain tags)
    - index.html (importmap + `<script type="module">` convention — mirror for the test page)
  </read_first>
  <behavior>
    P7-1 assertions the test page must run and print PASS/FAIL for (RESEARCH §Exit Gate Tests):
    - Determinism: `seedFor(parseWorldSeed('lone-pine'),'coarse') === seedFor(parseWorldSeed('lone-pine'),'coarse')`.
    - Domain independence: `seedFor(s,'coarse') !== seedFor(s,'fine')` and `!== seedFor(s,'regional')` for `s = parseWorldSeed('lone-pine')`.
    - World distinctness: `seedFor(parseWorldSeed('lone-pine'),'coarse') !== seedFor(parseWorldSeed('other-seed'),'coarse')`.
    - Parse equivalence (SEED-03): for an integer N, `parseWorldSeed(N)` equals `(N|0)>>>0`; and `parseWorldSeed('lone-pine')` equals `djb2('lone-pine')` (string path).
    - Coord streams (SEED-02 forward-looking): `seedFor(s,'road',0,0) !== seedFor(s,'road',1,0)` (coords mix in).
    - Unsigned range: every returned seed is an integer in `[0, 2^32)`.
  </behavior>
  <action>
    FIRST create `tests/seed-test.html`: a plain HTML page (mirror index.html head conventions; importmap optional since seed.js needs no THREE), a `<pre id="out">`, and a `<script type="module">` that `import { parseWorldSeed, seedFor, mulberry32 } from '../src/seed.js'` (also import or re-derive `djb2` for the parse-equivalence check — export `djb2` from seed.js to make this clean). Implement each assertion in the `<behavior>` block using `console.assert(cond, 'P7-1 FAIL: <reason>', actual)` AND append a human-readable PASS/FAIL line to `#out`. End with `console.log('P7-1 PASS: seed determinism verified')` only if all assertions held (track a fail counter; print the count). Run it once and confirm it FAILS (RED) because seed.js does not exist yet.

    THEN create `src/seed.js` with the four functions per RESEARCH/PATTERNS exact bodies: `djb2(str)` (h=5381 loop with `Math.imul(h,33) ^ charCodeAt`, `>>>0`); `parseWorldSeed(input)` (number → `(input|0)>>>0`, else `djb2(String(input))`); `seedFor(worldSeed, domainTag, ...coords)` (h=djb2(domainTag); mix worldSeed with `Math.imul(h ^ (worldSeed>>>0), 0x9e3779b9)>>>0`; fold each coord with `Math.imul(h ^ ((coord|0)>>>0), 0x85ebca6b)>>>0`; return `h>>>0`); `mulberry32(seed)` (returns the standard 4-line closure). Export `djb2, parseWorldSeed, seedFor, mulberry32`. Add a file header comment stating the HARD RULE: these bodies are copied verbatim into terrain.js WORKER_SOURCE and terrain-worker.js (Plan 03) and must stay in sync — no `export`/`import` keyword in those copies. Re-run the test page: all assertions PASS (GREEN).

    Do NOT use `eval`, `Function`, `innerHTML`, or any string→code path. The seed string flows only through `djb2` arithmetic (security: see threat model).
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./src/seed.js').then(m=>{const s=m.parseWorldSeed('lone-pine');const a=m.seedFor(s,'coarse'),b=m.seedFor(s,'coarse'),c=m.seedFor(s,'fine'),d=m.seedFor(m.parseWorldSeed('other-seed'),'coarse');let ok=true;const A=(t,x)=>{if(!x){console.error('FAIL',t);ok=false}};A('determinism',a===b);A('domain-indep',a!==c);A('world-distinct',a!==d);A('unsigned',Number.isInteger(a)&&a>=0&&a<2**32);A('int-parse',m.parseWorldSeed(42)===((42|0)>>>0));A('coord-mix',m.seedFor(s,'road',0,0)!==m.seedFor(s,'road',1,0));if(!ok)process.exit(1);console.log('P7-1 PASS (node)')}).catch(e=>{console.error(e);process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `src/seed.js` exports `parseWorldSeed`, `seedFor`, `mulberry32` (and `djb2`) and contains NO `import` statement, NO DOM reference, NO `THREE`.
    - `grep -E "eval|innerHTML|new Function" src/seed.js` returns nothing (security: no string→code path).
    - The node automated check above prints `P7-1 PASS (node)` and exits 0 (determinism, domain independence, world distinctness, unsigned range, int-parse, coord-mix all hold).
    - `tests/seed-test.html` imports from `../src/seed.js` and prints a PASS line + a fail counter to both console and `#out`.
    - Every `seedFor`/`parseWorldSeed` return is an integer in `[0, 2^32)` (unsigned).
  </acceptance_criteria>
  <done>src/seed.js exists with pure deterministic djb2/parseWorldSeed/seedFor/mulberry32; tests/seed-test.html runs the P7-1 gate green; the node assertion command exits 0. P7-1 exit gate satisfied — Plan 03 may now consume the seed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `?seed=` URL param / debug seed field → djb2 hash | The ONLY untrusted string input in the phase; arbitrary/empty/oversized strings must hash deterministically without crashing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-INJ | Tampering/Elevation | seed string parser | mitigate | `djb2` consumes the string only via `charCodeAt` arithmetic — never `eval`, `new Function`, or `innerHTML`. No code path turns the seed into executable code or DOM. Verified by grep in acceptance criteria. |
| T-07-02-ROB | Denial of Service | parseWorldSeed on oversized/empty input | mitigate | djb2 is O(n) with no allocation; empty string → 5381 (deterministic), any length → finite unsigned int. No unbounded recursion or memory growth. |

No HIGH-severity threats — static client-side app; the only untrusted input is a hashed seed string, parsed by pure integer arithmetic (no server, no storage, no code execution).
</threat_model>

<verification>
- P7-1 exit gate green via the node automated command and via `tests/seed-test.html` in the browser console.
- Security: grep confirms no `eval`/`new Function`/`innerHTML` in seed.js.
- seed.js is Worker-safe (no import/DOM/THREE) — ready for verbatim copy into the Worker source in Plan 03.
</verification>

<success_criteria>
- `parseWorldSeed` deterministically maps string or int → unsigned 32-bit int (SEED-01, SEED-03).
- `seedFor` produces deterministic, domain-independent, coord-sensitive sub-seeds (SEED-02, SEED-05).
- P7-1 exit gate passes; the module contains no string→code path (SEED-05 HARD RULE + security).
</success_criteria>

<output>
Create `.planning/phases/07-free-cam-seeded-layered-terrain/07-02-SUMMARY.md` when done.
</output>
