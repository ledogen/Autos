---
phase: 07-free-cam-seeded-layered-terrain
plan: 03
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - src/terrain.js
  - src/terrain-worker.js
  - src/main.js
  - tests/height-agreement-test.html
autonomous: true
requirements: [TERR-01, TERR-02, TERR-03, TERR-04, TERR-05, SEED-05]
user_setup: []

must_haves:
  # CONTEXT decisions covered: D-06, D-10, D-20
  truths:
    - "The Worker generates chunk heightmaps from a seeded three-layer height(x,z) = coarse ridged-multifractal + fine FBM (suspension texture) + regional-roughness modulator — no more fixed () => 0.5 permutation"
    - "The same worldSeed produces byte-identical chunk heights across two independent runs (P7-1 chunk-equality), and different seeds produce different heights"
    - "Physics contacts (queryContacts, queryVertexContacts) read terrain height/normal from a single analytic height(x,z) that never returns 0 for unloaded chunks — no flat-ground gap at chunk seams"
    - "sampleHeight (bilinear of built chunk) agrees with analyticHeight*amplitude at >=5 grid-aligned world positions within 1e-3 m (P7-2 exit gate)"
    - "WORKER_SOURCE in terrain.js and the standalone src/terrain-worker.js are byte-identical for every shared function"
  artifacts:
    - path: "src/terrain.js"
      provides: "seeded three-layer WORKER_SOURCE; analyticHeight(wx,wz,params)+analyticNormal exported/available to main.js; reinitWorker(worldSeed,params); rebuildAllChunksFromWorker()"
      contains: "analyticHeight"
    - path: "src/terrain-worker.js"
      provides: "byte-identical standalone copy of the seeded three-layer worker source"
      contains: "coarseHeight"
    - path: "src/main.js"
      provides: "queryContacts/queryVertexContacts use analyticHeight/analyticNormal (no sampleHeight for physics)"
      contains: "analyticHeight"
    - path: "tests/height-agreement-test.html"
      provides: "P7-2 height-agreement assertions at >=5 grid-aligned positions"
      contains: "P7-2"
  key_links:
    - from: "src/terrain.js WORKER_SOURCE"
      to: "src/terrain-worker.js"
      via: "verbatim copy of djb2/mulberry32/seedFor/createNoise2D/coarseHeight/fineHeight/regionalModulator/height"
      pattern: "coarseHeight"
    - from: "src/main.js queryContacts"
      to: "terrainSystem analyticHeight"
      via: "physics contact height/normal from analytic function, not sampleHeight"
      pattern: "analyticHeight"
---

<objective>
Replace the fixed 3-octave simplex (`createNoise2D(() => 0.5)`) with a seeded three-layer height function — coarse ridged-multifractal (Eastern-Sierra escarpments + flat valleys), fine FBM (suspension texture), and a low-frequency regional-roughness modulator that scales the fine amplitude across the map. The same `height(x,z)` body is used by BOTH the Web Worker mesh build AND a new main-thread `analyticHeight(wx,wz,params)` that physics contacts call directly (never returns 0 for unloaded chunks, fixing the chunk-seam gap). The Worker seeds its three noise layers from `seedFor(worldSeed, 'coarse'|'fine'|'regional')` via an `init` message. Deliver `tests/height-agreement-test.html` for the P7-2 exit gate.

Purpose: TERR-04's unified `height(x,z)` is the "#1 correctness constraint" and P7-2 is a hard exit gate. This plan is the core of the terrain rewrite and depends on the verified seed module (Plan 02).
Output: seeded `terrain.js` + byte-identical `terrain-worker.js`; analytic physics height in `main.js`; `tests/height-agreement-test.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-CONTEXT.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-VALIDATION.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-02-SUMMARY.md
@references/km elev ref.png

<interfaces>
<!-- From Plan 02 (src/seed.js) — copy these bodies VERBATIM into WORKER_SOURCE + terrain-worker.js (no export keyword in copies) -->
- djb2(str), seedFor(worldSeed, domainTag, ...coords), mulberry32(seed) — pure math.
<!-- Existing terrain.js structure (src/terrain.js) -->
- WORKER_SOURCE template literal (lines 37–177): simplex block + worker constants + `createNoise2D(() => 0.5)` (line 141) + onmessage 3-octave FBM loop (lines 151–176). REPLACE the fixed noise + FBM with seeded three-layer.
- TerrainSystem.update(carPos) (228), sampleHeight bilinear (244–279), sampleNormal central-diff (290–303), rebuildAllChunks Path-A (376–387), _flushPendingQueue geometry build (389–442), _updateChunkRing (336–362), _pendingWorker race-fix ordering (216–219, 435–441).
- Worker message send: `this._worker.postMessage({ type:'generate', cx, cz, key })` (line 359).
<!-- terrain-worker.js (standalone copy) -->
- Lines 30–95 simplex, 97–106 constants + fixed noise, 118–148 onmessage. Must mirror WORKER_SOURCE.
<!-- main.js physics callers -->
- queryContacts (493–531): `terrainSystem.sampleHeight(cx,cz)` (497) + `terrainSystem.sampleNormal(cx,cz)` (500).
- queryVertexContacts (435–484): `terrainSystem.sampleHeight(px,pz)` (439) + `sampleNormal` (443).
- RANGER_PARAMS is the params object; terrainAmplitude default 0.1 (data/ranger.js line 153).
<!-- Calibration target (references/km elev ref.png): ~640 m relief over 13.3 km; coarse undulation ±100-200 m @ ~0.5-1 km wavelength; occasional ~40-60% grade ridge faces ~300 m wide. Starting params (RESEARCH §Calibration): coarseAmplitude=350, coarseFreq=0.001, coarseOctaves=5, ridgeSharpness=2.5; fineAmplitude=1.5, fineFreq=0.05; regionalStrength=0.6, regionalScale=3000. Final lock-in is Plan 04. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed + three-layer height in WORKER_SOURCE and terrain-worker.js (SYNC — single task, single commit)</name>
  <files>src/terrain.js, src/terrain-worker.js</files>
  <read_first>
    - src/terrain.js (WORKER_SOURCE lines 37–177 IN FULL — simplex block, constants, fixed noise line 141, onmessage 151–176)
    - src/terrain-worker.js (FULL — must end byte-identical to WORKER_SOURCE for shared functions)
    - src/seed.js (Plan 02 — djb2/seedFor/mulberry32 bodies to copy verbatim)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md §Three-Layer Height Function (coarseHeight, fineHeight, regionalModulator, combined height; Worker init message) and §Common Pitfalls 1 & 2
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md §src/terrain.js, §src/terrain-worker.js, §Critical Sync Constraints
  </read_first>
  <action>
    SYNC HAZARD (RESEARCH §Pitfall 2 / PATTERNS §Critical Sync Constraints): `terrain.js`'s `WORKER_SOURCE` string and the standalone `src/terrain-worker.js` are a manual byte-copy with no automated sync. EVERY change in this task MUST be applied identically to BOTH files in THIS task / THIS commit. Do not split them.

    In BOTH the WORKER_SOURCE template literal (terrain.js) AND terrain-worker.js:
    1. Paste `djb2`, `seedFor`, and `mulberry32` from seed.js VERBATIM (no `export` keyword) above the `createNoise2D` definition. Keep the existing simplex `buildPermutationTable`/`createNoise2D` block (it takes a `random` arg).
    2. Add the three pure-math layer functions per RESEARCH §Three-Layer Height Function: `coarseHeight(wx, wz, noiseCoarse, params)` (ridged multifractal — per octave `shaped = Math.pow(1 - Math.abs(noiseCoarse(wx*freq, wz*freq)), ridgeSharpness)`, accumulate `shaped*amp`, `freq*=lacunarity`, `amp*=gain`; read `coarseAmplitude, coarseFreq, coarseOctaves, ridgeSharpness` from params; lacunarity 2.0, gain 0.5); `fineHeight(wx, wz, noiseFine, params)` (2-octave FBM at `fineFreq`, amplitude `fineAmplitude`); `regionalModulator(wx, wz, noiseRegional, params)` (low-freq noise at `1/regionalScale` remapped to `[1-regionalStrength, 1]`); and the combined `height(wx, wz, noiseCoarse, noiseFine, noiseRegional, params)` = `coarseHeight + fineHeight * regionalModulator`. These return RAW height (no terrainAmplitude multiply — amplitude is applied at geometry setY and in sampleHeight, preserving the existing contract).
    3. Replace the fixed `const noise2D = createNoise2D(() => 0.5)` (terrain.js line 141 / worker line 106) with three module-scope vars `let noiseCoarse, noiseFine, noiseRegional` initialized lazily.
    4. Extend `self.onmessage`: add an `init` branch — on `{type:'init', worldSeed, params}` set `_workerParams = params` and build `noiseCoarse = createNoise2D(mulberry32(seedFor(worldSeed,'coarse')))`, `noiseFine = createNoise2D(mulberry32(seedFor(worldSeed,'fine')))`, `noiseRegional = createNoise2D(mulberry32(seedFor(worldSeed,'regional')))`, then `return`. The `generate` branch now requires the noise to be initialized; if `noiseCoarse` is undefined, post an empty/zero chunk or guard with an init-required console.warn (do not crash). The `generate` loop fills `heights[zi*N+xi] = height(wx, wz, noiseCoarse, noiseFine, noiseRegional, _workerParams)` instead of the old 3-octave sum. Keep the transferable `postMessage({ key, cx, cz, heights }, [heights.buffer])` unchanged.
    5. Keep the startup `noise2D(0,0)` self-check pattern but adapt it (it can no longer call the removed fixed noise2D — replace with a harmless `[terrain-worker] ready` log, or defer the validity log to after init).

    After editing both, run the byte-equality check (verify block) to PROVE the WORKER_SOURCE inner text matches terrain-worker.js for the shared functions.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('src/terrain.js','utf8');const w=fs.readFileSync('src/terrain-worker.js','utf8');const fns=['function djb2','function seedFor','function mulberry32','function coarseHeight','function fineHeight','function regionalModulator','function height('];for(const f of fns){if(!w.includes(f)){console.error('worker missing',f);process.exit(1)}if(!t.includes(f)){console.error('WORKER_SOURCE missing',f);process.exit(1)}}if(t.includes('createNoise2D(function() { return 0.5; })')||t.includes('createNoise2D(() => 0.5)')){console.error('fixed permutation still present in terrain.js');process.exit(1)}if(w.includes('return 0.5')&&w.includes('createNoise2D(function() { return 0.5')){console.error('fixed permutation still present in terrain-worker.js');process.exit(1)}if(!t.includes(\"type: 'init'\")&&!t.includes(\"'init'\")){console.error('no init message handling');process.exit(1)}console.log('worker-source sync + seeded three-layer present')"</automated>
  </verify>
  <acceptance_criteria>
    - Both terrain.js WORKER_SOURCE and terrain-worker.js contain `djb2`, `seedFor`, `mulberry32`, `coarseHeight`, `fineHeight`, `regionalModulator`, and `height(` — verbatim, no `export` keyword (automated check above).
    - The fixed `() => 0.5` / `function(){return 0.5}` noise init is removed from BOTH files (no fixed permutation remains).
    - The Worker handles a `{type:'init', worldSeed, params}` message that builds three seeded noise closures; the `generate` loop calls the combined `height(...)`.
    - SYNC: the two files are byte-identical for every shared function body (this is the explicit anti-drift criterion — both edited in the same commit).
    - Layer functions are pure math (no DOM, no THREE, no import) — Worker-safe.
  </acceptance_criteria>
  <done>WORKER_SOURCE and terrain-worker.js both carry the seeded three-layer height function (coarse ridged + fine FBM + regional modulator) and an init message; the fixed permutation is gone; the two files are in sync (single commit).</done>
</task>

<task type="auto">
  <name>Task 2: TerrainSystem analyticHeight/analyticNormal + init wiring + Path-B rebuild; main.js physics switches to analytic</name>
  <files>src/terrain.js, src/main.js</files>
  <read_first>
    - src/terrain.js (TerrainSystem constructor 190–220, update 228, sampleHeight 244–279, sampleNormal 290–303, rebuildAllChunks 376–387, _flushPendingQueue 389–442, _updateChunkRing 336–362, postMessage 359)
    - src/terrain-worker.js (init/generate contract from Task 1)
    - src/main.js (queryContacts 493–531, queryVertexContacts 435–484, terrainSystem construction 565, spawn-reset terrain offset 636)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md §Unified height(x,z) Architecture, §Regeneration (init→dispose→re-request), §Common Pitfalls 4 & 6
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md §src/terrain.js, §src/main.js (queryContacts switch)
  </read_first>
  <action>
    In `terrain.js`, add a main-thread copy of the same layer math so physics can sample analytically. Define module-level `djb2/seedFor/mulberry32` and `coarseHeight/fineHeight/regionalModulator/height` in the terrain.js module scope (NOT inside WORKER_SOURCE) — these are the third copy (PATTERNS notes the math lives in the comment-delimited block; mark it `// ── Height function (shared with Worker — keep in sync) ──`). Add to the TerrainSystem class: a `_worldSeed` field, lazy seeded noise closures (`_noiseCoarse/_noiseFine/_noiseRegional` built from `seedFor(this._worldSeed, tag)` via mulberry32), and:
    - `analyticHeight(wx, wz)` → `height(wx, wz, this._noiseCoarse, this._noiseFine, this._noiseRegional, this._params)` (raw) `* (this._params.terrainAmplitude ?? 1.0)` so it matches geometry/sampleHeight scaling. Never returns 0 for unloaded chunks (RESEARCH §Pitfall 6).
    - `analyticNormal(wx, wz)` → central-difference over `analyticHeight` at EPS=0.5 (mirror sampleNormal's formula but call analyticHeight, fixing the normal/height mismatch noted in RESEARCH §Unified Architecture).
    - `reinitWorker(worldSeed, params)` → set `this._worldSeed`, rebuild the three main-thread noise closures, and `postMessage({type:'init', worldSeed, params})` to the Worker.
    - `rebuildAllChunksFromWorker()` → Path B: dispose ALL built chunk meshes (geometry.dispose + scene.remove), clear `_chunkMap`, clear `_pendingWorker` and `_pendingQueue`, then let the next `update()` re-request the ring (or proactively call `_updateChunkRing` at the last known center). Preserve the `_pendingWorker` race-fix ordering (only release a key in `_flushPendingQueue` after `_chunkMap` is set).
    In the constructor, call `this.reinitWorker(worldSeed, params)` once after the Worker is created so the first `generate` requests have noise initialized — pass a `worldSeed` constructor arg (main.js supplies it; default to `parseWorldSeed('lone-pine')` is set in Plan 04's wiring, but accept the arg now). Keep `sampleHeight` (bilinear) and the existing `terrainAmplitude` Path-A `rebuildAllChunks` UNCHANGED — `sampleHeight` is retained solely for the P7-2 test (RESEARCH §Unified Architecture).

    In `main.js`: switch BOTH physics callers off `sampleHeight`/`sampleNormal` onto the analytic path. In `queryContacts` (line 497–500) use `terrainSystem.analyticHeight(cx, cz)` for `terrainH` and `terrainSystem.analyticNormal(cx, cz)` for the normal. In `queryVertexContacts` (439, 443) use `analyticHeight(px,pz)` / `analyticNormal(px,pz)`. Keep the `terrainSystem ? ... : 0` null-guard form. In the spawn-reset terrain offset (line 636), switch to `terrainSystem.analyticHeight(...)` so the reset seats on the real surface even before chunks load (the canonical spawn function in Plan 04 will replace this block, but keep it consistent now). Construct TerrainSystem with a worldSeed arg — for THIS plan, import `parseWorldSeed` from `./seed.js` and pass `parseWorldSeed('lone-pine')` (Plan 04 replaces with the URL-derived `worldSeed`).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('src/terrain.js','utf8');const m=fs.readFileSync('src/main.js','utf8');for(const tok of ['analyticHeight','analyticNormal','reinitWorker','rebuildAllChunksFromWorker']){if(!t.includes(tok)){console.error('terrain.js missing',tok);process.exit(1)}}if(!m.includes('analyticHeight')||!m.includes('analyticNormal')){console.error('main.js physics not on analytic path');process.exit(1)}if(/queryContacts[\\s\\S]{0,400}sampleHeight/.test(m)){console.error('queryContacts still uses sampleHeight');process.exit(1)}console.log('analytic physics + Path-B wiring present')"</automated>
    <human-check>Load index.html. Drive the truck across a chunk seam on a slope: no fall-through / float at the boundary (analytic height has no unloaded-chunk gap). Terrain shows seeded relief, not the old flat 3-octave undulation.</human-check>
  </verify>
  <acceptance_criteria>
    - `terrain.js` exposes `analyticHeight`, `analyticNormal`, `reinitWorker`, `rebuildAllChunksFromWorker`; analyticHeight applies terrainAmplitude and never returns 0 for unloaded chunks.
    - `main.js` `queryContacts` and `queryVertexContacts` use `analyticHeight`/`analyticNormal` (NOT `sampleHeight`) — verified by the negative grep above.
    - The terrain.js main-thread layer math is marked as a keep-in-sync copy of the Worker functions.
    - `sampleHeight` bilinear path is retained unchanged (used by the P7-2 test only).
    - `_pendingWorker` race-fix ordering preserved through the Path-B rebuild.
  </acceptance_criteria>
  <done>Physics samples a single analytic height/normal with no chunk-seam gap; TerrainSystem can re-init the Worker and rebuild all chunks (Path B); sampleHeight remains for the P7-2 gate.</done>
</task>

<task type="auto">
  <name>Task 3: tests/height-agreement-test.html — P7-2 exit gate</name>
  <files>tests/height-agreement-test.html</files>
  <read_first>
    - src/terrain.js (sampleHeight 244–279, analyticHeight from Task 2, _flushPendingQueue raw-height storage 389–442, constructor + worker async)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md §Validation Architecture (P7-2 assertion block) and §Pitfall 4 (grid-aligned positions, tolerance)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-VALIDATION.md (P7-2 row, tolerance ≤ 1e-3)
    - index.html (importmap for three — the test must construct a real TerrainSystem)
  </read_first>
  <behavior>
    P7-2 (TERR-04 exit gate): after a chunk finishes building, for >=5 GRID-ALIGNED world positions [(0,0),(64,0),(0,64),(32,32),(16,48)] assert `|sampleHeight(wx,wz) - analyticHeight(wx,wz)| < 1e-3` m. Positions are grid-aligned to avoid sub-cell bilinear divergence (RESEARCH §Pitfall 4). The test must wait for the Worker to deliver and `_flushPendingQueue` to build the chunk(s) before sampling (async — poll until `sampleHeight(0,0)` is non-fallback / chunk present, or wrap in a Promise/timeout).
  </behavior>
  <action>
    Create `tests/height-agreement-test.html`: importmap head (three) mirroring index.html, a `<pre id="out">`, and a `<script type="module">` that imports `{ TerrainSystem }` from `../src/terrain.js` and `{ parseWorldSeed }` from `../src/seed.js`. Build a minimal `THREE.Scene`, instantiate `new TerrainSystem(scene, params, parseWorldSeed('lone-pine'))` with a params object carrying the calibration starting values (coarseAmplitude/coarseFreq/coarseOctaves/ridgeSharpness/fineAmplitude/fineFreq/regionalStrength/regionalScale + terrainAmplitude). Drive `terrainSystem.update({x:0,y:0,z:0})` in a rAF/interval loop so chunks request and build; once the chunk covering (0,0) is in `_chunkMap`, run the assertion loop from `<behavior>` using `console.assert` + append PASS/FAIL to `#out`; print `P7-2 PASS: height agreement verified` only if all 5 hold; otherwise print each failing position with bilinear, analytic, and diff. Add a 5 s watchdog that prints `P7-2 FAIL: chunk never built` if no chunk arrives (so the test never hangs silently).
    Note: this test runs in a browser (Worker required); it cannot run under node. The automated gate below is a static-structure check; the true P7-2 pass is the browser console line (record it in the SUMMARY).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('tests/height-agreement-test.html','utf8');for(const tok of ['P7-2','sampleHeight','analyticHeight','TerrainSystem','1e-3','from \\'../src/terrain.js\\'']){if(!s.includes(tok.replace(/\\\\/g,''))){console.error('missing',tok);process.exit(1)}}console.log('height-agreement test structure present')"</automated>
    <human-check>Serve the repo and open tests/height-agreement-test.html. Console prints `P7-2 PASS: height agreement verified` and #out shows 5 grid positions with diff < 1e-3 m. Paste the console line into the plan SUMMARY as the P7-2 gate evidence.</human-check>
  </verify>
  <acceptance_criteria>
    - `tests/height-agreement-test.html` imports TerrainSystem + parseWorldSeed, builds a real chunk, and asserts `|sampleHeight - analyticHeight| < 1e-3` at >=5 grid-aligned positions.
    - The test waits for async chunk build and has a watchdog (no silent hang).
    - Browser run prints `P7-2 PASS: height agreement verified` (recorded in SUMMARY) — this is the P7-2 exit gate.
    - Tolerance is 1e-3 m and positions are grid-aligned (Pitfall 4 honored).
  </acceptance_criteria>
  <done>P7-2 height-agreement test exists and passes in the browser; sampleHeight and analyticHeight agree within 1e-3 m at grid-aligned positions. TERR-04 unified-height correctness gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main thread → Web Worker (postMessage) | Structured-clone of `{worldSeed, params}` / `{cx,cz,key}`; same-origin, no untrusted remote data |
| seed string (via worldSeed int) → noise permutation | Already hashed to a 32-bit int upstream (Plan 02); Worker receives an int, not a raw string |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-DET | Tampering | seeded permutation / cross-thread agreement | mitigate | Worker and main thread derive noise from the SAME `seedFor(worldSeed, tag)` + mulberry32; P7-1 chunk-equality and P7-2 height-agreement tests prove determinism and cross-thread agreement. |
| T-07-03-PERF | Denial of Service | per-sample height cost at 60fps | accept | Worker runs off main thread (MAX_BUILDS_PER_FRAME=2 cap retained); analyticHeight is arithmetic-only at ~240 physics calls/s. FPS≥55 confirmed in Plan 04 perf checkpoint. |
| T-07-03-SYNC | Tampering | WORKER_SOURCE vs terrain-worker.js drift | mitigate | Byte-equality automated check (Task 1) + both files edited in one commit; drift would surface as divergent terrain between the embedded and standalone worker paths. |

No HIGH-severity threats — static client-side app; only cross-thread message is same-origin structured data derived from a hashed integer seed.
</threat_model>

<verification>
- P7-1 chunk-equality (same seed → identical heights) is exercised by the seed-test/terrain determinism; P7-2 height-agreement green in the browser.
- Worker-source sync proven by byte-equality automated check.
- Physics has no chunk-seam fall-through (analyticHeight never 0).
- Per 07-VALIDATION.md: run both exit-gate blocks after this wave; FPS spot-check (full perf gate in Plan 04).
</verification>

<success_criteria>
- Coarse ridged-multifractal + fine FBM + regional modulator drive a unified seeded `height(x,z)` used by Worker mesh and analytic physics sampler (TERR-01, TERR-02, TERR-03, TERR-04).
- Same seed → identical terrain; different seed → different terrain (SEED-05).
- P7-2 height-agreement exit gate passes within 1e-3 m (TERR-04).
- Off-thread generation keeps 60fps headroom (TERR-05; confirmed in Plan 04).
</success_criteria>

<output>
Create `.planning/phases/07-free-cam-seeded-layered-terrain/07-03-SUMMARY.md` when done (include the browser P7-2 console line as evidence).
</output>
