---
phase: 07-free-cam-seeded-layered-terrain
plan: 04
type: execute
wave: 3
depends_on: [07-01, 07-03]
files_modified:
  - data/ranger.js
  - src/debug.js
  - src/main.js
autonomous: false
requirements: [TERR-06, SEED-04, TERR-05]
user_setup: []

must_haves:
  # CONTEXT decisions covered: D-07, D-08, D-09, D-11, D-12, D-13, D-14, D-15, D-16
  truths:
    - "data/ranger.js commits a sensible default coarse/fine/regional param set + worldSeed default so a bare seed is deterministic and shareable (P7-3 lock = committed defaults; sliders stay live)"
    - "The debug panel shows an editable World Seed text field; typing a new seed and applying regenerates the terrain without a full page reload"
    - "The Terrain folder has live sliders for coarse (amplitude, wavelength/frequency, octaves, ridge sharpness), fine (amplitude, frequency), and regional (strength, scale)"
    - "Coarse/fine/regional/seed changes trigger a debounced (~150 ms) Path-B Worker rebuild; the amplitude-only slider stays an instant Path-A Y-rescale"
    - "A single canonical resolveSpawn(worldSeed,params) returns {position,heading}; initial load AND every regenerate teleport the truck to spawn, ground-probe analyticHeight, seat at ride height, zero velocity"
    - "The ?seed= URL param sets the initial worldSeed (default 'lone-pine'); refreshing the same seed reproduces the same terrain"
    - "Frame rate holds >=55 fps with the layered terrain active and physics sampling analyticHeight (TERR-05 checkpoint)"
  artifacts:
    - path: "data/ranger.js"
      provides: "committed coarse/fine/regional defaults + worldSeed default (P7-3 lock)"
      contains: "coarseAmplitude"
    - path: "src/debug.js"
      provides: "World Seed field + coarse/fine/regional sub-folders wired to Path-A vs Path-B callbacks"
      contains: "World Seed"
    - path: "src/main.js"
      provides: "?seed= parse, worldSeed, resolveSpawn canonical spawn, debounced Path-B rebuild + re-seat, slider callbacks"
      contains: "resolveSpawn"
  key_links:
    - from: "src/debug.js terrain sliders"
      to: "src/main.js rebuildTerrainFull (debounced Path B)"
      via: "callbacks.rebuildTerrainFull / changeSeed onChange"
      pattern: "rebuildTerrainFull"
    - from: "src/main.js regenerate"
      to: "terrainSystem.reinitWorker + resolveSpawn re-seat"
      via: "init Worker, dispose chunks, teleport+ground-probe truck"
      pattern: "resolveSpawn"
---

<objective>
Expose the three terrain layers as live debug sliders, add an editable World Seed field, and wire the two regeneration paths (D-09): the amplitude-only slider stays an instant Path-A `rebuildAllChunks` Y-rescale, while coarse/fine/regional shape sliders and the seed field trigger a debounced (~150 ms) Path-B rebuild (Worker `init` → dispose all chunks → re-request ring). Parse `?seed=` (default `'lone-pine'`) into the live `worldSeed`. Add the single canonical `resolveSpawn(worldSeed, params)` → `{position, heading}` (terrain-only low-slope resolver, designed so Phase 8 swaps in a road-graph probe at the same call site) and use it for both initial load and every regenerate to teleport + ground-probe + re-seat the truck (D-14/15/16). Commit a sensible default coarse/fine/regional param set into `data/ranger.js` (P7-3 lock = committed defaults, NO export button, sliders stay live). End with a FPS checkpoint (TERR-05).

Purpose: This plan makes the terrain tunable and reproducible (TERR-06, SEED-04), locks shareable defaults so Phase 8 has stable terrain to route over, and establishes the road-aware spawn seam.
Output: terrain param defaults in `data/ranger.js`; seed field + layer sliders in `debug.js`; URL parse + regeneration + canonical spawn in `main.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-CONTEXT.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-03-SUMMARY.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-01-SUMMARY.md
@references/km elev ref.png

<interfaces>
<!-- debug.js (src/debug.js) -->
- `initDebug(params, callbacks = {})` (line 44). Existing Terrain folder (lines 120–126): `terrainAmplitude` slider → `callbacks.rebuildTerrain()` (Path A); `rampEnabled` → `callbacks.setRampVisible`. Sub-folder pattern: `terrainFolder.addFolder('Coarse Layer')`. lil-gui text field: `gui.add(obj,'strKey')` renders an `<input>` (RESEARCH §Debug Panel, A7).
<!-- main.js (src/main.js) -->
- initDebug call (lines 558–561): `{ setRampVisible, rebuildTerrain }`. Add `rebuildTerrainFull`, `changeSeed`.
- TerrainSystem construction (line 565) — now takes a worldSeed arg (Plan 03). reinitWorker(worldSeed,params) + rebuildAllChunksFromWorker() available (Plan 03).
- computeStaticEquilibrium(p) (65–91) → {bodyY, strutComp}. Reset block (629–654) seats the truck — refactor into resolveSpawn + _reseatTruckAtSpawn.
- analyticHeight/analyticNormal on terrainSystem (Plan 03) — use for ground-probe.
- 3-PLACES RULE (MEMORY project_vehiclestate_three_places.md / PATTERNS §Critical Sync): any NEW vehicleState field must be added in vehicle.js SPAWN_STATE, main.js vehicleState literal, AND main.js reset. (This plan likely adds NO vehicleState field — spawn pos/heading flow through position/quaternion — but if one is added, all three places apply.)
<!-- data/ranger.js: append a "Phase 7 Terrain (coarse/fine/regional)" block near the existing terrainAmplitude (line 153). -->
<!-- Calibration starting values (RESEARCH §Calibration): coarseAmplitude=350, coarseFreq=0.001, coarseOctaves=5, ridgeSharpness=2.5; fineAmplitude=1.5, fineFreq=0.05; regionalStrength=0.6, regionalScale=3000. terrainAmplitude currently 0.1 — re-evaluate against the new layers (coarse already outputs metres; amplitude may become 1.0). Calibrate live from free-cam against references/km elev ref.png (~640 m relief / 13.3 km; ±100-200 m undulation; occasional 40-60% grade faces ~300 m wide). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit coarse/fine/regional defaults + worldSeed to data/ranger.js (P7-3 lock); add seed field + layer sliders to debug.js</name>
  <files>data/ranger.js, src/debug.js</files>
  <read_first>
    - data/ranger.js (Phase 6 Terrain block lines 149–157, terrainAmplitude=0.1)
    - src/debug.js (initDebug 44, Terrain folder 115–126, sub-folder/onChange pattern, lil-gui string field note)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md §Debug Panel Integration, §Calibration Reference Analysis (starting params), §Regeneration (Path A vs Path B)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md §src/debug.js (terrain folder, debounce, string field, callbacks)
  </read_first>
  <action>
    In `data/ranger.js`, add a clearly-commented "Phase 7 Terrain — coarse/fine/regional layers (P7-3 committed defaults; sliders stay live)" block alongside terrainAmplitude: `coarseAmplitude`, `coarseFreq`, `coarseOctaves`, `ridgeSharpness`, `fineAmplitude`, `fineFreq`, `regionalStrength`, `regionalScale` with the calibration starting values from RESEARCH (coarseAmplitude 350, coarseFreq 0.001, coarseOctaves 5, ridgeSharpness 2.5, fineAmplitude 1.5, fineFreq 0.05, regionalStrength 0.6, regionalScale 3000). Re-set `terrainAmplitude` to a value consistent with the new coarse layer (the coarse layer now outputs metres directly; default `terrainAmplitude: 1.0` unless calibration in Task 3 shows otherwise — leave a comment that this is the live Y-rescale multiplier, Path A). This committed block is the P7-3 "lock": a bare `?seed=lone-pine` is now deterministic and shareable. Do NOT add an export/capture button (D-12) — the user reports values and the agent hand-edits this file.

    In `src/debug.js` `initDebug`, extend the existing `Terrain` folder. Add a World Seed text field (D-13/SEED-04): keep a small state object `{ seed: 'lone-pine' }`, `terrainFolder.add(_seedState,'seed').name('World Seed').onChange(v => { if (typeof callbacks.changeSeed==='function') callbacks.changeSeed(v) })` (lil-gui renders a text input for a string value). Add three sub-folders via `terrainFolder.addFolder(...)`: "Coarse Layer" (amplitude 50–500 step 10, coarseFreq 0.0005–0.005 step 0.0001 named "Wavelength/Freq", coarseOctaves 1–6 step 1, ridgeSharpness 1.0–4.0 step 0.1), "Fine Layer" (fineAmplitude 0–10 step 0.1, fineFreq 0.01–0.2 step 0.005), "Regional Modulator" (regionalStrength 0–1 step 0.05, regionalScale 500–10000 step 100). EVERY coarse/fine/regional slider's `.onChange` calls `callbacks.rebuildTerrainFull()` (Path B). The existing `terrainAmplitude` slider KEEPS its `callbacks.rebuildTerrain()` (Path A, instant). Do not debounce in debug.js — the debounce lives in main.js's rebuildTerrainFull (Task 2).
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('data/ranger.js','utf8');const d=require('fs').readFileSync('src/debug.js','utf8');for(const k of ['coarseAmplitude','coarseFreq','coarseOctaves','ridgeSharpness','fineAmplitude','fineFreq','regionalStrength','regionalScale']){if(!r.includes(k)){console.error('ranger.js missing',k);process.exit(1)}if(!d.includes(k)){console.error('debug.js missing slider',k);process.exit(1)}}if(!d.includes('World Seed')){console.error('debug.js missing World Seed field');process.exit(1)}if(!d.includes('rebuildTerrainFull')||!d.includes('changeSeed')){console.error('debug.js missing Path-B/seed callbacks');process.exit(1)}console.log('ranger defaults + debug sliders present')"</automated>
    <human-check>Open the debug panel (backtick). The Terrain folder shows a World Seed text input and Coarse/Fine/Regional sub-folders with all listed sliders.</human-check>
  </verify>
  <acceptance_criteria>
    - `data/ranger.js` contains committed defaults for all 8 layer params (coarseAmplitude/coarseFreq/coarseOctaves/ridgeSharpness/fineAmplitude/fineFreq/regionalStrength/regionalScale) + a sensible terrainAmplitude — this is the P7-3 lock (no export button per D-12).
    - `debug.js` has a World Seed text field and Coarse/Fine/Regional sub-folders; all shape sliders call `rebuildTerrainFull`, the seed field calls `changeSeed`, and the amplitude slider keeps `rebuildTerrain` (Path A).
    - Slider ranges match D-08/RESEARCH.
  </acceptance_criteria>
  <done>Terrain layer defaults are committed to data/ranger.js (shareable bare seed); the debug panel exposes the seed field and all three layers' live sliders wired to the correct Path-A/Path-B callbacks.</done>
</task>

<task type="auto">
  <name>Task 2: ?seed= parse + worldSeed; canonical resolveSpawn + re-seat; debounced Path-B regenerate in main.js (D-09/13/14/15/16, SEED-04)</name>
  <files>src/main.js</files>
  <read_first>
    - src/main.js (imports 18–26, computeStaticEquilibrium 65–91, vehicleState literal 119–138, initDebug 558–561, TerrainSystem 565, reset block 629–654, render loop terrainSystem.update 681)
    - src/seed.js (parseWorldSeed)
    - src/terrain.js (reinitWorker, rebuildAllChunksFromWorker, analyticHeight, analyticNormal from Plan 03)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md §Regeneration Without Page Reload (two paths, debounce, spawn function, memory note), §Seed System (URL parsing)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md §src/main.js (spawn/re-seat, debounce, callbacks, 3-places rule)
  </read_first>
  <action>
    In `main.js`, import `parseWorldSeed` from `./seed.js`. At module top, parse the URL once: `const _urlSeed = new URLSearchParams(window.location.search).get('seed'); let worldSeed = parseWorldSeed(_urlSeed ?? 'lone-pine')` (D-11 default). Use `worldSeed` when constructing TerrainSystem (replace the Plan-03 placeholder `parseWorldSeed('lone-pine')` at line 565 with this live `worldSeed`).

    Add the canonical spawn function `resolveSpawn(worldSeed, params)` → `{ position: THREE.Vector3, heading: number }` per D-14/16 and RESEARCH §Spawn Function Design: seed a starting offset from `seedFor(worldSeed,'spawn')` (import seedFor too), probe an expanding ring of candidate (x,z) using `terrainSystem.analyticNormal(x,z)` and pick the lowest-gradient point with grade < ~15% (normal.y > cos(15°) ≈ 0.966); fall back to origin with a console.warn if none found in ~20–50 tries; set `position.y = analyticHeight(x,z)` and derive `heading` from seed bits. Add a prominent comment: "Phase 7 seam — terrain-only low-slope resolver; Phase 8 swaps the body to a road-graph probe (nearest road node + tangent heading) at this SAME call site; do not change the signature." (D-16).

    Refactor the inline reset/seat logic into `_reseatTruckAtSpawn()` (D-15): call `resolveSpawn(worldSeed, RANGER_PARAMS)`, `computeStaticEquilibrium`, set `vehicleState.position` to `spawnPos` with `.y = analyticHeight(spawnPos.x, spawnPos.z) + eq.bodyY`, set quaternion from heading via `setFromAxisAngle((0,1,0), heading)`, zero velocity + angularVelocity + all motion fields, and reset strutComp/strutCompVel to equilibrium — reuse the exact field list already in the reset block (629–654) so nothing is missed. Use `_reseatTruckAtSpawn()` BOTH at initial load (replace/augment the spawn seat so the truck starts on the low-slope spawn, not always origin) AND inside the R-reset branch AND on every regenerate. If you introduce any NEW vehicleState field, honor the 3-PLACES rule (vehicle.js SPAWN_STATE + main.js literal + main.js reset) — otherwise note in the SUMMARY that no new field was added.

    Add the debounced Path-B rebuild `debouncedRebuildFull()` (RESEARCH §Regeneration / D-09): a module-level `clearTimeout/setTimeout` (~150 ms) that calls `terrainSystem.reinitWorker(worldSeed, RANGER_PARAMS)`, `terrainSystem.rebuildAllChunksFromWorker()`, then `_reseatTruckAtSpawn()`. Wire the initDebug callbacks (lines 558–561) to add: `rebuildTerrainFull: () => debouncedRebuildFull()` and `changeSeed: (v) => { worldSeed = parseWorldSeed(v); debouncedRebuildFull() }`. Keep the existing `rebuildTerrain` (Path A `rebuildAllChunks`) and `setRampVisible`. Free-cam is unaffected by regenerate — it keeps flying (D-15); only the truck is re-seated.
  </action>
  <verify>
    <automated>node -e "const m=require('fs').readFileSync('src/main.js','utf8');for(const tok of ['resolveSpawn','_reseatTruckAtSpawn','debouncedRebuildFull','rebuildTerrainFull','changeSeed','URLSearchParams','parseWorldSeed']){if(!m.includes(tok)){console.error('main.js missing',tok);process.exit(1)}}if(!/setTimeout[\\s\\S]{0,200}reinitWorker/.test(m)&&!/reinitWorker[\\s\\S]{0,200}setTimeout/.test(m)&&!/debouncedRebuildFull[\\s\\S]*setTimeout/.test(m)){console.error('debounce not wired to reinitWorker');process.exit(1)}console.log('seed parse + resolveSpawn + debounced Path-B present')"</automated>
    <human-check>Load index.html?seed=lone-pine; note the terrain. Refresh → same terrain. In the debug panel, type a new seed and apply → terrain regenerates in place (no page reload) and the truck teleports to a low-slope spawn, seated at ride height, stationary. Drag a coarse slider → terrain rebuilds after you stop dragging (~150 ms), truck re-seats; the truck never drops onto a steep face. Drag terrainAmplitude → instant Y-rescale, no Worker churn.</human-check>
  </verify>
  <acceptance_criteria>
    - `worldSeed` is parsed from `?seed=` (default `'lone-pine'`) and passed to TerrainSystem; refreshing the same seed reproduces the same terrain (SEED-01/03 via determinism).
    - `resolveSpawn(worldSeed, params)` returns `{position, heading}` resolving to a terrain-only low-slope point, with the documented Phase-8 road-graph seam comment and an unchanged call signature.
    - `_reseatTruckAtSpawn()` is used at initial load, R-reset, and every regenerate; it ground-probes `analyticHeight`, seats at ride height, zeros velocity (D-15).
    - Seed-field change and coarse/fine/regional slider changes route through a ~150 ms debounced `reinitWorker → rebuildAllChunksFromWorker → re-seat`; amplitude stays Path-A instant (D-09).
    - 3-places rule honored for any new vehicleState field (or SUMMARY states none added).
    - Free-cam keeps flying through a regenerate (only the truck re-seats).
  </acceptance_criteria>
  <done>Seed is URL-driven and editable-in-panel with no-reload regeneration; a single canonical road-aware spawn function re-seats the truck on every regenerate via analytic ground-probe; the two regeneration paths (instant amplitude vs debounced shape/seed) are wired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Calibration + 60fps checkpoint (TERR-05) — tune coarse params from free-cam vs the Sierra reference, then report values to lock</name>
  <files>data/ranger.js</files>
  <read_first>
    - references/km elev ref.png (calibration target — ~640 m relief / 13.3 km; ±100-200 m undulation; occasional 40-60% grade faces ~300 m wide)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md §Performance, §Calibration Reference Analysis
    - data/ranger.js (the committed defaults from Task 1 — the values to refine)
  </read_first>
  <what-built>
    Free-cam (Plan 01) + seeded three-layer terrain (Plan 03) + live sliders, seed field, regeneration, and canonical spawn (Tasks 1–2). The terrain now has Eastern-Sierra character with live-tunable coarse/fine/regional layers and a committed default param set.
  </what-built>
  <action>
    This is a human-verify checkpoint. The executor serves the running build and presents it for calibration; the user tunes the live coarse/fine/regional/terrainAmplitude sliders from free-cam against references/km elev ref.png and reports the final values. The executor then hand-writes the user-approved values into data/ranger.js (D-12 — no export button) and commits, finalizing the P7-3 lock. No autonomous code change precedes approval beyond what Tasks 1–2 shipped. Perform the steps listed in how-to-verify below.
  </action>
  <how-to-verify>
    1. Serve the repo (`npx serve .`) and open `index.html` (default seed lone-pine). Backtick to open the debug panel; ensure stats.js / the FPS HUD is visible.
    2. Press Shift+C and fly the free-cam over the terrain. Confirm Eastern-Sierra character per D-06/D-07: steep escarpment faces AND flat valley floors; drivable mountain-pass country (steep enough to justify Phase 8 switchbacks but never undriveable).
    3. Drive the truck on open ground at speed: the fine layer should noticeably unsettle the suspension (D-10) — adjust the Fine Layer sliders until it does without being undriveable.
    4. Tune Coarse Layer (amplitude/wavelength/octaves/ridge sharpness) and Regional sliders until the relief and grade statistics match the reference image (~640 m over a comparable span; occasional 40-60% faces). Each shape change debounce-rebuilds and re-seats the truck — confirm the truck always lands on a sensible low-slope spawn.
    5. Watch the FPS HUD with ~25 chunks loaded and the truck driving (physics sampling analyticHeight): confirm it holds >= 55 fps (TERR-05). Note any sustained drops.
    6. When the look is right, READ the final coarse/fine/regional/terrainAmplitude slider values off the panel and report them. The agent will hand-write them into `data/ranger.js` (D-12 — no export button) and commit, finalizing the P7-3 lock.
  </how-to-verify>
  <acceptance_criteria>
    - Free-cam view shows escarpments + flat valleys consistent with references/km elev ref.png (D-06/D-07).
    - Fine layer visibly bounces the suspension on open ground at speed (D-10).
    - FPS holds >= 55 with terrain active + physics sampling analyticHeight (TERR-05).
    - User-approved coarse/fine/regional/terrainAmplitude values are written into data/ranger.js and committed (P7-3 lock finalized).
  </acceptance_criteria>
  <verify>
    <human-check>From free-cam, terrain matches references/km elev ref.png character (escarpments + flat valleys); fine layer bounces suspension on open ground at speed; FPS HUD holds >= 55 fps with ~25 chunks + physics on analyticHeight. User reports final slider values.</human-check>
  </verify>
  <done>User-approved coarse/fine/regional/terrainAmplitude values are committed to data/ranger.js (P7-3 lock finalized); FPS >= 55 confirmed (TERR-05); terrain shows Eastern-Sierra character from free-cam.</done>
  <resume-signal>Reply "approved" with the final slider values to write into data/ranger.js, OR describe what still looks wrong (too smooth / too jagged / undriveable / fps drop) so the params can be re-tuned.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `?seed=` URL param + debug seed field → worldSeed | The two untrusted string inputs this phase; both flow into `parseWorldSeed` (djb2 hash from Plan 02) |
| debug slider values → terrain params | Same-origin user input; numeric, range-bounded by lil-gui min/max |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-INJ | Tampering | seed field / ?seed= parsing | mitigate | The seed string is consumed ONLY by `parseWorldSeed`→`djb2` arithmetic and assigned to a number; it is never put into `innerHTML`, `eval`, or a URL/DOM sink. lil-gui renders it as a plain text input (no HTML interpolation). |
| T-07-04-ROB | Denial of Service | regenerate spam via slider drag / pasted huge seed | mitigate | Path-B rebuild is debounced ~150 ms (one rebuild per settle, not per drag pixel — RESEARCH §Pitfall 7). djb2 handles arbitrary/empty/oversized strings deterministically without crashing. |
| T-07-04-SPAWN | Information Disclosure / robustness | resolveSpawn search loop | accept | Bounded candidate search (≤50 tries) with origin fallback + console.warn; no infinite loop, no remote data. |

No HIGH-severity threats — static client-side app; the only untrusted inputs are a hashed seed string and range-bounded numeric sliders.
</threat_model>

<verification>
- SEED-04: seed field edit regenerates without page reload (manual, 07-VALIDATION.md).
- SEED-01/03: same `?seed=` reproduces same terrain on refresh (manual).
- TERR-06: all three layers tunable live (automated slider-presence + manual).
- TERR-05: >= 55 fps checkpoint (Task 3 human-verify).
- D-15/16: regenerate teleports + ground-probes + re-seats truck at a low-slope spawn (manual).
- P7-3 lock: committed defaults in data/ranger.js after user-approved calibration.
</verification>

<success_criteria>
- World seed is URL-settable and panel-editable with deterministic no-reload regeneration (SEED-04).
- Coarse/fine/regional layers are live-tunable; defaults are committed and shareable (TERR-06, P7-3).
- Canonical road-aware spawn re-seats the truck on every regenerate (D-14/15/16).
- 60fps target met with layered terrain + analytic physics (TERR-05).
</success_criteria>

<output>
Create `.planning/phases/07-free-cam-seeded-layered-terrain/07-04-SUMMARY.md` when done (record the final committed coarse/fine/regional values and the measured FPS).
</output>
