---
phase: 07-free-cam-seeded-layered-terrain
plan: 05
type: execute
wave: 4
depends_on: [07-04]
files_modified:
  - index.html
  - src/main.js
  - src/terrain.js
autonomous: false
requirements: [TERR-06]
user_setup: []

must_haves:
  truths:
    - "Esc from chase/cockpit opens a minimal dev pause menu (in free-cam, the browser-forced pointer-lock release happens first and the menu is reachable after)"
    - "The menu has an option labeled exactly 'grid world' that takes the car to a flat-plane grid world (flat ground + dev grid, car at origin) with terrain streaming PAUSED"
    - "The Phase 6 test ramp/plateau props no longer appear in the Sierra terrain world; they appear in grid world as the rollover-test rig"
    - "A 'return to world' option re-enables terrain streaming and re-seats the car at the canonical spawn point"
  artifacts:
    - path: "index.html"
      provides: "minimal pause-menu overlay div (grid world / return to world / resume)"
      contains: "grid world"
    - path: "src/main.js"
      provides: "Esc handler (gated on !freecam), pause-menu wiring, grid-world mode toggle, ramp conditional on grid-world"
      contains: "grid world"
    - path: "src/terrain.js"
      provides: "setEnabled(flag) pausing _updateChunkRing/_flushPendingQueue"
      contains: "setEnabled"
  key_links:
    - from: "index.html #pause-menu buttons"
      to: "src/main.js menu handlers"
      via: "click listeners toggle grid world / return to world / resume"
      pattern: "grid world"
    - from: "src/main.js grid-world toggle"
      to: "terrainSystem.setEnabled + ramp visibility/contacts"
      via: "pause streaming, show ramp at origin, hide terrain chunks"
      pattern: "setEnabled"
---

<objective>
Add a minimal Esc pause menu (D-17) with an option labeled exactly "grid world" (D-18) that switches to a flat-plane dev grid world (car at origin, terrain streaming paused) for clean physics/suspension tuning, and a "return to world" option that re-enables streaming and re-seats the car at the canonical spawn point. Retire the Phase 6 test ramp/plateau props from the Sierra terrain world and relocate them into grid world as the controlled rollover-test rig (D-19). Add `TerrainSystem.setEnabled(flag)` to pause chunk streaming.

Purpose: This completes the calibration workflow — grid world gives a clean flat surface (with the ramp rig) for physics tuning without natural slopes clipping the props, while the Sierra world stays prop-free. Depends on Plan 04's canonical spawn (`_reseatTruckAtSpawn`) for "return to world".
Output: pause-menu overlay in `index.html`; menu + grid-world mode + ramp relocation in `main.js`; `setEnabled` in `terrain.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-CONTEXT.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md
@.planning/phases/07-free-cam-seeded-layered-terrain/07-04-SUMMARY.md

<interfaces>
<!-- index.html: HUD div + importmap + <script type="module" src="src/main.js">. Add a #pause-menu overlay (display:none) styled minimal/dev. -->
<!-- main.js -->
- Ramp constants + meshes: RAMP_* (324–358), terrain()/RAMP_TRIS (349–369), rampMesh (536–547), ramp contacts in queryContacts (508–528) and queryVertexContacts (447–481) guarded by `RANGER_PARAMS.rampEnabled !== false`.
- Esc/keydown listeners style (581–593). getCameraMode (camera.js) — gate Esc on `!== 'freecam'` (RESEARCH §Pitfall 3: do not open menu on Esc in freecam — browser releases pointer lock first).
- _reseatTruckAtSpawn() (Plan 04) — used by "return to world".
- terrainSystem.update(streamCenter) (render loop, Plan 01 gate).
<!-- terrain.js: update() calls _updateChunkRing + _flushPendingQueue. Add setEnabled(flag); when disabled, update() early-returns (streaming paused). Chunk meshes stay in scene unless explicitly hidden. -->
<!-- RESEARCH §Pause Menu and Grid World: grid world = car to origin, terrain streaming off, hide terrain chunks / show flat grid + ramp at origin; return = streaming on, re-seat at spawn, ramp removed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TerrainSystem.setEnabled + grid-world ramp relocation (ramp out of Sierra world, into grid world)</name>
  <files>src/terrain.js, src/main.js</files>
  <read_first>
    - src/terrain.js (update 228–232, _updateChunkRing 336–362, _flushPendingQueue 389–442, _chunkMap)
    - src/main.js (RAMP_* constants 324–358, rampMesh 536–547, queryContacts ramp guard 508–528, queryVertexContacts ramp guard 447–481, rampEnabled default)
    - data/ranger.js (rampEnabled line 157)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md §Pause Menu and Grid World (ramp relocation, streaming pause)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md §src/main.js (ramp conditional → world-mode flag)
  </read_first>
  <action>
    In `terrain.js`, add `setEnabled(flag)` setting a `this._enabled` boolean (default true). In `update(streamCenter)`, early-return when `this._enabled === false` so `_updateChunkRing` and `_flushPendingQueue` stop (streaming paused). Add an optional `setChunksVisible(flag)` (or have grid-world iterate `_chunkMap` and toggle `chunk.mesh.visible`) so the Sierra chunks can be hidden in grid world without disposing them.

    In `main.js`, introduce a module-scope `let _gridWorldActive = false`. Replace the always-on Sierra ramp with a grid-world-only ramp: (a) in `queryContacts` and `queryVertexContacts`, change the ramp guard from `RANGER_PARAMS.rampEnabled !== false` to additionally require `_gridWorldActive` (ramp collision only exists in grid world); (b) set `rampMesh.visible = _gridWorldActive` (initially false — the ramp is NOT in the Sierra world per D-19). Keep the existing `setRampVisible` debug toggle working as a secondary control, but the world-mode flag is authoritative. Position the ramp at the origin for grid world (the existing RAMP_END_Z=-20 origin-relative geometry is acceptable as the rig; do not redesign the ramp — just gate its visibility/contacts on `_gridWorldActive`). Expose a small internal API used by Task 2: functions `enterGridWorld()` and `returnToWorld()` (defined in Task 2) will flip `_gridWorldActive`, toggle ramp visibility/contacts, call `terrainSystem.setEnabled(false/true)` and `setChunksVisible(false/true)`, and place the car.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('src/terrain.js','utf8');const m=require('fs').readFileSync('src/main.js','utf8');if(!t.includes('setEnabled')){console.error('terrain.js missing setEnabled');process.exit(1)}if(!m.includes('_gridWorldActive')){console.error('main.js missing _gridWorldActive');process.exit(1)}if(!/_gridWorldActive/.test(m.split('queryContacts')[1]||'')){console.error('ramp contacts not gated on _gridWorldActive');process.exit(1)}console.log('setEnabled + grid-world ramp gating present')"</automated>
    <human-check>In the Sierra world, no ramp/plateau is visible and the truck cannot collide with one. (Grid-world entry verified in Task 2.)</human-check>
  </verify>
  <acceptance_criteria>
    - `terrain.js` has `setEnabled(flag)`; `update()` early-returns when disabled (streaming paused); chunks can be hidden without disposal.
    - In the Sierra terrain world (`_gridWorldActive === false`) the ramp mesh is invisible AND its triangles are skipped in both queryContacts and queryVertexContacts (D-19 — ramp retired from Sierra world).
    - Ramp collision/visibility is gated on `_gridWorldActive` (authoritative over the legacy rampEnabled toggle).
  </acceptance_criteria>
  <done>Terrain streaming can be paused via setEnabled; the test ramp is gated out of the Sierra world and reserved for grid world.</done>
</task>

<task type="auto">
  <name>Task 2: Pause-menu overlay + Esc handler + grid world / return to world (D-17/18)</name>
  <files>index.html, src/main.js</files>
  <read_first>
    - index.html (body: HUD div + module script — add #pause-menu overlay)
    - src/main.js (keydown listeners 581–593, getCameraMode import from Plan 01, _reseatTruckAtSpawn from Plan 04, vehicleState, computeStaticEquilibrium, enterGridWorld/returnToWorld hooks from Task 1)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-RESEARCH.md §Pause Menu and Grid World, §Pitfall 3 (Esc + pointer lock coexistence)
    - .planning/phases/07-free-cam-seeded-layered-terrain/07-PATTERNS.md §src/main.js (Esc handler gating), §Esc/keyboard listener coexistence
  </read_first>
  <action>
    In `index.html`, add a minimal dev-aesthetic `<div id="pause-menu" style="display:none; ...fixed, centered, dark panel...">` containing three clickable items: "resume", "grid world" (label EXACTLY `grid world` per D-18), and "return to world". Use plain buttons/divs with ids (e.g. `#pm-resume`, `#pm-grid`, `#pm-return`). Keep styling minimal (monospace, semi-transparent dark bg) consistent with the HUD.

    In `main.js`, add an Esc `keydown` listener (separate listener is fine — PATTERNS §Esc coexistence) gated on `getCameraMode() !== 'freecam'` (RESEARCH §Pitfall 3: in free-cam, the browser releases pointer lock on Esc; do NOT also open the menu in the same Esc — avoids flash-open/close). When eligible, `togglePauseMenu()` flips `#pause-menu` display. Pausing here is UI-only — DO NOT freeze the physics loop (the truck must keep settling, per the CAM-02 spirit and RESEARCH note). Wire the three menu buttons:
    - resume: hide the menu.
    - "grid world" → `enterGridWorld()`: set `_gridWorldActive = true`; `terrainSystem.setEnabled(false)` + hide Sierra chunks; show a flat dev grid (reuse a THREE.GridHelper or the removed flat ground plane — add a simple grid/plane at y=0 if none exists); place the car at origin via `computeStaticEquilibrium` (y = eq.bodyY, identity quaternion, zero velocity); make the ramp visible + collidable (Task 1 gating); hide the menu.
    - "return to world" → `returnToWorld()`: set `_gridWorldActive = false`; hide ramp + flat grid; `terrainSystem.setEnabled(true)` + show Sierra chunks; `_reseatTruckAtSpawn()` (canonical spawn re-seat from Plan 04); hide the menu.
    Ensure the render-loop `terrainSystem.update(streamCenter)` still runs but no-ops while disabled (Task 1). Free-cam interaction: if the user is in free-cam, the first Esc only releases the mouse (browser) and they can then reach the menu via a second Esc (now `getCameraMode()` is still 'freecam' — note: provide a way to open the menu after pointer release; simplest is that Esc in chase/cockpit opens it, and the user exits free-cam with C first; document this in the SUMMARY if the second-Esc path is not wired).
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('index.html','utf8');const m=require('fs').readFileSync('src/main.js','utf8');if(!h.includes('pause-menu')){console.error('index.html missing pause-menu');process.exit(1)}if(!h.includes('grid world')){console.error('index.html missing exact \\'grid world\\' label');process.exit(1)}if(!h.includes('return to world')){console.error('index.html missing return to world');process.exit(1)}for(const tok of ['enterGridWorld','returnToWorld','Escape','setEnabled']){if(!m.includes(tok)){console.error('main.js missing',tok);process.exit(1)}}if(!/getCameraMode\(\)\s*!==\s*'freecam'/.test(m)){console.error('Esc not gated on !freecam');process.exit(1)}console.log('pause menu + grid world wiring present')"</automated>
    <human-check>From chase view press Esc: the minimal pause menu appears with resume / grid world / return to world. Click "grid world": car goes to a flat grid with the ramp rig, terrain stops streaming. Roll the truck off the ramp to test rollover on the clean surface. Click Esc → "return to world": terrain resumes and the car re-seats at the canonical spawn. In free-cam, Esc releases the mouse without flashing the menu open/closed.</human-check>
  </verify>
  <acceptance_criteria>
    - `index.html` has a `#pause-menu` overlay containing an item labeled EXACTLY `grid world` plus `return to world` and a resume control.
    - Esc opens the menu only when `getCameraMode() !== 'freecam'` (no flash in free-cam — Pitfall 3).
    - "grid world" pauses terrain streaming, places the car at origin on a flat grid, and shows the ramp rig; "return to world" resumes streaming and re-seats via `_reseatTruckAtSpawn()`.
    - Physics is NOT frozen while the menu is open.
  </acceptance_criteria>
  <done>A minimal Esc pause menu offers grid world (flat plane + ramp rig, streaming paused) and return to world (streaming resumed + canonical re-seat); free-cam Esc behavior does not flash the menu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pause-menu DOM buttons → main.js handlers | Same-origin user clicks; no untrusted data |
| Esc key + pointer-lock release | Browser-mediated; Esc release is browser-forced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-DOM | Tampering | pause-menu overlay | mitigate | The overlay is static HTML authored in index.html with fixed labels; no user string is injected into it (no innerHTML from input). Button handlers call fixed functions only. |
| T-07-05-STATE | Robustness | grid-world / return-to-world toggling | accept | `_gridWorldActive` is an internal boolean; mismatched toggles only affect local view state (no data loss — chunks are hidden, not disposed; streaming re-enables idempotently). |

No HIGH-severity threats — static client-side app; pause menu is authored DOM with no untrusted input and no network/storage.
</threat_model>

<verification>
- D-17/18: Esc pause menu with exact "grid world" label; flat-plane mode with paused streaming (manual).
- D-19: ramp retired from Sierra world, present in grid world (automated gating check + manual).
- "return to world" re-seats via canonical spawn (manual; depends on Plan 04).
- Esc/pointer-lock coexistence: no menu flash in free-cam (manual, Pitfall 3).
</verification>

<success_criteria>
- Minimal Esc pause menu with a "grid world" flat-plane tuning mode (streaming paused) and "return to world" canonical re-seat (D-17/18).
- Phase 6 ramp/plateau retired from the Sierra world and relocated to grid world as the rollover rig (D-19).
- Terrain streaming pauses/resumes cleanly via setEnabled.
</success_criteria>

<output>
Create `.planning/phases/07-free-cam-seeded-layered-terrain/07-05-SUMMARY.md` when done.
</output>
