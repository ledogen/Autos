---
phase: 08-road-routing
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/road.js
autonomous: true
requirements: [ROAD-01, ROAD-04]
must_haves:
  # D-06 (REVISED) — seam C0/C1 is FREE: one continuous polyline sliced at 64 m boundaries
  # ROAD-04 — queryable splines; queryNearest(x,z) → {point, tangent}
  # D-07 (consumed in 08-03) — queryNearest powers resolveSpawn (truck on road, facing down it)
  truths:
    - "The continuous trunk polyline is sliced at 64 m tile boundaries into per-tile Catmull-Rom splines whose consecutive endpoints/tangents match exactly (C0/C1) because they are one curve sliced — no shared-seam-waypoint machinery"
    - "queryNearest(wx, wz) returns the nearest network point and a unit tangent, or null beyond radius, allocation-light at query cadence"
    - "ensureTile(tileX, tileZ) warms the network around a tile so a subsequent queryNearest there is non-null"
  artifacts:
    - path: "src/road.js"
      provides: "Per-tile slicing of continuous polylines into Catmull-Rom splines + queryNearest + ensureTile over the streamed network"
      contains: "queryNearest"
  key_links:
    - from: "src/road.js queryNearest"
      to: "this._network polylines / per-tile splines"
      via: "samples sliced splines for nearest point + tangent"
      pattern: "getTangentAt|getTangent"
    - from: "src/road.js ensureTile"
      to: "_streamNetwork"
      via: "warms the network around the requested tile before slicing/query"
      pattern: "_streamNetwork|_sliceNetwork"
---

<objective>
Slice the canonical continuous trunk polylines (built by `_streamNetwork` in 08-01) into stable
per-tile Catmull-Rom splines, and rebuild the queryable centerline API (`queryNearest`, `ensureTile`)
over the streamed network.

Because the trunk is ONE continuous polyline sliced at 64 m boundaries, seam C0/C1 continuity is
automatic (D-06 REVISED): consecutive per-tile splines share exact endpoints and tangents with NO
shared-seam-waypoint / ghost-point machinery (the old approach that failed VERIFICATION.md is moot).
This delivers ROAD-04 (queryable splines) and the per-tile slice that Phase 9 consumes (Real-build
scope item 3), and `queryNearest(x,z) → {point, tangent}` that 08-03 wires into `resolveSpawn` (D-07).

Purpose: turn raw network polylines into the stable, queryable, per-tile-sliced spline output that is
the actual deliverable Phase 9 builds its ribbon mesh on.
Output: `src/road.js` with polyline→per-tile-spline slicing, `queryNearest`, and `ensureTile` operating
over `this._network`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-road-routing/08-CONTEXT.md
@.planning/phases/08-road-routing/08-01-SUMMARY.md
@.planning/spikes/MANIFEST.md

<interfaces>
<!-- From 08-01 (this plan's dependency). Executor uses these directly. -->

src/road.js (built in 08-01):
  this._network : Map<string, { points: THREE.Vector3[] }>   canonical continuous trunk polylines
  _streamNetwork(center)                                     (re)builds this._network around center (lazy)
  this._proto.radius                                         visible road radius (network window size)

src/terrain.js: export const CHUNK_SIZE = 64   (per-tile slice boundary; world tile = [tileX*64,(tileX+1)*64))

Phase 9 + resolveSpawn (downstream) expect from RoadSystem (keep these signatures STABLE):
  ensureTile(tileX, tileZ) → tile-ish (idempotent; warms network so query is non-null)
  queryNearest(wx, wz, radiusM=200) → { point: THREE.Vector3, tangent: THREE.Vector3 } | null
    point + tangent finite; tangent UNIT length (|len-1| < 1e-3); face-down-road heading = atan2(tangent.x, tangent.z)

Allocation discipline (RESEARCH anti-pattern): queryNearest is called near-60fps from resolveSpawn /
Phase 9 — reuse a module-scope scratch Vector3 (_scratchPt already exists) for the per-sample distance
probe; allocate only the two returned vectors.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slice continuous polylines into per-tile Catmull-Rom splines (seam C0/C1 free)</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js this._network shape + `_streamNetwork` (from 08-01 — the polylines to slice).
    - src/road.js existing `THREE.CatmullRomCurve3(..., false, 'centripetal', 0.5)` usage in
      `_streamNetwork` (match parameterization so sliced splines are consistent with the rendered line).
    - src/terrain.js CHUNK_SIZE export (= 64; the slice boundary).
    - .planning/phases/08-road-routing/VERIFICATION.md "Recommended architecture" (slice ONE curve →
      C0/C1 hold trivially; do NOT reintroduce shared-seam waypoints).
  </read_first>
  <action>
    Add a **distinct** `_sliceNetwork()` method (a separate public method on RoadSystem — do NOT
    fold it into `_streamNetwork`; plans 08-03 and 08-04 call `_sliceNetwork()` by name) that converts
    each canonical
    `this._network` polyline into per-tile Catmull-Rom splines, stored in a `this._tiles`
    Map<"tileX,tileZ", { spline: THREE.CatmullRomCurve3, points: THREE.Vector3[] }[]> (a tile may hold
    multiple road segments). For each polyline: build ONE continuous `THREE.CatmullRomCurve3(points,
    false, 'centripetal', 0.5)`, then walk it and cut it at every x or z crossing of a 64 m
    (`CHUNK_SIZE`) tile boundary, inserting the exact boundary-crossing point into BOTH the ending
    sub-polyline and the starting sub-polyline so the two adjacent per-tile splines share that exact
    point (C0) and, being samples of the same parent curve, share the tangent there (C1). Assign each
    sub-polyline to the tile that contains its midpoint (`Math.floor(midX/CHUNK_SIZE)`,
    `Math.floor(midZ/CHUNK_SIZE)`). Build each sub-polyline's spline with the SAME centripetal params.
    De-duplicate consecutive coincident control points before constructing each spline (centripetal
    divide-by-zero guard). The slice must be deterministic (pure function of the network polylines,
    which are pure functions of seed+coords+params). This replaces all old shared-seam-waypoint /
    ghost-point logic — none of it returns.
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -q 'this._tiles' src/road.js && grep -vn '^\s*\*\|^\s*//' src/road.js | grep -c '_seamPoint\|ghost' | grep -qx 0 && node --input-type=module -e "import('three').then(async THREE=>{const {RoadSystem}=await import('./src/road.js');const {parseWorldSeed}=await import('./src/seed.js');const p={coarseAmplitude:150,coarseFreq:0.0005,coarseOctaves:4,ridgeSharpness:1.6,maxRoadGrade:0.15,roadWDist:1,roadWAlt:0.85,roadWGrade:400,roadWOver:8000,roadWTurn:120,spurProbability:0.15};const r=new RoadSystem(parseWorldSeed('lone-pine'),p);r._proto.radius=400;r._streamNetwork(new THREE.Vector3(0,0,0));if(r._sliceNetwork)r._sliceNetwork();let segs=0;for(const a of r._tiles.values())segs+=a.length;console.log(segs>0?'SLICE_OK':'SLICE_EMPTY')}).catch(e=>{console.error(e.message);process.exit(1)})" 2>&1 | grep -q SLICE_OK && echo PASS</automated>
  </verify>
  <done>
    `this._tiles` holds per-tile Catmull-Rom spline segments sliced from the continuous network
    polylines; at least one tile carries a segment at origin on lone-pine; no `_seamPoint`/ghost-point
    code remains. C0/C1 across 64 m boundaries is asserted green by the 08-04 exit-gate harness.
  </done>
  <acceptance_criteria>
    - Per-tile splines are slices of ONE continuous curve — adjacent tiles share the exact boundary
      point (C0) and tangent (C1) with NO shared-seam-waypoint machinery (D-06 REVISED).
    - Slicing is deterministic and keyed by tile coords (ROAD-01 tile-able graph).
    - Spline parameterization matches the rendered network (centripetal, 0.5).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rebuild queryNearest + ensureTile over the streamed/sliced network (ROAD-04)</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js `_scratchPt` module-scope scratch (allocation guard) + the old `queryNearest` shape
      that was removed/stubbed in 08-01 (reuse its `getPointAt`/`getTangentAt` sampling contract).
    - src/road.js `this._tiles` (Task 1) + `_streamNetwork`/`_sliceNetwork` (08-01 + Task 1).
    - src/main.js `resolveSpawn` lines 126–154 — the consumer: it ensureTiles a 3×3 region then
      `queryNearest(baseX, baseZ, 200)` and reads `nearest.point` + `nearest.tangent` (signature must
      stay `{point, tangent}|null`).
  </read_first>
  <action>
    Rebuild `queryNearest(wx, wz, radiusM = 200)` to search the sliced per-tile splines in `this._tiles`
    (and/or the raw `this._network` polylines if a tile lacks splines): sample each candidate spline at
    N arc-length intervals using the module-scope `_scratchPt` for the per-sample distance probe (no
    per-sample allocation), track the nearest within `radiusM` (XZ distance), and return
    `{ point: spline.getPointAt(bestU), tangent: spline.getTangentAt(bestU) }` (the only two
    allocations) or `null` if nothing is within radius. Tangent must be unit length. Restrict the
    search to splines in tiles near (wx,wz) for O(1)-ish cost (e.g. iterate the 3×3 tile block around
    the query tile rather than the whole network). Rebuild `ensureTile(tileX, tileZ)` to: call
    `_streamNetwork` centered on the tile's world center (and `_sliceNetwork` if separate) so the
    network+slices around that tile exist, then return the tile's segment array (idempotent — repeated
    calls return the same cached segments). This restores the exact contract `resolveSpawn` already
    depends on (warm 3×3 tiles, then `queryNearest`) so no main.js change is needed for spawn to work
    (08-03 only refreshes wiring/comments). Keep `queryNearest` safe to call before any tile is warmed
    (returns null, no throw).
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && node --input-type=module -e "import('three').then(async THREE=>{const {RoadSystem}=await import('./src/road.js');const {parseWorldSeed}=await import('./src/seed.js');const p={coarseAmplitude:150,coarseFreq:0.0005,coarseOctaves:4,ridgeSharpness:1.6,maxRoadGrade:0.15,roadWDist:1,roadWAlt:0.85,roadWGrade:400,roadWOver:8000,roadWTurn:120,spurProbability:0.15};const r=new RoadSystem(parseWorldSeed('lone-pine'),p);r._proto.radius=400;for(let dx=-1;dx<=1;dx++)for(let dz=-1;dz<=1;dz++)r.ensureTile(dx,dz);let hit=null;for(let i=0;i<400&&!hit;i++){hit=r.queryNearest((i%20)*32-320,((i/20)|0)*32-320,400);}if(!hit){console.log('NO_HIT');process.exit(1)}const L=Math.hypot(hit.tangent.x,hit.tangent.y,hit.tangent.z);console.log(Math.abs(L-1)<1e-3&&isFinite(hit.point.x)?'QN_OK':'QN_BAD')}).catch(e=>{console.error(e.message);process.exit(1)})" 2>&1 | grep -q QN_OK && echo PASS</automated>
  </verify>
  <done>
    `queryNearest` returns `{point, tangent}` with a unit-length tangent and finite point for a query
    near the warmed network, and `null` (no throw) when nothing is in radius; `ensureTile` is
    idempotent and warms the network so a subsequent query there is non-null. The `resolveSpawn`
    consumer contract (3×3 ensureTile → queryNearest → atan2 heading) is satisfied unchanged.
  </done>
  <acceptance_criteria>
    - ROAD-04: road centerlines queryable as splines; `queryNearest(x,z) → {point, tangent}` (D-07-ready).
    - Allocation-light: per-sample scratch reused; only the two returned vectors allocated.
    - `ensureTile` idempotent; queryable network warms around the requested tile.
    - No old-router symbols reachable; query operates only over `this._network` / `this._tiles`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `this._tiles` exists and holds per-tile sliced splines; `this._network` is its source.
- No `_seamPoint` / ghost-point code in non-comment lines of src/road.js.
- Headless ESM: slicing produces ≥1 tile segment at origin; `queryNearest` returns a unit-tangent hit
  near the warmed network and `null` out of radius.
- (Deferred to 08-04 exit gate) C0 < 0.01 m and C1 < 5° across every 64 m seam.
</verification>

<success_criteria>
The continuous valley trunk is sliced into stable, deterministic per-tile Catmull-Rom splines with
free C0/C1 seam continuity, and is queryable via `queryNearest(x,z) → {point, tangent}` and
`ensureTile`. This is the stable per-tile spline output Phase 9 consumes and the query that puts the
truck on the road (D-07), delivered without any shared-seam-waypoint machinery.
</success_criteria>

<output>
Create `.planning/phases/08-road-routing/08-02-SUMMARY.md` when done.
</output>
