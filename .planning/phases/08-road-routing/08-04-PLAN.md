---
phase: 08-road-routing
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - test/test-road-seam.html
  - test/test-road.html
  - test/road-test-harness.js
autonomous: true
requirements: [ROAD-01, ROAD-02, ROAD-03, ROAD-04]
must_haves:
  # D-06 (REVISED) — seam-continuity exit gate must pass on the valley-trunk sliced splines
  # Real-build scope item 6 — re-run the Phase-8 verification / seam exit gate
  truths:
    - "test-road-seam.html asserts C0 (<0.01 m) and C1 (<5°) across 64 m seams on the valley-trunk sliced splines and passes green on the locked lone-pine terrain"
    - "test-road.html asserts determinism (same seed → identical network), queryNearest {point, unit tangent}, switchback lateral extent on a steep ramp, and a soft cost model (no hard 'no path'), all against the new network API"
    - "TEST_PARAMS carries the locked D-09 cost-model params so the harnesses route the real architecture"
  artifacts:
    - path: "test/test-road-seam.html"
      provides: "Seam-continuity exit gate over the sliced valley-trunk splines (the phase exit gate)"
      contains: "C0"
    - path: "test/road-test-harness.js"
      provides: "TEST_PARAMS with D-09 cost weights; assert + mockCoarseHeight helpers"
      contains: "roadWAlt"
  key_links:
    - from: "test/test-road-seam.html"
      to: "src/road.js sliced per-tile splines (this._tiles)"
      via: "ensureTile + adjacent-tile spline endpoint/tangent comparison"
      pattern: "ensureTile|queryNearest"
    - from: "test/road-test-harness.js TEST_PARAMS"
      to: "src/road.js _streamNetwork / _protoEdgeCost"
      via: "supplies the D-09 weights the router reads"
      pattern: "roadWAlt"
---

<objective>
Refresh the browser exit-gate harnesses to assert the VALLEY-TRUNK architecture (not the retired
per-tile router), and re-run the Phase-8 seam exit gate (Real-build scope item 6). The seam gate
(`test/test-road-seam.html`) is the phase exit gate (D-06 REVISED): C0/C1 continuity across 64 m
boundaries must be green before Phase 9. Because the trunk is one continuous polyline sliced at tile
boundaries, this should now PASS where the old shared-seam-waypoint approach failed.

These are browser harnesses (browser-only project — execution is the user's UAT). This plan keeps and
updates them and states the acceptance assertions so the user can run them as the exit gate. Updates:
- `road-test-harness.js` — `TEST_PARAMS` carries the locked D-09 cost params (wAlt 0.85 / wGrade 400 /
  wOver 8000 / maxGrade 0.15 / wTurn 120 / wDist 1; keep coarse-layer locked values + mockCoarseHeight).
- `test-road-seam.html` — slice the network around origin, assert C0/C1 across every 64 m seam.
- `test-road.html` — determinism, queryNearest {point, unit tangent}, switchback lateral extent on the
  steep mock ramp, and "soft model never returns no-path" (replacing the old hard-grade-block assertions).

Purpose: prove the rebuilt road system passes the exit gate the old architecture could not, against
the real locked terrain.
Output: three updated test files; the seam gate green-able in a browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-road-routing/08-CONTEXT.md
@.planning/phases/08-road-routing/08-02-SUMMARY.md
@.planning/phases/08-road-routing/08-03-SUMMARY.md

<interfaces>
<!-- The harnesses import the real src/road.js + src/seed.js. Use the 08-01/08-02/08-03 API. -->

src/road.js (post 08-01..03):
  new RoadSystem(worldSeed, params[, coarseHeightOverride])
  setRadius(r) / init(scene) / update(center) / _streamNetwork(center) / _sliceNetwork()
  this._tiles : Map<"tileX,tileZ", { spline: THREE.CatmullRomCurve3, points: Vector3[] }[]>
  ensureTile(tileX, tileZ) — warms network so query is non-null
  queryNearest(wx, wz, radiusM=200) → { point, tangent } | null  (tangent unit length)

src/seed.js: parseWorldSeed(str|int) → uint32

test/road-test-harness.js (existing): export assert(label, cond), mockCoarseHeight(wx,wz)= wz*0.5
  (50% ramp), TEST_PARAMS (coarse-layer locked + Phase-8 routing params)

Browser run: python3 test/nocache-server.py 8138 → open http://127.0.0.1:8138/test/test-road-seam.html
  and .../test-road.html ; read DevTools console for PASS/FAIL.

Exit-gate thresholds (KEEP): C0 endA.distanceTo(startB) < 0.01 m ; C1 angle(tangents) < 5° ; tangent
unit length |len-1| < 1e-3 ; determinism diff < 1e-9.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TEST_PARAMS to the D-09 cost model + retarget the seam exit gate to sliced splines</name>
  <files>test/road-test-harness.js, test/test-road-seam.html</files>
  <read_first>
    - test/road-test-harness.js (TEST_PARAMS, assert, mockCoarseHeight) — the routing params block to
      replace with D-09 weights; keep coarse-layer locked values + mockCoarseHeight ramp.
    - test/test-road-seam.html — the C0/C1 seam exit gate (currently uses ensureTile + `_getTile(...)
      .spline`); retarget to the sliced `this._tiles` per-tile splines from 08-02.
    - src/road.js `this._tiles` shape + `ensureTile` + `_sliceNetwork` (08-02) — how to obtain
      adjacent-tile splines for the seam comparison.
  </read_first>
  <action>
    In test/road-test-harness.js: replace the Phase-8 routing fields of `TEST_PARAMS`
    (maxRoadGrade 0.12, routeGridSize, roadSlopePenalty, roadAltWeight) with the locked D-09 params —
    `maxRoadGrade: 0.15, roadWDist: 1, roadWAlt: 0.85, roadWGrade: 400, roadWOver: 8000, roadWTurn: 120,
    spurProbability: 0.15` — keeping the coarse-layer locked block (coarseAmplitude 150 / coarseFreq
    0.0005 / coarseOctaves 4 / ridgeSharpness 1.6 / terrainAmplitude 1.0) and `assert` + `mockCoarseHeight`
    unchanged.

    In test/test-road-seam.html: keep the page framing/importmap but retarget the seam test to the
    valley-trunk sliced splines. Build a RoadSystem on 'lone-pine' with TEST_PARAMS, `setRadius` wide
    enough to cover a 3×3 tile region around origin, stream + slice (`ensureTile` over the 3×3 grid or
    `update(origin)` then `_sliceNetwork`). For each pair of east-west adjacent tiles that both carry a
    road segment, find the two segment splines that meet at the shared 64 m boundary and assert
    (a) C0: the ending spline's boundary endpoint `.distanceTo` the starting spline's boundary endpoint
    < 0.01 m, and (b) C1: angle between the two boundary tangents < 5°. Because both come from slicing
    ONE continuous curve at that boundary, both must hold. Also keep the determinism sub-test: two
    RoadSystem instances, same seed → identical boundary tangent (diff < 1e-9). Emit the EXIT GATE
    PASS/FAIL summary line. If a given tile pair shares no road (sparse network — valid), skip that
    pair rather than failing; assert the gate over the seams that DO carry road, and assert at least one
    seam was checked.
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -q 'roadWAlt' test/road-test-harness.js && grep -vn '^\s*//\|^\s*\*' test/road-test-harness.js | grep -c 'routeGridSize\|roadSlopePenalty\|roadAltWeight' | grep -qx 0 && grep -q 'C0' test/test-road-seam.html && grep -q 'ensureTile\|_sliceNetwork\|update(' test/test-road-seam.html && grep -vn '<!--' test/test-road-seam.html | grep -c '_routeTile\|_seamPoint\|_deriveEdgeWaypoints' | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>
    TEST_PARAMS carries the D-09 weights (roadWAlt etc.) and no retired routing params; test-road-seam.html
    builds the valley-trunk network, slices it, and asserts C0 (<0.01 m) + C1 (<5°) on the sliced
    per-tile splines plus determinism, with no references to the retired per-tile router symbols.
  </done>
  <acceptance_criteria>
    - D-06 exit gate retargeted to the sliced valley-trunk splines (one curve, sliced — C0/C1 free).
    - TEST_PARAMS uses the locked D-09 cost model; coarse-layer locked values + mockCoarseHeight kept.
    - Gate skips genuinely road-free seams (sparse network) but asserts ≥1 seam and determinism.
    - No assertions reference the retired `_routeTile`/`_seamPoint`/ghost-point machinery.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Refresh test-road.html — determinism, queryNearest, switchback, soft-model (ROAD-01..04)</name>
  <files>test/test-road.html</files>
  <read_first>
    - test/test-road.html — the full suite (ROAD-01 determinism, ROAD-02 hard-grade, ROAD-03 switchback,
      ROAD-04 query, C0 seam, ensureTile idempotent, queryNearest, setDebugVisible, D-06 seam) — most
      assertions reference the retired per-tile API and must be rewritten against the new network API.
    - src/road.js new API (08-01..03): _streamNetwork/_sliceNetwork/this._network/this._tiles,
      ensureTile, queryNearest, setDebugVisible, setRadius, update.
    - test/road-test-harness.js mockCoarseHeight (50% ramp) — drives the switchback assertion.
  </read_first>
  <action>
    Rewrite test/test-road.html's assertions against the valley-trunk network API:
    - ROAD-01 determinism: two RoadSystems with the same seed produce IDENTICAL network geometry —
      stream+slice both around origin and assert equal `_network`/`_tiles` segment counts and an equal
      sampled spline point (distance < 1e-6) and equal `queryNearest` result at a fixed (x,z).
    - ROAD-02/D-09 soft model: assert the router NEVER returns "no path" — on the real terrain a streamed
      network at origin is non-empty (≥1 segment), and assert no routed edge cost is Infinity (the soft
      over-cap model). REMOVE the old hard-grade-block assertion (`grade ≤ maxRoadGrade` on every edge) —
      it contradicts D-02 REVISED; replace it with: most edges are at/under maxGrade but genuine passes
      may exceed it (soft target), so assert the network exists and is finite rather than a hard cap.
    - ROAD-03 switchback: build a RoadSystem with `mockCoarseHeight` (50% ramp), stream a connection that
      must gain altitude, and assert the resulting polyline's lateral X-extent exceeds one routing cell
      (PROTO_CELL = 10 m) — i.e. it switchbacks laterally rather than climbing straight (turn-penalty A*
      + altitude term, per D-09 / spike 001).
    - ROAD-04 query: `ensureTile` 3×3 then `queryNearest` returns `{point, tangent}` with finite point +
      unit tangent (|len-1| < 1e-3); `ensureTile` idempotent; `setDebugVisible(true)` builds lines on a
      stub scene and `(false)` hides without dispose (count stable).
    - Keep the folded D-06 C0/C1 seam sub-test mirroring test-road-seam.html (single-page full suite).
    Remove every assertion that calls `_routeTile`/`_getTileWaypointsOnly`/`_deriveEdgeWaypoints`/
    `_seamPoint`. Keep the importmap + console PASS/FAIL framing.
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -vn '<!--' test/test-road.html | grep -c '_routeTile\|_getTileWaypointsOnly\|_deriveEdgeWaypoints\|_seamPoint' | grep -qx 0 && grep -q 'queryNearest' test/test-road.html && grep -q 'switchback' test/test-road.html && grep -qi 'determinism' test/test-road.html && echo PASS</automated>
  </verify>
  <done>
    test-road.html asserts determinism (identical network/query across same-seed instances), the soft
    cost model (non-empty finite network, no hard "no path"), switchback lateral extent on the mock
    ramp, and queryNearest {point, unit tangent} + ensureTile idempotency + setDebugVisible toggle — all
    against the new API, with zero references to the retired per-tile router symbols.
  </done>
  <acceptance_criteria>
    - ROAD-01..04 re-asserted against the valley-trunk network API.
    - Old hard-grade-block assertion replaced by the soft-model "never no path" assertion (D-02 REVISED).
    - Switchback assertion runs on mockCoarseHeight and checks lateral extent.
    - No retired per-tile router symbols referenced.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- test/road-test-harness.js: TEST_PARAMS has roadWAlt/roadWGrade/roadWOver/roadWTurn + maxRoadGrade
  0.15; no routeGridSize/roadSlopePenalty/roadAltWeight.
- test/test-road-seam.html: asserts C0 (<0.01 m) + C1 (<5°) on sliced splines; no retired symbols.
- test/test-road.html: determinism + queryNearest + switchback + soft-model assertions; no retired
  symbols.
- USER UAT (browser): `python3 test/nocache-server.py 8138` → both pages print all PASS, seam EXIT GATE
  PASS — green before Phase 9.
</verification>

<success_criteria>
The exit-gate harnesses assert the valley-trunk architecture and pass on the locked lone-pine terrain:
C0/C1 seam continuity holds on the sliced splines (D-06 REVISED — the gate the old model failed), the
network is deterministic and queryable, switchbacks emerge on steep ground, and the soft cost model
never returns "no path." Phase 8 is verifiable and ready to gate Phase 9.
</success_criteria>

<output>
Create `.planning/phases/08-road-routing/08-04-SUMMARY.md` when done.
</output>
