---
phase: 08-road-routing
plan: 07
type: execute
wave: 3
gap_closure: true
depends_on: ["08-06"]
files_modified:
  - src/road.js
  - src/debug.js
  - src/main.js
autonomous: false
requirements: [ROAD-01, ROAD-04]
must_haves:
  # D-05 — shipped viz = centerline splines only, lil-gui checkbox, clean by default
  # D-03 — maxGrade (+ dominant cost weights) live sliders → debounced deterministic re-route
  # D-07 — resolveSpawn → queryNearest puts truck ON the road facing down it (PRESERVE existing wiring)
  # D-06 — seam exit gate (test/test-road-seam.html) green-able
  # Closes 08-VERIFICATION gaps: truths 6 (failed) and 7 (partial)
  truths:
    - "Shipped road viz is centerline splines only, toggled by a single lil-gui checkbox, off (clean) by default; the proto 'Valley Trunk' folder and retired per-tile sliders (roadSlopePenalty/roadAltWeight) are gone"
    - "Moving the maxGrade or D-09 cost-weight sliders re-streams the valley-trunk network deterministically (debounced); the truck spawns on the nearest road facing down it via queryNearest"
    - "main.js drives the network via roadSystem.update(streamCenter) each frame; no updateProto/setProto*/onProto* references remain in road.js, debug.js, or main.js"
  artifacts:
    - path: "src/road.js"
      provides: "Unified update(center) streamer + buildDebugLines/setDebugVisible centerline viz from this._tiles; proto-only API (setProtoEnabled/setProtoParam/updateProto) removed"
      contains: "update"
    - path: "src/debug.js"
      provides: "Roads folder: Show Road Splines + maxGrade + D-09 cost sliders (roadWAlt/roadWGrade/roadWOver/roadWTurn); Valley Trunk (proto) folder and retired sliders removed"
      contains: "roadWAlt"
    - path: "src/main.js"
      provides: "roadSystem.update(streamCenter) in render loop + debounced re-stream + preserved resolveSpawn queryNearest wiring; proto wiring retired"
      contains: "roadSystem.update("
  key_links:
    - from: "src/debug.js Roads folder sliders"
      to: "src/main.js debouncedRoadRebuild"
      via: "onRoadParamChange callback → invalidate + re-stream network"
      pattern: "onRoadParamChange"
    - from: "src/main.js resolveSpawn"
      to: "roadSystem.queryNearest"
      via: "nearest road point + atan2(tangent) heading (D-07 — PRESERVE)"
      pattern: "queryNearest"
    - from: "src/main.js render loop"
      to: "roadSystem.update(streamCenter)"
      via: "streamed around the same center as terrain each frame"
      pattern: "roadSystem.update\\("
---

<objective>
Finish productionization: make the shipped debug viz the real centerline-only, checkbox-toggled,
clean-by-default visualization (D-05) driven from `this._tiles`; replace the retired per-tile sliders
with the D-09 cost-weight sliders wired to a debounced deterministic re-stream (D-03); retire ALL
prototype scaffolding (the "Valley Trunk (proto)" folder, `setProto*`/`updateProto`); switch the render
loop to a unified `roadSystem.update(streamCenter)`; and PRESERVE the already-correct `resolveSpawn`
queryNearest wiring (D-07). Then re-run the seam exit gate (test/test-road-seam.html) — it must pass.

GROUND TRUTH (verified by grep on committed files — the 08-03 SUMMARY's PASS claim is FALSE):
- src/main.js still calls `roadSystem.updateProto(streamCenter)` (line 1014), `setProtoRadius(640)`
  (line 779), and wires `onProtoToggle`/`onProtoParam` (lines 758–759); `debouncedRoadRebuild`
  (lines 240–245) still calls `invalidateCache` + `buildDebugLines`.
- src/debug.js still has the `roadSlopePenalty` slider (line 206), `roadAltWeight` slider (line 209),
  and the full "Valley Trunk (proto)" subfolder with `_protoState` + onProtoToggle/onProtoParam
  (lines 213–229).
- src/road.js (after 08-05/08-06) still owns the `_proto` viz API (`setProtoEnabled`, `setProtoParam`,
  `setProtoRadius`, `updateProto`, `_clearProtoLines`) used only by the proto toggle.
PRESERVE: `resolveSpawn` in main.js (lines 126–153) — its ensureTile-3×3 + queryNearest +
`atan2(tangent.x, tangent.z)` heading wiring is correct and must NOT be broken.

Purpose: deliver the shipped viz, live re-route, and spawn; remove every "(proto)" seam so the valley
trunk is simply the road system; and make the D-06 exit gate green.
Output: a unified shipped viz in src/road.js; a clean Roads folder in src/debug.js; render-loop
streaming + preserved spawn wiring in src/main.js; test/test-road-seam.html passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-road-routing/08-CONTEXT.md
@.planning/phases/08-road-routing/08-VERIFICATION.md
@.planning/phases/08-road-routing/08-05-SUMMARY.md
@.planning/phases/08-road-routing/08-06-SUMMARY.md

<interfaces>
<!-- From 08-05 / 08-06 (dependencies). Plus the existing main.js/debug.js seams to rewire. -->

src/road.js (08-05/08-06): _streamNetwork(center), _sliceNetwork(), this._network, this._tiles,
  ensureTile(tileX,tileZ)→{spline,waypoints}, queryNearest(wx,wz,radiusM)→{point,tangent}|null,
  _protoAnchor(mx,mz), _protoConnect(a,b), _buildDebugLine2(pts,color), this._proto.params (D-09 weights),
  this._proto.radius, this._proto.lines, this._proto.surfaceY, _clearProtoLines(), invalidateCache()
  STILL PRESENT (to retire here): setProtoEnabled, setProtoParam, setProtoRadius, updateProto

src/main.js seams (CURRENT line numbers):
  resolveSpawn (lines 126–153) — ensureTile 3×3 + queryNearest(baseX,baseZ,200) + atan2(tangent.x,tangent.z) (D-07) — PRESERVE
  seed-change re-instantiation (lines ~221–227) — new RoadSystem + init + (re)build lines
  debouncedRoadRebuild (lines 240–245) — invalidateCache + buildDebugLines → re-point to re-stream
  callbacks block (lines 755–759) — onRoadVizToggle / onRoadParamChange / onProtoToggle / onProtoParam
  init block (lines 774–779) — new RoadSystem; init(scene); setSurfaceSampler; setProtoRadius(640)
  render loop (line 1014) — roadSystem.updateProto(streamCenter)

src/debug.js seams:
  Roads folder (lines 196–229) — Show Road Splines (198) + maxRoadGrade (201) + roadSlopePenalty (206) +
    roadAltWeight (209) + the entire "Valley Trunk (proto)" subfolder (_protoState @219, lines 213–229)

data/ranger.js (08-05): roadWDist/roadWAlt/roadWGrade/roadWOver/roadWTurn/maxRoadGrade 0.15

test/test-road-seam.html (EXIT GATE — already updated by 08-04, do NOT edit): ensureTile 3×3, reads
  tile.spline.getPoint(1.0)/getPoint(0.0)/getTangentAt; asserts C0 < 0.01 m, C1 < 5°, totalSeams >= 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shipped centerline viz (D-05) from this._tiles; add update(center); retire proto API in road.js</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js `buildDebugLines`/`setDebugVisible` (stubbed in 08-05) and the proto render machinery
      `_clearProtoLines`/`_buildDebugLine2`/`updateProto`/`setProtoEnabled`/`setProtoParam`/
      `setProtoRadius`/`setSurfaceSampler`/`this._proto.surfaceY`/`this._proto.lines`.
    - src/road.js `this._network`/`this._tiles` (08-05/08-06) — the geometry to render.
    - src/road.js `_streamNetwork`/`_sliceNetwork`/`invalidateCache` — the streaming pipeline `update`
      drives.
  </read_first>
  <action>
    Make the shipped viz render the network centerlines (D-05: centerline splines only, clean by
    default). Rebuild `buildDebugLines()` to clear prior lines then add one `THREE.Line` per
    `this._tiles` segment spline (sample each via `spline.getPoints(...)`, or per `this._network`
    polyline) using the existing `_buildDebugLine2`, applying the surface-Y lift
    (`this._proto.surfaceY` sampler if set, else `+1.0`) so lines sit on the rendered terrain. Rebuild
    `setDebugVisible(visible)` to toggle `line.visible` (no dispose/recreate — GC anti-pattern),
    auto-building lines on first enable, with `this._debugVisible` defaulting false.

    Add a single per-frame entry point `update(center)` that calls `_streamNetwork(center)` +
    `_sliceNetwork()` and, if `this._debugVisible`, rebuilds the lines for the new window (respecting
    the existing move-threshold / dirty / debounce gating so it is cheap when nothing changed). Fold the
    proto path into the shipped path: add `setRadius(r)` (sets `this._proto.radius`), KEEP
    `setSurfaceSampler` (feeds line placement). REMOVE `setProtoEnabled`, `setProtoParam`,
    `setProtoRadius`, and `updateProto` (and the proto-only line drawing) — there is ONE viz now,
    toggled by `setDebugVisible`, tuned by params. Ensure `invalidateCache()` clears
    `_network`/`_tiles`/lines and marks dirty so the next `update` re-streams. Live D-09 weight edits
    arrive by debug sliders mutating `this._params`; ensure each re-stream refreshes `this._proto.params`
    from `this._params` so slider changes take effect.
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -vn '^\s*\*\|^\s*//' src/road.js | grep -c 'setProtoEnabled\|setProtoParam\|setProtoRadius\|updateProto' | grep -qx 0 && grep -q 'setRadius' src/road.js && node --input-type=module -e "import('three').then(async THREE=>{const {RoadSystem}=await import('./src/road.js');const {parseWorldSeed}=await import('./src/seed.js');const p={coarseAmplitude:150,coarseFreq:0.0005,coarseOctaves:4,ridgeSharpness:1.6,maxRoadGrade:0.15,roadWDist:1,roadWAlt:0.85,roadWGrade:400,roadWOver:8000,roadWTurn:120,spurProbability:0.15};const added=[];const scene={add:o=>added.push(o),remove:o=>{const i=added.indexOf(o);if(i>=0)added.splice(i,1)}};const r=new RoadSystem(parseWorldSeed('lone-pine'),p);r.init(scene);if(r.setRadius)r.setRadius(400);r.update(new THREE.Vector3(0,0,0));r.setDebugVisible(true);console.log(added.length>0&&r._debugVisible===true?'VIZ_OK':'VIZ_BAD')}).catch(e=>{console.error(e.message);process.exit(1)})" 2>&1 | grep -q VIZ_OK && echo PASS</automated>
  </verify>
  <done>
    Enabling viz adds centerline `THREE.Line` objects built from the streamed/sliced network;
    `_debugVisible` is false by default; toggling off sets `line.visible=false` without disposing;
    `update(center)` streams+slices+(optionally)redraws; the proto-only API
    (`setProtoEnabled`/`setProtoParam`/`setProtoRadius`/`updateProto`) is gone from road.js; `setRadius`
    exists.
  </done>
  <acceptance_criteria>
    - D-05: shipped viz is centerline splines only, checkbox-toggled, clean (off) by default.
    - ONE viz path (`setDebugVisible` + `update`/`buildDebugLines`); proto scaffolding retired in road.js.
    - Lines toggle via `.visible` (no per-toggle GC); `this._tiles`/`this._network` drives the geometry.
    - Live D-09 weight edits re-stream deterministically (weights refreshed from this._params).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire debug.js Roads folder (D-09 sliders) + main.js render loop/re-route; retire proto wiring; PRESERVE resolveSpawn</name>
  <files>src/debug.js, src/main.js</files>
  <read_first>
    - src/debug.js Roads folder (lines 196–229) — remove `roadSlopePenalty` (206)/`roadAltWeight` (209)
      sliders and the ENTIRE "Valley Trunk (proto)" subfolder (`_protoState` @219, lines 213–229);
      KEEP `Show Road Splines` (198) + `maxRoadGrade` (201).
    - src/main.js callbacks block (lines 755–759), init block (774–779), `debouncedRoadRebuild`
      (240–245), render loop (line 1014), `resolveSpawn` (126–153 — PRESERVE), seed-change
      re-instantiation (221–227).
    - data/ranger.js D-09 params (08-05): roadWDist/roadWAlt/roadWGrade/roadWOver/roadWTurn/maxRoadGrade.
    - src/road.js new API from Task 1: `update(center)`, `setRadius(r)`, `setSurfaceSampler`,
      `setDebugVisible`, `invalidateCache`, `buildDebugLines`, `queryNearest`, `ensureTile`.
  </read_first>
  <action>
    In src/debug.js: keep the `Roads` folder with `Show Road Splines` (→ `onRoadVizToggle`) and the
    `maxRoadGrade` slider (→ `onRoadParamChange`). REPLACE the retired `roadSlopePenalty`/`roadAltWeight`
    sliders with the D-09 dominant cost-weight sliders bound to `params` — `roadWAlt` (0–3, step 0.05),
    `roadWGrade` (0–2000, step 20), `roadWOver` (0–40000, step 500), `roadWTurn` (0–800, step 20) — each
    firing `onRoadParamChange` (D-03). DELETE the entire `Valley Trunk (proto)` subfolder, the
    `_protoState` object, and the `onProtoToggle`/`onProtoParam`/`fireProto` wiring. Sliders write
    directly into the live `params` object.

    In src/main.js:
    (a) Render loop (line 1014) — replace `roadSystem.updateProto(streamCenter)` with
        `roadSystem.update(streamCenter)`.
    (b) Init block (774–779) — replace `setProtoRadius(640)` with `setRadius(640)`; keep
        `setSurfaceSampler((x,z)=>terrainSystem.analyticHeight(x,z))`.
    (c) Callbacks block (755–759) — REMOVE the `onProtoToggle`/`onProtoParam` callbacks; keep
        `onRoadVizToggle → setDebugVisible` and `onRoadParamChange → debouncedRoadRebuild`.
    (d) `debouncedRoadRebuild` (240–245) — on fire: `roadSystem.invalidateCache()` (marks the network
        dirty) so the next `update` re-streams with the new D-09 weights (deterministic re-route; roads
        are pure fns of seed+coords+params), then `roadSystem.buildDebugLines()` if currently visible.
    (e) `resolveSpawn` (126–153) — PRESERVE: it already ensureTiles the 3×3 spawn region then
        `queryNearest(baseX,baseZ,200)` and sets `heading = Math.atan2(tangent.x, tangent.z)` (D-07).
        Do not change its logic; only update stale comments that say "per-tile router" to reference the
        valley-trunk network.
    (f) Seed-change re-instantiation (221–227) — keep working with the new API:
        `new RoadSystem(worldSeed, RANGER_PARAMS)` + `init(scene)` + `setRadius(640)` +
        `setSurfaceSampler(...)` + rebuild lines if previously visible.
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -vn '^\s*\*\|^\s*//' src/debug.js | grep -c 'Valley Trunk (proto)\|onProtoToggle\|onProtoParam\|roadSlopePenalty\|roadAltWeight\|_protoState' | grep -qx 0 && grep -q 'Show Road Splines' src/debug.js && grep -q 'roadWAlt' src/debug.js && grep -vn '^\s*\*\|^\s*//' src/main.js | grep -c 'updateProto\|setProtoEnabled\|setProtoParam\|setProtoRadius\|onProtoToggle\|onProtoParam' | grep -qx 0 && grep -q 'roadSystem.update(' src/main.js && grep -q 'queryNearest' src/main.js && grep -q 'setRadius' src/main.js && echo PASS</automated>
  </verify>
  <done>
    debug.js Roads folder has Show Road Splines + maxGrade + roadWAlt/roadWGrade/roadWOver/roadWTurn
    sliders and NO proto folder, _protoState, or retired sliders; main.js calls
    `roadSystem.update(streamCenter)` in the render loop, retains the `queryNearest`-based
    `resolveSpawn` (D-07), uses `setRadius`, and has no `updateProto`/`setProto*`/`onProto*` references.
    Slider changes fire `onRoadParamChange → debouncedRoadRebuild → invalidateCache + buildDebugLines`.
  </done>
  <acceptance_criteria>
    - D-03: maxGrade + D-09 cost-weight sliders live → debounced deterministic re-route.
    - D-07: resolveSpawn queryNearest wiring PRESERVED (truck on nearest road, facing down it).
    - All proto wiring (folder, _protoState, callbacks, updateProto, setProto*) retired across
      debug.js + main.js.
    - Seed-change re-instantiation preserves viz state and uses the new API (setRadius/update).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the seam exit gate (D-06) and confirm green</name>
  <read_first>
    - test/test-road-seam.html (the exit gate — already wired to ensureTile + tile.spline by 08-04).
    - test/road-test-harness.js (TEST_PARAMS — already carries D-09 params).
  </read_first>
  <action>
    Claude has finished all code changes (08-05/08-06 + Tasks 1–2 above). This checkpoint runs the D-06
    seam exit gate in a browser to confirm the valley-trunk sliced splines satisfy C0/C1 across 64 m
    tile boundaries. Start a local HTTP server (`npx serve .`), open test/test-road-seam.html, and
    verify it reports `EXIT GATE D-06: PASS`. Do NOT edit the harness — it is the already-updated 08-04
    gate. If it fails, capture the failing seam coordinates / C0 / C1 values for diagnosis.
  </action>
  <what-built>
    The valley-trunk network (08-05), per-tile slicing + ensureTile-returning-{spline} + queryNearest
    (08-06), and the shipped viz + retired proto scaffolding (Tasks 1–2) make the production routing
    core match what test/test-road-seam.html asserts: ensureTile returns a single `.spline` per tile,
    sliced from one continuous polyline, so adjacent tiles share C0 (<0.01 m) and C1 (<5°).
  </what-built>
  <how-to-verify>
    1. Serve the repo over HTTP (ES modules require it): from the project root run `npx serve .`
       (no install) — or use VS Code Live Server.
    2. Open `http://localhost:3000/test/test-road-seam.html` (adjust port to whatever `serve` prints).
    3. Confirm the page reports `EXIT GATE D-06: PASS`:
       - every checked seam shows C0 dist < 0.01 m and C1 angle < 5°,
       - `Seams checked` (totalSeams) is ≥ 1 (network is not fully sparse across the 3×3 grid),
       - no assertion lines are marked FAIL.
    4. (Optional sanity) Open `http://localhost:3000/test/test-road.html` and confirm ROAD-01..04
       assertions pass against the valley-trunk API (determinism, soft-cost, queryNearest, C0/C1).
       NOTE: if test-road.html still references `_tileCache` (removed in 08-06), that single
       determinism assertion may error — report it; it is a harness staleness item (WR-01), not a
       production-code regression, and does not block the D-06 seam gate.
  </how-to-verify>
  <verify>
    <human-check>test/test-road-seam.html reports EXIT GATE D-06: PASS — all checked seams C0 < 0.01 m and C1 < 5°, totalSeams >= 1, zero FAIL lines.</human-check>
  </verify>
  <resume-signal>Type "approved" once the seam exit gate shows PASS, or describe the failing seams/assertions.</resume-signal>
  <done>
    test/test-road-seam.html exit gate is green (D-06 satisfied) over the valley-trunk sliced splines.
  </done>
  <acceptance_criteria>
    - D-06 exit gate PASS in-browser: C0 < 0.01 m and C1 < 5° on every checked seam; totalSeams >= 1.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- src/debug.js: no `Valley Trunk (proto)` / `onProtoToggle` / `onProtoParam` / `_protoState` /
  `roadSlopePenalty` / `roadAltWeight` in non-comment lines; has `Show Road Splines` + `roadWAlt` sliders.
- src/main.js: no `updateProto` / `setProtoEnabled` / `setProtoParam` / `setProtoRadius` / `onProtoToggle` /
  `onProtoParam`; has `roadSystem.update(`, `setRadius`, and `queryNearest` (resolveSpawn preserved).
- src/road.js: no `setProtoEnabled`/`setProtoParam`/`setProtoRadius`/`updateProto` in non-comment lines;
  has `update` + `setRadius`.
- Headless ESM: viz adds lines on enable.
- In-browser: test/test-road-seam.html reports EXIT GATE D-06: PASS.
</verification>

<success_criteria>
The shipped viz is centerline-only, checkbox-toggled, clean by default (D-05); maxGrade and the D-09
cost weights are live sliders driving a debounced deterministic re-stream (D-03); the truck spawns on
the nearest road facing down it (D-07, preserved); every "(proto)" seam is gone (08-VERIFICATION truths
6 and 7 closed); and the seam exit gate (D-06) is green. The valley trunk is simply the road system.
(Spurs / D-01 deferred to post-functional polish.)
</success_criteria>

<output>
Create `.planning/phases/08-road-routing/08-07-SUMMARY.md` when done.
</output>
