---
phase: 08-road-routing
plan: 05
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - src/road.js
  - data/ranger.js
autonomous: true
requirements: [ROAD-01, ROAD-02, ROAD-03]
must_haves:
  # D-08 — valley-following streaming-anchor model is the REAL RoadSystem core (not a disabled proto)
  # D-09 — soft-cost A* (altitude + grade² + finite over-cap + turn penalty), never "no path"
  # D-02 (REVISED) — soft over-cap penalty, NEVER a hard Infinity grade block
  # D-04 — dominant wAlt term makes the route wrap AROUND high ground
  # Closes 08-VERIFICATION gaps: truths 1, 2, 3 (failed)
  truths:
    - "RoadSystem builds a continuous valley-following trunk polyline from seeded macro-anchors (256 m grid) via soft-cost A*, with no per-tile west-east A* and no hard grade block anywhere in road.js"
    - "Every routed edge cost is finite — the router NEVER returns 'no path' on the locked lone-pine coarse terrain"
    - "The trunk polyline is post-processed (segment dedupe + collinear-simplify + loop removal) into the canonical network store this._network keyed deterministically by macro-row"
  artifacts:
    - path: "src/road.js"
      provides: "Valley-trunk streaming network core (anchors, soft-cost turn-penalty A*, continuous polyline + simplify) is the live core; old per-tile router and hard grade block removed"
      contains: "_streamNetwork"
    - path: "data/ranger.js"
      provides: "Locked D-09 cost-model defaults (roadWDist/roadWAlt/roadWGrade/roadWOver/roadWTurn/maxRoadGrade 0.15)"
      contains: "roadWAlt"
  key_links:
    - from: "src/road.js _protoEdgeCost (now the canonical cost)"
      to: "data/ranger.js roadW* params"
      via: "this._proto.params seeded from this._params D-09 defaults (live-tunable, no hardcoded weight literals)"
      pattern: "P\\.wAlt"
    - from: "src/road.js _streamNetwork"
      to: "_protoAnchor / _protoConnect"
      via: "streamed macro-anchor chain → continuous polyline → this._network"
      pattern: "_protoConnect"
---

<objective>
Make the validated valley-following streaming-anchor trunk the REAL `RoadSystem` routing core, and
DELETE the per-tile west→east A* / hard-grade-block path that is currently the only live router.

GROUND TRUTH (verified by grep on the committed src/road.js — the 08-01 SUMMARY's PASS claim is FALSE,
none of this work landed): src/road.js still contains the complete per-tile router — `_seamPoint`
(line 271), `_deriveEdgeWaypoints` (302), `_edgeCost` with `if (grade > this._params.maxRoadGrade)
return Infinity` (line 336), `_heuristic` (349), `_routeTile` (369), `_getTileWaypointsOnly` (479),
`_buildTileSpline` (518), `_getTile` (551) — wired into the live public `ensureTile`/`queryNearest`.
The valley-trunk soft-cost engine (`_protoAnchor`, `_protoConnect`, `_protoEdgeCost`, `_protoSimplify`,
`_removeLoops`, `updateProto`) exists but ONLY inside the `_proto` prototype path (`enabled:false`,
lines 688–983), not the canonical API. `_streamNetwork`, `this._network` do NOT exist.

This plan productionizes the spike-001 prototype engine into the canonical network builder and removes
the failed per-tile machinery. Cost model is LOCKED (D-09):
`edgeCost = wDist·horiz + wAlt·h + wGrade·grade² + wOver·max(0, grade − maxGrade) + wTurn·(Δheading/45°)`.
Defaults: wAlt 0.85, wGrade 400, wOver 8000, maxGrade 0.15, wTurn 120, wDist 1. The over-cap penalty is
FINITE/soft — there is NEVER an `Infinity` edge (D-02 REVISED — the hard block caused the original
"no path" failure).

Purpose: a deterministic, infinite, valley-wrapping trunk is the single source of truth the slicing
(08-06) and viz/wiring (08-07) build on.
Output: `src/road.js` with `_streamNetwork(center)` populating `this._network`; `data/ranger.js`
carrying the locked D-09 cost defaults; the old per-tile router gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-road-routing/08-CONTEXT.md
@.planning/phases/08-road-routing/08-VERIFICATION.md
@.planning/spikes/001-valley-route-wraps-mountain/README.md

<interfaces>
<!-- The valley-trunk engine being productionized already lives in src/road.js as the _proto path.
     Executor uses these existing VALIDATED methods directly (spike 001). Do NOT re-derive the algorithm. -->

src/road.js (existing prototype methods — KEEP + promote, do NOT rewrite the algorithm):
  _protoInit()                          builds this._proto { params, anchors, segs, lines, radius, lastCenter, dirty, surfaceY }
  _protoAnchor(mx, mz)                  deterministic valley-snapped anchor (seedFor 'roadanchor', bounded gradient-descent ≤ PROTO_SNAP_CAP)
  _protoEdgeCost(fromH, toH, horiz, P)  soft cost: P.wDist·horiz + P.wAlt·toH + P.wGrade·grade² + P.wOver·max(0,grade−P.maxGrade)  — FINITE
  _protoConnect(a, b)                   turn-penalty soft-cost A* (state = cell+heading, P.wTurn per-45°); endpoints anchored; interior _protoSimplify'd; never fails
  _protoSimplify(points, angleDeg)      collinear-simplify (drop non-turn waypoints)
  _removeLoops(pts)                     proximity-based loop/fold removal
  updateProto(center)                   streams per-row east connections into continuous polylines (lines ~899–983; its NETWORK-build half is what this plan promotes; its _scene.add line-drawing belongs to 08-07)

src/road.js module constants (existing, KEEP): PROTO_ANCHOR_SPACING=256, PROTO_CELL=10, PROTO_MARGIN=200,
  PROTO_REGEN_MOVE=96, PROTO_PARAM_DEBOUNCE=160, PROTO_SNAP_CAP, PROTO_DIRS, PROTO_COVER_*, PROTO_LOOP_*,
  PROTO_RUN_MIN, MinHeap (the proto A* uses it), _coarseHeight (byte-identical to terrain.js coarseHeight)

src/terrain.js: export const CHUNK_SIZE = 64 (tile size — road slicing in 08-06 aligns to this)

data/ranger.js (existing Phase-8 road block to REPLACE, lines 187–217): maxRoadGrade 0.12,
  routeGridSize 16, roadSlopePenalty 50, roadAltWeight 0.1, spurProbability 0.15
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the per-tile A* / hard-block router and lock the D-09 cost defaults in params</name>
  <files>src/road.js, data/ranger.js</files>
  <read_first>
    - src/road.js lines 41–54 (SEAM_SAMPLES constant — to delete) and 56–80 (PROTO_* constants — KEEP).
    - src/road.js lines 251–566 (the per-tile router to DELETE: _seamPoint @271, _deriveEdgeWaypoints
      @302, _edgeCost with `return Infinity` @336, _heuristic @349, _routeTile @369,
      _getTileWaypointsOnly @479, _buildTileSpline @518, _getTile @551).
    - src/road.js lines 568–686 (ensureTile @579, queryNearest @596, invalidateCache @635,
      buildDebugLines @651, setDebugVisible @677 — old bodies route through deleted methods; these are
      REBUILT in 08-06/08-07, so neutralize their old bodies here so no live path reaches _routeTile/_getTile).
    - src/road.js lines 688–984 (the _proto engine to KEEP intact: _protoInit @700, setProto* @722–731,
      _protoAnchor @746, _protoEdgeCost @774, _protoSimplify @783, _removeLoops @804, _protoConnect @829,
      updateProto @899).
    - data/ranger.js lines 187–217 (old Phase-8 road param block to replace).
    - .planning/phases/08-road-routing/08-VERIFICATION.md (why the line-336 hard block failed — do not re-introduce).
  </read_first>
  <action>
    In src/road.js, DELETE the per-tile router methods entirely: `_seamPoint`, `_deriveEdgeWaypoints`,
    `_edgeCost` (the one containing `if (grade > this._params.maxRoadGrade) return Infinity`),
    `_heuristic`, `_routeTile`, `_getTileWaypointsOnly`, `_buildTileSpline`, and `_getTile`. DELETE the
    module constant `SEAM_SAMPLES` (line 54). KEEP `MinHeap`, `_coarseHeight`, `_coarseH`, the
    constructor, `init`, `_reinitNoise`, and the ENTIRE `_proto` engine
    (`_protoInit`/`setProto*`/`_protoAnchor`/`_protoEdgeCost`/`_protoSimplify`/`_removeLoops`/
    `_protoConnect`/`updateProto`) plus `_buildDebugLine`/`_buildDebugLine2`.

    For `ensureTile`, `queryNearest`, `invalidateCache`, `buildDebugLines`, `setDebugVisible`: these are
    fully rebuilt against the new network in 08-06 (ensureTile/queryNearest) and 08-07 (viz). Remove
    their OLD bodies that reach `_getTile`/`_routeTile`/`_tileCache` so no live call path touches the
    deleted methods — leave minimal safe stubs (e.g. `ensureTile`/`queryNearest` return a benign value;
    `invalidateCache` clears whatever caches remain) that keep src/road.js importing cleanly as an ES
    module. Do NOT worry about main.js/test references yet (08-07 / the already-updated harnesses
    re-wire them). Update the file header doc-comment (lines 1–27) to describe the valley-trunk
    streaming model and drop the "Per-tile A* / shared seam crossings / hard grade block" language.

    In data/ranger.js, replace the Phase-8 road block (lines 187–217) with the LOCKED D-09 defaults as
    live-tunable params: `maxRoadGrade: 0.15`, `roadWDist: 1`, `roadWAlt: 0.85`, `roadWGrade: 400`,
    `roadWOver: 8000`, `roadWTurn: 120`, and KEEP `spurProbability: 0.15` (retained for the deferred
    D-01 spur pass). Document each with its D-09 role: wAlt = stay-low valley-seeking dominant term;
    wOver = FINITE over-cap penalty (never Infinity); wTurn = per-45° switchback/straight shaping;
    maxGrade = soft target the over-cap penalty measures against. DELETE `routeGridSize`,
    `roadSlopePenalty`, `roadAltWeight` (retired per-tile model).
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -vn '^\s*\*\|^\s*//' src/road.js | grep -c 'return Infinity\|_routeTile\|_seamPoint\|_deriveEdgeWaypoints\|_buildTileSpline\|SEAM_SAMPLES' | grep -qx 0 && grep -q 'roadWAlt' data/ranger.js && grep -vn '^\s*\*\|^\s*//' data/ranger.js | grep -c 'routeGridSize\|roadSlopePenalty\|roadAltWeight' | grep -qx 0 && node --input-type=module -e "import('./src/road.js').then(()=>console.log('IMPORT_OK')).catch(e=>{console.error(e.message);process.exit(1)})" 2>&1 | grep -q IMPORT_OK && echo PASS</automated>
  </verify>
  <done>
    grep finds zero non-comment occurrences of `return Infinity`, `_routeTile`, `_seamPoint`,
    `_deriveEdgeWaypoints`, `_buildTileSpline`, or `SEAM_SAMPLES` in src/road.js; data/ranger.js defines
    `roadWAlt` (plus roadWDist/roadWGrade/roadWOver/roadWTurn and maxRoadGrade 0.15) and no longer
    defines routeGridSize/roadSlopePenalty/roadAltWeight; src/road.js imports cleanly under node ESM.
  </done>
  <acceptance_criteria>
    - No hard grade block (`grade > … → Infinity`) anywhere in non-comment src/road.js (D-02 REVISED).
    - Old per-tile router symbols fully removed; no live call path reaches them.
    - data/ranger.js carries the locked D-09 defaults wAlt 0.85 / wGrade 400 / wOver 8000 /
      maxRoadGrade 0.15 / wTurn 120 / wDist 1, all as tunable params; spurProbability preserved;
      retired per-tile params gone.
    - The _proto engine (_protoAnchor/_protoConnect/_protoEdgeCost/_protoSimplify/_removeLoops) left intact.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Promote the streaming network-builder to canonical road data (_streamNetwork)</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js `updateProto(center)` (lines 899–983) — the per-row continuous-polyline build is the
      logic to PROMOTE; its `_scene.add(line)` drawing + `+1.0`/surfaceY y-lift is the part to EXCLUDE
      from data-building (render is 08-07).
    - src/road.js `_protoInit` (lines 700–720) — the `_proto` store (anchors, segs, params, radius,
      lastCenter, dirty) the network builder reuses; note `params` is currently HARDCODED here.
    - src/road.js `_protoEdgeCost` (774) and `_protoConnect` (829) — confirm they read weights from the
      passed `P` object so seeding `this._proto.params` ← `this._params` D-09 defaults takes effect.
    - src/road.js `_protoConnect` overlap/loop helpers and the PROTO_COVER_*/PROTO_RUN_MIN constants
      used by the per-row run-splitting in updateProto.
    - .planning/spikes/001-valley-route-wraps-mountain/README.md (Results — continuous per-row polyline
      + loop removal + simplify is the validated pipeline).
  </read_first>
  <action>
    Add a `_streamNetwork(center)` method to RoadSystem that builds — and caches in a new
    `this._network` Map store — the canonical valley-trunk polylines around `center`, WITHOUT drawing
    any scene lines (rendering is 08-07). Factor the data-building half of `updateProto` into it: over
    the streamed macro-cell window (`mx0..mx1 / mz0..mz1` from `this._proto.radius` and
    `PROTO_ANCHOR_SPACING`), for each row `mz` concatenate the row's east
    `_protoConnect(_protoAnchor(mx,mz), _protoAnchor(mx+1,mz))` segments into ONE continuous polyline
    (drop the shared anchor between consecutive segments), build a
    `THREE.CatmullRomCurve3(rowWps, false, 'centripetal', 0.5)`, sample it
    (`getPoints(max(24, rowWps.length*2))`), run `_removeLoops`, then split into kept runs using the
    existing inter-row same-direction overlap suppression (PROTO_COVER_* spatial hash; register a row's
    points only AFTER emitting it so a straight road never self-culls). Store each kept run
    (length ≥ PROTO_RUN_MIN) as a network entry — `this._network` maps a deterministic key
    (`"<mz>:<runIndex>"`) to `{ points: THREE.Vector3[] }` whose `points` are the canonical centerline.
    Do NOT apply the `+1.0` visual y-lift or surfaceY sampler here — those are render-only (08-07); the
    network y is raw routed height.

    Seed `this._proto.params` from the D-09 `this._params`
    (roadWDist→wDist, roadWAlt→wAlt, roadWGrade→wGrade, roadWOver→wOver, roadWTurn→wTurn,
    maxRoadGrade→maxGrade) in `_protoInit` (replacing the current hardcoded literals) so the network
    uses the locked params and 08-07 live-slider edits flow through. Honor lazy streaming: keep the
    move-threshold (`PROTO_REGEN_MOVE`), `dirty` flag, and `PROTO_PARAM_DEBOUNCE` gating, and
    clear+rebuild `this._network` on a real re-stream. Bound `this._network` size during endless play
    (mirror the existing anchors/segs caps). Re-point `updateProto`'s network-building to delegate to
    `_streamNetwork` (single source of truth) — `updateProto` may still draw lines from `this._network`
    for the existing proto toggle until 08-07 replaces the viz; the network DATA must come only from
    `_streamNetwork`.
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -q '_streamNetwork' src/road.js && grep -q 'this._network' src/road.js && node --input-type=module -e "import('three').then(async THREE=>{const {RoadSystem}=await import('./src/road.js');const {parseWorldSeed}=await import('./src/seed.js');const p={coarseAmplitude:150,coarseFreq:0.0005,coarseOctaves:4,ridgeSharpness:1.6,maxRoadGrade:0.15,roadWDist:1,roadWAlt:0.85,roadWGrade:400,roadWOver:8000,roadWTurn:120,spurProbability:0.15};const r=new RoadSystem(parseWorldSeed('lone-pine'),p);r._proto.radius=300;r._streamNetwork(new THREE.Vector3(0,0,0));console.log(r._network.size>0?'NET_OK':'NET_EMPTY')}).catch(e=>{console.error(e.message);process.exit(1)})" 2>&1 | grep -q NET_OK && node --input-type=module -e "import('three').then(async THREE=>{const {RoadSystem}=await import('./src/road.js');const {parseWorldSeed}=await import('./src/seed.js');const p={coarseAmplitude:150,coarseFreq:0.0005,coarseOctaves:4,ridgeSharpness:1.6,maxRoadGrade:0.15,roadWDist:1,roadWAlt:0.42,roadWGrade:400,roadWOver:8000,roadWTurn:120,spurProbability:0.15};const r=new RoadSystem(parseWorldSeed('lone-pine'),p);console.log(r._proto&&r._proto.params&&r._proto.params.wAlt===0.42?'WIRED':'UNWIRED')}).catch(e=>{console.error(e.message);process.exit(1)})" 2>&1 | grep -q WIRED && echo PASS</automated>
  </verify>
  <done>
    `_streamNetwork(center)` exists, populates `this._network` with continuous trunk polylines
    (non-empty for a 300 m radius at origin on lone-pine), allocates no scene lines, and the network
    uses the D-09 cost weights from params (`this._proto.params.wAlt` reflects the constructed
    `roadWAlt`). A headless ESM smoke run builds a non-empty network.
  </done>
  <acceptance_criteria>
    - `this._network` is the canonical road geometry store (continuous per-row polylines; dedupe +
      simplify + loop-removal applied), built ONLY by `_streamNetwork`.
    - Network build is pure-function-of-(worldSeed, coords, params): identical inputs → identical
      polylines (strict determinism asserted by the 08-04 harness in 08-07).
    - No hard grade block; every connection returns a finite path (D-02 REVISED / D-09).
    - Lazy streaming preserved (move-threshold + dirty + debounce gating; bounded `_network` cache).
    - `this._proto.params` is SEEDED from `this._params` (no hardcoded weight literals): constructing
      with `roadWAlt: 0.42` yields `this._proto.params.wAlt === 0.42`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `grep -vn '^\s*\*\|^\s*//' src/road.js | grep -c 'return Infinity'` is 0 (no hard grade block).
- The old per-tile symbols (`_routeTile`, `_seamPoint`, `_deriveEdgeWaypoints`, `_buildTileSpline`,
  `SEAM_SAMPLES`) do not appear in non-comment lines of src/road.js.
- `data/ranger.js` defines roadWDist/roadWAlt/roadWGrade/roadWOver/roadWTurn + maxRoadGrade 0.15 and
  no routeGridSize/roadSlopePenalty/roadAltWeight.
- A headless ESM smoke import of src/road.js builds a non-empty `_network` at origin on lone-pine, and
  `_proto.params` is seeded from constructor params.
</verification>

<success_criteria>
The valley-following streaming-anchor model is the real RoadSystem routing core. The old per-tile /
hard-block router is gone (08-VERIFICATION truths 1, 2, 3 closed). `_streamNetwork` produces canonical
continuous trunk polylines using the locked D-09 soft cost model (altitude-dominated, finite over-cap,
turn penalty), wrapping around high ground and never failing to find a path. The network is the single
source of truth for slicing, viz, and queries in 08-06 / 08-07.
</success_criteria>

<output>
Create `.planning/phases/08-road-routing/08-05-SUMMARY.md` when done.
</output>
