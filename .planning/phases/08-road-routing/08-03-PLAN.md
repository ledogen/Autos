---
phase: 08-road-routing
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/road.js
  - src/debug.js
  - src/main.js
autonomous: true
requirements: [ROAD-01, ROAD-04]
must_haves:
  # D-05 — shipped viz = centerline splines only, lil-gui checkbox, clean by default
  # D-03 — maxGrade (+ dominant cost weights) live sliders → debounced deterministic re-route
  # D-07 — resolveSpawn → queryNearest puts truck ON the road facing down it
  # (D-01 spurs DEFERRED out of Phase 8 — trunk-only; see 08-CONTEXT deferred)
  truths:
    - "Shipped road viz is centerline splines only, toggled by a single lil-gui checkbox, off (clean) by default; the proto 'Valley Trunk' folder and retired per-tile sliders are gone"
    - "Moving the maxGrade or cost-weight sliders re-routes the network deterministically (debounced); the truck spawns on the nearest road facing down it via queryNearest"
  artifacts:
    - path: "src/road.js"
      provides: "setDebugVisible/buildDebugLines drive the shipped centerline viz from this._network/_tiles; one unified update(center) streamer"
      contains: "buildDebugLines"
    - path: "src/debug.js"
      provides: "Roads folder: Show Road Splines checkbox + maxGrade + cost-weight sliders → debounced re-route; proto folder removed"
      contains: "Show Road Splines"
    - path: "src/main.js"
      provides: "Stream-network call each frame + debounced re-route + resolveSpawn queryNearest wiring (proto wiring retired)"
      contains: "queryNearest"
  key_links:
    - from: "src/debug.js Roads folder sliders"
      to: "src/main.js debouncedRoadRebuild"
      via: "onRoadParamChange callback → invalidate + re-stream network"
      pattern: "onRoadParamChange"
    - from: "src/main.js resolveSpawn"
      to: "roadSystem.queryNearest"
      via: "nearest road point + atan2(tangent) heading (D-07)"
      pattern: "queryNearest"
    - from: "src/main.js render loop"
      to: "roadSystem stream-network update"
      via: "streamed around the same center as terrain each frame"
      pattern: "streamCenter"
---

<objective>
Finish the productionization: make the shipped debug viz the real centerline-only, checkbox-toggled,
clean-by-default visualization (D-05) driven from the streamed network; wire the live maxGrade +
cost-weight sliders to a debounced deterministic re-route (D-03); and confirm `resolveSpawn` puts the
truck on the nearest road facing down it via `queryNearest` (D-07). Retire all remaining prototype
scaffolding (the "Valley Trunk (proto)" lil-gui folder, the retired per-tile sliders, the
`setProto*`/`updateProto`-as-proto wiring).

(Spurs — D-01 — are DEFERRED out of Phase 8: a trunk-only network already yields usable splines;
sparse spurs are post-functional polish. See 08-CONTEXT deferred.)

Purpose: deliver the remaining Real-build scope items (shipped viz, live re-route, spawn) and remove
every "(proto)" seam so the valley trunk is simply the road system, tuned from the normal Roads debug
folder.
Output: a unified shipped viz in `src/road.js`; a clean Roads folder in `src/debug.js`; render-loop
streaming + spawn wiring in `src/main.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-road-routing/08-CONTEXT.md
@.planning/phases/08-road-routing/08-01-SUMMARY.md
@.planning/phases/08-road-routing/08-02-SUMMARY.md

<interfaces>
<!-- From 08-01 / 08-02 (dependencies). Plus the existing main.js/debug.js seams to rewire. -->

src/road.js (08-01/08-02): _streamNetwork(center), _sliceNetwork(), this._network, this._tiles,
  queryNearest(wx,wz,radiusM)→{point,tangent}|null, ensureTile(tileX,tileZ),
  _protoAnchor(mx,mz), _protoConnect(a,b), _buildDebugLine2(pts,color), _proto.params (D-09 weights),
  _proto.radius, _proto.lines, _clearProtoLines()

src/main.js seams:
  resolveSpawn (lines 109–207) — already calls ensureTile 3×3 + queryNearest + atan2(tangent) (D-07); keep
  debouncedRoadRebuild (lines 233–248) — currently invalidateCache + buildDebugLines; re-point to network
  render loop (lines 1011–1014) — terrainSystem.update(streamCenter); roadSystem.updateProto(streamCenter)
  callbacks block (lines 754–779) — onRoadVizToggle / onRoadParamChange / onProtoToggle / onProtoParam;
    roadSystem.setSurfaceSampler / setProtoRadius / setProtoEnabled
  seed-change re-instantiation (lines ~221–227) — new RoadSystem + init + (re)build lines

src/debug.js seams:
  Roads folder (lines 183–229) — Show Road Splines checkbox + maxRoadGrade slider + roadSlopePenalty/
    roadAltWeight sliders (RETIRED params) + the entire "Valley Trunk (proto)" subfolder (REMOVE)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shipped centerline viz (D-05) driven from the network; retire proto scaffolding in road.js</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js `buildDebugLines` / `setDebugVisible` (old per-tile bodies removed/stubbed in 08-01)
      and `_clearProtoLines` / `_buildDebugLine2` / `updateProto` / `setProtoEnabled` / `setProtoParam`
      / `setProtoRadius` / `setSurfaceSampler` / `_proto.surfaceY` (proto render machinery).
    - src/road.js `this._network` / `this._tiles` (08-01/08-02) — the geometry to render.
  </read_first>
  <action>
    Make the shipped viz render the network centerlines (D-05: centerline splines only, clean by
    default). Rebuild `buildDebugLines()` to clear prior lines then add one `THREE.Line` per
    `this._tiles` segment spline (or per `this._network` polyline) via the existing line builder
    (`_buildDebugLine2`), applying the surface-Y lift (`this._proto.surfaceY` sampler if set, else
    `+1.0`) so lines sit on the rendered terrain. Rebuild `setDebugVisible(visible)` to toggle
    `line.visible` (no dispose/recreate — GC anti-pattern), auto-building lines on first enable, with
    `_debugVisible` defaulting false. Add a single per-frame entry point `update(center)` (renaming the
    proto streamer) that calls `_streamNetwork(center)` + `_sliceNetwork()` and, if `_debugVisible`,
    rebuilds the lines for the new window. Fold the proto path into the shipped path: rename
    `setProtoRadius`→`setRadius`; keep `setSurfaceSampler` (it feeds line placement); REMOVE
    `setProtoEnabled`, `setProtoParam`, and the separate `updateProto`/proto-only line drawing — there
    is one viz now, toggled by `setDebugVisible`, tuned by params. Ensure `invalidateCache()` clears
    `_network`/`_tiles`/lines and marks dirty so the next `update` re-streams. Live D-09 weight edits
    arrive by mutating the shared `this._params` (debug sliders write params directly); make sure
    `_streamNetwork` reads weights from `this._params` (or refreshes `this._proto.params` from
    `this._params`) each re-stream so slider changes take effect.
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -vn '^\s*\*\|^\s*//' src/road.js | grep -c 'setProtoEnabled\|setProtoParam\|updateProto' | grep -qx 0 && node --input-type=module -e "import('three').then(async THREE=>{const {RoadSystem}=await import('./src/road.js');const {parseWorldSeed}=await import('./src/seed.js');const p={coarseAmplitude:150,coarseFreq:0.0005,coarseOctaves:4,ridgeSharpness:1.6,maxRoadGrade:0.15,roadWDist:1,roadWAlt:0.85,roadWGrade:400,roadWOver:8000,roadWTurn:120,spurProbability:0.15};const added=[];const scene={add:o=>added.push(o),remove:o=>{const i=added.indexOf(o);if(i>=0)added.splice(i,1)}};const r=new RoadSystem(parseWorldSeed('lone-pine'),p);r.init(scene);if(r.setRadius)r.setRadius(400);r.update(new THREE.Vector3(0,0,0));r.setDebugVisible(true);console.log(added.length>0&&r._debugVisible===true?'VIZ_OK':'VIZ_BAD')}).catch(e=>{console.error(e.message);process.exit(1)})" 2>&1 | grep -q VIZ_OK && echo PASS</automated>
  </verify>
  <done>
    Enabling viz adds centerline `THREE.Line` objects to the scene built from the streamed network;
    `_debugVisible` is false by default; toggling off sets `line.visible=false` without disposing; the
    proto-only API (`setProtoEnabled`/`setProtoParam`/`updateProto`) is gone from road.js (one viz path).
  </done>
  <acceptance_criteria>
    - D-05: shipped viz is centerline splines only, checkbox-toggled, clean (off) by default.
    - One viz path (`setDebugVisible` + `update`/`buildDebugLines`) — proto scaffolding retired.
    - Lines toggle via `.visible` (no per-toggle GC); network drives the geometry.
    - Live D-09 weight edits re-stream deterministically (weights read from this._params).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire debug.js Roads folder + main.js render loop, re-route, and spawn (D-03 / D-07); retire proto wiring</name>
  <files>src/debug.js, src/main.js, src/road.js</files>
  <read_first>
    - src/debug.js Roads folder (lines 183–229) — remove the retired `roadSlopePenalty`/`roadAltWeight`
      sliders and the ENTIRE "Valley Trunk (proto)" subfolder; keep `Show Road Splines` + `maxRoadGrade`.
    - src/main.js callbacks block (lines 754–779), `debouncedRoadRebuild` (233–248), render loop
      (1011–1014), `resolveSpawn` (109–207), and the seed-change re-instantiation (221–227).
    - data/ranger.js D-09 params (08-01): roadWDist/roadWAlt/roadWGrade/roadWOver/roadWTurn/maxRoadGrade.
  </read_first>
  <action>
    In src/debug.js: keep the `Roads` folder with `Show Road Splines` (→ `onRoadVizToggle`) and the
    `maxRoadGrade` slider (→ `onRoadParamChange`); replace the retired `roadSlopePenalty`/`roadAltWeight`
    sliders with the D-09 dominant cost-weight sliders bound to `params` — `roadWAlt` (0–3, step 0.05),
    `roadWGrade` (0–2000, step 20), `roadWOver` (0–40000, step 500), `roadWTurn` (0–800, step 20) — each
    firing `onRoadParamChange` (D-03: maxGrade + dominant weights live). DELETE the entire `Valley Trunk
    (proto)` subfolder, its `_protoState` mirror, and the `onProtoToggle`/`onProtoParam` wiring. Sliders
    write directly into the live `params` object.

    In src/main.js: (a) render loop — replace `roadSystem.updateProto(streamCenter)` with
    `roadSystem.update(streamCenter)` (the unified streamer from Task 2). (b) Init block — replace
    `setProtoRadius(640)` with `setRadius(640)`, keep
    `setSurfaceSampler((x,z)=>terrainSystem.analyticHeight(x,z))`, and remove the
    `onProtoToggle`/`onProtoParam` callbacks; keep `onRoadVizToggle → setDebugVisible` and
    `onRoadParamChange → debouncedRoadRebuild`. (c) `debouncedRoadRebuild` — on fire:
    `roadSystem.invalidateCache()` (which marks the network dirty) so the next `update` re-streams with
    the new D-09 weights (deterministic re-route; roads are pure fns of seed+coords+params), then rebuild
    debug lines if visible. (d) `resolveSpawn` — verify it still ensureTiles the 3×3 spawn region then
    `queryNearest(baseX,baseZ,200)` and sets `heading = Math.atan2(tangent.x, tangent.z)` (D-07); update
    its comments to reference the valley-trunk network (not the per-tile router). (e) seed-change
    re-instantiation — keep working with the new API: `new RoadSystem(worldSeed, RANGER_PARAMS)` +
    `init(scene)` + `setRadius` + `setSurfaceSampler` + rebuild lines if previously visible.
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -vn '^\s*\*\|^\s*//' src/debug.js | grep -c 'Valley Trunk (proto)\|onProtoToggle\|onProtoParam\|roadSlopePenalty\|roadAltWeight' | grep -qx 0 && grep -q 'Show Road Splines' src/debug.js && grep -q 'roadWAlt' src/debug.js && grep -vn '^\s*\*\|^\s*//' src/main.js | grep -c 'updateProto\|setProtoEnabled\|onProtoToggle\|setProtoParam' | grep -qx 0 && grep -q 'roadSystem.update(' src/main.js && grep -q 'queryNearest' src/main.js && echo PASS</automated>
  </verify>
  <done>
    debug.js Roads folder has Show Road Splines + maxGrade + roadWAlt/roadWGrade/roadWOver/roadWTurn
    sliders and NO proto folder or retired sliders; main.js calls `roadSystem.update(streamCenter)` in
    the render loop, retains the `queryNearest`-based `resolveSpawn` (D-07), and has no `updateProto`/
    `setProto*`/`onProto*` references. Slider changes fire `onRoadParamChange → debouncedRoadRebuild`.
  </done>
  <acceptance_criteria>
    - D-03: maxGrade + dominant cost-weight sliders live → debounced deterministic re-route.
    - D-07: resolveSpawn places the truck on the nearest road facing down it via queryNearest.
    - All proto wiring (folder, callbacks, updateProto) retired across debug.js + main.js.
    - Seed-change re-instantiation preserves viz state and the new API.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- src/debug.js: no `Valley Trunk (proto)` / `onProtoToggle` / `onProtoParam` / `roadSlopePenalty` /
  `roadAltWeight` in non-comment lines; has `Show Road Splines` + `roadWAlt` sliders.
- src/main.js: no `updateProto` / `setProtoEnabled` / `onProtoToggle` / `setProtoParam`; has
  `roadSystem.update(` and `queryNearest`.
- src/road.js: no `setProtoEnabled`/`setProtoParam`/`updateProto` in non-comment lines.
- Headless ESM: viz adds lines on enable.
</verification>

<success_criteria>
The shipped viz is centerline-only, checkbox-toggled, clean by default (D-05); maxGrade and the
dominant D-09 cost weights are live sliders driving a debounced deterministic re-route (D-03); and the
truck spawns on the nearest road facing down it (D-07). Every "(proto)" seam is gone — the valley trunk
is simply the road system. (Spurs / D-01 deferred to post-functional polish.)
</success_criteria>

<output>
Create `.planning/phases/08-road-routing/08-03-SUMMARY.md` when done.
</output>
