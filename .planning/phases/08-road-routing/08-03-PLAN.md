---
phase: 08-road-routing
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/main.js
  - test/test-road-seam.html
  - test/test-road.html
autonomous: true
requirements: [ROAD-01, ROAD-04]
must_haves:
  truths:
    - "The truck spawns ON the nearest road, facing down the road (tangent heading)"
    - "resolveSpawn keeps its exact signature (worldSeed, params) → {position, heading} — no call-site change"
    - "Across a 3×3 tile grid, the tangent angle difference at every 64 m tile seam is < 5° (no kink) — the phase EXIT GATE"
    - "If no road node is within the spawn search radius, resolveSpawn falls back to terrain-only spawn with a console.warn (bounded, deterministic)"
  artifacts:
    - path: "src/main.js"
      provides: "resolveSpawn body swapped to nearest-road-node + tangent heading (D-07), terrain-only fallback preserved"
      contains: "roadSystem.queryNearest"
    - path: "test/test-road-seam.html"
      provides: "D-06 exit-gate test — C1 seam continuity (tangent angle < 5°) across a 3×3 tile grid"
      contains: "test-road-seam"
  key_links:
    - from: "src/main.js resolveSpawn"
      to: "roadSystem.ensureTile + roadSystem.queryNearest"
      via: "eager 3×3 spawn-region generation then nearest-point probe"
      pattern: "roadSystem\\.(ensureTile|queryNearest)"
    - from: "test/test-road-seam.html"
      to: "tile A spline getTangentAt(1.0) vs tile B getTangentAt(0.0)"
      via: "angle-between-tangents assertion < 5°"
      pattern: "getTangentAt"
  # D-07 swap resolveSpawn body to nearest-road-node + tangent heading, signature unchanged
  # D-06 seam continuity (no kinks at 64 m boundaries) is the EXIT GATE — verified by test-road-seam.html
---

<objective>
Swap the `resolveSpawn` body in `main.js` from the Phase 7 terrain-only resolver to a road-graph probe (nearest road node + tangent heading) at the same call site with the same signature (D-07), and build the C1 seam-continuity exit-gate test (D-06) that asserts no kinks at 64 m tile boundaries across a 3×3 tile grid. This is the phase exit gate.

Purpose: D-07 fills the Phase 7 spawn seam — the truck should spawn ON the road facing down it, not on arbitrary flat terrain. D-06 (seam continuity, no kinks) is the explicit phase exit gate that must be green before Phase 9 builds any ribbon mesh on these splines.

Output: swapped resolveSpawn body (with preserved terrain-only fallback), `test/test-road-seam.html` exit-gate test, and the seam assertion added to the main test harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-road-routing/08-CONTEXT.md
@.planning/phases/08-road-routing/08-RESEARCH.md
@.planning/phases/08-road-routing/08-PATTERNS.md
@.planning/phases/08-road-routing/08-01-SUMMARY.md
@.planning/phases/08-road-routing/08-02-SUMMARY.md

<interfaces>
<!-- From plan 01/02 (src/road.js). Use directly. -->
- export class RoadSystem { constructor(worldSeed, params) }
- ensureTile(tileX, tileZ): { waypoints, spline }   // forces generation
- queryNearest(wx, wz, radiusM=200): { point: THREE.Vector3, tangent: THREE.Vector3 } | null
- _getTile(tileX, tileZ).spline: THREE.CatmullRomCurve3   // getPointAt(u), getTangentAt(u)
- export const CHUNK_SIZE = 64

EXISTING resolveSpawn contract to PRESERVE (src/main.js lines 104-174):
- signature: function resolveSpawn(wseed, params)
- return: { position: THREE.Vector3, heading: number }
- spawn-region seed: seedFor(wseed, 'spawn'); base offset ±100m:
    baseX = ((spawnSeed & 0xFFFF)/0xFFFF - 0.5)*200
    baseZ = (((spawnSeed >>> 16) & 0xFFFF)/0xFFFF - 0.5)*200
- terrain-only body (lines 120-173) is the FALLBACK to retain
- module-scope `roadSystem` exists (plan 02); terrainSystem exists
- terrainSystem.analyticHeight(x,z) gives the surface Y for placing position.y

From src/seed.js: seedFor(worldSeed, 'spawn') → uint32
</interfaces>

# CRITICAL CONSTRAINTS:
# - D-07: SAME call site, SAME signature (wseed, params) → {position, heading}. Only the body changes.
# - resolveSpawn runs at init BEFORE any road tile is generated → must eagerly ensureTile the 3×3 spawn region first (RESEARCH Pitfall 5).
# - If queryNearest returns null (no road within radius), fall back to the EXISTING terrain-only body (preserve it), with console.warn. Bounded + deterministic.
# - D-06 seam test is the EXIT GATE — must be green before Phase 9. Uses pure coarseHeight via RoadSystem; same-seed deterministic.
# - position.y = terrainSystem.analyticHeight(nearest.point.x, nearest.point.z) so the truck sits on the visual surface (analyticHeight is correct for placement; the ROUTER used raw coarseHeight, but spawn PLACEMENT uses analyticHeight to match rendered terrain).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap resolveSpawn body — nearest road node + tangent heading (D-07)</name>
  <files>src/main.js</files>
  <read_first>
    - src/main.js lines 104-174 (current resolveSpawn body + seam comment — this is the swap target; preserve as fallback)
    - .planning/phases/08-road-routing/08-PATTERNS.md §src/main.js resolveSpawn body swap (eager 3×3 generation + queryNearest + heading = atan2(tangent.x, tangent.z) + fallback)
    - .planning/phases/08-road-routing/08-RESEARCH.md §Pitfall 5 (resolveSpawn querying un-generated tiles), §Open Q4 (search radius, eager 3×3, 200m radius, fallback)
    - .planning/phases/08-road-routing/08-02-SUMMARY.md (ensureTile/queryNearest contract delivered)
  </read_first>
  <action>
    Rewrite the body of `resolveSpawn(wseed, params)` in src/main.js (do NOT rename, do NOT change params or return shape):
    - Keep computing `spawnSeed = seedFor(wseed, 'spawn')` and the base offset baseX/baseZ exactly as the existing body (±100m formula).
    - NEW: if `roadSystem` exists, eagerly generate the 3×3 tiles around the spawn region: compute baseTX = Math.floor(baseX / CHUNK_SIZE), baseTZ = Math.floor(baseZ / CHUNK_SIZE); loop dtx,dtz in [-1,0,1] calling `roadSystem.ensureTile(baseTX+dtx, baseTZ+dtz)` (RESEARCH Pitfall 5 — query needs generated tiles). Then `const nearest = roadSystem.queryNearest(baseX, baseZ, 200)`.
    - If `nearest` is non-null: position = new THREE.Vector3(nearest.point.x, terrainSystem.analyticHeight(nearest.point.x, nearest.point.z), nearest.point.z); heading = Math.atan2(nearest.tangent.x, nearest.tangent.z) (face down the road, D-07). Return { position, heading }. (Use analyticHeight for placement so the truck rests on the rendered surface — comment this: router uses raw coarseHeight for grade, spawn placement uses analyticHeight for visual surface match.)
    - If `nearest` is null OR roadSystem absent: `console.warn('[resolveSpawn] No road node within radius — falling back to terrain-only spawn')` and execute the PRESERVED Phase 7 terrain-only body (the existing flat-candidate search + analyticHeight + heading-from-seed). Keep that fallback code intact (bounded ≤50 tries, deterministic).
    - Update the seam comment block: mark Phase 8 complete (D-07 / D-16 filled) — note the body now probes the road graph first with terrain-only fallback, and the signature is unchanged.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/main.js','utf8'); const re=[/function resolveSpawn\s*\(\s*wseed\s*,\s*params\s*\)/,/roadSystem\.ensureTile\(/,/roadSystem\.queryNearest\(/,/Math\.atan2\([^)]*tangent/,/falling back to terrain-only|terrain-only spawn/,/seedFor\(wseed,\s*'spawn'\)/]; const l=['signature-unchanged','eager-ensureTile','queryNearest','tangent-heading','fallback-warn','spawn-seed-preserved']; let ok=true; re.forEach((r,i)=>{if(!r.test(s)){console.error('FAIL:',l[i]);ok=false}}); if(!/return\s*\{\s*\n?\s*position/.test(s)){/* return shape still object */} process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `resolveSpawn` signature is still `function resolveSpawn (wseed, params)` and returns `{ position: THREE.Vector3, heading: number }` (no call-site change elsewhere in main.js).
    - Body eagerly calls `roadSystem.ensureTile` over a 3×3 tile region then `roadSystem.queryNearest(baseX, baseZ, 200)`.
    - On a road hit: heading computed via `Math.atan2(nearest.tangent.x, nearest.tangent.z)`; position.y via `terrainSystem.analyticHeight(...)`.
    - Null/absent road path runs the preserved terrain-only fallback with a `console.warn`.
    - Manual browser check (index.html): on load the truck spawns ON a visible road centerline (within ~road width) facing along it; reloading the same seed spawns at the same spot/heading.
  </acceptance_criteria>
  <done>resolveSpawn probes nearest road node + tangent heading with preserved terrain-only fallback; signature and call site unchanged; truck spawns on-road facing down it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seam-continuity exit-gate test (D-06) + fold seam check into main harness</name>
  <files>test/test-road-seam.html, test/test-road.html</files>
  <read_first>
    - test/test-road.html (plan 01 harness — importmap, assert/import pattern to mirror)
    - test/road-test-harness.js (assert helper)
    - .planning/phases/08-road-routing/08-RESEARCH.md §Validation Architecture (D-06 row: tangent at t=1.0 of tile A vs t=0.0 of tile B, angle < 5°), §Pitfall 2 (seam kinks)
    - .planning/phases/08-road-routing/08-PATTERNS.md §test/test-road.html
  </read_first>
  <behavior>
    - For each east-west adjacent tile pair in a 3×3 grid, the angle between tile A's tangent at t=1.0 and tile B's tangent at t=0.0 is < 5° (C1 continuity — no kink). This is the EXIT GATE.
    - C0 holds: tile A getPoint(1.0) ≈ tile B getPoint(0.0) within 0.01 m at every shared seam.
    - The seam result is deterministic: same seed → same tangents → same pass on two runs.
  </behavior>
  <action>
    Create test/test-road-seam.html as a standalone page (copy the importmap from index.html verbatim, mirror test-road.html structure). In a `<script type="module">`:
    - Import RoadSystem from ../src/road.js, parseWorldSeed from ../src/seed.js, assert from ./road-test-harness.js, and RANGER_PARAMS (or TEST_PARAMS) for params.
    - Build one RoadSystem, ensureTile over a 3×3 tile grid (tileX,tileZ in -1..1).
    - For each east-west adjacent pair (tX, tZ)→(tX+1, tZ): get tangentA = tileA.spline.getTangentAt(1.0), tangentB = tileB.spline.getTangentAt(0.0); compute angle = acos(clamp(tangentA·tangentB, -1, 1)) in degrees; `assert('D-06 seam C1 (tX,tZ): angle < 5°', angleDeg < 5)`. Also assert C0: `getPoint(1.0).distanceTo(neighbor.getPoint(0.0)) < 0.01`.
    - Add a summary log: total seams checked, max angle observed, PASS/FAIL overall. This file IS the exit gate (D-06).

    Then add the same seam assertions to test/test-road.html so the full suite (ROAD-01/02/03/04 + seam) runs from one page too.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('test/test-road-seam.html','utf8'); const re=[/getTangentAt\(1(\.0)?\)/,/getTangentAt\(0(\.0)?\)/,/< 5/,/D-06/,/RoadSystem/]; const l=['tangent-A-end','tangent-B-start','5deg-threshold','D-06-label','RoadSystem']; let ok=true; re.forEach((r,i)=>{if(!r.test(s)){console.error('FAIL:',l[i]);ok=false}}); if(!fs.readFileSync('test/test-road.html','utf8').match(/getTangentAt/)){console.error('FAIL: seam assertion not folded into main harness');ok=false} process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/test-road-seam.html` exists, copies the index.html importmap, builds a RoadSystem, ensureTile over a 3×3 grid.
    - For every east-west adjacent seam, it asserts `angle(tangentA@t=1.0, tangentB@t=0.0) < 5°` (D-06 exit gate) and C0 distance < 0.01 m.
    - Loading test/test-road-seam.html in a browser logs all `PASS:` seam lines, a summary with max angle observed, and zero `FAIL:` — this green result is the documented phase exit gate.
    - The seam C1 assertion is also present in test/test-road.html (full suite from one page).
    - Re-running on the same seed yields identical pass results (determinism).
  </acceptance_criteria>
  <done>Seam-continuity exit-gate test exists and passes: no kink (tangent angle < 5°) at any 64 m tile boundary across a 3×3 grid; folded into the main harness; deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed → resolveSpawn | Spawn position derives from seedFor(wseed,'spawn') + road graph; all pure deterministic math; no untrusted input crosses. |

Browser-only, single-origin, no backend/network/PII sim. resolveSpawn is a pure init-time computation; the seam test is dev-only browser tooling. Honest threat surface for this plan is effectively empty.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Denial of Service | resolveSpawn eager 3×3 tile generation at init | mitigate | Bounded to 9 tiles, each capped-grid A*; queryNearest radius-limited (200m); terrain-only fallback is the existing ≤50-try bounded loop |
| T-08-06 | Availability | spawn off-road if road config pathological | mitigate | Null queryNearest → preserved terrain-only fallback + console.warn (deterministic, never infinite) |
| T-08-SC | Tampering | npm installs | n/a | No package installs this plan (RESEARCH Package Legitimacy Audit: zero new deps) |

No network, file-write, or data-handling surface in this plan.
</threat_model>

<verification>
Load `test/test-road-seam.html` (the EXIT GATE) — all seam C1 assertions PASS (tangent angle < 5°), summary shows max angle < 5°, zero FAIL.
Load `test/test-road.html` — full suite (ROAD-01/02/03/04 + seam) all PASS.
Load `index.html`:
- Truck spawns ON a visible road centerline, facing down the road.
- Reload same seed → same spawn position + heading.
- Road splines cross 64 m tile seams with no visible kink (D-06 visible on the centerline).
</verification>

<success_criteria>
- resolveSpawn body swapped to nearest road node + tangent heading (D-07), signature unchanged, terrain-only fallback preserved.
- Truck spawns on-road facing down it; deterministic per seed.
- Seam-continuity exit-gate test (D-06) is green: no kink (< 5°) at any tile boundary across a 3×3 grid.
- Phase exit gate satisfied — splines are stable and seam-continuous, ready for Phase 9 ribbon mesh.
</success_criteria>

<output>
Create `.planning/phases/08-road-routing/08-03-SUMMARY.md` when done.
</output>
