---
phase: 08-road-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/test-road.html
  - test/road-test-harness.js
  - src/road.js
  - data/ranger.js
autonomous: true
requirements: [ROAD-01, ROAD-02, ROAD-03]
must_haves:
  truths:
    - "Constructing RoadSystem twice with the same worldSeed produces identical spline sample points (determinism)"
    - "No edge in any routed tile exceeds maxRoadGrade (hard grade limit)"
    - "On a synthetic steep ramp terrain, the centerline does not climb straight up — it switchbacks laterally"
    - "Roads route over raw coarseHeight only — terrain amplitude slider changes do not re-route roads"
  artifacts:
    - path: "src/road.js"
      provides: "RoadSystem class — per-tile A* router, seeded edge waypoints, Catmull-Rom splines with ghost control points"
      contains: "export class RoadSystem"
      min_lines: 250
    - path: "test/test-road.html"
      provides: "Browser-console assertion harness for ROAD-01/02/03 (and seam test loaded in plan 03)"
      contains: "RoadSystem"
    - path: "test/road-test-harness.js"
      provides: "assert() helper + mockCoarseHeight steep-ramp synthetic terrain for switchback isolation"
      contains: "mockCoarseHeight"
  key_links:
    - from: "src/road.js"
      to: "seedFor(worldSeed, 'roads', tileX, tileZ)"
      via: "import from ./seed.js"
      pattern: "seedFor\\([^,]+,\\s*['\"]roads['\"]"
    - from: "src/road.js"
      to: "createNoise2D(mulberry32(seedFor(worldSeed,'coarse')))"
      via: "own coarse noise closure (NOT terrainSystem private field, NOT analyticHeight)"
      pattern: "seedFor\\([^,]+,\\s*['\"]coarse['\"]"
  # D-01 sparse trunk + seeded spurs — trunk routing implemented here; spur seeding stub included
  # D-02 max grade ~12% with hard block + quadratic slope cost driving tighter hairpins
  # D-04 valley/pass-seeking via altitude cost term in edgeCost
---

<objective>
Build the core of `src/road.js`: a deterministic per-tile A* router over raw `coarseHeight`, with quadratic slope cost, a hard max-grade block, valley-seeking altitude weighting, seeded tile-edge waypoints, and per-tile Catmull-Rom splines that carry ghost control points for C1 seam continuity. Ship a browser-console test harness first so every routing behavior is automatically verifiable.

Purpose: This is the only novel algorithm in v1.1 and the highest-risk phase. Determinism, the hard grade limit, and switchback emergence are the load-bearing correctness properties — they must be test-locked before any query API, debug viz, or spawn integration is built on top.

Output: `test/road-test-harness.js`, `test/test-road.html`, `src/road.js` (RoadSystem core), and new routing params in `data/ranger.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-road-routing/08-CONTEXT.md
@.planning/phases/08-road-routing/08-RESEARCH.md
@.planning/phases/08-road-routing/08-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from project source — use directly, no exploration. -->

From src/seed.js (frozen — do not modify):
- export function seedFor(worldSeed, domainTag, ...coords): number   // uint32 sub-seed
- export function mulberry32(seed): () => number                      // [0,1) PRNG closure
- export function parseWorldSeed(input): number                      // string|int → uint32

From data/ranger.js (RANGER_PARAMS coarse-layer block — LOCKED at end of Phase 7):
- coarseAmplitude: 150     // metres, full-scale range of coarse layer
- coarseFreq:      0.0005  // 1/m base frequency (2 km wavelength)
- coarseOctaves:   4
- ridgeSharpness:  1.6
- terrainAmplitude: 1.0    // visual Y-rescale ONLY — router MUST be independent of this

From src/terrain.js — the coarseHeight formula to REPLICATE in road.js (raw, pre-amplitude):
```
function coarseHeight(wx, wz, noiseCoarse, params) {
    const { coarseAmplitude, coarseFreq, coarseOctaves, ridgeSharpness } = params
    let h = 0, freq = coarseFreq, amp = coarseAmplitude
    const gain = 0.5, lacunarity = 2.0
    for (let o = 0; o < coarseOctaves; o++) {
        const n = noiseCoarse(wx * freq, wz * freq)
        const ridged = 1.0 - Math.abs(n)
        const shaped = Math.pow(ridged, ridgeSharpness)
        h += shaped * amp
        freq *= lacunarity; amp *= gain
    }
    return h
}
```
This is the ONLY valid height source for routing. terrain.js builds its main-thread coarse noise as `createNoise2D(mulberry32(seedFor(worldSeed,'coarse')))`. road.js must build a byte-identical closure the same way to get identical raw heights.

importmap (from index.html) exposes:
- "three"        → three@0.184.0 module build  (THREE.CatmullRomCurve3, THREE.Vector3)
- "simplex-noise"→ simplex-noise@4.0.3 esm     (export createNoise2D) — same lib terrain.js extracted from
</interfaces>

# CRITICAL CONSTRAINTS (locked — see CONTEXT.md / RESEARCH.md):
# - Router uses raw coarseHeight ONLY. NEVER terrainSystem.sampleHeight (chunk-load-order dependent).
#   NEVER terrainSystem.analyticHeight (applies terrainAmplitude — breaks amplitude-independent grade math).
# - road.js builds its OWN coarse noise closure via seedFor(worldSeed,'coarse'); never reads a TerrainSystem private field.
# - Pure function of (worldSeed, tileX, tileZ, params): the tile cache is memoization only, never state.
# - No new npm packages; no build system. road.js is a plain ES6 module loaded via importmap.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — test harness + synthetic steep-ramp terrain</name>
  <files>test/road-test-harness.js, test/test-road.html, data/ranger.js</files>
  <read_first>
    - test/terrain-unit.js (existing browser-console test style in this project)
    - test/assert-m4-07-ramp-slide.mjs (existing assertion-script PASS/FAIL pattern)
    - index.html lines 63-70 (importmap block to copy verbatim into test HTML)
    - data/ranger.js lines 148-170 (RANGER_PARAMS coarse-layer block — add road params adjacent)
    - .planning/phases/08-road-routing/08-RESEARCH.md §Validation Architecture and §Wave 0 Gaps
    - .planning/phases/08-road-routing/08-PATTERNS.md §test/test-road.html
  </read_first>
  <action>
    Add routing params to RANGER_PARAMS in data/ranger.js (new "Phase 8 Road Routing" comment block adjacent to the coarse-layer block): maxRoadGrade=0.12 (ratio; D-02 ~12%), routeGridSize=16 (16×16 cells per 64 m tile = 4 m cells; A2), roadSlopePenalty=50 (A4 starting value), roadAltWeight=0.1 (A4 starting value), spurProbability=0.15 (D-01/A1). Comment each with units and the RESEARCH assumption ID it derives from.

    Create test/road-test-harness.js as an ES6 module exporting: (1) `assert(label, condition)` that console.logs `PASS: <label>` or `FAIL: <label>`; (2) `mockCoarseHeight(wx, wz)` returning a deterministic steep uniform ramp `wz * 0.5` (50% grade in +Z) so the switchback test (Task 4 verify) is isolated from live noise — a 50% ramp far exceeds the 12% max-grade so a straight climb is impossible and switchbacks MUST emerge; (3) a `TEST_PARAMS` object mirroring the RANGER_PARAMS coarse + road params so tests do not import the full data file.

    Create test/test-road.html as a standalone page: copy the importmap from index.html verbatim (so `three` and `simplex-noise` resolve), then a single `<script type="module">` that imports RoadSystem from ../src/road.js and the harness helpers, and runs the assertion suite. For THIS task, scaffold the file with the importmap, imports, and a placeholder assert that logs `PASS: harness loads` — the real assertions are filled in by Tasks 2-4 verify steps. The seam-continuity assertion (D-06) is added later by plan 03.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('data/ranger.js','utf8'); const k=['maxRoadGrade','routeGridSize','roadSlopePenalty','roadAltWeight','spurProbability']; const miss=k.filter(x=>!s.includes(x+':')); if(miss.length){console.error('MISSING params:',miss);process.exit(1)} console.log('PASS: road params present')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('test/road-test-harness.js','utf8'); if(!/export function assert/.test(s)||!/export function mockCoarseHeight|export const mockCoarseHeight/.test(s)){console.error('harness missing exports');process.exit(1)} console.log('PASS: harness exports')"</automated>
  </verify>
  <acceptance_criteria>
    - `data/ranger.js` defines maxRoadGrade (0.12), routeGridSize (16), roadSlopePenalty (50), roadAltWeight (0.1), spurProbability (0.15), each with a unit/source comment.
    - `test/road-test-harness.js` exports `assert(label, condition)` (logs `PASS:`/`FAIL:` prefix) and `mockCoarseHeight(wx, wz)` returning `wz * 0.5`.
    - `test/test-road.html` contains the index.html importmap verbatim (`simplex-noise@4.0.3` and `three@0.184.0` entries present) and a `<script type="module">` importing from `../src/road.js`.
    - Opening test/test-road.html in a browser logs `PASS: harness loads` with no module-resolution errors in the console.
  </acceptance_criteria>
  <done>Road params exist in ranger.js; harness module exports assert + mockCoarseHeight; test HTML loads modules without error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: road.js skeleton — noise closure, raw coarseHeight, seeded edge waypoints, MinHeap</name>
  <files>src/road.js</files>
  <read_first>
    - src/terrain.js lines 1-28 (module header style), 128-176 (createNoise2D body — note it is private; road.js imports from 'simplex-noise' instead), 183-200 + 284-300 (coarseHeight formula to replicate), 326-376 (TerrainSystem constructor shape), 420-455 (reinitWorker noise-init pattern)
    - src/seed.js (seedFor / mulberry32 signatures — frozen, import these)
    - .planning/phases/08-road-routing/08-PATTERNS.md §src/road.js (class/constructor/noise/header patterns to copy)
    - .planning/phases/08-road-routing/08-RESEARCH.md §Pattern 4 (deriveEdgeWaypoints) and §Code Examples MinHeap
  </read_first>
  <behavior>
    - Two RoadSystem instances built with the same worldSeed produce identical `_coarseH(wx,wz)` values at arbitrary coords (determinism of the noise closure).
    - `_deriveEdgeWaypoints(tileX, tileZ)` returns the same west-entry/east-exit Vector3 positions across two instances of the same seed.
    - Edge waypoints lie exactly on tile boundaries: west entry x === tileX*64, east exit x === (tileX+1)*64.
    - MinHeap pop() always returns the lowest-priority item pushed.
  </behavior>
  <action>
    Create src/road.js as a documented ES6 module. Header JSDoc block per PATTERNS.md §file-header (responsibilities + the three NEVER anti-patterns: never sampleHeight, never analyticHeight, never call from physics loop).

    Imports: `import * as THREE from 'three'`, `import { seedFor, mulberry32 } from './seed.js'`, `import { createNoise2D } from 'simplex-noise'`. Re-export `export const CHUNK_SIZE = 64` (must equal terrain.js CHUNK_SIZE; comment the coupling).

    Module-scope private `_coarseHeight(wx, wz, noiseCoarse, params)` — replicate the terrain.js coarseHeight formula VERBATIM (gain=0.5, lacunarity=2.0, ridged = 1-|n|, shaped = pow(ridged, ridgeSharpness)). This guarantees road grade math uses the same raw heights as terrain. JSDoc must state it is byte-identical to terrain.js coarseHeight and pre-amplitude.

    Module-scope `class MinHeap` (push(item, priority)/pop()/size getter) per RESEARCH §Code Examples — A* open set.

    `export class RoadSystem` with constructor(worldSeed, params): store _worldSeed, _params, `_tileCache = new Map()`, `_debugLines = []`, `_scene = null`, then call `_reinitNoise()`. `_reinitNoise()` builds `this._noiseCoarse = createNoise2D(mulberry32(seedFor(this._worldSeed, 'coarse')))` — same derivation as terrain.js so heights match. Add private `_coarseH(wx, wz)` instance method delegating to `_coarseHeight(wx, wz, this._noiseCoarse, this._params)`.

    `_deriveEdgeWaypoints(tileX, tileZ)`: build `rng = mulberry32(seedFor(this._worldSeed, 'roads', tileX, tileZ))`. Trunk routes East-West (RESEARCH Open Q3). West entry: x = tileX*64, z = tileZ*64 + rng()*64, y = _coarseH(x,z). East exit: x = (tileX+1)*64, z = tileZ*64 + rng()*64, y = _coarseH(x,z). Return `{ entry: Vector3, exit: Vector3 }`. JSDoc the East-West trunk convention and that the east exit of tile (tX,tZ) IS the west entry of tile (tX+1,tZ) by ownership convention (derived identically because both call seedFor with the owning tile's coords — document which tile owns which edge per RESEARCH §Pattern 3 ownership convention).

    Do NOT implement A* or splines yet (Tasks 3-4). Stub `_routeTile`/`_getTile` to throw "not implemented" so the file imports cleanly.
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./src/road.js').then(m=>{if(!m.RoadSystem)process.exit(1);console.log('PASS: road.js imports, RoadSystem exported')}).catch(e=>{console.error(e.message);process.exit(1)})" 2>&1 | tail -3</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/road.js','utf8'); const checks=[/createNoise2D\(mulberry32\(seedFor\([^,]+,\s*['\\\"]coarse['\\\"]\)\)\)/,/seedFor\([^,]+,\s*['\\\"]roads['\\\"]/,/class MinHeap/,/export class RoadSystem/]; const labels=['coarse-noise-closure','roads-seed','MinHeap','RoadSystem']; let ok=true; checks.forEach((re,i)=>{if(!re.test(s)){console.error('FAIL:',labels[i]);ok=false}}); if(/analyticHeight|sampleHeight/.test(s)){console.error('FAIL: forbidden terrain call present');ok=false} process.exit(ok?0:1); "</automated>
  </verify>
  <acceptance_criteria>
    - `src/road.js` imports cleanly under node ESM resolution mock OR browser (RoadSystem export present); no syntax errors.
    - Source contains `createNoise2D(mulberry32(seedFor(<seed>, 'coarse')))` (own closure) and `seedFor(<seed>, 'roads', tileX, tileZ)`.
    - Source contains NEITHER the literal `analyticHeight` NOR `sampleHeight` (grep returns no match).
    - `_coarseHeight` body matches terrain.js: contains `1.0 - Math.abs`, `Math.pow(ridged`, `gain = 0.5`, `lacunarity = 2.0`.
    - In test-road.html, asserting two same-seed instances return equal `_coarseH(123,456)` passes; `_deriveEdgeWaypoints(0,0).entry.x === 0` and `.exit.x === 64`.
  </acceptance_criteria>
  <done>road.js skeleton imports cleanly; own coarse noise closure + roads edge waypoints are deterministic and on tile boundaries; no forbidden terrain calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A* router — quadratic slope cost, hard grade block, valley-seeking</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js (current state from Task 2 — _deriveEdgeWaypoints, _coarseH, MinHeap)
    - .planning/phases/08-road-routing/08-RESEARCH.md §Pattern 2 (edgeCost + heuristic + 8-directional grid + the switchback insight), §Pitfall 1 (altitude doubling-back red herring), §Pitfall 6 (16×16 grid prevents ridge-cutting)
    - test/road-test-harness.js (mockCoarseHeight steep ramp for the switchback assertion)
  </read_first>
  <behavior>
    - On mockCoarseHeight (50% ramp in +Z), routing a tile whose entry/exit force a net +Z climb produces a centerline whose max consecutive-waypoint grade is ≤ maxRoadGrade (0.12) — proving the hard block forces lateral switchback travel instead of a straight 50% climb.
    - On the same ramp, the centerline spans noticeable X-lateral distance (not a straight Z line) — switchback arms are present (assert total path X-extent > one grid cell, i.e. the path moves sideways).
    - Every edge in the returned waypoint list satisfies `|dh|/sqrt(dx²+dz²) <= maxRoadGrade + 1e-6`.
    - Two same-seed routes over identical params return identical waypoint arrays.
  </behavior>
  <action>
    Implement the per-tile A* in src/road.js. Add a private `_routeTile(tileX, tileZ)` that: (1) calls `_deriveEdgeWaypoints` for start (west entry) and goal (east exit); (2) builds a routeGridSize×routeGridSize grid (params.routeGridSize, default 16 → 4 m cells over the 64 m tile) of cells, each cell caching its world (wx, wz) and raw h = _coarseH(wx, wz); (3) runs A* with 8-directional connectivity (cardinal + diagonal; diagonal geometric distance ×√2) from the grid cell nearest the entry to the grid cell nearest the exit, using MinHeap as the open set and a per-tile visited Set (per-tile only — RESEARCH Pitfall 1: no global visited state).

    Implement `_edgeCost(fromCell, toCell)` per RESEARCH §Pattern 2: dx,dz horizontal deltas, dh raw-height delta; `grade = |dh| / sqrt(dx²+dz²)`; HARD BLOCK `if (grade > this._params.maxRoadGrade) return Infinity`; `dist = sqrt(dx²+dz²+dh²)`; quadratic slope penalty `grade*grade*roadSlopePenalty`; valley-seeking altitude term `toCell.h * roadAltWeight` (lower raw coarseHeight → cheaper, satisfying D-04). Return dist + slopeCost + altCost. Heuristic = XZ Euclidean distance to goal (admissible). JSDoc each term with the decision it serves (D-02 hard block, D-04 valley-seek).

    Reconstruct the path (came-from map), convert grid cells back to world Vector3 waypoints (use raw _coarseH for Y), and return `{ waypoints: Vector3[], spline: null }` (spline filled in Task 4). If A* finds no path (all edges from a frontier are over-grade — can happen on pathological terrain), fall back to a direct entry→exit two-point list and console.warn — document this as the bounded degenerate case.

    Wire `_getTile(tileX, tileZ)` to memoize `_routeTile` results in `_tileCache` keyed `` `${tileX},${tileZ}` `` (PATTERNS.md cache pattern). Cache is memoization only — same seed always recomputes identically.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/road.js','utf8'); const re=[/_edgeCost/,/grade\s*>\s*this\._params\.maxRoadGrade/,/return Infinity/,/roadSlopePenalty/,/roadAltWeight/,/_getTile/]; const l=['_edgeCost','hard-grade-block','Infinity-block','slope-penalty','alt-weight','_getTile']; let ok=true; re.forEach((r,i)=>{if(!r.test(s)){console.error('FAIL:',l[i]);ok=false}}); if(/grade\s*\*\s*grade|grade\s*\*\*\s*2/.test(s)===false){console.error('FAIL: quadratic slope term');ok=false} process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/road.js` defines `_edgeCost` containing `if (grade > this._params.maxRoadGrade) return Infinity` (hard block, ROAD-02) and a quadratic term (`grade * grade` or `grade ** 2`) multiplied by roadSlopePenalty.
    - `_edgeCost` includes the valley-seeking term `toCell.h * ... roadAltWeight` (D-04).
    - In test-road.html: routing tile (0,0) on mockCoarseHeight, every consecutive waypoint pair satisfies `|dh|/sqrt(dx²+dz²) <= maxRoadGrade + 1e-6` → PASS (ROAD-02, ROAD-03 hard limit).
    - In test-road.html: on the 50% ramp the routed centerline X-extent exceeds one grid cell (4 m) → switchback lateral travel present → PASS (ROAD-03).
    - In test-road.html: two same-seed `_routeTile(0,0).waypoints` arrays are element-wise equal within 1e-9 → PASS (ROAD-01 determinism).
  </acceptance_criteria>
  <done>A* router with quadratic slope cost + hard grade block + valley-seeking produces deterministic, never-over-grade, switchbacking waypoint lists cached per tile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Catmull-Rom splines with ghost control points (C0/C1 seam-ready)</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js (current state — _routeTile returns waypoints, spline:null)
    - .planning/phases/08-road-routing/08-RESEARCH.md §Pattern 3 (ghost control points + ownership + staging to avoid circular dependency), §Code Examples CatmullRomCurve3 API, §Pitfall 2 (seam kinks from missing ghost points)
    - test/road-test-harness.js
  </read_first>
  <behavior>
    - `_getTile(0,0).spline` is a THREE.CatmullRomCurve3; `getPointAt(0.5)` returns a finite Vector3 and `getTangentAt(0.5)` returns a unit-length vector (|len-1| < 1e-3).
    - C0 across seam: `tileA.spline.getPoint(1.0)` equals `tileB.spline.getPoint(0.0)` within 0.01 m for east-west adjacent tiles (0,0)/(1,0).
    - The spline control-point list for a tile includes one ghost point prepended (left neighbor's last internal waypoint) and one appended (right neighbor's first internal waypoint).
    - Determinism: same-seed tile spline sample at t=0.5 identical across two instances.
  </behavior>
  <action>
    Implement spline construction in src/road.js with strict stage ordering (RESEARCH §Pattern 3 caveat — avoid circular dependency): separate edge-waypoint derivation (Task 2, no neighbor dependency) from spline construction (needs neighbor ghost points). Add `_buildTileSpline(tileX, tileZ, waypoints)`:
    - Fetch ghost-from-left = the last internal waypoint of tile (tileX-1, tileZ) and ghost-from-right = the first internal waypoint of tile (tileX+1, tileZ), by calling `_getTileWaypointsOnly(tileX±1, tileZ)` — a lightweight helper that returns ONLY the routed waypoint array (runs/caches A* waypoints) WITHOUT building that neighbor's spline, preventing recursion (tile A builds spline → asks B for waypoints only → B does not build spline → no cycle).
    - Build control point list `[ghostLeft, ...waypoints, ghostRight]` filtering null ghosts (world-edge tiles).
    - Construct `new THREE.CatmullRomCurve3(pts, false, 'centripetal', 0.5)` (RESEARCH: 'centripetal' avoids self-intersection at hairpins).
    Restructure `_getTile` into staged form: (1) ensure routed waypoints exist in cache (memoized, spline-free), (2) build + cache the spline with ghost points on first spline access. Cache entry shape: `{ waypoints, spline }`. JSDoc the staging and why ghost points give C1 (tangent_i = τ(P_{i+1}−P_{i-1}); shared ghosts → identical tangent both sides).

    The full C1-tangent seam exit-gate assertion (D-06, angle < 5°) is added by plan 03's seam test; this task delivers the ghost-point mechanism and the C0 continuity check.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/road.js','utf8'); const re=[/CatmullRomCurve3\(/,/centripetal/,/_buildTileSpline/,/ghost/i]; const l=['CatmullRomCurve3','centripetal-type','_buildTileSpline','ghost-points']; let ok=true; re.forEach((r,i)=>{if(!r.test(s)){console.error('FAIL:',l[i]);ok=false}}); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/road.js` builds splines via `new THREE.CatmullRomCurve3(pts, false, 'centripetal', 0.5)`.
    - A waypoint-only helper exists so neighbor ghost lookup does NOT trigger neighbor spline construction (no infinite recursion) — verified by routing a 3×3 tile grid in test-road.html without stack overflow.
    - In test-road.html: `_getTile(0,0).spline.getTangentAt(0.5)` has length within 1e-3 of 1.0 → PASS (ROAD-04 query primitive sane).
    - In test-road.html: `tileA(0,0).spline.getPoint(1.0).distanceTo(tileB(1,0).spline.getPoint(0.0)) < 0.01` → PASS (C0 seam continuity; full C1 gate verified in plan 03).
    - Determinism: same-seed tile (0,0) spline `getPoint(0.5)` equal across two instances within 1e-6 → PASS (ROAD-01).
  </acceptance_criteria>
  <done>Per-tile Catmull-Rom splines build with prepended/appended ghost control points, no recursion, C0-continuous across seams, deterministic, unit tangents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL `?seed=` → router | The only external input is the world-seed string; it flows through `parseWorldSeed`/`djb2` charCodeAt arithmetic only (no eval, no code path) — already mitigated in Phase 7 (seed.js header). |

This is a browser-only, single-origin, no-backend, no-network, no-PII physics sim. There is no auth surface, no server, no data persistence, and no untrusted data ingestion beyond the seed string. The honest threat surface for this phase is effectively empty.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | seed string → router | accept (already mitigated upstream) | seed flows through djb2 charCodeAt only; no execution path (seed.js header, Phase 7) |
| T-08-02 | Denial of Service | per-tile A* on first access | mitigate | Grid capped at routeGridSize² (256 cells); A* bounded; lazy per-tile generation only — never per-frame (RESEARCH Pitfall, anti-pattern: never call road.js from physics loop) |
| T-08-SC | Tampering | npm/pip/cargo installs | n/a | No package installs this phase — RESEARCH Package Legitimacy Audit confirms zero new deps (Three.js + simplex-noise@4.0.3 already present via importmap) |

No network, file-write, or data-handling surface exists in this phase. No further STRIDE categories apply.
</threat_model>

<verification>
Load `test/test-road.html` in a browser (via Live Server or `npx serve`). Console must show:
- PASS: harness loads
- PASS: ROAD-01 determinism (coarse height + waypoints + spline sample equal across two same-seed instances)
- PASS: ROAD-02 hard grade limit (no edge exceeds maxRoadGrade)
- PASS: ROAD-03 switchback emergence (lateral travel on 50% ramp)
- PASS: ROAD-04 query primitives (unit tangent, finite point)
- PASS: C0 seam continuity (tile A t=1 == tile B t=0)
No FAIL lines, no console errors.
</verification>

<success_criteria>
- `src/road.js` exports RoadSystem with a deterministic per-tile A* router over raw coarseHeight.
- No routed edge exceeds maxRoadGrade (ROAD-02); switchbacks emerge on steep terrain (ROAD-03).
- Same seed → identical waypoints and spline samples (ROAD-01 determinism).
- Per-tile Catmull-Rom splines carry ghost control points and are C0-continuous across seams.
- Browser test harness reports all PASS, no FAIL.
</success_criteria>

<output>
Create `.planning/phases/08-road-routing/08-01-SUMMARY.md` when done.
</output>
