---
phase: 08-road-routing
plan: 06
type: execute
wave: 2
gap_closure: true
depends_on: ["08-05"]
files_modified:
  - src/road.js
autonomous: true
requirements: [ROAD-01, ROAD-04]
must_haves:
  # D-06 (REVISED) — seam C0/C1 is FREE: one continuous polyline sliced at 64 m boundaries
  # ROAD-04 — queryable splines; queryNearest(x,z) → {point, tangent}
  # D-07 (consumed in 08-07) — queryNearest powers resolveSpawn (truck on road, facing down it)
  # Closes 08-VERIFICATION gaps: truths 4, 5 (failed) + seam exit-gate contract for test-road-seam.html
  truths:
    - "The continuous trunk polyline is sliced at 64 m tile boundaries into per-tile Catmull-Rom splines whose consecutive endpoints/tangents match exactly (C0/C1) because they are one curve sliced — no shared-seam-waypoint machinery"
    - "ensureTile(tileX, tileZ) warms the network and returns a tile object exposing a single .spline (and .waypoints) so the seam exit-gate harness can read tile.spline.getPoint(1.0)/getPoint(0.0)"
    - "queryNearest(wx, wz) returns the nearest network point and a unit tangent, or null beyond radius, allocation-light at query cadence — operating over this._tiles, never the removed per-tile cache"
  artifacts:
    - path: "src/road.js"
      provides: "Per-tile slicing of continuous polylines into Catmull-Rom splines (this._tiles), ensureTile returning {spline,waypoints}, queryNearest over the sliced network"
      contains: "_sliceNetwork"
  key_links:
    - from: "src/road.js queryNearest"
      to: "this._tiles per-tile sliced splines"
      via: "samples sliced splines for nearest point + unit tangent"
      pattern: "getTangentAt|getTangent"
    - from: "src/road.js ensureTile"
      to: "_streamNetwork / _sliceNetwork"
      via: "warms + slices the network around the requested tile, returns the tile's representative spline"
      pattern: "_sliceNetwork"
---

<objective>
Slice the canonical continuous trunk polylines (built by `_streamNetwork` in 08-05) into stable
per-tile Catmull-Rom splines, and rebuild the queryable centerline API (`ensureTile`, `queryNearest`)
over the streamed/sliced network — replacing the old-router bodies stubbed in 08-05.

Because the trunk is ONE continuous polyline sliced at 64 m boundaries, seam C0/C1 continuity is
automatic (D-06 REVISED): consecutive per-tile splines share exact endpoints and tangents with NO
shared-seam-waypoint / ghost-point machinery (the old approach that failed VERIFICATION.md is moot).
This delivers ROAD-04 and the per-tile slice Phase 9 consumes, and `queryNearest(x,z) → {point,
tangent}` that 08-07 wires into `resolveSpawn` (D-07).

EXIT-GATE CONTRACT (test/test-road-seam.html, already updated by 08-04 — DO NOT edit the harness):
the harness calls `ensureTile(tX, tZ)` for the 3×3 grid and reads a SINGLE `tile.spline` per tile —
`tile.spline.getPoint(1.0)` / `getPoint(0.0)` / `getTangentAt(1.0|0.0)` — comparing E-W adjacent pairs
for C0 (<0.01 m) and C1 (<5°), and requires at least ONE pair where both tiles carry a spline. So
`ensureTile` MUST return `{ spline, waypoints }` (a single representative per-tile spline), NOT a bare
array of segments — the slice exposure must satisfy this contract.

Purpose: turn raw network polylines into the stable, queryable, per-tile-sliced spline output Phase 9
builds its ribbon mesh on, and make the seam exit gate green-able.
Output: `src/road.js` with `_sliceNetwork()` → `this._tiles`, `ensureTile` returning `{spline,
waypoints}`, and `queryNearest` over the sliced network.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-road-routing/08-CONTEXT.md
@.planning/phases/08-road-routing/08-VERIFICATION.md
@.planning/phases/08-road-routing/08-05-SUMMARY.md
@.planning/spikes/MANIFEST.md

<interfaces>
<!-- From 08-05 (this plan's dependency). Executor uses these directly. -->

src/road.js (built in 08-05):
  this._network : Map<"<mz>:<runIndex>", { points: THREE.Vector3[] }>   canonical continuous trunk polylines
  _streamNetwork(center)                                                (re)builds this._network around center (lazy)
  this._proto.radius                                                    visible road radius (network window size)
  this._proto.params                                                    D-09 weights seeded from this._params

src/road.js scratch (existing, reuse): const _scratchPt = new THREE.Vector3()   (module-scope query allocation guard)

src/terrain.js: export const CHUNK_SIZE = 64   (per-tile slice boundary; world tile = [tileX*64,(tileX+1)*64))

test/test-road-seam.html (EXIT GATE — already updated by 08-04, do NOT edit):
  road.ensureTile(tX,tZ) → tile ; reads tile.spline.getPoint(1.0)/getPoint(0.0)/getTangentAt(1.0|0.0)
  C0: endA.distanceTo(startB) < 0.01 m ; C1: angle(tanA,tanB) < 5° ; requires totalSeams >= 1

resolveSpawn (src/main.js, lines 126–153 — already correct, KEEP): ensureTile 3×3 then
  queryNearest(baseX, baseZ, 200) → reads nearest.point + nearest.tangent ; heading = atan2(tangent.x, tangent.z)
  → queryNearest MUST stay { point: THREE.Vector3, tangent: THREE.Vector3 } | null with UNIT tangent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slice continuous polylines into per-tile Catmull-Rom splines (this._tiles, seam C0/C1 free)</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js `this._network` shape + `_streamNetwork` (from 08-05 — the polylines to slice).
    - src/road.js the `THREE.CatmullRomCurve3(..., false, 'centripetal', 0.5)` usage in `_streamNetwork`
      (match parameterization so sliced splines are consistent with the rendered/source curve).
    - src/terrain.js CHUNK_SIZE export (= 64; the slice boundary).
    - src/road.js the existing `_proto`/`_streamNetwork`/`_removeLoops` source as the reference pipeline
      for how polylines are produced (so slicing consumes them faithfully).
    - test/test-road-seam.html lines 63–124 — the exact `ensureTile`→`tile.spline` contract the slice
      output must satisfy (single representative spline per tile; adjacent tiles share boundary point).
    - .planning/phases/08-road-routing/08-VERIFICATION.md "truth 4" (slice ONE curve → C0/C1 hold
      trivially; do NOT reintroduce shared-seam waypoints).
  </read_first>
  <action>
    Add a DISTINCT `_sliceNetwork()` method (a separate method on RoadSystem — do NOT fold it into
    `_streamNetwork`; 08-07 and the harnesses call `_sliceNetwork()` by name) that converts each
    canonical `this._network` polyline into per-tile Catmull-Rom splines stored in a new
    `this._tiles` Map. For each polyline: build ONE continuous
    `THREE.CatmullRomCurve3(points, false, 'centripetal', 0.5)`, then walk the polyline and CUT it at
    every x or z crossing of a 64 m (`CHUNK_SIZE`) tile boundary, inserting the exact boundary-crossing
    point (linear interpolation of the segment at the integer-boundary coordinate) into BOTH the ending
    sub-polyline and the starting sub-polyline so the two adjacent per-tile splines share that exact
    point (C0) and, being samples of the same parent curve geometry, align tangents there (C1). Assign
    each sub-polyline to the tile containing its midpoint
    (`Math.floor(midX/CHUNK_SIZE)`, `Math.floor(midZ/CHUNK_SIZE)`). Build each sub-polyline's spline with
    the SAME centripetal params (`false, 'centripetal', 0.5`), de-duplicating consecutive coincident
    control points first (centripetal divide-by-zero guard); skip sub-polylines with < 2 distinct points.

    Store `this._tiles` as `Map<"tileX,tileZ", { spline, points, waypoints }[]>` (a tile MAY hold
    multiple road segments). Slicing must be deterministic (pure function of the network polylines,
    which are pure functions of seed+coords+params). This replaces all old shared-seam-waypoint /
    ghost-point logic — none of it returns. Have `invalidateCache()` (and `_streamNetwork`'s re-stream
    path) clear `this._tiles` alongside `this._network` so a re-stream re-slices cleanly.
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -q 'this._tiles' src/road.js && grep -q '_sliceNetwork' src/road.js && grep -vn '^\s*\*\|^\s*//' src/road.js | grep -c '_seamPoint\|ghost' | grep -qx 0 && node --input-type=module -e "import('three').then(async THREE=>{const {RoadSystem}=await import('./src/road.js');const {parseWorldSeed}=await import('./src/seed.js');const p={coarseAmplitude:150,coarseFreq:0.0005,coarseOctaves:4,ridgeSharpness:1.6,maxRoadGrade:0.15,roadWDist:1,roadWAlt:0.85,roadWGrade:400,roadWOver:8000,roadWTurn:120,spurProbability:0.15};const r=new RoadSystem(parseWorldSeed('lone-pine'),p);r._proto.radius=400;r._streamNetwork(new THREE.Vector3(0,0,0));r._sliceNetwork();let segs=0;for(const a of r._tiles.values())segs+=a.length;console.log(segs>0?'SLICE_OK':'SLICE_EMPTY')}).catch(e=>{console.error(e.message);process.exit(1)})" 2>&1 | grep -q SLICE_OK && echo PASS</automated>
  </verify>
  <done>
    `this._tiles` holds per-tile Catmull-Rom spline segments sliced from the continuous network
    polylines; at least one tile carries a segment at origin on lone-pine; no `_seamPoint`/ghost-point
    code remains. Adjacent tiles share the exact boundary point (C0) and aligned tangent (C1) by
    construction (asserted green by the 08-04 seam harness in 08-07).
  </done>
  <acceptance_criteria>
    - Per-tile splines are slices of ONE continuous curve — adjacent tiles share the exact boundary
      point (C0) and tangent (C1) with NO shared-seam-waypoint machinery (D-06 REVISED).
    - Slicing is deterministic and keyed by tile coords (ROAD-01 tile-able graph).
    - Spline parameterization matches the source network curve (`false, 'centripetal', 0.5`).
    - `invalidateCache`/re-stream clears `this._tiles` so re-routing re-slices.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rebuild ensureTile (returns {spline,waypoints}) + queryNearest over the sliced network (ROAD-04 / exit-gate contract)</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js the `ensureTile`/`queryNearest` stubs left by 08-05 (Task 1 there neutralized the old
      bodies); replace them with the network-backed implementations.
    - src/road.js `this._tiles` (Task 1) + `_streamNetwork`/`_sliceNetwork` (08-05 + Task 1).
    - src/road.js module-scope `_scratchPt` (allocation guard) — reuse for the per-sample distance probe.
    - test/test-road-seam.html lines 63–124 — ensureTile must return a tile with a single `.spline`;
      requires at least one E-W adjacent pair where BOTH tiles carry a spline (network warmed across
      the 3×3 grid must produce continuous coverage, not isolated fragments).
    - src/main.js `resolveSpawn` lines 126–153 — the consumer: ensureTiles a 3×3 region then
      `queryNearest(baseX, baseZ, 200)` and reads `nearest.point` + `nearest.tangent`
      (signature must stay `{point, tangent}|null`, tangent UNIT length).
  </read_first>
  <action>
    Rebuild `ensureTile(tileX, tileZ)` to: call `_streamNetwork` centered on the tile's world center
    (`(tileX+0.5)*CHUNK_SIZE, (tileZ+0.5)*CHUNK_SIZE`) then `_sliceNetwork()` so the network+slices
    around that tile exist, and RETURN a single tile object `{ spline, waypoints }` for `(tileX,tileZ)`:
    pick the tile's representative segment from `this._tiles` (the longest segment, or the one whose
    arc spans the tile most fully) and expose its `spline` and `points` (as `waypoints`); return an
    object with `spline: null` (no throw) when the tile carries no road. Idempotent — repeated calls
    with the same coords return the same cached tile object (memoize per `"tileX,tileZ"` so the seam
    harness's two passes over the grid see identical splines). Because the network is streamed over a
    radius that spans the whole 3×3 grid, the trunk runs continuously across adjacent tiles, so at least
    one E-W adjacent pair carries a spline on both sides (exit-gate `totalSeams >= 1`).

    Rebuild `queryNearest(wx, wz, radiusM = 200)` to search the sliced per-tile splines in `this._tiles`
    (restrict to the 3×3 tile block around the query tile for O(1)-ish cost; fall back to raw
    `this._network` polylines only if a tile lacks splines): sample each candidate spline at N
    arc-length intervals using the module-scope `_scratchPt` for the per-sample distance probe (no
    per-sample allocation), track the nearest within `radiusM` (XZ distance), and return
    `{ point: spline.getPointAt(bestU), tangent: spline.getTangentAt(bestU) }` (the only two
    allocations) or `null` if nothing is within radius. Tangent must be UNIT length
    (`getTangentAt` returns normalized). Keep `queryNearest` safe to call before any tile is warmed
    (returns null, no throw). No old-router symbol (`_tileCache`, `_getTile`) may be referenced.
  </action>
  <verify>
    <automated>cd /Users/ledogen/CodeShit/CarGame && grep -vn '^\s*\*\|^\s*//' src/road.js | grep -c '_tileCache\|_getTile\b' | grep -qx 0 && node --input-type=module -e "import('three').then(async THREE=>{const {RoadSystem}=await import('./src/road.js');const {parseWorldSeed}=await import('./src/seed.js');const p={coarseAmplitude:150,coarseFreq:0.0005,coarseOctaves:4,ridgeSharpness:1.6,maxRoadGrade:0.15,roadWDist:1,roadWAlt:0.85,roadWGrade:400,roadWOver:8000,roadWTurn:120,spurProbability:0.15};const r=new RoadSystem(parseWorldSeed('lone-pine'),p);r._proto.radius=400;let withSpline=0;for(let dx=-1;dx<=1;dx++)for(let dz=-1;dz<=1;dz++){const t=r.ensureTile(dx,dz);if(t&&t.spline)withSpline++;}if(withSpline<2){console.log('TILE_SPLINE_LOW '+withSpline);process.exit(1)}let hit=null;for(let i=0;i<400&&!hit;i++){hit=r.queryNearest((i%20)*32-320,((i/20)|0)*32-320,400);}if(!hit){console.log('NO_HIT');process.exit(1)}const L=Math.hypot(hit.tangent.x,hit.tangent.y,hit.tangent.z);console.log(Math.abs(L-1)<1e-3&&isFinite(hit.point.x)?'QN_OK':'QN_BAD')}).catch(e=>{console.error(e.message);process.exit(1)})" 2>&1 | grep -q QN_OK && echo PASS</automated>
  </verify>
  <done>
    `ensureTile` returns `{spline, waypoints}` (single representative spline per tile; `spline:null`
    when no road), is idempotent, and warms+slices the network so ≥2 tiles in the 3×3 grid carry a
    spline; `queryNearest` returns `{point, tangent}` with a unit-length tangent and finite point near
    the warmed network, and `null` (no throw) out of radius; no `_tileCache`/`_getTile` references remain.
  </done>
  <acceptance_criteria>
    - ROAD-04: road centerlines queryable as splines; `queryNearest(x,z) → {point, tangent}` (D-07-ready).
    - `ensureTile` returns a single `.spline` per tile (exit-gate contract for test-road-seam.html).
    - Allocation-light: per-sample scratch reused; only the two returned vectors allocated.
    - `ensureTile` idempotent; warmed 3×3 grid carries a spline on both sides of ≥1 E-W seam.
    - Query operates only over `this._tiles` / `this._network`; no `_tileCache`/`_getTile` reachable.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `this._tiles` exists and holds per-tile sliced splines; `_sliceNetwork` is its builder; `this._network`
  is its source.
- No `_seamPoint`/ghost-point and no `_tileCache`/`_getTile` in non-comment lines of src/road.js.
- Headless ESM: slicing produces ≥1 tile segment at origin; ≥2 tiles in the 3×3 grid carry a spline;
  `queryNearest` returns a unit-tangent hit near the warmed network and `null` out of radius.
- (Asserted green by the 08-04 seam harness in 08-07) C0 < 0.01 m and C1 < 5° across every checked seam.
</verification>

<success_criteria>
The continuous valley trunk is sliced into stable, deterministic per-tile Catmull-Rom splines with free
C0/C1 seam continuity (08-VERIFICATION truths 4, 5 closed), `ensureTile` returns the single-`.spline`
tile object the seam exit gate reads, and the network is queryable via `queryNearest(x,z) → {point,
tangent}`. This is the stable per-tile spline output Phase 9 consumes and the query that puts the truck
on the road (D-07), delivered without any shared-seam-waypoint machinery.
</success_criteria>

<output>
Create `.planning/phases/08-road-routing/08-06-SUMMARY.md` when done.
</output>
