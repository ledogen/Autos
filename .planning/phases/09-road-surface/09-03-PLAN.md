---
phase: 09-road-surface
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/road-mesh.js
  - src/road.js
  - src/main.js
  - data/ranger.js
  - test/test-road-mesh.html
  - test/test-road-carve.html
autonomous: true
requirements: [SURF-01, SURF-03]
must_haves:
  truths:
    - "A ~10 m fixed-width ribbon mesh is swept along the road splines and appears in the scene"
    - "The cross-section has a centerline crown and curvature-driven camber that banks into turns (verified on the surface geometry/normal, not body-roll)"
    - "Road mesh tiles stream in and dispose with their co-located terrain chunk (no leak, no per-frame rebuild)"
  artifacts:
    - path: "src/road-mesh.js"
      provides: "RoadMeshSystem: ribbon sweep with crown+camber, tile-keyed lifecycle, shared vertex-color material"
      contains: "sweepRibbon"
    - path: "src/main.js"
      provides: "RoadMeshSystem wired into the streaming loop + carve-table builder feeds gradeY with crown/camber tilt"
      contains: "RoadMeshSystem"
  key_links:
    - from: "src/road-mesh.js sweepRibbon"
      to: "src/road.js this._tiles spline + design grade"
      via: "spline.getPointAt/getTangentAt + designGradeY"
      pattern: "getPointAt|getTangentAt"
    - from: "src/road-mesh.js crown/camber"
      to: "carveTable gradeY (terrain carve)"
      via: "tilt folded into gradeY so analyticNormal feels it"
      pattern: "crownProfile|camber"
  # D-04 crown + curvature camber as real geometry/normal; camber-strength + crown-height sliders
---

<objective>
Sweep the ribbon mesh (SURF-01) along the Phase-8 splines using the smoothed design grade, with a centerline crown and curvature-driven camber (SURF-03 / D-04). The crown/camber must be REAL surface geometry whose normal physics feels — so the same crown/camber profile is folded into the carve `gradeY` table (Plan 02) and into the visual ribbon mesh, keeping them in agreement.

Purpose: SURF-01 (fixed-width ribbon) + SURF-03 (crown + curvature camber). Banking ~2-6° proportional to curve tightness; subtle water-shedding crown. Expose camber-strength + crown-height debug sliders (D-04).
Output: `road-mesh.js` (RoadMeshSystem), crown/camber folded into carve gradeY, main.js wiring, new ranger.js params, smoke + normal assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-road-surface/09-CONTEXT.md
@.planning/phases/09-road-surface/09-RESEARCH.md
@.planning/phases/09-road-surface/09-PATTERNS.md
@src/road.js
@src/road-carve.js
@src/terrain.js
@src/main.js
@data/ranger.js
@test/test-road-mesh.html
@test/test-road-carve.html

<interfaces>
<!-- Verified integration points. -->
- road.js: this._tiles.get("tileX,tileZ") → ARRAY of segments { spline, points, runKey, ... } (a tile may hold several slices). ensureTile(tileX,tileZ), queryNearest(wx,wz), setDebugVisible.
- CatmullRomCurve3: getPointAt(u), getTangentAt(u) (UNIT vector, arc-length-correct), getLength().
- road-carve.js: crownProfile(uLat, halfWidth, crownHeight). Design grade per tile from Plan 02.
- terrain.js: CHUNK_SIZE=64 (export), chunk lifecycle in _updateChunkRing (5×5 ring, RING_RADIUS=2), dispose pattern (scene.remove + geometry.dispose). MAX_BUILDS_PER_FRAME cap pattern. Shared-material pattern (do NOT dispose per-mesh).
- road-mesh patterns (from PATTERNS.md): BufferGeometry from spline, MeshPhongMaterial{vertexColors:true, side:FrontSide}, _scratchPt GC-free, "X,Z" key format co-located with terrain chunk lifetime.
- Ribbon construction (RESEARCH §Ribbon Mesh Sweep): N_LONG ≈ ceil(L/2)+2 samples; CROSS_SEGS=8; right = (tan.z,0,-tan.x).normalize(); per-vertex Y = designGradeY + crownProfile + uLat*sin(camberAngle); quad-strip → 2 tris/quad; geometry.computeVertexNormals().
- Curvature (RESEARCH §Crown and Camber): finite-diff on getTangentAt; signed via cross = T0.x*T1.z - T0.z*T1.x; camberAngle = clamp(camberStrength*signedKappa, -6°, +6°). Guard zero-length tangent (NaN). camberStrength default 200 m.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/road-mesh.js — ribbon sweep with crown + curvature camber, tile-keyed streaming lifecycle</name>
  <files>src/road-mesh.js</files>
  <read_first>
    - src/road-mesh.js (does not exist — create)
    - src/road.js (this._tiles/_sliceNetwork ~1025-1144, queryNearest ~363, _buildDebugLine ~1156, _scratchPt ~45, CatmullRomCurve3 ~1140)
    - src/terrain.js (chunk ring lifecycle, dispose ~630-635, shared material ~353, MAX_BUILDS_PER_FRAME ~673, CHUNK_SIZE export)
    - src/road-carve.js (crownProfile)
    - data/ranger.js (Phase 9 carve params from Plan 02)
    - .planning/phases/09-road-surface/09-RESEARCH.md (§Ribbon Mesh Sweep, §Crown and Camber, Pitfall — NaN curvature)
    - .planning/phases/09-road-surface/09-PATTERNS.md (§road-mesh.js — every excerpt)
  </read_first>
  <action>
    Create `src/road-mesh.js` exporting a `RoadMeshSystem` class. Imports: `import * as THREE from 'three'`, `import { CHUNK_SIZE } from './terrain.js'`, `import { crownProfile } from './road-carve.js'`. One shared material: `new THREE.MeshPhongMaterial({ vertexColors: true, side: THREE.FrontSide })` — do NOT dispose per-tile (matches terrain._material). Use a module-scope `_scratchPt`/`_scratchTan` to avoid per-sample Vector3 allocation.

    Add `sweepRibbon(spline, designGradeY[], params)` building a `THREE.BufferGeometry`: sample `N_LONG = ceil(spline.getLength()/2)+2` arc positions; at each `u` get `pos=getPointAt(u)`, `tan=getTangentAt(u)`, `right=(tan.z,0,-tan.x).normalize()`; compute signed curvature via finite-diff on getTangentAt (`cross = T0.x*T1.z - T0.z*T1.x`, sign × magnitude), guard zero-length tangent (skip/zero camber to avoid NaN — research pitfall). `camberAngle = clamp(params.camberStrength * signedKappa, -6*DEG, +6*DEG)`. Build `CROSS_SEGS=8` lateral vertices per section at `uLat = (j/CROSS_SEGS - 0.5)*params.roadWidth`; vertex `Y = designGradeY[i] + crownProfile(uLat, params.roadHalfWidth, params.crownHeight) + uLat*Math.sin(camberAngle)`; `X = pos.x + right.x*uLat`, `Z = pos.z + right.z*uLat`. Index as a quad strip (2 tris/quad, consistent CCW winding). Call `geometry.computeVertexNormals()`.

    Add tile lifecycle: `ensureRoadTile(tileX,tileZ)` builds road tile geometry for every segment in `road.ensureTile(tileX,tileZ)` / `this._tiles.get(key)` (a tile holds an ARRAY — iterate all slices), keyed `"X,Z"` (same format as terrain chunk). `disposeRoadTile(key)` removes from scene and calls `geometry.dispose()` (NOT material). Build frame-spread under a cap (e.g. 1 road tile/frame alongside terrain's MAX_BUILDS_PER_FRAME). The mesh is a standalone scene object, NOT a terrain chunk. Add a purity/determinism comment.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/road-mesh.js','utf8'); if(!/class RoadMeshSystem/.test(c)) throw new Error('missing RoadMeshSystem'); if(!/computeVertexNormals/.test(c)) throw new Error('missing normals'); console.log('OK')" && grep -c "sweepRibbon\|crownProfile\|camber" src/road-mesh.js</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `road-mesh.js` exports RoadMeshSystem with `sweepRibbon` producing a BufferGeometry with positions, normals (computeVertexNormals), and a quad-strip index.
    - SOURCE: per-section camber uses signed spline curvature clamped to ±6°; zero-length-tangent guard present (no NaN).
    - SOURCE: road tiles keyed "X,Z" co-located with terrain chunk lifetime; geometry disposed on evict, shared material never disposed per-tile.
    - BEHAVIOR: ribbon width = params.roadWidth (10 m); crown peak at centerline.
  </acceptance_criteria>
  <done>RoadMeshSystem sweeps a crowned, cambered ribbon along splines with streaming tile lifecycle.</done>
</task>

<task type="auto">
  <name>Task 2: Fold crown+camber into carve gradeY; wire RoadMeshSystem into main.js; add ranger.js + smoke/normal assertions</name>
  <files>src/road.js, src/main.js, data/ranger.js, test/test-road-mesh.html, test/test-road-carve.html</files>
  <read_first>
    - src/main.js (terrain + road system wiring, streaming loop, scene setup, reset/spawn)
    - src/road.js (carve-table builder from Plan 02 — gradeY computation per vertex)
    - src/road-carve.js (crownProfile, carveBlend)
    - src/road-mesh.js (Task 1)
    - data/ranger.js (Phase 9 params)
    - test/test-road-mesh.html, test/test-road-carve.html (scaffolds)
    - .planning/phases/09-road-surface/09-RESEARCH.md (§analyticNormal after carve — crown/camber arrive via gradeY; Open Q4)
    - .planning/phases/09-road-surface/09-PATTERNS.md (§debug.js callback contract, §road-mesh.js scene lifecycle)
  </read_first>
  <action>
    In `data/ranger.js`, add `crownHeight: 0.05` (m centerline crown, D-04/A12) and `camberStrength: 200` (m·rad/rad curvature→camber gain, D-04/A4) to the Phase 9 block.

    Fold crown + camber into the carve gradeY so physics feels the SAME surface the mesh shows (research §analyticNormal after carve): in the carve-table builder (Plan 02), the per-vertex `gradeY` for on-ribbon vertices must include `crownProfile(uLat, roadHalfWidth, crownHeight)` and the camber tilt `uLat*sin(camberAngle)` at that vertex's lateral offset — NOT just the flat design grade. This keeps `analyticNormal` (central diff over analyticHeight) returning the crowned/cambered normal, so the truck feels the bank without a special case. Confirm the mesh sweep (Task 1) and the carve gradeY use the same crownProfile + camber formula and the same params.

    Wire `RoadMeshSystem` into `main.js`: construct it with the scene + road system + params; in the streaming loop call `ensureRoadTile`/`disposeRoadTile` co-located with terrain chunk ring updates; on road re-stream clear road tiles (they rebuild from the now-stable network). Ensure free-cam and chase view both render the road mesh.

    Extend `test/test-road-mesh.html` smoke: construct RoadMeshSystem over a fixed-seed road, build one tile, assert the resulting geometry has >0 vertices and a valid (finite, length≈1) normal at a centerline vertex — label `SURF-01 ribbon mesh built (verts=__)`. Extend `test/test-road-carve.html` with a crown/camber normal assertion (SURF-03): at an on-road position on a curved section, assert `analyticNormal` tilts toward the inside of the curve (lateral normal component sign matches turn direction) and the centerline crown produces a small non-zero lateral gradient — label `SURF-03 camber direction (x,z): banks inside`.
  </action>
  <verify>
    <automated>grep -c "RoadMeshSystem" src/main.js && grep -c "crownHeight\|camberStrength" data/ranger.js && grep -c "SURF-01\|SURF-03" test/test-road-mesh.html test/test-road-carve.html</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: carve gradeY for on-ribbon vertices includes crownProfile + camber tilt (same formula/params as sweepRibbon) — crown/camber is real geometry felt by analyticNormal.
    - SOURCE: main.js constructs RoadMeshSystem and drives ensureRoadTile/disposeRoadTile in the streaming loop; road tiles cleared on re-stream.
    - SOURCE: ranger.js has crownHeight (0.05) and camberStrength (200) with decision tags.
    - HARNESS-OUTPUT: test-road-mesh.html prints `PASS SURF-01 ribbon mesh built`; test-road-carve.html prints `PASS SURF-03 camber direction` (banks toward inside of curve); height-agreement gate from Plan 02 still PASS (mesh Y now includes crown/camber and matches physics).
  </acceptance_criteria>
  <done>Crown/camber is real geometry in both mesh and physics; ribbon renders in-scene; smoke + camber-direction assertions PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug slider values → crown/camber params | Local numeric slider values, bounded in debug.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | sweepRibbon curvature | mitigate | Guard zero-length tangent in splineCurvature; clamp camberAngle to ±6° to prevent NaN/garbage normals (RESEARCH Threat Patterns). |
| T-09-05 | (none applicable) | RoadMeshSystem | accept | Browser-only static game; no server/network/auth/persistence. Mesh geometry from own math; no external input. No high-severity threat applies. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | NO packages installed (hand-rolled + Three.js r184); RESEARCH audit confirms zero. No legitimacy checkpoint required. |
</threat_model>

<verification>
- Open test/test-road-mesh.html: ribbon mesh smoke PASS.
- Open test/test-road-carve.html: camber-direction PASS + height-agreement still PASS.
- In-game (manual): drive a curved road; surface visibly tilts inside-low; truck feels crown + bank through suspension (verified on surface geometry, not body-roll).
</verification>

<success_criteria>
- SURF-01: fixed-width ribbon swept along splines, streaming with chunk lifecycle.
- SURF-03: crown + curvature camber as real geometry/normal, in mesh AND physics agreement.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-03-SUMMARY.md` when done.
</output>
