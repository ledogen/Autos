---
phase: 09-road-surface
plan: "15"
type: execute
wave: 3
depends_on: ["09-14"]
files_modified:
  - src/road-mesh.js
  - data/ranger.js
  - src/debug.js
autonomous: false
gap_closure: true
requirements: [SURF-05]
must_haves:
  truths:
    - "The ribbon edge skirt reads as dirt-brown, not asphalt-dark, so cuts and shoulder ramps look like dirt"
    - "The dirt color is a tunable param in data/ranger.js with a debug.js color picker"
    - "The change is color/material only — the skirt geometry from plan 09-10 is unchanged"
  artifacts:
    - path: "src/road-mesh.js"
      provides: "Dirt-brown skirt vertex colors"
      contains: "roadDirtColor"
    - path: "data/ranger.js"
      provides: "roadDirtColor param"
      contains: "roadDirtColor"
    - path: "src/debug.js"
      provides: "Dirt color picker slider"
      contains: "roadDirtColor"
  key_links:
    - from: "src/road-mesh.js sweepRibbon skirt verts"
      to: "params.roadDirtColor"
      via: "skirt vertex color = dirt RGB instead of asphalt RC/GC/BC"
      pattern: "roadDirtColor"
  # D-05 D-08 D-09
---

<objective>
Make dirt shoulders/cuts VISIBLE. Color the ribbon edge skirt (road-mesh.js sweepRibbon, the verts at local indices CROSS_SEGS+1 / CROSS_SEGS+2) a dirt-brown via a new `roadDirtColor` param so cuts and shoulder ramps read as dirt rather than asphalt-dark. This is a color/material change only — the skirt geometry already exists (plan 09-10) and the carve already works (plan 09-11, user-confirmed cuts work).

Purpose: the skirt apron and carved shoulder currently render asphalt-dark (RC,GC,BC = 0.15,0.15,0.17), so the engineered dirt foundation / cut faces look like more asphalt or like ambiguous terrain (D-05/D-08/D-09: dirt foundation on fill, engineered cutout on cut). A dirt-brown skirt color makes the dirt read as dirt.

Output: dirt-brown ribbon skirts with a tunable color, closing the final required fix; then the phase-level human verification of the full Phase 9 surface (continuity + camber + viz + dirt).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-road-surface/09-CONTEXT.md
@./CLAUDE.md

<interfaces>
From src/road-mesh.js sweepRibbon (lines ~296-314): the two skirt verts per section are colored asphalt base today:
- leftSkirtBase = (i*vertsPerSection + (CROSS_SEGS+1))*3 → colors[leftSkirtBase + 0/1/2] = RC,GC,BC
- rightSkirtBase = (i*vertsPerSection + (CROSS_SEGS+2))*3 → colors[rightSkirtBase + 0/1/2] = RC,GC,BC
- RC,GC,BC = 0.15,0.15,0.17 (asphalt dark) defined at line ~186.
- Material is MeshPhongMaterial with vertexColors:true (line ~95) — vertex colors drive surface color, no texture.

From data/ranger.js: params are plain values; colors elsewhere in the project are not yet stored as params (this is the first roadDirtColor). lil-gui supports `.addColor(params, 'key')` for a hex or {r,g,b} value.

From src/debug.js Road Surface folder (lines ~237-277): fireSurface() triggers the debounced road rebuild. Color is baked into vertex buffers at build time, so the picker must fire fireSurface (geometry/color rebuild), NOT a live-material update.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add roadDirtColor param + color the ribbon skirt verts dirt-brown</name>
  <files>data/ranger.js, src/road-mesh.js, src/debug.js</files>
  <read_first>
    - src/road-mesh.js skirt color block (lines ~296-314) + RC/GC/BC definition (line ~186)
    - data/ranger.js Plan-09 road block (lines ~332-365) — param + comment style
    - src/debug.js Road Surface folder (lines ~264-277) — slider/picker placement + fireSurface
  </read_first>
  <action>
    data/ranger.js: add `roadDirtColor: '#6b5836'` to RANGER_PARAMS in the Plan-09 road block (a muted earthy dirt-brown hex; comment: "hex — dirt-brown vertex color for ribbon edge skirts / cut & fill shoulder faces so dirt reads as dirt, not asphalt (D-05/D-08/D-09). Tunable via debug color picker; rebuilds road on change."). Use a string hex so lil-gui addColor handles it directly.

    src/road-mesh.js sweepRibbon: parse the dirt color ONCE before the section loop into linear-space RGB floats. Add near the RC/GC/BC definition: read `params.roadDirtColor` (default '#6b5836'); convert hex to r,g,b in [0,1] (parse the 6-hex digits, divide by 255). Store as `DR, DG, DB`. Use a small inline hex parse (no new import; THREE.Color is available but keep it simple — `parseInt(hex.slice(1,3),16)/255` etc., guarding a missing/short string by falling back to 0.42,0.345,0.21). In the skirt color writes (the four `colors[leftSkirtBase+...] = RC/GC/BC` and `colors[rightSkirtBase+...] = RC/GC/BC` assignments), replace RC/GC/BC with DR/DG/DB so the skirt apron renders dirt-brown. Do NOT change the top-surface asphalt color (RC/GC/BC stays for the drivable ribbon top + markings). Do NOT change skirt geometry / vertsPerSection / winding (plan 09-10 layout must stay — the 09-12 harness depends on vertsPerSection=13).

    src/debug.js: add `surfaceFolder.addColor(params, 'roadDirtColor').name('Dirt Color').onChange(fireSurface)` in the Road Surface folder (color baked at build → fireSurface full rebuild).

    Do NOT touch terrain-worker.js (skirts are main-thread ribbon geometry; worker stores raw heights only).
  </action>
  <verify>
    <automated>node --check src/road-mesh.js && node --check data/ranger.js && node --check src/debug.js && grep -c "roadDirtColor" src/road-mesh.js && grep -c "roadDirtColor" data/ranger.js && grep -c "roadDirtColor" src/debug.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "roadDirtColor" data/ranger.js` >= 1, `grep -c "roadDirtColor" src/debug.js` >= 1, `grep -c "roadDirtColor" src/road-mesh.js` >= 1
    - The skirt color writes use the dirt RGB (DR/DG/DB), not RC/GC/BC: `grep -c "leftSkirtBase\|rightSkirtBase" src/road-mesh.js` >= 4 and the skirt color lines reference DR/DG/DB (verify by reading the block; `grep -c "DR\|DG\|DB" src/road-mesh.js` >= 4 on the skirt assignments)
    - Top-surface asphalt RC/GC/BC unchanged: `grep -c "0.15, 0.15, 0.17" src/road-mesh.js` >= 1
    - vertsPerSection stride unchanged: `grep -c "vertsPerSection = (CROSS_SEGS + 1) + 2" src/road-mesh.js` == 1
    - `node --check` passes for all three files
    - `git diff --stat src/terrain-worker.js` empty
  </acceptance_criteria>
  <done>The ribbon edge skirts render dirt-brown via the tunable roadDirtColor param; the asphalt top surface and skirt geometry are unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: human verify dirt shoulders + full Phase 9 surface</name>
  <action>Human verification only — no code changes. Pause execution; the user runs the steps in how-to-verify below, then resume on the resume-signal.</action>
  <what-built>PHASE-9 SURFACE FINAL VERIFY: the road follows the continuous routed centerline (no seam steps, no load lag — plan 09-13), camber is rate-limited and matched visual==physics (plan 09-14), the cyan viz is truthful (plan 09-13), and the ribbon skirts now read as dirt-brown (this plan).</what-built>
  <how-to-verify>
    1. Start a local server (`python3 test/nocache-server.py` or `npx serve .`), open the sim.
    2. DIRT SHOULDERS: Fly the free cam over a road on rolling terrain and over a switchback/hillside cut. The edge skirt apron and the cut/fill shoulder faces should read as dirt-brown, clearly distinct from the dark-grey asphalt ribbon top and from the surrounding terrain. Cuts should look like engineered dirt/rock benches, not more asphalt.
    3. DIRT COLOR PICKER: Debug panel (backtick) → Roads → Road Surface → "Dirt Color". Change it (e.g. toward a redder or greyer brown); after the road rebuilds, the skirt color should update. Set it back to taste.
    4. FULL SURFACE REGRESSION RE-CHECK (carryover from 09-13/09-14): drive across tile seams (smooth, no step), watch load (no ~1 s stall), toggle the centerline viz (continuous cyan spline), drive a tightening curve + S-bend (smooth bank, no snap), confirm the truck rides on the asphalt.
    5. Optionally re-open `http://localhost:8000/test/test-road-height-agreement.html` and confirm the 3-clause RE-ARCH exit gate (terrain-below + ribbon-driven + continuity) still shows all PASS (the carve corner-target change in 09-13 must not break clause (a)).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 9 surface gaps, or describe remaining issues.</resume-signal>
</task>

</tasks>

<verification>
- `node --check` passes for road-mesh.js, ranger.js, debug.js.
- roadDirtColor wired param → skirt color → picker (grep gates).
- Skirt geometry (vertsPerSection=13) and asphalt top color unchanged.
- terrain-worker.js byte-untouched.
- Human confirms dirt shoulders read as dirt; full-surface regression re-check passes; 3-clause exit gate still PASS.
</verification>

<success_criteria>
- Ribbon edge skirts / cut & fill shoulder faces read as dirt-brown (human-verified), distinct from asphalt and terrain.
- roadDirtColor is a tunable param + color picker.
- The full Phase 9 surface (continuity, camber, viz, dirt) passes human verification and the re-arch exit gate.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-15-SUMMARY.md` when done.
</output>
