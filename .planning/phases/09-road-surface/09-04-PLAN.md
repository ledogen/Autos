---
phase: 09-road-surface
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - src/road.js
  - src/road-mesh.js
  - src/road-carve.js
  - data/ranger.js
  - test/test-road-mesh.html
autonomous: true
requirements: [SURF-07]
must_haves:
  truths:
    - "Where two roads cross, they mesh as a single merged at-grade paved footprint (not z-fighting overlapping ribbons)"
    - "The junction is reproducible and stable while driving — no pop/rebuild as you fly past (window-invariant, cached like this._tiles)"
    - "Both roads reconcile to one shared node elevation; each road's design grade blends to that elevation approaching the node"
  artifacts:
    - path: "src/road.js"
      provides: "_detectJunctions() over this._network using module-scope _segXZ; this._junctions cache; shared-node elevation"
      contains: "_detectJunctions"
    - path: "src/road-mesh.js"
      provides: "buildJunctionFootprint: fillet arcs + triangulation + leg trim, flat crown inside box"
      contains: "buildJunctionFootprint"
  key_links:
    - from: "src/road.js _detectJunctions"
      to: "_segXZ module scope (from Plan 01)"
      via: "pairwise XZ segment intersection over this._network runs"
      pattern: "_segXZ"
    - from: "src/road-mesh.js junction footprint"
      to: "carve gradeY (same signed carve)"
      via: "footprint embedded via the same cut-and-fill carve"
      pattern: "carve|gradeY"
  # D-12 junction-aware merged at-grade footprint; D-13 fillet arcs + footprint + leg trim; D-14 shared node elevation; D-15 primary algorithm target
---

<objective>
Build merged at-grade road junctions (SURF-07, the primary deliverable, D-12..D-15): detect inter-run crossings over `this._network`, build a closed footprint polygon from tangent fillet arcs between adjacent leg outer edges, triangulate it, trim each leg ribbon back to the footprint, flatten the crown inside the box, reconcile to one shared node elevation, and embed it with the same signed carve. Deterministic + window-stable (cached like `this._tiles`).

Purpose: SURF-07 — one shared paved footprint per crossing, no z-fighting overlapping ribbons, stable while driving. Built junction-aware (depends on the window-invariant network from Plan 01 and the ribbon/carve from Plans 02-03).
Output: `_detectJunctions` + `this._junctions` cache + shared-node elevation in road.js; `buildJunctionFootprint` (fillet + triangulation + trim + flat crown) in road-mesh.js; ear-clip fallback in road-carve.js; junction render assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-road-surface/09-CONTEXT.md
@.planning/phases/09-road-surface/09-RESEARCH.md
@.planning/phases/09-road-surface/09-PATTERNS.md
@.planning/todos/pending/feat-road-intersections.md
@src/road.js
@src/road-mesh.js
@src/road-carve.js
@data/ranger.js
@test/test-road-mesh.html

<interfaces>
<!-- Verified integration points. -->
- road.js: _segXZ now module-scope (Plan 01) — _segXZ(ax,az,bx,bz,cx,cz,dx,dz) returns {x,z} crossing or null. this._network = Map "mz:runIndex" to {points: Vector3[]}. _sliceNetwork/_slicedFrom memo identity guard. _streamNetwork cache-clear site (~914-918) clears this._tiles — add this._junctions.clear() there. Constructor network/tiles init ~588-606.
- road-mesh.js: RoadMeshSystem, sweepRibbon, shared material, tile lifecycle (Plan 03).
- road-carve.js: pure no-import functions; ear-clip belongs here (Worker-safe, no imports).
- Junction algorithm (RESEARCH §Junction Algorithm, the PRIMARY target):
  Step1 detect: pairwise _segXZ over runs; nodeKey = round(ix.x)+","+round(ix.z); posY = avg of 4 segment endpoint Ys. Guards: half-angle<10deg -> skip fillet, rectangular merge; legs.length>4 -> rectangular fallback.
  Step2 gather/sort legs by bearing atan2(dx,dz).
  Step3 fillet: P_i = node + halfWidth*perp(d_i); R_f = halfWidth*tan(theta/2); cap at 3*halfWidth for acute (<20deg) -> straight bevel; sample arc at ceil(R_f*pi/2)+2 pts (min 3).
  Step4 footprint polygon -> fan-from-centroid if convex (cross-products same sign), else earClip; shoelace winding check (reverse if area<0) — Pitfall 6.
  Step5 shared elevation: nodeY = avg(designGradeA,designGradeB); blend each road grade to nodeY over blendLength=30 m approaching node.
  Step6 leg trim: ribbon swept only up to footprint boundary (segment-polygon intersection via _segXZ).
  Step7 determinism: this._junctions cache, cleared on re-stream, rebuilt by _detectJunctions; pure fn of this._network (window-invariant after Plan 01).
- Inside the box: camberAngle=0, crownHeight=0 (flat paved, D-13).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _detectJunctions + this._junctions cache + shared-node elevation in road.js; ear-clip + convex-fan in road-carve.js</name>
  <files>src/road.js, src/road-carve.js, data/ranger.js</files>
  <read_first>
    - src/road.js (_segXZ module-scope from Plan 01, this._network keying ~978, _sliceNetwork/_slicedFrom memo ~1026, _streamNetwork clear site ~914-918, constructor network/tiles init ~588-606)
    - src/road-carve.js (pure no-import discipline — ear-clip + fan go here)
    - .planning/phases/09-road-surface/09-RESEARCH.md (§Junction Algorithm Steps 1, 2, 5, 7; ear-clip code; degenerate guards)
    - .planning/phases/09-road-surface/09-PATTERNS.md (§road.js — _detectJunctions memo pattern, junction cache clear)
    - .planning/todos/pending/feat-road-intersections.md (junction intent, X- and T-crossings)
  </read_first>
  <action>
    In `src/road.js` add `_detectJunctions()` returning a Map of nodeKey to a record { pos (Vector3), legs (array of {runKey, segIdx, dir}), nodeY, simpleMerge (bool) }. Iterate pairwise over the network runs (ri less than rj over [...this._network.values()]); for each segment pair call module-scope `_segXZ`; on a crossing compute nodeKey from rounded (ix.x, ix.z), posY as the average of the 4 segment-endpoint Ys, and record the legs leaving the node (each leg dir = unit vector from node toward the adjacent run point). Guards: crossing half-angle below 10 degrees marks simpleMerge=true (rectangular box, skip fillet); more than 4 legs marks simpleMerge=true (must not crash on 3+ roads meeting). Memoize with the `_slicedFrom`-style identity guard: early-return if `this._junctionsFrom === this._network` and the cache is non-empty; set `this._junctionsFrom = this._network` at the end. Initialize `this._junctions = new Map()` and `this._junctionsFrom = null` in the constructor near the this._tiles init. Add `if (this._junctions) this._junctions.clear()` at the _streamNetwork cache-clear site alongside this._tiles.clear(). Purity comment: "Pure function of this._network — deterministic + window-invariant by transitivity (D-16)."

    Shared-node elevation (D-14): compute nodeY = average of the two roads' design grades at the crossing. Provide a hook for the design-grade pass (Plan 02) to blend each road's grade toward nodeY over the last blendLength (default 30 m) before the node, using approach_Y(s) = lerp(designGradeY(s), nodeY, max(0, 1 - dist_to_node/blendLength)). Store nodeY on the junction record so both road-mesh and the carve builder read the same value. Add `roadJunctionBlendLength: 30` to ranger.js (m, D-14/A8).

    In `src/road-carve.js` add pure no-import `earClip(polygon)` and `triangulateConvexFan(polygon)` plus a convexity test (all cross-products same sign). ear-clip returns a flat triangle-index array, bounded at n*3 iterations (DoS guard — fall back to fan if exceeded). No imports (Worker-safe discipline); SYNC RULE comment if the body is also mirrored into WORKER_SOURCE.
  </action>
  <verify>
    <automated>grep -c "_detectJunctions" src/road.js; grep -c "earClip" src/road-carve.js; grep -c "roadJunctionBlendLength" data/ranger.js; node -e "const c=require('fs').readFileSync('src/road-carve.js','utf8').replace(/export function/g,'function'); if(/\bimport\b/.test(c)) throw new Error('imports'); console.log('no-import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: road.js has `_detectJunctions()` using module-scope `_segXZ` over this._network run pairs, with a `this._junctions`/`this._junctionsFrom` memo and a clear at the re-stream site.
    - SOURCE: degenerate guards present — half-angle below 10 degrees and more than 4 legs both fall back to simpleMerge (no crash).
    - SOURCE: shared-node elevation nodeY computed and a 30 m grade-blend hook provided; `roadJunctionBlendLength` in ranger.js.
    - SOURCE: road-carve.js has pure no-import earClip (bounded iterations) + convex-fan triangulation.
  </acceptance_criteria>
  <done>Junctions detected deterministically over the window-invariant network, cached and window-stable; ear-clip/fan available; shared-node elevation reconciled.</done>
</task>

<task type="auto">
  <name>Task 2: buildJunctionFootprint in road-mesh.js — fillet arcs, triangulation, leg trim, flat crown, same carve embed; junction render assertion</name>
  <files>src/road-mesh.js, data/ranger.js, test/test-road-mesh.html</files>
  <read_first>
    - src/road-mesh.js (RoadMeshSystem, sweepRibbon, shared material, tile lifecycle from Plan 03)
    - src/road.js (_detectJunctions from Task 1, this._junctions, nodeY)
    - src/road-carve.js (earClip, triangulateConvexFan, crownProfile, carveBlend)
    - data/ranger.js (roadFilletRadius — add here; roadHalfWidth, roadJunctionBlendLength)
    - test/test-road-mesh.html (smoke scaffold)
    - .planning/phases/09-road-surface/09-RESEARCH.md (§Junction Algorithm Steps 3, 4, 6; Pitfall 6 winding; Open Q3 tile assignment)
    - .planning/phases/09-road-surface/09-PATTERNS.md (§road-mesh.js, §debug.js fillet slider)
  </read_first>
  <action>
    Add `roadFilletRadius: 5` to ranger.js (m, default = roadHalfWidth, D-13/A5).

    In `src/road-mesh.js` add `buildJunctionFootprint(node, params)`: gather and sort the node's legs by bearing (atan2(dx,dz)); for each adjacent leg pair connect their OUTER edges (P_i = node + halfWidth*perp(d_i)) with a tangent fillet arc of radius R_f = halfWidth*tan(theta/2) where theta is the interior crossing half-angle, capped at 3*halfWidth (acute below 20 degrees -> straight bevel). Sample each arc at ceil(R_f*pi/2)+2 points (min 3). Concatenate the arc endpoints into a closed footprint polygon. Run a shoelace winding check and reverse if signed area < 0 (Pitfall 6). Triangulate: convexity test then triangulateConvexFan (95% case) else earClip (from road-carve.js). For simpleMerge nodes use a rectangular footprint. Build the footprint as a BufferGeometry on the shared road material; set vertex Y from nodeY (FLAT — camberAngle=0, crownHeight=0 inside the box, D-13). Embed the box via the SAME signed carve (the carve-table builder must raise/lower terrain under the footprint to nodeY, so analyticHeight agrees — "project downward"). Trim each leg ribbon back to the footprint boundary (segment-polygon intersection via _segXZ) so ribbons do not overlap the box (no z-fighting). Assign the junction mesh to the tile containing the node position; build it when that tile loads (Open Q3). Dispose with the tile.

    Extend `test/test-road-mesh.html`: construct a fixed-seed scenario containing a known crossing; call road._detectJunctions(); assert at least one junction node exists and buildJunctionFootprint returns a geometry with a closed, correctly-wound (signed area > 0 after the check) polygon and >0 triangles — label e.g. "SURF-07 junction footprint built (verts, tris)". Add a no-z-fight assertion: assert trimmed leg ribbon endpoints do not extend past the footprint boundary (no overlapping coplanar quads at the node). 
  </action>
  <verify>
    <automated>grep -c "buildJunctionFootprint" src/road-mesh.js; grep -c "roadFilletRadius" data/ranger.js; grep -c "SURF-07" test/test-road-mesh.html</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `buildJunctionFootprint` builds fillet arcs (R_f = halfWidth*tan(theta/2), 3*halfWidth cap, bevel below 20deg), a winding-corrected closed polygon, fan-or-earClip triangulation, and trims legs to the footprint.
    - SOURCE: footprint Y is flat at nodeY (crown/camber zeroed inside the box, D-13) and embedded via the same carve so analyticHeight agrees over the footprint.
    - SOURCE: junction mesh assigned to the node's tile and disposed with it; ranger.js has roadFilletRadius (5).
    - HARNESS-OUTPUT: test-road-mesh.html prints `PASS SURF-07 junction footprint built` (closed, positive signed area, >0 tris) and the no-z-fight (trimmed legs) assertion PASSes.
  </acceptance_criteria>
  <done>Crossings render as one merged flat at-grade paved footprint with trimmed legs, no z-fighting, embedded by the same carve; window-stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug slider values -> fillet radius / blend length | Local numeric slider values, bounded in debug.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Denial of Service | earClip on degenerate polygon | mitigate | Bound ear-clip at n*3 iterations; fall back to fan-from-centroid if exceeded (RESEARCH Threat Patterns). |
| T-09-07 | Denial of Service | acute / 3+ road junction | mitigate | half-angle below 10deg and legs > 4 fall back to a simple rectangular merge box — never crashes or hangs. |
| T-09-08 | (none applicable) | _detectJunctions | accept | Browser-only static game; junction geometry from own math over local seed; no server/network/auth/external input. No high-severity threat applies. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | NO packages installed (hand-rolled + Three.js r184); RESEARCH audit confirms zero. No legitimacy checkpoint required. |
</threat_model>

<verification>
- Open test/test-road-mesh.html: junction footprint built + no-z-fight assertions PASS.
- In-game (manual): find a road crossing; confirm one merged paved footprint (no overlapping z-fighting ribbons); fly past it at speed — no pop/rebuild (window-stable).
</verification>

<success_criteria>
- SURF-07: crossings mesh as a single merged at-grade paved footprint, no z-fighting, deterministic and stable while driving.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-04-SUMMARY.md` when done.
</output>
