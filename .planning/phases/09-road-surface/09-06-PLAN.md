---
phase: 09-road-surface
plan: 06
type: execute
wave: 6
depends_on: ["09-05"]
files_modified:
  - src/road-carve.js
  - src/road.js
  - src/road-mesh.js
  - data/ranger.js
  - test/test-road-carve.html
autonomous: true
requirements: [SURF-06]
must_haves:
  truths:
    - "Driving slowly on a low-quality road stretch, pothole/crack micro-perturbations are felt as slight vertical jolts through the suspension"
    - "Pothole severity is driven by the same per-stretch roadQuality value as the markings (one source) — high quality smooth, low quality frequent/deep"
    - "Pothole micro-noise perturbs ONLY the road surface (carve gradeY), and is applied identically in mesh build and physics sampler"
  artifacts:
    - path: "src/road-carve.js"
      provides: "Pure no-import potholeNoise(arcS, uLat, roadQuality, params) deterministic micro-perturbation"
      contains: "potholeNoise"
    - path: "data/ranger.js"
      provides: "Pothole params (amplitude, frequency) behind a default-off-able toggle"
      contains: "pothole"
  key_links:
    - from: "src/road-carve.js potholeNoise"
      to: "carve gradeY (mesh + physics)"
      via: "added to on-ribbon gradeY identically in both paths"
      pattern: "potholeNoise"
    - from: "src/road-mesh.js / road.js"
      to: "roadQuality hook (Plan 05)"
      via: "severity = f(1 - roadQuality)"
      pattern: "roadQuality"
  # D-03 SURF-06 severity from the same per-stretch roadQuality value; SURF-06 is a STRETCH goal (implement only if P9 lands under budget)
---

<objective>
STRETCH GOAL (SURF-06, D-03): add pothole/crack micro-noise that perturbs ONLY the road surface, with severity driven by the same per-stretch `roadQuality` hook as the markings. Felt as slight vertical jolts through the suspension at low speed. Applied identically in the mesh build and the physics sampler (folded into the carve gradeY) so the height-agreement gate still holds.

Purpose: SURF-06 — the labeled `roadQuality` hook already shipped (Plan 05); this plan implements the actual bumps. It is LOW-PRIORITY and OPTIONAL: only execute if Plans 01-05 landed under budget. If skipped, the phase still satisfies all non-stretch requirements; the roadQuality hook makes this trivially addable later.
Output: pure `potholeNoise` in road-carve.js, severity wiring from roadQuality, carve gradeY perturbation in both paths, pothole params, micro-noise continuity assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-road-surface/09-CONTEXT.md
@.planning/phases/09-road-surface/09-RESEARCH.md
@.planning/phases/09-road-surface/09-PATTERNS.md
@src/road-carve.js
@src/road.js
@src/road-mesh.js
@data/ranger.js
@test/test-road-carve.html

<interfaces>
<!-- Verified integration points. -->
- road-carve.js: pure no-import functions (sampleCarve, crownProfile, carveBlend, earClip from prior plans) — potholeNoise goes here, same no-import discipline, mirrored into WORKER_SOURCE + terrain-worker.js if the carve gradeY is built Worker-side; here the gradeY table is built main-thread (Plan 02) so the perturbation lives where gradeY is computed and in the mesh sweep — keep them identical.
- road-mesh.js: sweepRibbon per-vertex Y (gradeY + crown + camber); roadQuality hook (Plan 05) per arc position.
- road.js: carve-table builder computes per-vertex gradeY (Plan 02) — pothole perturbation added to on-ribbon gradeY here too.
- D-03: severity from the SAME roadQuality value; high = smooth, low = frequent/deep.
- Micro-noise must be deterministic (pure fn of arcS, uLat, roadQuality, params) and small enough to feel only at low speed (a few cm).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pure potholeNoise to road-carve.js; wire severity from roadQuality; perturb carve gradeY identically in mesh + physics; continuity assertion</name>
  <files>src/road-carve.js, src/road.js, src/road-mesh.js, data/ranger.js, test/test-road-carve.html</files>
  <read_first>
    - src/road-carve.js (pure no-import discipline; sampleCarve/crownProfile/carveBlend; SYNC RULE comment)
    - src/road.js (carve-table builder gradeY from Plan 02; design grade)
    - src/road-mesh.js (sweepRibbon per-vertex Y; roadQuality hook from Plan 05)
    - data/ranger.js (Phase 9 block, roadQuality params)
    - test/test-road-carve.html (height-agreement + carve-continuity gates)
    - .planning/phases/09-road-surface/09-RESEARCH.md (§Road Quality + Pothole Hook; D-03)
    - .planning/phases/09-road-surface/09-PATTERNS.md (§Determinism comment)
  </read_first>
  <action>
    Add a pure no-import `potholeNoise(arcS, uLat, roadQuality, params)` to `src/road-carve.js` returning a small signed Y perturbation (metres, a few cm scale). Deterministic: derive from a hashed lattice of (arcS, uLat) so the same world position always yields the same bump (no Math.random, no Date). Severity scales with `(1 - roadQuality)` (high quality -> near-zero; low quality -> frequent/deeper) — using the SAME roadQuality hook as the markings (D-03). Parameters from ranger.js: `potholeAmplitude` (m, default ~0.04), `potholeFrequency` (per m, default ~0.3), and a `potholeEnabled` flag (default true; allows zeroing the effect). Keep the no-import discipline; if the carve gradeY is ever computed Worker-side, mirror the body with a SYNC RULE comment.

    Apply the perturbation to the on-ribbon gradeY IDENTICALLY in both paths so height-agreement holds: (a) the carve-table builder in road.js adds `potholeNoise(...)` to the on-ribbon per-vertex gradeY, and (b) the road-mesh sweepRibbon adds the SAME `potholeNoise(...)` to the visual vertex Y. Both read the same roadQuality hook and the same params. Perturbation must be zero outside the ribbon (only the road surface, never the surrounding terrain). Keep it small enough that it does not break the carve-continuity gate (sub-step deltas stay continuous).

    Add ranger.js Phase 9 entries: potholeEnabled (bool, D-03/SURF-06), potholeAmplitude (m), potholeFrequency (per m), with decision-tagged comments. Extend `test/test-road-carve.html` with a SURF-06 assertion: on a LOW-quality on-road stretch assert potholeNoise produces a non-zero perturbation and that the perturbed gradeY in the carve table equals the perturbed mesh vertex Y at the same position (height-agreement preserved with potholes on) — label "SURF-06 pothole height-agreement + nonzero on low quality". Confirm carve-continuity still PASSes with potholes enabled.
  </action>
  <verify>
    <automated>grep -c "potholeNoise" src/road-carve.js; grep -c "pothole" data/ranger.js; grep -c "SURF-06" test/test-road-carve.html; node -e "const c=require('fs').readFileSync('src/road-carve.js','utf8').replace(/export function/g,'function'); if(/\bimport\b/.test(c)) throw new Error('imports'); console.log('no-import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `potholeNoise(arcS, uLat, roadQuality, params)` is pure (no imports, no Math.random/Date) and severity scales with (1 - roadQuality) using the same roadQuality hook as markings (D-03).
    - SOURCE: the SAME potholeNoise is added to on-ribbon gradeY in both the carve-table builder (physics) and sweepRibbon (mesh); zero outside the ribbon.
    - SOURCE: ranger.js has potholeEnabled/potholeAmplitude/potholeFrequency with decision tags.
    - HARNESS-OUTPUT: test-road-carve.html prints `PASS SURF-06 pothole height-agreement` (perturbed mesh Y == perturbed physics Y) and carve-continuity still PASS with potholes enabled.
  </acceptance_criteria>
  <done>Pothole micro-noise perturbs only the road surface, severity from roadQuality, identical in mesh + physics, height-agreement + continuity preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug/ranger params -> pothole amplitude/frequency | Local numeric values, bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | (none applicable) | potholeNoise | accept | Browser-only static game; perturbation from own deterministic math; no server/network/auth/external input. No high-severity threat applies. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | NO packages installed (hand-rolled + Three.js r184); RESEARCH audit confirms zero. No legitimacy checkpoint required. |
</threat_model>

<verification>
- Open test/test-road-carve.html: SURF-06 pothole height-agreement PASS; carve-continuity still PASS.
- In-game (manual): drive slowly on a low-quality stretch — feel slight vertical jolts; high-quality stretches feel smooth.
</verification>

<success_criteria>
- SURF-06 (stretch): pothole/crack micro-noise on the road surface only, severity from roadQuality, identical in mesh + physics. (Skip this plan entirely if Plans 01-05 consumed the phase budget — the roadQuality hook already shipped.)
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-06-SUMMARY.md` when done.
</output>
