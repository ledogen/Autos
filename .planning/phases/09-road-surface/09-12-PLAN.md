---
phase: 09-road-surface
plan: "12"
type: execute
wave: 3
depends_on: ["09-10", "09-11"]
gap_closure: true
files_modified:
  - test/test-road-height-agreement.html
  - test/road-test-harness.js
autonomous: false
requirements: [SURF-04, SURF-05, SURF-06, SURF-07]
must_haves:
  # The NEW exit gate replaces the 09-09 equality gate (terrainMeshY == ribbonY), which is now WRONG.
  truths:
    - "The exit-gate test asserts (a) under the road footprint, sampled terrain-mesh Y ≤ ribbon surface Y − clearanceMargin (terrain never pokes through)"
    - "The exit-gate test asserts (b) physics on-road sampled Y == ribbon surface Y (the truck drives the visible ribbon)"
    - "The exit-gate test asserts (c) the ribbon is longitudinally continuous — no vertical step > threshold between adjacent sections within a tile AND across the tile boundary"
    - "The retired 09-09 equality assertion (terrainMeshY == ribbonY at 1e-3) is removed/replaced — it is no longer the gate"
  artifacts:
    - path: "test/test-road-height-agreement.html"
      provides: "new 3-clause exit gate: terrain-below + ribbon-driven + longitudinal continuity"
      contains: "clearanceMargin"
    - path: "test/road-test-harness.js"
      provides: "terrain carve-table readback helper for the below-margin assertion"
      contains: "export function"
  key_links:
    - from: "test/test-road-height-agreement.html"
      to: "terrain-mesh Y (carve table) vs ribbon vertex Y"
      via: "assert terrainMeshY <= ribbonY - clearanceMargin"
      pattern: "clearanceMargin"
    - from: "test/test-road-height-agreement.html"
      to: "physics _sampleCarveWorld Y vs ribbon vertex Y"
      via: "assert |physicsY - ribbonY| <= tol on-road"
      pattern: "_sampleCarveWorld"
  # New exit gate replaces 09-09 equality — see truths.
---

<objective>
Replace the now-WRONG 09-09 exit gate (`terrainMeshY == ribbonY`) with the re-architecture's THREE-clause
exit gate. Under the decal-ribbon-on-top model, terrain is DELIBERATELY below the ribbon, so an equality
assertion is incorrect and must be retired. The new gate is:

  (a) **Terrain-below:** under the road footprint, sampled terrain-mesh Y ≤ ribbon surface Y − clearanceMargin
      (terrain never pokes through the ribbon).
  (b) **Ribbon-driven:** physics on-road sampled Y == ribbon surface Y (the truck drives the visible ribbon).
  (c) **Longitudinally continuous:** no vertical step > a small threshold between adjacent ribbon sections
      within a tile AND across the tile boundary (no new longitudinal seam jump).

Purpose: a correct, statically-runnable gate that matches the new architecture and guards against the
specific failure modes (pokethrough, truck-on-trough-floor, seam steps).
Output: reworked test/test-road-height-agreement.html (3 clauses) + a terrain carve-table readback helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-road-surface/09-09-SUMMARY.md
@.planning/phases/09-road-surface/09-VERIFICATION.md

<interfaces>
<!-- Extracted from the existing test + sources. Build against these. -->

Existing test (test/test-road-height-agreement.html — 09-09): builds RoadSystem + RoadMeshSystem over seed
'lone-pine' with syntheticRawHeight(wx,wz)=8*sin(wx*0.02)+6*cos(wz*0.015)+5, wires the SAME fn as
RoadMeshSystem terrainRef AND rs.setRawHeightSampler(...). Calls rms._buildRoadTile(tx,tz,key), reads
geo.attributes.position at centerline vertex. CASE 1/2 assert |ribbonVertexY - physicsY| <= 1e-3 (this is
the ribbon-driven clause — KEEP). CASE 3 non-flat guard (KEEP). The harness exports ribbonCenterlineVertex.

Helper (test/road-test-harness.js): export function ribbonCenterlineVertex(geo, sectionIdx, crossSegs=8)
returns {x,y,z} from geo.attributes.position at index sectionIdx*(crossSegs+1)+floor(crossSegs/2).
NOTE: after Plan 10 the per-section stride becomes vertsPerSection = (CROSS_SEGS+1)+2 (skirt verts).
ribbonCenterlineVertex MUST be updated to the new stride or take a stride argument.

Physics ribbon Y: rs._sampleCarveWorld(vx, vz, raw) -> { blendW, gradeY }; physicsY = raw + blendW*(gradeY-raw)
(reproduces terrain.js analyticHeight line 566). On-road centerline blendW≈1 so physicsY≈gradeY=ribbon Y.

Terrain-mesh Y: terrain.js _buildCarveTable returns Float32Array table (N*N*2): [blendW, gradeY_preamp].
terrainMeshY at a grid vertex = raw + blendW*(gradeY_preamp*amp - raw). After Plan 11 the carve target is
targetY - clearanceMargin, so terrainMeshY under the footprint should be ≤ ribbonY - clearanceMargin.
To read it in the test WITHOUT a full TerrainSystem+Worker, either (i) call a small exposed carve helper, or
(ii) reconstruct the below-margin target from rs.sampleDesignGradeAt(spline,arcS,rawSampler,p) - clearanceMargin
and assert ribbonY - that >= clearanceMargin - epsilon. Prefer (ii) (no Worker/DOM terrain build needed):
ribbonY (read from geometry) minus (sampleDesignGradeAt(...) - clearanceMargin) should be >= clearanceMargin
within tolerance, i.e. the carve bed sits clearanceMargin below the ribbon's smoothed base. Pick whichever
matches the actual Plan 11 implementation; clause (a) must assert "terrain target ≤ ribbon − clearanceMargin".

CROSS_SEGS = 8 (road-mesh.js:51); RANGER_PARAMS roadClearanceMargin default 0.5 (Plan 11), roadSkirtDepth 0.4 (Plan 10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rework exit-gate test to 3 clauses + update harness helper</name>
  <files>test/test-road-height-agreement.html, test/road-test-harness.js</files>
  <read_first>
    - test/test-road-height-agreement.html (full file — CASE 1/2/3 structure to rework)
    - test/road-test-harness.js (ribbonCenterlineVertex — stride update needed for Plan 10 skirts)
    - .planning/phases/09-road-surface/09-11-PLAN.md (the below-margin carve contract clause (a) tests)
    - .planning/phases/09-road-surface/09-10-PLAN.md (skirt stride vertsPerSection = (CROSS_SEGS+1)+2)
  </read_first>
  <action>
    Update test/road-test-harness.js `ribbonCenterlineVertex(geo, sectionIdx, crossSegs=8)` to accept the new
    per-section stride introduced by Plan 10's skirts. Add a `vertsPerSection` parameter (default
    `(crossSegs+1)+2`) and index as `sectionIdx*vertsPerSection + floor(crossSegs/2)`. Keep backward-compat by
    defaulting vertsPerSection so existing callers still work. Export it (already exported).

    Rework test/test-road-height-agreement.html into the three-clause exit gate. RETIRE the equality framing —
    rename the file's banner/title to "Phase 9 RE-ARCH EXIT GATE (terrain-below + ribbon-driven + longitudinal
    continuity)" and DELETE any assertion whose meaning is "terrainMeshY == ribbonY". Concretely:

    CLAUSE (b) RIBBON-DRIVEN (reuse existing CASE 1/2 machinery): keep the build of RoadSystem + RoadMeshSystem
    over 'lone-pine' with the synthetic non-flat terrain wired as BOTH terrainRef and rs.setRawHeightSampler.
    Keep asserting `|ribbonVertexY - physicsY| <= 1e-3` at 5 centerline sections, both potholeEnabled false and
    true. Update the ribbon vertex read to the new stride (pass vertsPerSection to ribbonCenterlineVertex).

    CLAUSE (a) TERRAIN-BELOW (new): at the SAME 5 on-footprint sample positions, compute the terrain carve
    target the way Plan 11 builds it (the smoothed design-grade base minus clearanceMargin — use
    `rs.sampleDesignGradeAt(spline, arcS, rawSampler, params) - params.roadClearanceMargin`, OR call the actual
    Plan-11 carve helper if one was exposed). Assert `ribbonVertexY - terrainTargetY >= params.roadClearanceMargin - 1e-3`
    at every sampled position — i.e. terrain sits at least clearanceMargin below the ribbon (terrain never
    pokes through). Read clearanceMargin from RANGER_PARAMS (or the test's params clone).

    CLAUSE (c) LONGITUDINAL CONTINUITY (new): read ribbon centerline Y at EVERY section across the tile (not
    just 5) and assert the absolute Y delta between adjacent sections is below a step threshold
    (e.g. STEP_THRESHOLD = 0.5 m — sized to allow steep-but-continuous grades while catching a degenerate
    seam jump). Additionally build the ADJACENT tile (tx+1 or tz+1, whichever the road crosses) and assert the
    Y delta between the last section of tile A and the first section of tile B is below the same threshold
    (no longitudinal seam step across the tile boundary). If the road does not cross the chosen neighbor, pick
    the neighbor the spline actually enters (query the segs of the candidate neighbor tile and skip the
    cross-boundary sub-assertion with a logged SKIP if none — but the within-tile continuity assertion always runs).

    KEEP CASE 3 (non-flat terrain variation guard) so a flat terrain can't make the gate pass trivially.

    Emit clear PASS/FAIL console lines per clause, a max-divergence summary per clause, and a final
    "RE-ARCH EXIT GATE: ALL CLAUSES PASS" line only if all three clauses pass. No production src/ files are
    modified. No fenced code blocks in the PLAN action; the executor writes the HTML/JS test itself.
  </action>
  <acceptance_criteria>
    - source: `grep -c "clearanceMargin\|roadClearanceMargin" test/test-road-height-agreement.html` returns >= 1 (clause a)
    - source: `grep -c "_sampleCarveWorld\|physicsY" test/test-road-height-agreement.html` returns >= 1 (clause b retained)
    - source: `grep -c "continu\|STEP_THRESHOLD\|step" test/test-road-height-agreement.html` returns >= 1 (clause c)
    - source: clause (c) cross-tile — `grep -c "tx + 1\|tz + 1\|tx+1\|tz+1\|neighbor\|adjacent tile\|boundary" test/test-road-height-agreement.html` returns >= 1
    - retirement: the test no longer asserts terrain==ribbon equality — `grep -c "terrainMeshY == ribbonY\|terrain.*==.*ribbon" test/test-road-height-agreement.html` returns 0
    - harness: `grep -c "vertsPerSection" test/road-test-harness.js` returns >= 1 (stride updated for skirts)
    - behavior: `node --check test/road-test-harness.js` PASS
    - worker-untouched: `grep -c "clearanceMargin\|roadSkirtDepth\|polygonOffset" src/terrain-worker.js` returns 0
    - no production change: `git diff --stat src/` shows NO src/ file modified by this plan
  </acceptance_criteria>
  <verify>node --check test/road-test-harness.js</verify>
  <done>Test asserts all three clauses (terrain-below ≤ ribbon−clearanceMargin, physics==ribbon on-road, longitudinal continuity within-tile and across the tile boundary); 09-09 equality assertion retired; ribbonCenterlineVertex stride updated for skirts; node --check passes; no src/ files touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The re-architecture exit gate (test/test-road-height-agreement.html) now asserts the three correct
    clauses for the decal-ribbon-on-top model: (a) terrain stays ≤ ribbon − clearanceMargin (no pokethrough),
    (b) physics on-road == ribbon Y (truck drives the ribbon), (c) ribbon is longitudinally continuous within
    a tile and across the tile boundary. The old equality assertion is retired. The test requires a browser
    (Three.js + importmap) to run — node --check confirms syntax only.
  </what-built>
  <how-to-verify>
    1. `python3 test/nocache-server.py` (defaults to :8000) or `npx serve .` from the repo root.
    2. Open `http://localhost:8000/test/test-road-height-agreement.html` and open DevTools console (F12).
    3. Confirm the banner reads as the RE-ARCH exit gate (terrain-below + ribbon-driven + longitudinal continuity).
    4. Confirm PASS lines for all three clauses:
       - CLAUSE (a) terrain-below: `ribbonY - terrainTargetY >= clearanceMargin` at every sampled section.
       - CLAUSE (b) ribbon-driven: `|ribbonY - physicsY| <= 1e-3` at every section, potholeEnabled false AND true.
       - CLAUSE (c) continuity: adjacent-section Y delta < threshold within the tile AND across the tile boundary.
       - CASE 3 non-flat guard still passes.
    5. Confirm the final line "RE-ARCH EXIT GATE: ALL CLAUSES PASS" prints and NO `FAIL:` line appears.
  </how-to-verify>
  <resume-signal>Type "approved" if all three clauses + the non-flat guard PASS and no FAIL line appears, or paste the failing clause output.</resume-signal>
</task>

</tasks>

<verification>
- node --check passes on test/road-test-harness.js.
- Test asserts clause (a) terrain ≤ ribbon − clearanceMargin, clause (b) physics==ribbon on-road, clause (c)
  longitudinal continuity within-tile and across the tile boundary.
- 09-09 equality assertion (terrainMeshY == ribbonY) retired.
- ribbonCenterlineVertex stride updated for Plan 10 skirts.
- terrain-worker.js untouched; no src/ production files modified.
- Human: all three clauses + non-flat guard pass in the browser.
</verification>

<success_criteria>
- The exit gate matches the re-architecture: terrain-below, ribbon-driven, longitudinally continuous.
- The wrong 09-09 equality assertion is removed.
- Test runs statically clean (node --check) and is browser-runnable.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-12-SUMMARY.md` when done.
</output>
