---
phase: 09-road-surface
plan: 20
type: execute
wave: 3
depends_on: ["09-19"]
files_modified:
  - src/road.js
  - src/road-mesh.js
  - data/ranger.js
  - src/debug.js
autonomous: true
requirements: [SURF-01, SURF-04]
must_haves:
  # D5 ring hysteresis (kills edge thrash #2) + D4 stateless queryNearest arm-disambiguation (kills invisible-ramp launch #3)
  truths:
    - "D5/SURF-01: crossing a tile edge does not load/unload the ribbon — a keep-radius larger than the build-radius holds tiles for ~1-tile margin (fixes bug #2 thrash)"
    - "D4/SURF-04: queryNearest prefers the spline on whose footprint the query lies (smallest lateral / interior projection), so switchback arms never flip → no invisible-ramp launch (fixes bug #3)"
    - "D4: physics stays a pure 2D height field — queryNearest signature is unchanged (stateless arm-disambiguation, no analyticHeight/_sampleCarveWorld signature change)"
  artifacts:
    - path: "src/road-mesh.js"
      provides: "keep-radius hysteresis in tile dispose (keep > build radius)"
      contains: "keep"
    - path: "src/road.js"
      provides: "queryNearest lateral/interior-projection arm preference; collectChunkSplinePoints nearest-selection consistent with it"
      contains: "queryNearest"
    - path: "data/ranger.js"
      provides: "roadTileKeepMargin param (hysteresis)"
      contains: "roadTileKeepMargin"
  key_links:
    - from: "src/road-mesh.js syncToChunkRing dispose pass"
      to: "keep-radius = build-radius + roadTileKeepMargin"
      via: "hysteresis guard before disposeRoadTile"
      pattern: "roadTileKeepMargin|keepRadius"
    - from: "src/road.js queryNearest"
      to: "lateral-distance / interior-projection arm preference"
      via: "tie-break by footprint membership not global-nearest discrete sample"
      pattern: "signedLat|lateral|interior"
---

<objective>
Two tightly-coupled lifecycle fixes the design groups together:
- D5 ring hysteresis: ribbon tiles use a keep-radius larger than the build-radius so they are NOT disposed the instant a tile leaves the terrain active ring — keep a ~1-tile margin. Kills bug #2 (tile-edge crossing loads/unloads the ribbon — thrash).
- D4 stateless queryNearest arm-disambiguation: switchback arms are always laterally separated (never vertically stacked — user-confirmed), so physics stays a pure 2D height field. Make queryNearest prefer the spline on whose footprint the query lies (smallest LATERAL distance / interior projection), not the globally-nearest discrete sample. Kills bug #3 (queryNearest flips to the wrong arm → invisible-ramp launch → car drops to terrain). Keep `collectChunkSplinePoints` nearest-selection consistent with queryNearest so carve and physics agree.

Purpose: De-risked by D0 (arms now separate by > road width), these become robust safety nets. Both keep physics signatures unchanged.
Output: hysteresis keep-radius in road-mesh.js + roadTileKeepMargin param/slider; lateral-projection arm preference in queryNearest + matching logic in collectChunkSplinePoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-road-surface/09-REFACTOR-DESIGN.md
@.planning/phases/09-road-surface/09-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase — executor uses these directly. -->

src/road-mesh.js syncToChunkRing(activeKeys) (lines 434-456):
  - enqueues missing tiles via ensureRoadTile(cx,cz)
  - disposes tiles NOT in activeKeys via disposeRoadTile(key)   ← D5 hysteresis applies here
  activeKeys comes from terrainSystem.getActiveChunkKeys() (terrain RING_RADIUS=2 → 5×5 ring).

src/road.js queryNearest(wx, wz, radiusM=200) (lines 424-556):
  - probeSpline finds nearest DISCRETE sample (bestD2 = globally nearest squared XZ distance, line 447)
    across ALL splines in the radius block. THIS is the global-nearest selection D4 must refine:
    when two arms are both in range, pick the arm whose FOOTPRINT (lateral/interior projection) the
    query lies on, not whichever discrete sample is marginally closer.
  - already has the 09-17 projection refine (lines 470-522) that returns { point, tangent, runKey, arcS, spline }.
  - SIGNATURE MUST NOT CHANGE (D4: stateless, pure 2D height field). _sampleCarveWorld (road.js:1449)
    and analyticHeight call it unchanged.

src/road.js collectChunkSplinePoints(centerX, centerZ, radiusM) (lines 559-617):
  - the SINGLE getPointAt site for _buildCarveTable; samples nearby splines into a flat [x,y,z,...] array.
  - _buildCarveTable then does a plain squared-distance nearest search over those samples (terrain.js:876-881).
  - D4 coupling: the carve's nearest-arm selection should be consistent with queryNearest's footprint
    preference so the carved trough and the physics height pick the SAME arm at switchbacks.

signedLat formula (road.js:1461): signedLat = dx*tz - dz*tx (lateral distance from centerline, sign = side).
this._params.roadHalfWidth (5), roadShoulderWidth (2.5) — footprint half-width = halfWidth + shoulderWidth.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D5 ring hysteresis — keep-radius larger than build-radius</name>
  <read_first>
    - src/road-mesh.js lines 434-456 (syncToChunkRing dispose pass)
    - src/road-mesh.js lines 400-411 (disposeRoadTile)
    - src/terrain.js lines 442-443, 737-748 (getActiveChunkKeys + RING_RADIUS=2)
    - data/ranger.js lines 332-365 (Plan 09-10/11 road surface param block — add the new param here)
    - .planning/phases/09-road-surface/09-REFACTOR-DESIGN.md D5 (lines 54-56)
  </read_first>
  <action>
    Add a `roadTileKeepMargin` param to data/ranger.js (default ~1 tile, i.e. 1, units = tiles) with a comment referencing D5 (hysteresis: keep tiles within build-ring + this margin so a tile-edge crossing does not thrash the ribbon). In src/road-mesh.js `syncToChunkRing`, change the dispose pass so a built tile is disposed ONLY when it is outside the KEEP set, where the keep set = the active build ring expanded by `roadTileKeepMargin` tiles. Compute the keep set from the active ring's bounding tile coordinates (parse the "X,Z" keys, find min/max tile X/Z, expand by the margin) OR from the current stream center + (RING_RADIUS + margin); a tile within the expanded ring is KEPT even if not in activeKeys. Enqueue (build) still uses the original activeKeys (build-radius). This yields keep-radius > build-radius. Add `roadTileKeepMargin` to the src/debug.js Roads folder (or Road Surface sub-folder) as a slider (range 0–3 tiles, step 1) firing onRoadSurfaceChange. Do NOT change MAX_ROAD_BUILDS_PER_FRAME.
  </action>
  <verify>
    <automated>node --check src/road-mesh.js && node --check data/ranger.js && node --check src/debug.js && grep -c 'roadTileKeepMargin' src/road-mesh.js data/ranger.js | grep -qv ':0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` exits 0 on road-mesh.js, ranger.js, debug.js.
    - `roadTileKeepMargin` exists in ranger.js with a D5 comment and is read in syncToChunkRing.
    - The dispose pass keeps tiles within (build-ring + roadTileKeepMargin); the build/enqueue pass still uses the unexpanded activeKeys (keep-radius > build-radius).
    - A "keep margin" slider exists in the debug Roads folder.
  </acceptance_criteria>
  <done>Ribbon tiles persist for a ~1-tile margin beyond the build ring — no load/unload thrash at tile edges.</done>
</task>

<task type="auto">
  <name>Task 2: D4 stateless arm-disambiguation in queryNearest (lateral/interior projection)</name>
  <read_first>
    - src/road.js lines 424-467 (queryNearest probeSpline global-nearest selection)
    - src/road.js lines 469-522 (existing 09-17 projection refine + return shape)
    - src/road.js lines 1452-1464 (signedLat / latDist formula in _sampleCarveWorld — reuse the same lateral math)
    - .planning/phases/09-road-surface/09-REFACTOR-DESIGN.md D4 (lines 50-52)
  </read_first>
  <action>
    Refine queryNearest's arm selection (road.js probeSpline / the bestD2 comparison) so when MULTIPLE splines fall within the radius, it prefers the spline on whose FOOTPRINT the query lies — i.e. smallest LATERAL distance (perpendicular to that spline's tangent) / interior projection — rather than whichever discrete sample has marginally smaller raw XZ distance. Concretely: track, per candidate spline, the projected point's signed lateral distance (using the same `signedLat = dx*tz - dz*tx` math as _sampleCarveWorld) and prefer a candidate whose |lateral| is within its footprint half-width (roadHalfWidth + roadShoulderWidth) over a candidate that is merely closer at a discrete sample but lies OUTSIDE its footprint. Implement as a tie-break layered on the existing probe: keep the existing nearest-discrete bracket+refine, but bias selection toward the spline the query is interior to. CRITICAL: do NOT change the queryNearest signature or its return object shape `{ point, tangent, runKey, arcS, spline }` — physics (_sampleCarveWorld, analyticHeight) stays a pure 2D height field (D4). Keep it allocation-disciplined (reuse _scratchPt; no per-call Vector3 churn in the hot path beyond the two returned vectors). Add a comment tagging this as D4 stateless arm-disambiguation.
  </action>
  <verify>
    <automated>node --check src/road.js && grep -c 'D4\|arm-disambiguation\|interior\|lateral' src/road.js | grep -qv '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/road.js` exits 0.
    - queryNearest's signature and return shape `{ point, tangent, runKey, arcS, spline }` are UNCHANGED (grep the return statement — same keys).
    - The arm selection biases toward the footprint the query is interior to (grep shows lateral/interior projection logic with a D4 tag).
    - No new Vector3 allocations added to the per-call hot path beyond the existing two returned vectors.
    - `git diff --stat src/terrain-worker.js` empty.
  </acceptance_criteria>
  <done>queryNearest prefers the spline whose footprint the query lies on — switchback arms no longer flip, signature unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Make collectChunkSplinePoints nearest-selection consistent with queryNearest</name>
  <read_first>
    - src/road.js lines 559-617 (collectChunkSplinePoints — flat-array sampler for the carve hot path)
    - src/terrain.js lines 860-911 (_buildCarveTable inner loop — plain squared-distance nearest over the samples)
    - src/terrain-worker.js (MUST stay byte-identical — carve is main-thread post-read only)
    - .planning/phases/09-road-surface/09-REFACTOR-DESIGN.md cross-system couplings (lines 61-65, D4 bullet)
  </read_first>
  <action>
    Ensure the carve path picks the SAME arm as queryNearest at switchbacks (design coupling: "D4's nearest-arm logic should be consistent between physics queryNearest and the carve's collectChunkSplinePoints nearest-point selection"). The carve inner loop (terrain.js:876-881) currently does a plain global squared-distance nearest over the flat sample array — which can pick the wrong arm exactly like the old queryNearest did. Carry enough per-sample data in collectChunkSplinePoints (e.g. include the spline tangent components or a runKey/arm tag in the flat array, OR widen the stride) so the carve's nearest search can apply the same footprint/lateral preference. Update _buildCarveTable's inner nearest search to use that footprint-aware preference (prefer the sample whose lateral projection is interior to its footprint), matching queryNearest's D4 rule, so the carved trough and the physics height select the same arm → no step where the carve flips from arm-A to arm-B. CRITICAL Worker CARVE SYNC: do NOT add any symbol to src/terrain-worker.js; collectChunkSplinePoints and _buildCarveTable are main-thread only and the carve never enters the worker — terrain-worker.js stays byte-identical. Keep the inner loop allocation-free (no closures, no per-vertex objects — preserve the Plan 09-16 PERF CONTRACT comment at terrain.js:860-865).
  </action>
  <verify>
    <automated>node --check src/road.js && node --check src/terrain.js && git diff --stat src/terrain-worker.js | grep -q . && echo "WORKER CHANGED — FAIL" || echo "worker untouched OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/road.js` and `node --check src/terrain.js` exit 0.
    - collectChunkSplinePoints carries the data needed for footprint-aware nearest selection (stride/tag widened or tangent included).
    - _buildCarveTable's inner nearest search applies the same footprint/lateral preference as queryNearest (consistent arm at switchbacks).
    - The Plan 09-16 PERF CONTRACT (zero getPointAt / zero queryNearest / zero closures in the inner loop) is preserved.
    - `git diff --stat src/terrain-worker.js` EMPTY (byte-identical mirror).
  </acceptance_criteria>
  <done>The carve nearest-arm selection matches queryNearest — carve and physics agree on the arm at switchbacks, worker untouched.</done>
</task>

</tasks>

<verification>
- `node --check` clean on all four files.
- queryNearest signature/return shape unchanged (pure 2D height field preserved).
- Keep-radius > build-radius (hysteresis) in road-mesh.js.
- Carve and physics select the same arm; `git diff --stat src/terrain-worker.js` EMPTY.
</verification>

<success_criteria>
- D5/SURF-01: no ribbon thrash at tile edges (keep-radius hysteresis).
- D4/SURF-04: no invisible-ramp launch — queryNearest + carve both prefer the interior-footprint arm; physics signature unchanged.
- Worker byte-identical.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-20-SUMMARY.md` when done.
</output>
