---
phase: 09-road-surface
plan: 23
type: execute
wave: 6
depends_on: ["09-20", "09-21", "09-22"]
files_modified:
  - test/spline-continuity.mjs
autonomous: true
requirements: [SURF-03, SURF-04, SURF-05]
must_haves:
  # Harness fixtures — the headless gates for D4/D3/D2
  truths:
    - "SURF-04/D4: a self-approaching/switchback harness fixture proves queryNearest does not flip arms (no arm-flip → no invisible-ramp launch)"
    - "SURF-05/D3: a two-close-arms-at-different-heights fixture proves the carve does not undermine/step (lower arm's cut never removes the upper arm's support)"
    - "SURF-03/D2: a camber-rate fixture proves the slew-limited camber stays within MAX_DCAMBER_DEG_PER_M with no clamp-flip spike"
    - "All role:'gate' fixtures exit 0 — these are the headless gates the phase must satisfy"
  artifacts:
    - path: "test/spline-continuity.mjs"
      provides: "switchback no-arm-flip (D4), two-arms no-undermine (D3), camber-rate (D2) gate fixtures"
      contains: "switchback"
  key_links:
    - from: "test/spline-continuity.mjs FIXTURES"
      to: "process exit code"
      via: "role:'gate' fixtures all PASS → exit 0"
      pattern: "role:\\s*'gate'"
---

<objective>
Extend the headless harness `test/spline-continuity.mjs` with the gate fixtures the refactor design names as the verification gates: (a) a self-approaching/switchback fixture proving D4 (queryNearest no arm-flip), (b) a two-close-arms-at-different-heights fixture proving D3 (no undermining / no step), (c) a camber-rate fixture proving D2 (slew-limited camber within threshold). All gate fixtures must exit 0. These are the headless gates that close the phase (the combined in-sim human pass is the final acceptance, handled by the orchestrator).

Purpose: Lock the D2/D3/D4 fixes behind automated gates so regressions are caught headlessly (no browser WebGL needed).
Output: three role:'gate' fixtures added; `node test/spline-continuity.mjs` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-road-surface/09-REFACTOR-DESIGN.md
@.planning/phases/09-road-surface/09-PATTERNS.md

<interfaces>
<!-- Extracted from test/spline-continuity.mjs — executor uses these directly. -->

Harness structure (test/spline-continuity.mjs):
  - imports ONLY signedCurvature / crownProfile from ../src/road-carve.js (zero-install ESM) — line 19.
  - FIXTURES array (lines 235-330): each fixture { name, role, description, points|sliceA/B, [physicsMode] }.
  - role:'gate' affects exit code; demo-expected-fail fixtures are printed but never change exit (lines 12-13).
  - computeMetrics(fixture) (lines 388-470): builds a Catmull-Rom spline from points, measures
    maxVStep / maxDKappa / maxDCamber / boundaryMismatch.
  - computePhysicsMetrics(fixture) (lines 346-387): physicsMode fixtures — marches a query point along
    the spline, runs nearest vs refine, returns { maxDY_nearest, maxDY_refine }. The 09-17
    physics-sampling-continuity fixture already uses this (nearest must FAIL, refine must PASS).
  - Thresholds: MAX_VSTEP_M, MAX_DKAPPA, MAX_DCAMBER_DEG_PER_M=2.0 (line 26), MAX_BOUNDARY_MISMATCH_M,
    MAX_PHYSICS_DY_M=0.05 (line 36).
  - Existing gate fixtures: gentle-baseline, tile-seam-mismatch, physics-sampling-continuity, and
    (after plan 09-18) hairpin. Existing demo-expected-fail: tight-turn, steep-grade.

Pattern for a new gate: add to FIXTURES with role:'gate', a points array (or physicsMode marker),
  and a measurable assertion that PASSES under the fixed D2/D3/D4 behavior and would FAIL without it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D4 switchback no-arm-flip gate fixture</name>
  <read_first>
    - test/spline-continuity.mjs lines 307-387 (physics-mode fixture + computePhysicsMetrics — the analog for an arm-flip probe)
    - test/spline-continuity.mjs lines 235-330 (FIXTURES array)
    - .planning/phases/09-road-surface/09-REFACTOR-DESIGN.md verification (line 68, "self-approaching / switchback fixture (D4 — no arm-flip)")
  </read_first>
  <action>
    Add a `role:'gate'` fixture named `switchback-no-arm-flip` modeling a self-approaching switchback: two laterally-separated arms that come close in XZ but stay separated (mirrors D0's filleted hairpin). Vendor a small nearest-arm selector in the harness that mirrors queryNearest's D4 footprint/interior-projection preference (smallest lateral distance to the arm whose footprint the query lies on), and a brute global-nearest selector (the old behavior). March a query point along one arm with a small lateral offset and assert: the footprint-preference selector NEVER switches to the other arm (arm-flip count == 0), while the brute global-nearest selector WOULD flip (demonstrating the gate catches the bug). Gate threshold: arm-flip count == 0 for the footprint selector. Print both counts. Keep it zero-install (import only from ../src/road-carve.js). All existing gate fixtures still exit 0.
  </action>
  <verify>
    <automated>node --check test/spline-continuity.mjs && node test/spline-continuity.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node test/spline-continuity.mjs` exits 0 with `switchback-no-arm-flip` PASS in the gate table.
    - The fixture measures arm-flip count == 0 for the footprint-preference selector and prints the brute selector's (non-zero) flip count for contrast.
    - All pre-existing gate fixtures still PASS.
  </acceptance_criteria>
  <done>A switchback fixture proves queryNearest's footprint preference does not flip arms; exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: D3 two-close-arms-at-different-heights no-undermine gate fixture</name>
  <read_first>
    - test/spline-continuity.mjs lines 388-470 (computeMetrics — spline build + Y-step measurement)
    - test/spline-continuity.mjs lines 235-330 (FIXTURES)
    - .planning/phases/09-road-surface/09-REFACTOR-DESIGN.md verification (line 68, "two-close-arms-at-different-heights fixture (D3 — no undermining / no step)")
  </read_first>
  <action>
    Add a `role:'gate'` fixture named `two-arms-no-undermine` modeling two switchback arms running close together at DIFFERENT heights (e.g. upper arm at Y+h, lower arm at Y, separated laterally by ≈ 2·minRadius). Vendor a carve-floor evaluator in the harness that mirrors the D3 max-floor guard: over a lateral cross-section spanning both arms, compute each arm's carve target (arm Y − clearanceMargin) and take the MAX over covering arms at each lateral position. Assert: the resulting carve floor under the UPPER arm never drops below (upperArmY − clearanceMargin) — i.e. the lower arm's cut does not undermine the upper arm's support (no step where the carve flips below the upper arm's floor). Also assert the footprint bound (each arm's footprint ≤ ½ separation) holds for the fixture geometry. Gate threshold: undermine depth == 0 (carve floor under the upper arm ≥ upperArmY − clearanceMargin − ε). Print the measured min floor under each arm. Zero-install.
  </action>
  <verify>
    <automated>node --check test/spline-continuity.mjs && node test/spline-continuity.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node test/spline-continuity.mjs` exits 0 with `two-arms-no-undermine` PASS in the gate table.
    - The fixture proves the carve floor under the upper arm is never below (upperArmY − clearanceMargin) — max-floor guard holds; undermine depth == 0.
    - All pre-existing gate fixtures still PASS.
  </acceptance_criteria>
  <done>A two-close-arms fixture proves the carve max-floor guard prevents undermining/step; exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: D2 camber-rate gate fixture (slew limit, no clamp-flip spike)</name>
  <read_first>
    - test/spline-continuity.mjs lines 26, 40-41, 444-470 (MAX_DCAMBER_DEG_PER_M + camber metric computation)
    - test/spline-continuity.mjs lines 260-283 (existing tight-turn demo-fail fixture — the unmitigated camber spike)
    - .planning/phases/09-road-surface/09-REFACTOR-DESIGN.md verification (line 68, "re-use the existing camber-rate fixture (D2)")
  </read_first>
  <action>
    Add (or promote to role:'gate') a `camber-rate` fixture that drives camber through a curvature sign-change (an S-curve / alternating corners) where the UNLIMITED instantaneous camber would clamp-flip and spike the camber rate. Vendor the D2 slew-rate limiter in the harness (forward-march limit |dCamber/ds| ≤ roadCamberRate, then ±6° clamp) matching the production camberProfile from plan 09-21, and apply it to the sampled curvature before measuring maxDCamber. Assert: the slew-limited maxDCamber ≤ MAX_DCAMBER_DEG_PER_M (2.0 °/m) AND there is no spike at the curvature zero-crossing (the per-step camber-rate stays bounded through the sign change). For contrast, print the UNLIMITED maxDCamber (which would exceed the threshold — the original 12.76°/m tight-turn artifact noted in STATE.md). Gate threshold: slew-limited maxDCamber ≤ MAX_DCAMBER_DEG_PER_M. Zero-install.
  </action>
  <verify>
    <automated>node --check test/spline-continuity.mjs && node test/spline-continuity.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node test/spline-continuity.mjs` exits 0 with the `camber-rate` gate fixture PASS.
    - The slew-limited maxDCamber ≤ MAX_DCAMBER_DEG_PER_M (2.0 °/m); the unlimited value is printed for contrast and exceeds it.
    - No camber-rate spike at the curvature zero-crossing under the slew limit.
    - All gate fixtures (gentle-baseline, tile-seam-mismatch, physics-sampling-continuity, hairpin, switchback-no-arm-flip, two-arms-no-undermine, camber-rate) PASS → exit 0.
  </acceptance_criteria>
  <done>A camber-rate fixture proves the slew limiter keeps banking within threshold with no zero-crossing spike; full gate set exits 0.</done>
</task>

</tasks>

<verification>
- `node --check test/spline-continuity.mjs` exits 0.
- `node test/spline-continuity.mjs` exits 0 with ALL role:'gate' fixtures PASS (D4 switchback, D3 two-arms, D2 camber-rate, plus the pre-existing baseline/seam/physics/hairpin gates).
- Fixtures are zero-install (import only from ../src/road-carve.js).
</verification>

<success_criteria>
- SURF-04/D4: switchback no-arm-flip gate.
- SURF-05/D3: two-close-arms no-undermine gate.
- SURF-03/D2: camber-rate gate.
- Full headless gate set exits 0 — the phase's automated gates are green.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-23-SUMMARY.md` when done.
</output>
