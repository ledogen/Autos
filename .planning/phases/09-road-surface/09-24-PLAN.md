---
phase: 09-road-surface
plan: 24
type: execute
wave: 7
depends_on: ["09-22", "09-23"]
files_modified:
  - src/road-mesh.js
  - data/ranger.js
  - src/debug.js
autonomous: false
requirements: [SURF-02, SURF-05]
must_haves:
  # Dirt-color shoulders (09-15 scope, independent/cosmetic) — skirts read as dirt, not asphalt
  truths:
    - "SURF-05: the ribbon edge skirts (cut/fill shoulders) read as dirt-brown, not asphalt — visually distinguishing the engineered shoulder from the paved surface"
    - "SURF-02: skirt dirt color comes from a roadDirtColor param + color picker (no asset files), consistent with the procedural-vertex-color discipline"
  artifacts:
    - path: "src/road-mesh.js"
      provides: "skirt verts colored from roadDirtColor instead of asphalt RC/GC/BC"
      contains: "roadDirtColor"
    - path: "data/ranger.js"
      provides: "roadDirtColor param"
      contains: "roadDirtColor"
    - path: "src/debug.js"
      provides: "roadDirtColor color picker in the Road Surface folder"
      contains: "roadDirtColor"
  key_links:
    - from: "src/road-mesh.js sweepRibbon skirt verts"
      to: "roadDirtColor"
      via: "skirt color replaces asphalt RC/GC/BC"
      pattern: "roadDirtColor"
---

<objective>
Color the ribbon edge skirts (the downward aprons at the cut/fill shoulders) dirt-brown via a `roadDirtColor` param + color picker, so cuts/fill shoulders read as dirt rather than asphalt. Currently the skirt verts use the asphalt base color (RC/GC/BC) in sweepRibbon (road-mesh.js ~296-314). This is the deferred 09-15 cosmetic scope — independent of the lifecycle/camber refactor.

This plan runs LAST (wave 7) so its road-mesh.js / ranger.js / debug.js edits are serialized after the refactor waves and it carries the combined final human-verify pass.

Purpose: Visual distinction between the paved surface and the engineered dirt shoulder (D-05/D-08/D-09 material intent). Cosmetic, no functional/physics change.
Output: roadDirtColor param + picker; skirt verts colored from it; ends with a human-verify checkpoint that also covers the combined in-sim acceptance for the whole refactor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-road-surface/09-REFACTOR-DESIGN.md
@.planning/phases/09-road-surface/09-PATTERNS.md
@.planning/phases/09-road-surface/09-CONTEXT.md

<interfaces>
<!-- Extracted from the codebase — executor uses these directly. -->

src/road-mesh.js sweepRibbon skirt verts (lines 296-314):
  - leftSkirtBase / rightSkirtBase verts get colors[...] = RC / GC / BC (asphalt base 0.15,0.15,0.17).
  - This is the ONLY place skirt color is written. Replace RC/GC/BC at the two skirt-color sites
    with the dirt color components derived from params.roadDirtColor.

Asphalt base constant (road-mesh.js:186): RC=0.15, GC=0.15, BC=0.17 — leave for the TOP surface;
  only the skirt verts change to dirt.

Color param pattern: lil-gui addColor for a hex color. data/ranger.js stores a hex int.
src/debug.js Road Surface sub-folder (lines 237-245) — fireSurface fires onRoadSurfaceChange (rebuilds
  the ribbon so the new skirt color takes effect). Use surfaceFolder.addColor(params, 'roadDirtColor').

CLAUDE.md: no asset files, vertex-color only — roadDirtColor is a procedural vertex color (D-01 discipline).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: roadDirtColor param + Road Surface color picker</name>
  <files>data/ranger.js, src/debug.js</files>
  <read_first>
    - data/ranger.js lines 332-365 (Plan 09-10/11 road surface param block — add roadDirtColor here)
    - src/debug.js lines 237-245 (Road Surface sub-folder fireSurface pattern)
    - .planning/phases/09-road-surface/09-CONTEXT.md D-05/D-08/D-09 (dirt vs asphalt material intent)
  </read_first>
  <action>
    Add `roadDirtColor` to data/ranger.js (a dirt-brown default as a hex int, e.g. a muted brown ~0x6b5a3e, with a comment referencing SURF-05/D-08 dirt-shoulder intent and units = hex color, no asset files per D-01). In src/debug.js add a color picker to the Road Surface sub-folder: `surfaceFolder.addColor(params, 'roadDirtColor').name('Dirt Shoulder Color').onChange(fireSurface)` so editing it triggers a ribbon rebuild (onRoadSurfaceChange). Follow the existing fireSurface pattern exactly.
  </action>
  <verify>
    <automated>node --check data/ranger.js && node --check src/debug.js && grep -c 'roadDirtColor' data/ranger.js src/debug.js | grep -qv ':0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` exits 0 on ranger.js and debug.js.
    - `roadDirtColor` exists in ranger.js (hex, SURF-05/D-08 comment) and has an addColor picker in the Road Surface folder firing onRoadSurfaceChange.
  </acceptance_criteria>
  <done>roadDirtColor param + color picker exist and trigger a ribbon rebuild on change.</done>
</task>

<task type="auto">
  <name>Task 2: Color skirt verts from roadDirtColor</name>
  <files>src/road-mesh.js</files>
  <read_first>
    - src/road-mesh.js lines 296-314 (skirt vert color writes — the two leftSkirtBase/rightSkirtBase color sites)
    - src/road-mesh.js line 186 (RC/GC/BC asphalt base — leave for the top surface)
    - src/road-mesh.js sweepRibbon params destructure (lines 167-174)
  </read_first>
  <action>
    In src/road-mesh.js `sweepRibbon`, derive dirt color components once near the top (destructure params.roadDirtColor and split into r,g,b in 0–1 linear space, e.g. via THREE.Color or bit-shift on the hex int — match the linear-space convention RC/GC/BC use). Replace the asphalt RC/GC/BC writes at the two skirt-color sites (leftSkirtBase colors and rightSkirtBase colors, lines ~305-307 and ~312-314) with the dirt color components. Leave the TOP-surface verts (and markings) on the asphalt base unchanged — only the skirt aprons become dirt. Do NOT change the vertsPerSection=13 stride, vert positions, or the index buffer.
  </action>
  <verify>
    <automated>node --check src/road-mesh.js && grep -c 'roadDirtColor' src/road-mesh.js | grep -qv '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/road-mesh.js` exits 0.
    - The two skirt-color sites write dirt color components derived from params.roadDirtColor (not RC/GC/BC).
    - The top-surface asphalt color + markings are unchanged; vertsPerSection=13 stride and positions unchanged.
    - `git diff --stat src/terrain-worker.js` empty.
  </acceptance_criteria>
  <done>Ribbon edge skirts render in roadDirtColor; the paved top surface stays asphalt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Combined in-sim verification (dirt shoulders + full refactor pass)</name>
  <files>(none — verification only)</files>
  <action>All code for this plan + the full refactor (09-18..09-23) is already implemented and committed. This task is verification only — no code changes. Present the verification steps below and pause for the human.</action>
  <what-built>roadDirtColor dirt skirts PLUS the full lifecycle/camber refactor (D0 fillet, D1 versioning, D5 hysteresis, D4 arm-disambiguation, D2 shared camber, D3 carve cross-section).</what-built>
  <how-to-verify>
    1. Start a local HTTP server: `python3 test/nocache-server.py` (or `npx serve .`). Open the sim.
    2. DIRT SHOULDERS (this plan): drive onto a raised (fill) road and a hillside (cut) road — the downward edge aprons read dirt-brown, distinct from the dark-grey asphalt top. Debug panel → Roads → Road Surface → "Dirt Shoulder Color": change it; the ribbon rebuilds and skirts update.
    3. SWITCHBACKS (D0/D2/D3/D4): drive a switchback — banking is smooth (no sharp/discontinuous camber, no clamp-flip spike at corner alternation), no clip/gap on banked turns, no invisible-ramp launch (car stays on the ribbon, does not drop to terrain). Hairpin arms are rounded (no self-overlap).
    4. TILE EDGES (D1/D5): drive across several 64 m tile seams — no ribbon load/unload thrash, no divergence; the car stays on the ribbon (no fall-through after the network re-slices).
    5. MAXGRADE SLIDER (D1): Debug → Roads → change "Max Grade (ratio)" — the route re-routes AND the terrain carve rebuilds to match the new route (the carved trough follows the new road, not the old one).
  </how-to-verify>
  <verify><human-check>Human confirms dirt shoulders + smooth switchback banking + no tile-edge thrash + maxGrade rebuilds carve.</human-check></verify>
  <resume-signal>Type "approved" or describe issues per-area (dirt color / switchback banking / tile-edge thrash / maxGrade carve rebuild).</resume-signal>
  <done>Human confirms all five verification areas pass in-sim.</done>
</task>

</tasks>

<verification>
- `node --check` clean on road-mesh.js, ranger.js, debug.js.
- Skirt verts colored from roadDirtColor; top surface asphalt unchanged.
- `git diff --stat src/terrain-worker.js` empty.
- Combined in-sim human pass: dirt shoulders + smooth switchback banking + no tile-edge thrash + maxGrade rebuilds carve.
</verification>

<success_criteria>
- SURF-05: cut/fill shoulders read as dirt, not asphalt.
- SURF-02: dirt color from a procedural param + picker, no asset files.
- The combined in-sim refactor acceptance pass (D0–D5) is confirmed by the human.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-24-SUMMARY.md` when done.
</output>
