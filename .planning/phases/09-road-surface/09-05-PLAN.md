---
phase: 09-road-surface
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - src/road-mesh.js
  - src/terrain.js
  - src/debug.js
  - src/main.js
  - data/ranger.js
  - test/test-road-mesh.html
autonomous: true
requirements: [SURF-02]
must_haves:
  truths:
    - "The road surface looks like dark-grey asphalt with lane markings, generated with no external asset files"
    - "Road quality varies in ~500 m stretches (high/mid/low) deterministically from seed + along-road position, blended at boundaries so markings do not snap"
    - "Five material zones (asphalt, engineered cutout, dirt foundation, natural cliff, general terrain) are feathered/blended at every boundary, with cutout visually distinct from natural cliff"
  artifacts:
    - path: "src/road-mesh.js"
      provides: "Vertex-color asphalt base + per-stretch roadQuality tier markings + labeled roadQuality hook"
      contains: "roadQuality"
    - path: "src/terrain.js"
      provides: "5-zone terrain vertex colors (carve-driven cutout/dirt + slope-driven cliff) feathered by blendW"
      contains: "cliff"
    - path: "src/debug.js"
      provides: "Roads-folder surface sliders (width, crown, camber, fill, slopes, shoulder, design-grade window, fillet)"
      contains: "onRoadSurfaceChange"
  key_links:
    - from: "src/road-mesh.js roadQuality"
      to: "src/seed.js seedFor/mulberry32"
      via: "seedFor(worldSeed,'roadquality',runKey,stretchIdx)"
      pattern: "roadQuality|seedFor"
    - from: "src/debug.js Roads folder"
      to: "src/main.js onRoadSurfaceChange callback"
      via: "fireRoadSurface callback -> rebuild road mesh + carve"
      pattern: "onRoadSurfaceChange"
  # D-01 procedural asphalt no assets; D-02 per-500m quality tiers blended; D-03 labeled roadQuality hook; D-09 5-zone feathered; D-10 cutout != cliff; D-11 slope cliff shading (splittable); D-04/D-07 debug sliders
---

<objective>
Make the road look like worn asphalt and the world read as engineered-vs-wild: procedural dark-grey asphalt with per-~500 m road-quality-tiered lane markings (D-01/D-02), a labeled `roadQuality` hook on the surface (D-03), a 5-zone feathered material system (D-09/D-10) with slope-based natural cliff shading (D-11), and the full set of Roads-folder debug sliders for live tuning (D-04/D-07, Claude's-discretion magnitudes).

Purpose: SURF-02 (procedural asphalt + markings, no assets) plus the visual material system and live tuning sliders. All deterministic from (seed, world coords, params).
Output: vertex-color asphalt + quality tiers + roadQuality hook in road-mesh.js; 5-zone terrain colors in terrain.js; new debug sliders + main.js callback; new ranger.js material params; asphalt smoke assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-road-surface/09-CONTEXT.md
@.planning/phases/09-road-surface/09-RESEARCH.md
@.planning/phases/09-road-surface/09-PATTERNS.md
@src/road-mesh.js
@src/terrain.js
@src/debug.js
@src/main.js
@src/seed.js
@data/ranger.js
@test/test-road-mesh.html

<interfaces>
<!-- Verified integration points. -->
- road-mesh.js: RoadMeshSystem, sweepRibbon (per-vertex write loop is where vertex colors go), shared MeshPhongMaterial{vertexColors:true}. buildJunctionFootprint (markings broken through the box, D-12).
- seed.js: seedFor(worldSeed, domainTag, ...coords), mulberry32(seed) -> rng()->[0,1).
- terrain.js: _flushPendingQueue Y loop + computeVertexNormals (~690-695); chunk.carveData blendW per vertex (Plan 02); shared terrain material currently MeshPhongMaterial{color:0xb89060} (~353) — switch to vertexColors:true and write per-vertex color attribute. analyticNormal / vertex normal for slope.
- debug.js: Roads folder ~196-215; fireRoadParam pattern; initDebug(params, callbacks={}, options={}) ~44; callback contract comment ~186-195. Add onRoadSurfaceChange to the contract.
- main.js: debounced rebuild convention (D-09 / rebuildTerrainFull). onRoadSurfaceChange -> rebuild road mesh + carve tables (rebuildAllChunksFromWorker for the carve, re-sweep road tiles).
- RESEARCH: asphalt base color (0.15,0.15,0.17) linear; markings white (0.9,0.9,0.9); roadQuality = mulberry32(seedFor(seed,'roadquality',hashRunKey(runKey),stretchIdx))() with STRETCH=500 m; boundary smoothstep over 10 m. Tiers: High solid center+edge; Mid solid center+intermittent edge (8m on/4m off); Low translucent center only. 5 zones + colors table; cliff smoothstep(0.3,0.6, 1-normal.y).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vertex-color asphalt + per-500m roadQuality tiers + markings + labeled roadQuality hook in road-mesh.js</name>
  <files>src/road-mesh.js, data/ranger.js</files>
  <read_first>
    - src/road-mesh.js (sweepRibbon per-vertex loop, buildJunctionFootprint, shared material)
    - src/seed.js (seedFor, mulberry32)
    - data/ranger.js (Phase 9 block)
    - .planning/phases/09-road-surface/09-RESEARCH.md (§Vertex-Color Asphalt + Road Quality)
    - .planning/phases/09-road-surface/09-PATTERNS.md (§Determinism comment, §MeshPhongMaterial vertexColors)
  </read_first>
  <action>
    In `src/road-mesh.js` write a per-vertex color attribute during the ribbon sweep. Asphalt base color (0.15, 0.15, 0.17) linear (dark cool grey). Add `roadQuality(arcS, runKey, worldSeed)`: stretchIdx = floor(arcS / ROAD_QUALITY_STRETCH) with ROAD_QUALITY_STRETCH=500 m; value = mulberry32(seedFor(worldSeed, 'roadquality', hashRunKey(runKey), stretchIdx))() in [0,1); blend across the stretch boundary with smoothstep over a 10 m transition (frac = (arcS % 500)/10) so the tier does not snap (D-02). Expose this as a LABELED hook on the surface/segment (e.g. store per-arc roadQuality so SURF-06 in Plan 06 and the markings share one source — D-03).

    Draw lane markings as bright vertex-color patches (no texture, no asset): centerline if |uLat| < 0.15 m, edge lines if |uLat - roadHalfWidth| < 0.10 m, gated by the tier:
    - High (quality high): solid centerline + solid edge lines, full white (0.9,0.9,0.9).
    - Mid: solid centerline + intermittent edge lines (arcS % 12 < 8), faded.
    - Low: very faint centerline only (brightness ~0.3), no edge lines.
    Interpolate marking presence/brightness with the blended roadQuality so transitions are smooth. Inside the junction footprint, break markings through the box (asphalt with markings interrupted, D-12). Add ranger.js: `roadQualityStretch: 500` (m, D-02) and `roadQualityBlend: 10` (m boundary transition, A9). Purity comment: deterministic from (seed, runKey, arcS).
  </action>
  <verify>
    <automated>grep -c "roadQuality" src/road-mesh.js; grep -c "roadQualityStretch" data/ranger.js; node -e "const c=require('fs').readFileSync('src/road-mesh.js','utf8'); if(!/seedFor/.test(c)) throw new Error('roadQuality must use seedFor'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: ribbon vertices carry an asphalt base color + tier-driven marking colors; no asset file imported (procedural only, D-01).
    - SOURCE: `roadQuality(arcS, runKey, worldSeed)` is a pure seedFor/mulberry32 function over 500 m stretches, smoothstep-blended over 10 m; a labeled roadQuality hook is stored on the surface (D-03).
    - SOURCE: High/Mid/Low marking patterns implemented; markings break through the junction box.
    - BEHAVIOR: same seed reproduces the same tier at the same along-road position (no flicker on re-stream).
  </acceptance_criteria>
  <done>Procedural worn-asphalt with per-stretch quality-tiered markings and a shared roadQuality hook; deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: 5-zone feathered terrain materials (carve cutout/dirt + slope cliff) in terrain.js; Roads-folder debug sliders + main.js callback; asphalt smoke assertion</name>
  <files>src/terrain.js, src/debug.js, src/main.js, data/ranger.js, test/test-road-mesh.html</files>
  <read_first>
    - src/terrain.js (_flushPendingQueue Y+normal loop ~690, shared material ~353, chunk.carveData blendW from Plan 02, analyticNormal/vertex normal for slope)
    - src/debug.js (Roads folder ~196-215, fireRoadParam, callback contract ~186-195, initDebug ~44)
    - src/main.js (debounced rebuild convention, road system + RoadMeshSystem wiring)
    - data/ranger.js (Phase 9 params)
    - .planning/phases/09-road-surface/09-RESEARCH.md (§5-Zone Material System, D-09/D-10/D-11)
    - .planning/phases/09-road-surface/09-PATTERNS.md (§debug.js sliders, §terrain.js color pass)
  </read_first>
  <action>
    In `src/terrain.js`, switch the shared terrain material to `MeshPhongMaterial({ vertexColors: true })` and write a per-vertex color attribute in `_flushPendingQueue` (and the rebuild path). Five zones, feathered (no hard lines, D-09):
    - General terrain: existing brown (0.72,0.60,0.47) baseline.
    - Natural cliff (D-11): slope = 1 - vertexNormal.y; cliffBlend = smoothstep(roadCliffSlopeLo, roadCliffSlopeHi, slope) (defaults 0.3/0.6); lerp general->cliff grey (0.60,0.58,0.55). Wild/weathered read.
    - Engineered cutout (carve delta<0, dist<shoulder): uniform grey-tan (0.55,0.50,0.42) — man-made/uniform, distinct from cliff (D-10).
    - Dirt foundation (carve delta>0, dist<fillToe): warm tan (0.65,0.55,0.38).
    Drive the road-zone (cutout/dirt) blend by the carve `blendW` from chunk.carveData so colors feather near the road automatically (free side effect of the carve). Asphalt itself lives on the road mesh (Task 1), not the terrain. Add ranger.js: `roadCliffSlopeLo: 0.3`, `roadCliffSlopeHi: 0.6` (A10, D-11). Note: D-11 cliff shading is splittable to a follow-up if needed — keep it here unless it pushes the plan over budget.

    In `src/debug.js`, add to the Roads folder (following the fireRoadParam pattern) the full surface slider set bound to `params`, each firing a new `fireRoadSurface` callback: roadWidth (6..14,.5), crownHeight (0..0.2,.005), camberStrength (50..500,10), roadFillHeight (0..4,.1), roadCutSlope (0.5..2,.05), roadFillSlope (1.5..5,.1), roadShoulderWidth (1..6,.5), designGradeWindow (10..150,5), roadFilletRadius (0.5..10,.5), and optionally roadCliffSlopeLo/Hi. Add `onRoadSurfaceChange` to the callback contract comment block. In `src/main.js`, wire `callbacks.onRoadSurfaceChange` to a debounced rebuild (re-bake carve tables + rebuildAllChunksFromWorker + re-sweep road mesh tiles), consistent with the existing onRoadParamChange convention.

    Extend `test/test-road-mesh.html` with an asphalt smoke assertion (SURF-02): build a road tile, assert the geometry has a color attribute and that a centerline marking vertex is brighter than an off-marking asphalt vertex on a High-quality stretch — label "SURF-02 asphalt + markings (centerline brighter)".
  </action>
  <verify>
    <automated>grep -c "vertexColors" src/terrain.js; grep -c "onRoadSurfaceChange" src/debug.js src/main.js; grep -c "roadCliffSlopeLo\|cliff" src/terrain.js; grep -c "SURF-02" test/test-road-mesh.html</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: terrain shared material uses vertexColors; _flushPendingQueue (and rebuild path) writes per-vertex colors for all 5 zones, feathered via carve blendW and slope smoothstep.
    - SOURCE: engineered cutout color is distinct from natural cliff color (D-10); cliff thresholds in ranger.js.
    - SOURCE: debug.js Roads folder has the full surface slider set firing onRoadSurfaceChange; main.js debounced-rebuilds carve + road mesh on that callback; contract comment updated.
    - HARNESS-OUTPUT: test-road-mesh.html prints `PASS SURF-02 asphalt + markings`.
  </acceptance_criteria>
  <done>5-zone feathered materials (cutout != cliff), full live-tuning slider set wired, asphalt smoke PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| debug slider values -> material/surface params | Local numeric slider values, bounded ranges in debug.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | (none applicable) | vertex-color material pass | accept | Browser-only static game; colors from own math + seed; no server/network/auth/external input. Slider values are bounded numerics. No high-severity threat applies. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | NO packages installed (hand-rolled + Three.js r184); RESEARCH audit confirms zero. No legitimacy checkpoint required. |
</threat_model>

<verification>
- Open test/test-road-mesh.html: asphalt + markings smoke PASS.
- In-game (manual): roads read as dark asphalt with markings that visibly degrade over ~500 m stretches; cut faces read man-made vs wild natural cliffs; drag the new Roads-folder sliders and confirm the road mesh + carve rebuild live.
</verification>

<success_criteria>
- SURF-02: procedural dark-grey asphalt with tiered markings, no asset files.
- D-09/D-10/D-11: 5-zone feathered materials, cutout distinct from cliff, slope cliff shading.
- Full debug-slider tuning set wired to live rebuild.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-05-SUMMARY.md` when done.
</output>
