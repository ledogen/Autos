---
phase: 09-road-surface
plan: "31"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/road.js
  - src/road-carve.js
  - test/spline-continuity.mjs
  - test/diag-minradius-pipeline.mjs
  - data/ranger.js
autonomous: false
gap_closure: true
requirements: [SURF-01, SURF-03]
must_haves:
  truths:
    # Full spec + rationale: 09-CENTERLINE-CONDITIONER-DESIGN.md (read first). VBC-01..09 in 09-CONTEXT.md.
    - "DETERMINISM (Step 0, foundation): generated centerline is window-invariant — same (seed,params) → identical geometry at a world position regardless of stream center. Headless probe: two stream centers → identical run geometry. Reverses BUG-11 WONTFIX."
    - "ROUTER CHARACTER (Step 1, cost only): KEEP valley-following (wAlt bias), long straights/variable spacing, tight turns, switchbacks, traverses. ADD climb-anticipation (penalize being below start→goal altitude ramp). Improve switchback parallelism. NO hard A* curvature gate."
    - "MIN RADIUS (Step 2, minimal spline pass, HARD): only corners that would fold the ±halfWidth ribbon are touched, rounded just to the FOLD-SAFE floor (~halfWidth+clearance ≈7–8m); straights untouched; tight turns stay tight. Constructive arc at violating corners (filletMinRadius relaxation FAILED — leaves hairpin ~2m)."
    - "MAX GRADE (Step 2, SOFT): enforced 'if possible'; occasional deviations welcome/navigable/characterful."
    - "CAMBER (Step 3): signed curvature sampled at a CONSISTENT ARC-LENGTH window (spacing-invariant) → continuous camber WITHOUT uniform-resampling the centerline (keeps variable-spacing character)."
    - "SIMPLIFICATION (Step 4, done-signal): _removeLoops / _removeSelfCrossings / _filletMinRadius / dead _limitCurvature DELETED, transition nets removed, road.js smaller. Not just 'folds gone'."
  artifacts:
    - path: "test/diag-minradius-pipeline.mjs"
      provides: "Gates on real dumps: WINDOW-INVARIANCE, MIN-RADIUS (dense), MINIMAL, CAMBER, CHARACTER metrics"
      contains: "minRadius"
    - path: "src/road.js"
      provides: "World-anchored deterministic generation + character cost (climb-anticipation, switchback); cleanup stack deleted"
      contains: "_protoConnect"
    - path: "src/road-carve.js"
      provides: "Pure (THREE-free) minimal constructive min-radius pass + arc-length-window curvature for camber"
      contains: "Radius"
  key_links:
    - from: "src/road.js _streamNetwork / _protoConnect"
      to: "world-anchored deterministic runs + minimal min-radius (replaces the cleanup stack)"
---

<objective>
Rewrite the road centerline generation to fix BUG-12 (folds), BUG-11 (non-determinism), and camber
discontinuities at the SOURCE — one generation layer — and DELETE the cleanup stack (the QUAL-03
simplification). NOT a conditioner layer, NO hard A* curvature gate, NO uniform resampling. Keep the
router's liked character (valleys, long straights, tight turns, switchbacks). Full design + decisions +
gotchas + verification: **09-CENTERLINE-CONDITIONER-DESIGN.md** (authoritative — read before executing).
</objective>

<tasks>
Ordered (foundation → character → validity → cleanup); see design doc for the detail of each.
1. Headless gates first (TDD): wire real dumps as fixtures in diag-minradius-pipeline.mjs; add
   WINDOW-INVARIANCE probe, MIN-RADIUS (dense), MINIMAL, CAMBER (arc-length window), CHARACTER metrics.
2. Step 0 — Determinism: world-anchored row-run span + margin, consume interior → window-invariant.
   Gate green; valley-following + reproducibility preserved. [in-sim checkpoint]
3. Step 1 — Router character: _protoEdgeCost/turn model — climb-anticipation + switchback parallelism;
   keep valley bias + straights + tight turns. No curvature gate. [in-sim checkpoint]
4. Step 2 — Minimal spline pass: constructive min-radius (hard, fold-safe floor, violating corners only)
   + soft max-grade. Gates green on real dumps.
5. Step 3 — Camber: consistent-arc-length curvature sampling. Gate green. Keep nets; in-sim verify.
6. Step 4 — Delete _removeLoops/_removeSelfCrossings/_filletMinRadius/_limitCurvature + nets; re-verify;
   confirm road.js shrank. [in-sim checkpoint]
</tasks>

<verification>
- Headless: diag-minradius-pipeline.mjs + spline-continuity.mjs all gates green, exit 0.
- In-sim: seed 8 seam (no kink/tear), seed 6 camber smooth, seed reproducible across reload + param
  tweak, rd_minr ≥ floor, valleys/straights/switchbacks still feel right.
- Simplification: cleanup stack + nets removed, road.js LOC down.
</verification>

<deferred>
- Junction redesign (VBC-08), vertical/3D radius (VBC-02), full QUAL-03 road.js shrink beyond the cleanup-stack deletion.
- Hybrid-A* arc-primitive routing — fallback only if the grid fit can't hit the gates.
</deferred>
