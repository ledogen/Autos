---
phase: 09-road-surface
plan: "31"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/road-carve.js
  - src/road.js
  - test/spline-continuity.mjs
  - data/ranger.js
autonomous: false
gap_closure: true
requirements: [SURF-01, SURF-03]
must_haves:
  truths:
    # VBC-01..06 (09-CONTEXT.md): valid-by-construction centerline, radius hard / grade soft, XZ-only, per-road.
    - "The road centerline's delivered XZ turn radius is >= roadMinTurnRadius EVERYWHERE on the open road (no sub-radius coil reaches the ribbon) — enforced at generation, verified headlessly."
    - "Gentle curves (radius already >= minRadius) are NOT tightened — the fix modifies only tight spans and splices into preserved geometry (pure-pursuit is rejected: it tightens gentle curves to the cap)."
    - "The conditioned centerline has ~UNIFORM arc-length spacing (no 1.25m..98m gaps) so per-point signedCurvature is stable → camber is continuous (fixes the seed-6 camber-discontinuity report). Tiny dense clusters are gone → no degenerate sliver slices at tile seams (fixes seed-8 seam tear). Uniform resample ALONE is insufficient (preserves sharp vertices) — combine with curvature rounding."
    - "Radius is the HARD constraint; grade may be violated to honor radius (VBC-01). XZ only; no vertical/3D radius (VBC-02)."
    - "Constraint is per-road; no extra tightening near other roads (VBC-06). Junctions are OUT of scope (separate VBC-08 effort)."
    - "The existing corrective passes (_removeLoops/_removeSelfCrossings/_filletMinRadius) stay as a safety net until the new generator is confirmed crease-free in-sim across varied seeds (VBC-07); they are removed in a follow-up, not here."
  artifacts:
    - path: "src/road-carve.js"
      provides: "Pure (THREE-free) valid-by-construction min-radius enforcer that satisfies ENFORCE + PRESERVE"
      contains: "MinRadius"
    - path: "test/spline-continuity.mjs"
      provides: "Gate: hairpin/90-corner output minR >= minRadius AND gentle-R40 preserved (>= ~floor)"
      contains: "minRadius"
  key_links:
    - from: "src/road.js _streamNetwork"
      to: "the new enforcer (replacing reliance on filletMinRadius for tight corners)"
---

<objective>
Fix BUG-12 at the source (QUAL-03 / VBC mandate): make the routed road centerline
valid-by-construction in XZ — every corner >= roadMinTurnRadius as the spline is drawn — so a
±roadHalfWidth ribbon can never fold. Open-road only; junctions deferred (VBC-08).

VERIFIED ROOT CAUSE (2026-06-16, `test/diag-minradius-pipeline.mjs`): the post-hoc relaxation
`filletMinRadius` (src/road-carve.js) CANNOT open a tight coil — a raw 180° hairpin (apex ~1.4 m)
comes out at ~1.99 m even targeting 15 m → a ~2 m switchback apex folds the 5 m ribbon. This is an
OPEN-ROAD generation failure, not (only) junctions. The earlier "hairpin gate green" used a
PRE-filleted hairpin, so it never tested the relaxation's real convergence (gate blind spot).

The fix must satisfy BOTH headless properties (see diag harness):
  (A) ENFORCE — output min XZ radius >= minRadius for hairpin + 90° corner.
  (B) PRESERVE — a gentle R=40 curve stays ~40 (NOT dragged to the cap). Pure-pursuit FAILS (B)
      and is explicitly rejected — do not use it.
</objective>

<real_geometry_confirmation>
Validated against REAL dumps (press-'p' tool; Logs/road-run-dump-1781627151455.json seed-8 run -1:1,
and -1781627245285.json seed-6 run 0:0):
- The centerline IS sub-min-radius. circumradius on the raw (sparse, uneven) network points read
  ~14 m — but that was a SAMPLING ARTIFACT: points are spaced 1.25 m … 98 m apart, so a 3-point
  circumradius spans across a sharp vertex and reads smooth. Resampling uniformly to 3 m reveals the
  TRUE corner radius ≈ 1.5 m. → MEASURE CURVATURE ON DENSE/UNIFORM SAMPLES, never raw network points.
- The ribbon (road-mesh.js sweepRibbon) places cross-sections AT the network/slice control points
  (not spline samples) — so sharp network vertices fold the ±halfWidth ribbon directly (seed-8 seam
  tear), and tiny dense point clusters at tile-boundary grazes make degenerate sliver slices.
- Uneven spacing → noisy per-point signedCurvature → camber discontinuities (seed-6 report; 201
  curvature-jump spikes). So the conditioner MUST ALSO output ~uniform arc-length spacing.
- VERIFIED dead-end: uniform linear resample ALONE drops minR to 1.5 m (preserves sharp vertices) —
  must be COMBINED with curvature rounding. Build the conditioner against these dumps as fixtures.
</real_geometry_confirmation>

<context>
Already done this session (committed): roadMinTurnRadius 12→15 (data/ranger.js, slider exists);
`rd_minr` logger diagnostic (src/road.js debugSampleAt + src/logger.js) — logs the truck's run
centerline local XZ radius so an in-sim fold where rd_minr is still >= ~15 m proves junction/mesh,
not the per-run spline.

Decisions: .planning/phases/09-road-surface/09-CONTEXT.md (VBC-01..09).
Philosophy: .planning/todos/pending/qual-road-system-simplify.md (QUAL-03).
Scope boundary: this reopens the Phase-8 router within P9 (generation algorithm only; keep
determinism / pure-coarseHeight / window-invariance — D-16).
</context>

<tasks>
1. **Pure enforcer (src/road-carve.js) + headless gate first (TDD).** Iterate against
   `test/diag-minradius-pipeline.mjs` until ENFORCE + PRESERVE both pass, then promote the
   fixtures into `test/spline-continuity.mjs` as real gates (hairpin/corner out minR >= minRadius;
   gentle-R40 preserved).
   RULED OUT (verified 2026-06-16, do NOT retry): (i) `filletMinRadius` midpoint relaxation —
   leaves hairpin at ~2 m; (ii) wide-stencil relaxation — collapses points (hairpin→0.01 m,
   S-curve→0.20 m) or barely moves (corner→1.66 m); (iii) global pure-pursuit — enforces radius
   but tightens gentle R40→15 (fails PRESERVE). USE A CONSTRUCTIVE method:
   detect tight spans (sustained cumulative heading
   change with arc/angle < minRadius — cf. retired `_limitCurvature` span detection at road.js
   ~1168) and REPLACE only those spans with a min-radius arc/biarc tangent to the entry/exit legs,
   widening arm separation where a hairpin's legs are closer than ~2·minRadius. Leave non-tight
   geometry untouched (satisfies PRESERVE). Pure {x,y,z}, deterministic, THREE-free, Y linearly
   carried along inserted arc.
2. **Wire into generation (src/road.js _streamNetwork).** Apply the enforcer to the connected
   run polyline at generation (the spot filletMinRadius runs, ~line 1417), BEFORE slicing. Keep
   the existing safety-net passes running after it (VBC-07). Confirm the per-slice Catmull-Rom the
   ribbon sweeps does not reintroduce sub-radius curvature (resample density / tension); verify via
   the vendored CR in the harness if needed.
3. **In-sim verification (human checkpoint).** Drive varied seeds incl. switchbacks; confirm no
   ribbon folds on open road; use `rd_minr` to confirm the centerline holds >= minRadius. (Junction
   creases may remain — that's the deferred VBC-08 effort, not this plan.)
</tasks>

<verification>
- Headless: `node test/spline-continuity.mjs` exits 0 with the new hairpin/corner ENFORCE gates +
  gentle-PRESERVE gate green.
- `node test/diag-minradius-pipeline.mjs` shows the filletMinRadius/enforcer column >= minRadius for
  hairpin + corner, gentle-R40 still ~40.
- In-sim: open-road switchbacks no longer fold the ribbon (human pass).
</verification>

<deferred>
- Removing the safety-net passes (VBC-07) — follow-up once proven crease-free in-sim.
- Junction crease redesign (VBC-08: parallel→merge+rediverge; orthogonal→fillet blend) + tie-ends/3-way (VBC-09).
- Full QUAL-03 road.js shrink + valley-exit-search isolation.
</deferred>
