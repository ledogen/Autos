---
phase: 09-road-surface
plan: 28
type: execute
wave: 3
depends_on: ["09-25", "09-26"]
files_modified:
  - src/road-mesh.js
autonomous: true
requirements: [SURF-01, SURF-03]
must_haves:
  truths:
    - "sweepRibbon builds the cross-section frame from the continuous tangent runProfile(arcS).tx/tz, not a per-slice tangent"
    - "Adjacent slices' boundary cross-sections are identical → ±halfWidth edges weld (no gap, no overlap)"
    - "Ribbon stays sealed at sharp corners regardless of corner sharpness (continuity > roundness, BUG-12 closed)"
    - "Grade/camber/crown reads in the ribbon stay consistent with P2 (same profile)"
  artifacts:
    - path: "src/road-mesh.js"
      provides: "sweepRibbon section frame from continuous tangent; optional shared-boundary edge weld"
      contains: "runProfile"
  key_links:
    - from: "sweepRibbon section frame"
      to: "runProfile(arcS).tx/tz"
      via: "perpendicular derived from the continuous run tangent → identical boundary cross-sections"
      pattern: "runProfile"
    # Closes BUG-12 via tangent continuity across slice seams
---

<objective>
P3 — BUG-12: ribbon frame from the continuous tangent (weld slice edges).
`road-mesh.js sweepRibbon` currently builds the cross-section frame
(tangent → perpendicular) per slice via `spline.getTangentAt`, so adjacent
slices' ±halfWidth edges don't meet at a sharp tangent change → the ribbon
tears/overlaps at sharp corners.

Read the section tangent from `runProfile(arcS).tx/tz` (continuous across slice
seams) so adjacent slices' boundary cross-sections are IDENTICAL → edges weld →
no gap/overlap, regardless of corner sharpness (user steer: continuity >
roundness). If frame-from-profile is insufficient at the very sharpest apex,
additionally weld the shared boundary edge vertices. Keep grade/camber/crown
reads on the same profile (consistent with P2).

Purpose: seal the ribbon at sharp corners.
Output: sweepRibbon section frame sourced from the continuous tangent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-road-surface/09-CONTINUOUS-PROFILE-DESIGN.md
@.planning/phases/09-road-surface/09-PATTERNS.md

<interfaces>
<!-- P0 sampler (09-25) + the sweepRibbon frame build. From src/road.js and src/road-mesh.js. -->

P0 (09-25): `runProfile(arcS, runKey) → { gradeY, camberRad, tx, tz }` — tx/tz are the unit run tangent.

sweepRibbon — src/road-mesh.js (~167):
- `sweepRibbon(spline, designGradeY, points, params, runKey='', arcS0=0, arcS1=0)`.
- `arcS(u) = arcS0 + (arcS1 − arcS0)·u` (~223) — the run-global arc at this section.
- `camberSign = arcS1 >= arcS0 ? 1 : -1` (~171).
- Current section frame: `spline.getTangentAt(u, _scratchTan)` (~206) then perpendicular from it (~213
  degenerate-tangent guard → unit X fallback). THE per-slice tangent that causes BUG-12.
- Lateral sweep: `uLat` from `-halfWidth` to `+halfWidth`; `crownY = crownProfile(uLat, halfWidth, crownHeightVal)`;
  `camberAngle = camberSign * this._road.camberProfile(arcS, runKey)` (~231).
- `this._road` is the RoadSystem instance (exposes runProfile after 09-25).
- Module-scope scratch `_scratchTan` (no per-section allocation) — keep that discipline for tx/tz reads.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section frame from runProfile tangent (weld slice-boundary cross-sections)</name>
  <files>src/road-mesh.js</files>
  <read_first>
    - src/road-mesh.js lines 100–300 (sweepRibbon — full: arcS(u) computation, getTangentAt frame, perpendicular, lateral sweep)
    - src/road-mesh.js lines 40–90 (_scratchTan + module-scope allocation discipline; _road reference)
    - src/road.js the runProfile signature (09-25) — tx/tz unit tangent fields
    - .planning/phases/09-road-surface/09-CONTINUOUS-PROFILE-DESIGN.md (P3 section, lines 49–50)
  </read_first>
  <action>
    In `sweepRibbon`, after computing `arcS = arcS0 + (arcS1 − arcS0)·u` for the section, read the
    section tangent from the continuous profile: `const rp = this._road.runProfile(arcS, runKey)` then
    use `rp.tx`/`rp.tz` as the section tangent INSTEAD of `spline.getTangentAt(u, _scratchTan)`. Derive
    the cross-section perpendicular (right vector) from that continuous tangent using the SAME sign
    convention already in the file (right = (tz, 0, −tx) per the lateral-offset math). Keep the
    degenerate-tangent guard (zero-length → unit X fallback) applied to `rp.tx/rp.tz`. CRITICAL frame
    direction: the run tangent points along the run's canonical direction; a slice may run E→W
    (arcS1 < arcS0). Use `camberSign` (or the sign of `arcS1 − arcS0`) to flip the tangent into THIS
    slice's sweep direction so the ribbon faces the right way and winding is not inverted. Because both
    slices that share a boundary resolve to the SAME `arcS` at the seam, `runProfile` returns the SAME
    tx/tz → their boundary cross-sections (centerline ± perpendicular·halfWidth) are identical → the
    ±halfWidth edge vertices coincide → edges weld. Keep grade/camber/crown reads on the same profile
    (the centerline Y can come from `rp.gradeY` for consistency with P2, or keep `designGradeY`
    if it is already the run-arc-sourced grade — verify they agree and prefer rp.gradeY so all three
    paths share one source). Maintain the module-scope scratch discipline: do not allocate per section
    beyond the one runProfile result (note as perf chokepoint if needed).
  </action>
  <verify>
    <automated>node --check src/road-mesh.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/road-mesh.js` exits 0
    - `grep -n "runProfile" src/road-mesh.js` shows the section tangent read
    - `spline.getTangentAt` is no longer the source of the section perpendicular frame in sweepRibbon (replaced by rp.tx/rp.tz) — verify by inspection
    - The tangent is flipped per slice direction (camberSign / sign(arcS1−arcS0)) so winding is not inverted
    - The degenerate-tangent guard still applies (zero-length → unit X fallback)
    - `git diff --stat src/terrain-worker.js` empty
  </acceptance_criteria>
  <done>sweepRibbon's cross-section frame comes from the continuous run tangent; shared-boundary cross-sections are identical so edges weld.</done>
</task>

<task type="auto">
  <name>Task 2: Shared-boundary edge weld fallback for the sharpest apexes</name>
  <files>src/road-mesh.js</files>
  <read_first>
    - src/road-mesh.js lines 560–600 (boundary control-point sharing comment ~569: adjacent slices SHARE the boundary point C0/C1)
    - the sweepRibbon edge-vertex emission (the ±halfWidth left/right edge vertices per section)
    - .planning/phases/09-road-surface/09-INSIM-VERIFY.md (open issue 3: residual hairpin overlap at apexes where arms < ~2·minRadius)
  </read_first>
  <action>
    Add a guarded edge-weld so the very sharpest apexes (where router arms are closer than
    ~2·minRadius — open issue 3) cannot gap even if the frame-from-profile leaves a sub-mm seam.
    At each slice's u=0 and u=1 boundary sections, snap the emitted ±halfWidth edge vertices to the
    EXACT shared boundary cross-section computed from `runProfile(arcS_boundary)` (arcS0 for u=0,
    arcS1 for u=1) — i.e. compute the boundary edge vertices once from the profile tangent at the
    shared arcS and reuse them for both adjacent slices, guaranteeing bit-identical boundary edge
    positions. Keep this minimal and behind a clear comment that it is the continuity-over-roundness
    weld (D-design steer). Do NOT change the spline geometry or router output. Preserve determinism
    (no Math.random / Date). If Task 1's frame-from-profile already makes boundary edges coincide to
    floating-point equality (likely, since both slices read the same arcS), this task is a defensive
    assertion-style snap rather than a new computation — implement it as the explicit snap so the
    weld is guaranteed by construction, not by floating-point luck.
  </action>
  <verify>
    <automated>node --check src/road-mesh.js && node test/spline-continuity.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/road-mesh.js` exits 0
    - `node test/spline-continuity.mjs` exits 0 (existing gates green; the ribbon-edge-weld gate lands in 09-30)
    - Boundary edge vertices at a shared slice seam are computed from runProfile at the shared arcS for BOTH adjacent slices (bit-identical) — verify by inspection
    - No spline/router geometry modified; no new allocations per section beyond the runProfile result
    - `git diff --stat src/terrain-worker.js` empty
  </acceptance_criteria>
  <done>Shared boundary edge vertices are welded by construction from the profile at the shared arcS; sharpest apexes cannot gap.</done>
</task>

</tasks>

<verification>
- `node --check src/road-mesh.js` exits 0
- `node test/spline-continuity.mjs` — existing gates green (ribbon-edge-weld gate added in 09-30)
- sweepRibbon section frame + boundary edges sourced from the continuous run tangent (runProfile.tx/tz)
- `git diff --stat src/terrain-worker.js` empty
</verification>

<success_criteria>
BUG-12 closed: adjacent slices' boundary cross-sections are identical (same arcS →
same tangent → same edge vertices), so the ribbon's ±halfWidth edges weld with no
gap or overlap at sharp corners — continuity over roundness.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-28-SUMMARY.md` when done.
</output>
