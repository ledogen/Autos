---
phase: 09-road-surface
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/road-carve.js
  - src/road.js
  - src/terrain.js
  - src/terrain-worker.js
  - data/ranger.js
  - test/test-road-carve.html
autonomous: true
requirements: [SURF-04, SURF-05]
must_haves:
  truths:
    - "Driving onto a road, the truck rides on the carved surface — analyticHeight returns the road design grade, not the raw terrain, at on-road positions"
    - "Stepping sampleHeight across the carve boundary shows no vertical step discontinuity (steep-but-continuous cut faces allowed; degenerate vertical seams disallowed)"
    - "The carve blend result is identical in the Worker mesh build (_flushPendingQueue Y write) and in analyticHeight/sampleHeight"
  artifacts:
    - path: "src/road-carve.js"
      provides: "Pure no-import sampleCarve/crownProfile/carveBlend; cut-and-fill blend"
      contains: "sampleCarve"
    - path: "src/terrain.js"
      provides: "analyticHeight/sampleHeight/_flushPendingQueue carve hook; carveTable Transferable postMessage"
      contains: "carveData"
    - path: "data/ranger.js"
      provides: "Phase 9 carve params (roadShoulderWidth, roadFillHeight, roadCutSlope, roadFillSlope, designGradeWindow)"
      contains: "designGradeWindow"
  key_links:
    - from: "src/terrain.js analyticHeight"
      to: "src/road-carve.js sampleCarve"
      via: "this._roadCarve sample"
      pattern: "carve.*blendW"
    - from: "src/road.js design grade"
      to: "carveTable per chunk"
      via: "delta = designGradeY - rawTerrainHeight"
      pattern: "designGrade"
  # D-05 cut-and-fill signed cross-section; D-06 smoothed design grade; D-07 fill cap + slope sliders; D-08 cut/fill slopes continuous by construction
---

<objective>
Build the carve: a pure no-import `road-carve.js` (cut-and-fill via one signed cross-section), a smoothed road "design grade", and the carve hook applied IDENTICALLY in the terrain Worker mesh build and in `analyticHeight`/`sampleHeight`. This is the phase EXIT GATE — height-agreement (mesh Y == physics Y on-road) and carve-continuity (no vertical seam across the carve boundary).

Purpose: SURF-04 (physics carries road height+normal) and SURF-05 (cut-and-fill embed, identical in mesh + physics) — the architecturally most sensitive piece. Carve is a POST-READ blend via `chunk.carveData` Float32Array, NEVER baked into `chunk.heights` (locked discipline).
Output: `road-carve.js`, design-grade smoothing in road.js, carve hook in terrain.js + terrain-worker.js, new ranger.js params, and the exit-gate assertions in test-road-carve.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-road-surface/09-CONTEXT.md
@.planning/phases/09-road-surface/09-RESEARCH.md
@.planning/phases/09-road-surface/09-PATTERNS.md
@src/terrain.js
@src/terrain-worker.js
@src/road.js
@data/ranger.js
@test/test-road-carve.html

<interfaces>
<!-- Verified integration points (grep-confirmed). -->
- terrain.js: CHUNK_SIZE=64, GRID_SAMPLES=65, CELL_SIZE=CHUNK_SIZE/(GRID_SAMPLES-1)=1.0.
- terrain.js analyticHeight(wx,wz) ~486: `const raw = height(...) * (params.terrainAmplitude ?? 1.0); return raw` — add carve hook after raw.
- terrain.js analyticNormal(wx,wz) ~505: central diff (EPS=0.5) over analyticHeight — UNCHANGED, picks up carve automatically.
- terrain.js sampleHeight(wx,wz) ~528: bilinear over chunk.heights * amp — add same carve blend (height-agreement path).
- terrain.js _flushPendingQueue Y loop ~690: `for i: pos.setY(i, heights[i]*amp)` — wrap in carve blend read from chunk.carveData.
- terrain.js _updateChunkRing postMessage ~643: `postMessage({type:'generate',cx,cz,key})` — extend to send carveTable as Transferable `[carveTable.buffer]`.
- terrain.js workerParams allowlist ~444: only plain-number fields cross to Worker (NEVER whole params — DataCloneError). carveTable is the new Transferable, NOT a params field.
- terrain-worker.js onmessage ~194: destructure carveTable from e.data; apply in height loop. terrain-worker.js is a BYTE-IDENTICAL mirror of WORKER_SOURCE — edit both in the same commit (T-07-03-SYNC).
- road.js _streamNetwork canonical runs (from Plan 01); _sliceNetwork/this._tiles per-tile splines; queryNearest. CatmullRomCurve3 getPointAt(u)/getTangentAt(u) are arc-length-correct.
- seed.js seedFor/mulberry32 already inlined in WORKER_SOURCE for the height fn — same sync discipline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/road-carve.js (pure no-import carve), add ranger.js carve params, add design-grade smoothing in road.js</name>
  <files>src/road-carve.js, data/ranger.js, src/road.js</files>
  <read_first>
    - src/road-carve.js (does not exist — create)
    - src/terrain.js (sampleHeight bilinear ~528-563 for sampleCarve model; WORKER_SOURCE seed/height sync block ~44-322; SYNC RULE ~281)
    - src/road.js (_sliceNetwork/this._tiles ~1025-1144, queryNearest ~363, CatmullRomCurve3 usage ~1140, _lerpVec3 ~55, _scratchPt ~45)
    - data/ranger.js (Phase 8 road param block ~187-232, comment style)
    - .planning/phases/09-road-surface/09-RESEARCH.md (§Cut-and-Fill Carve, §Design Grade Smoothing, §Code Examples sampleCarve)
    - .planning/phases/09-road-surface/09-PATTERNS.md (§road-carve.js, §Worker-Safe Pure-Function Discipline, §data/ranger.js)
  </read_first>
  <action>
    Create `src/road-carve.js` as a Worker-safe module. Top comment: function BODIES (no export) are copied verbatim into terrain.js WORKER_SOURCE and terrain-worker.js — SYNC RULE: any edit here must be reflected in both copy sites (same discipline as the height()/seed sync). Export these pure functions (no imports, no Math.random, no Date):
    - `sampleCarve(wx, wz, carveTable, N, originX, originZ, cellSize)` returns `{ blendW, gradeY }` via bilinear interpolation of the 2-value-per-cell table layout `[blendW_0, gradeY_0, blendW_1, gradeY_1, ...]` — same bilinear math as terrain.js sampleHeight, with clamped indices `Math.max(0, Math.min(N-2, ...))`.
    - `crownProfile(uLat, halfWidth, crownHeight)` returns parabolic `crownHeight * (1 - t*t)`, `t = uLat/halfWidth` (peak at centerline, 0 at edges).
    - `carveBlend(raw, dist, designGradeY, halfWidth, shoulderWidth)`: if `dist < halfWidth` return designGradeY (on ribbon); else shoulder lerp `t = max(0, 1 - (dist-halfWidth)/shoulderWidth)`, return `raw + t*(designGradeY - raw)`. This is the single blend both mesh and physics call. (Crown/camber tilt is folded into gradeY by the table builder in Plan 03, not here.)
    Each function documented as "Pure function — deterministic (D-16)."

    In `data/ranger.js`, add a "Phase 9 Road Surface — D-04/D-05/D-07/D-08 carve params" block with multi-line domain comments + inline unit/decision tags:
    `roadWidth: 10` (m, D-04), `roadHalfWidth: 5` (derived), `roadShoulderWidth: 2.5` (m blend zone, D-05), `roadFillHeight: 2.0` (m max fill cap, D-07), `roadCutSlope: 1.0` (H:V cut face ~45°, D-08), `roadFillSlope: 3.0` (H:V dirt embankment, D-08), `designGradeWindow: 50` (m sliding-window half-width, D-06). (crownHeight/camberStrength/roadFilletRadius are added by later plans.)

    In `src/road.js`, add a per-tile design-grade pass `_smoothDesignGrade(spline, terrainRef)` (or equivalent): sample the spline at ~2 m arc-length intervals via `getPointAt(u)`, compute `analyticHeight` at each XZ, then a sliding-window average of half-width `designGradeWindow` (default 50 m) to produce smoothed `designGradeY` per sample. Extend the sample window 2-3 samples past each tile boundary (use adjacent tile's spline in `this._tiles`) and clamp endpoints to match adjacent tiles (Pitfall 7). Memoize per tile (pure function of spline + params + terrain). Add a purity comment.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/road-carve.js','utf8').replace(/export function/g,'function'); if(/\bimport\b/.test(c)) throw new Error('road-carve must have no imports'); console.log('OK no-import')" && grep -c "designGradeWindow" data/ranger.js && grep -c "designGrade" src/road.js</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `src/road-carve.js` exists, contains no top-level `import` statement, and exports `sampleCarve`, `crownProfile`, `carveBlend`.
    - SOURCE: `data/ranger.js` contains roadWidth, roadHalfWidth, roadShoulderWidth, roadFillHeight (2.0), roadCutSlope (1.0), roadFillSlope (3.0), designGradeWindow (50) with decision-tagged comments.
    - SOURCE: road.js has a design-grade smoothing pass that sliding-window-averages analyticHeight over `designGradeWindow` with tile-boundary extension.
    - BEHAVIOR: design grade is smoother than raw terrain (fine ±0.5 m noise suppressed) while preserving coarse grade.
  </acceptance_criteria>
  <done>road-carve.js pure functions exist; carve params in ranger.js; design-grade smoothing in road.js, memoized and boundary-stable.</done>
</task>

<task type="auto">
  <name>Task 2: Build carveTable per chunk; apply identical carve in _flushPendingQueue, analyticHeight, sampleHeight, and Worker; sync WORKER_SOURCE + terrain-worker.js</name>
  <files>src/terrain.js, src/terrain-worker.js, test/test-road-carve.html</files>
  <read_first>
    - src/terrain.js (analyticHeight ~486, analyticNormal ~505, sampleHeight ~528-563, _flushPendingQueue Y loop ~690, _updateChunkRing postMessage ~643, workerParams allowlist ~444, WORKER_SOURCE height loop, SYNC RULE ~25/281)
    - src/terrain-worker.js (onmessage ~194-230, byte-mirror of WORKER_SOURCE)
    - src/road-carve.js (created in Task 1 — the pure functions to inline)
    - src/road.js (queryNearest, design grade from Task 1; this._tiles per-tile splines)
    - .planning/phases/09-road-surface/09-RESEARCH.md (§carveBlend Integration, §Carve applied identically in Worker, Pitfalls 1/2/3/5)
    - .planning/phases/09-road-surface/09-PATTERNS.md (§terrain.js, §terrain-worker.js, §Float32Array Transferable Pattern)
  </read_first>
  <action>
    Add a carve-table builder (main thread — has road access). For a chunk `(cx,cz)` build a `Float32Array(GRID_SAMPLES*GRID_SAMPLES*2)` storing per-vertex `[blendW, gradeY]`: for each grid vertex world XZ, query the nearest road (`queryNearest`), compute lateral `dist` from centerline; if `dist > halfWidth + shoulderWidth + (fill toe extent)` set blendW=0 (gradeY=raw, far from road). Otherwise compute `delta = designGradeY - rawTerrainHeight` (positive=fill, negative=cut), clamp fill `delta = min(delta, roadFillHeight)`; set gradeY = designGradeY and blendW per `carveBlend`'s shoulder ramp. Fill toe distance = `halfWidth + shoulderWidth + delta/tan(atan(1/roadFillSlope))`; cut uses `roadCutSlope`. Store this on the chunk as `chunk.carveData` — NEVER write into `chunk.heights` (Pitfall 1).

    Wire the carve hook IDENTICALLY at all four sites (all call the same `carveBlend` logic from road-carve.js):
    1. terrain.js `_flushPendingQueue` Y loop: `const blendW = chunk.carveData ? chunk.carveData[i*2] : 0; const gradeY = chunk.carveData ? chunk.carveData[i*2+1] : raw; pos.setY(i, raw + blendW*(gradeY-raw))`.
    2. terrain.js `analyticHeight`: after `raw`, `if (this._roadCarve) { const c = this._roadCarve.sampleWorld(wx,wz); if (c && c.blendW > 1e-6) return raw + c.blendW*(c.gradeY-raw) }`.
    3. terrain.js `sampleHeight`: same carve blend appended after the bilinear raw (height-agreement path).
    4. terrain.js WORKER_SOURCE height loop AND terrain-worker.js byte-mirror: destructure `carveTable` from the generate message and apply the same `raw + blendW*(gradeY-raw)` per vertex.
    `analyticNormal` is UNCHANGED — it central-differences analyticHeight and picks up the carve automatically (crown/camber tilt arrives via gradeY).

    Send the carve table to the Worker as a Transferable: extend `_updateChunkRing` postMessage to `postMessage({type:'generate',cx,cz,key,carveTable}, [carveTable.buffer])` (Pitfall 5 — never omit the Transferable; build a fresh table per chunk since the buffer is consumed on transfer). On road re-stream, trigger `rebuildAllChunksFromWorker()` (research Open Q1 recommendation — simplest path for P9). Add the SYNC RULE comment at the WORKER_SOURCE carve site and the terrain-worker.js carve site (T-07-03-SYNC style). Edit WORKER_SOURCE and terrain-worker.js in the SAME commit and keep them byte-identical.

    Extend `test/test-road-carve.html` with the EXIT-GATE assertions: (a) height-agreement — at 5+ on-road world positions assert `|analyticHeight(wx,wz) - mesh-vertex-Y(wx,wz)| < 0.01 m` AND `|sampleHeight - analyticHeight| < 0.01 m`; (b) carve-continuity — step sampleHeight at ~0.25 m increments across the carve boundary (centerline → past shoulder/toe) and assert no single-step delta exceeds a continuity threshold that would indicate a degenerate vertical seam (allow steep-but-continuous faces: e.g. assert max adjacent-step delta < (max plausible slope)*step + epsilon, NOT a flat threshold). Label e.g. `SURF-05 carve-continuity (x,z): maxStep=__m`.
  </action>
  <verify>
    <automated>grep -c "carveData" src/terrain.js && grep -c "carveTable" src/terrain-worker.js && diff <(grep -A40 'CARVE SYNC' src/terrain.js 2>/dev/null) /dev/null >/dev/null 2>&1; grep -c "carve" test/test-road-carve.html</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `chunk.carveData` (Float32Array, 2/vertex) is built and read; `chunk.heights` is never overwritten with blended values (Pitfall 1).
    - SOURCE: the carve blend (`raw + blendW*(gradeY-raw)`) appears at all four sites: _flushPendingQueue, analyticHeight, sampleHeight, and the Worker height loop — all using road-carve.js logic.
    - SOURCE: `_updateChunkRing` posts `carveTable` with `[carveTable.buffer]` Transferable; WORKER_SOURCE and terrain-worker.js carve sections are byte-identical with SYNC RULE comments.
    - HARNESS-OUTPUT (EXIT GATE): test-road-carve.html prints `PASS` for height-agreement (< 0.01 m at 5+ on-road positions, including sampleHeight==analyticHeight) and `PASS` for carve-continuity (no degenerate vertical seam; steep-but-continuous faces allowed).
  </acceptance_criteria>
  <done>Carve applied identically in mesh build and physics; height-agreement and carve-continuity exit gates PASS; Worker mirror byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main thread → terrain Worker (postMessage) | carveTable Float32Array crosses; structured-clone/Transferable boundary, local-only data |
| debug slider values → carve params | Local numeric slider values, bounded in debug.js |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Denial of Service | carveTable postMessage | mitigate | Always pass `[carveTable.buffer]` as Transferable; build a fresh table per chunk after transfer (Pitfall 5). Prevents silent DataCloneError dropping carve. |
| T-09-03 | Tampering | analyticHeight carve hook | accept | Carve values are own-math Float32Array, no external input. No injection vector — local numeric data only. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | This phase installs NO packages (hand-rolled + existing Three.js r184); RESEARCH Package Legitimacy Audit confirms zero packages. No legitimacy checkpoint required. |
</threat_model>

<verification>
- Open test/test-road-carve.html: height-agreement and carve-continuity assertions PASS; window-invariance (Plan 01) still PASS.
- In-game (manual): drive onto a road over rolling ground — truck rides the raised causeway, no float/sink; drive onto a cut section — truck sits in the notch, no launch from a vertical seam.
</verification>

<success_criteria>
- SURF-04: physics analyticHeight/analyticNormal carry the carved road height + normal.
- SURF-05: cut-and-fill carve, applied identically in Worker mesh build and physics sampler; continuous by construction.
- Exit gates (height-agreement, carve-continuity) green in test-road-carve.html.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-02-SUMMARY.md` when done.
</output>
