---
phase: 09-road-surface
plan: 18
type: execute
wave: 1
depends_on: []
files_modified:
  - src/road.js
  - data/ranger.js
  - src/debug.js
  - test/spline-continuity.mjs
autonomous: true
requirements: [SURF-01, SURF-03]
must_haves:
  # D0 — true minimum-turn-radius arc-fillet replaces the coil-excision _limitCurvature
  truths:
    - "D0/SURF-01: hairpin spline arms separate by ≥ road width — the wide ribbon inner edge never folds onto itself"
    - "D0/SURF-03: minRadius is floored at ≥ roadHalfWidth + clearance so the ribbon cannot self-overlap by construction"
    - "D0: the canonical routed run is unchanged by the post-pass except where the implied corner radius < minRadius (arc-fillet rounds it)"
  artifacts:
    - path: "src/road.js"
      provides: "_filletMinRadius arc-fillet post-pass replacing _limitCurvature at the _streamNetwork call site"
      contains: "_filletMinRadius"
    - path: "data/ranger.js"
      provides: "roadMinTurnRadius floored to ≥ roadHalfWidth + clearance"
    - path: "test/spline-continuity.mjs"
      provides: "hairpin gate fixture — ribbon inner edge does not fold"
      contains: "hairpin"
  key_links:
    - from: "src/road.js _streamNetwork (line ~1166)"
      to: "_filletMinRadius(pts, minRadius)"
      via: "post-pass call replacing _limitCurvature"
      pattern: "_filletMinRadius\\("
---

<objective>
Replace the coil-excision `_limitCurvature` post-pass (road.js:944) with a TRUE minimum-turn-radius arc-fillet pass (`_filletMinRadius`). Where the implied corner radius between two legs is below minRadius, insert a circular arc of radius = minRadius tangent to both legs so the centerline is rounded to the radius (not excised). Floor minRadius at ≥ roadHalfWidth + clearance so the wide ribbon's inner edge cannot fold onto itself. Re-check Max Grade after filleting.

Purpose: This is the upstream root (D0). Rounding hairpins so arms separate by > road width de-risks D3 (footprint overlap) and D4 (near-coincident arms) downstream — they become safety nets rather than primary fixes.
Output: `_filletMinRadius` in road.js, floored `roadMinTurnRadius` in ranger.js, a hairpin gate fixture in the headless harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-road-surface/09-REFACTOR-DESIGN.md
@.planning/phases/09-road-surface/09-PATTERNS.md
@.planning/phases/09-road-surface/09-CONTEXT.md

<interfaces>
<!-- Extracted from src/road.js — executor uses these directly, no exploration needed. -->

_streamNetwork post-pass chain (road.js:1164-1166) — the ONLY call site to replace:
  pts = this._removeLoops(pts)
  pts = this._removeSelfCrossings(pts)
  pts = this._limitCurvature(pts, this._proto.params.minTurnRadius)   // ← replace this line

_limitCurvature(pts, minRadius) (road.js:944-982) — coil-EXCISION only: scrutinizes spans
  that turn ≥150° (TURN_MIN) and DELETES the coil (line 972: p = [...slice(0,i+1), ...slice(j+1)]).
  It NEVER rounds a turn to minRadius. This is the bug.

_lerpVec3(a, b, t) (road.js:55-61) — module-scope Vector3 lerp helper (reuse for arc point construction).

this._proto.params.minTurnRadius — seeded from RANGER_PARAMS.roadMinTurnRadius via _refreshParams.
this._proto.params is refreshed every re-stream (road.js:1092 _refreshParams()).

Determinism contract (road.js:1163): "Post-passes run on the FULL canonical run — not a windowed slice (D-16)."
_filletMinRadius MUST be a pure function of (pts, minRadius) — no Math.random, no session state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace _limitCurvature with _filletMinRadius arc-fillet pass</name>
  <read_first>
    - src/road.js lines 944-982 (_limitCurvature — the function being replaced)
    - src/road.js lines 1163-1167 (the _streamNetwork post-pass call site)
    - src/road.js lines 55-61 (_lerpVec3 helper to reuse)
    - .planning/phases/09-road-surface/09-REFACTOR-DESIGN.md D0 section (lines 24-29)
  </read_first>
  <action>
    Add a new method `_filletMinRadius(pts, minRadius)` to the RoadSystem class (place it adjacent to `_limitCurvature`, road.js ~944). Algorithm: walk the polyline; at each interior vertex compute the turn angle between incoming and outgoing leg directions; the implied corner radius is `legLen / tan(turnAngle/2)` (or use the standard fillet relation tangentLen = R / tan((π-turnAngle)/2)). Where the implied radius < minRadius, replace the sharp corner with a circular arc of radius = minRadius tangent to both legs: trim each leg back by the fillet tangent length and insert arc sample points (use `_lerpVec3` / direct THREE.Vector3 construction; interpolate Y linearly across the inserted arc so the routed grade stays continuous). At a 180° hairpin this yields a semicircle of radius minRadius → arms separated by ~2·minRadius. Keep the method a PURE function of (pts, minRadius) — deterministic, window-invariant (run on the canonical run like the other post-passes). At the _streamNetwork call site (road.js:1166) replace `this._limitCurvature(pts, this._proto.params.minTurnRadius)` with `this._filletMinRadius(pts, this._proto.params.minTurnRadius)`. Keep `_limitCurvature` defined but unreferenced (back-compat / harness), OR delete it if no other call site exists — grep first. After filleting, the routed grade may shift; add an inline note that Max Grade is re-evaluated by the existing soft-cost router on the next re-stream (the router already balances grade per D-09; the fillet does not bypass it). Do NOT modify `_protoConnect`/`_protoAnchor` (Phase 8 routing geometry stays).
  </action>
  <verify>
    <automated>node --check src/road.js && grep -v '^[[:space:]]*[/*]' src/road.js | grep -c '_filletMinRadius(' | grep -qv '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/road.js` exits 0.
    - `grep -c '_filletMinRadius' src/road.js` ≥ 2 (definition + call site).
    - The _streamNetwork post-pass at road.js ~1166 calls `_filletMinRadius`, NOT `_limitCurvature` (verify: `grep -n '_limitCurvature(pts' src/road.js` returns 0 call sites in _streamNetwork, only a possible definition).
    - `git diff --stat src/terrain-worker.js` shows the worker UNTOUCHED (this task does not touch carve).
  </acceptance_criteria>
  <done>The arc-fillet pass rounds tight corners to minRadius (instead of excising coils); _streamNetwork uses it.</done>
</task>

<task type="auto">
  <name>Task 2: Floor roadMinTurnRadius ≥ roadHalfWidth + clearance + slider range</name>
  <read_first>
    - data/ranger.js lines 223-228 (roadMinTurnRadius param + comment)
    - data/ranger.js lines 246, 358 (roadHalfWidth=5, roadClearanceMargin=0.5)
    - src/debug.js line 219 (Min Turn Radius slider)
    - src/road.js _refreshParams (where minTurnRadius is seeded from params)
  </read_first>
  <action>
    In data/ranger.js update the `roadMinTurnRadius` comment block and default per D0: the floor must be ≥ roadHalfWidth + clearance ("a little wider than the road" — user). With roadHalfWidth 5 + clearance, set a sane default that produces non-folding hairpins (the design wants arms separated by > road width; pick a default such as 12–15 m and document the floor rationale referencing D0). In src/road.js `_refreshParams` (or wherever `this._proto.params.minTurnRadius` is assigned from `this._params.roadMinTurnRadius`), enforce the floor: `minTurnRadius = max(roadMinTurnRadius, roadHalfWidth + clearanceMargin + ε)` so a slider drag below the floor still cannot fold the ribbon. In src/debug.js raise the "Min Turn Radius (m)" slider lower bound to the floor value (currently `20, 300, 5` at line 219) so the UI cannot select a folding radius. Update the param comment to reference D0 (arc-fillet, not coil-excision) instead of the stale QUAL-01 excision wording.
  </action>
  <verify>
    <automated>node --check data/ranger.js && node --check src/road.js && node --check src/debug.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node --check data/ranger.js`, `node --check src/road.js`, `node --check src/debug.js` all exit 0.
    - road.js seeds `this._proto.params.minTurnRadius` with a floor of ≥ roadHalfWidth + clearanceMargin (grep confirms a `Math.max(` clamp around the minTurnRadius assignment).
    - The ranger.js comment for roadMinTurnRadius references D0 / arc-fillet (not "excised"/"coil").
  </acceptance_criteria>
  <done>minRadius is floored ≥ roadHalfWidth + clearance in code and the slider cannot select below it.</done>
</task>

<task type="auto">
  <name>Task 3: Hairpin gate fixture — ribbon inner edge does not fold</name>
  <read_first>
    - test/spline-continuity.mjs lines 235-330 (FIXTURES array, role:'gate' structure)
    - test/spline-continuity.mjs lines 388-470 (computeMetrics — how a fixture spline is built + measured)
    - test/spline-continuity.mjs lines 19, 40-41 (imports + CAMBER_STRENGTH/halfWidth constants)
    - .planning/phases/09-road-surface/09-REFACTOR-DESIGN.md verification section (line 68)
  </read_first>
  <action>
    Extend test/spline-continuity.mjs with a `role:'gate'` fixture named `hairpin` (a 180° switchback path whose nominal corner radius BEFORE filleting is below minRadius). Add a metric proving the ribbon inner edge does not fold: sweep a ribbon cross-section of half-width = roadHalfWidth along the fixture spline (offset each centerline point by ±halfWidth along the XZ right-normal) and assert the inner-edge polyline never reverses arc direction / never self-intersects (the inner edge stays monotonically advancing — no negative inner-edge segment relative to the centerline tangent). Apply the same arc-fillet rounding the production `_filletMinRadius` does (vendor a small fillet helper in the harness, OR construct the fixture points already filleted to minRadius and assert the swept inner edge has zero folds). Gate threshold: inner-edge fold count == 0. Keep it a vendored, zero-install Node ESM fixture consistent with the existing harness (import only from ../src/road-carve.js). All existing gate fixtures must still exit 0.
  </action>
  <verify>
    <automated>node test/spline-continuity.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node test/spline-continuity.mjs` exits 0 with the new `hairpin` gate fixture PASS in the printed gate table.
    - The hairpin fixture measures inner-edge fold count == 0 (printed in output).
    - All pre-existing gate fixtures (gentle-baseline, tile-seam-mismatch, physics-sampling-continuity) still PASS.
    - `node --check test/spline-continuity.mjs` exits 0.
  </acceptance_criteria>
  <done>The headless harness has a hairpin gate fixture proving the filleted spline's ribbon inner edge does not fold; exit 0.</done>
</task>

</tasks>

<verification>
- `node --check` clean on all four files.
- `node test/spline-continuity.mjs` exits 0 with the hairpin gate fixture PASS.
- `git diff --stat src/terrain-worker.js` empty (D0 does not touch carve).
- Phase 8 routing geometry untouched: `git diff src/road.js` shows no edits to `_protoConnect` / `_protoAnchor`.
</verification>

<success_criteria>
- _filletMinRadius rounds tight corners to minRadius (arc-fillet), replacing coil-excision at the canonical-run post-pass.
- roadMinTurnRadius floored ≥ roadHalfWidth + clearance, enforced in code + slider range.
- Hairpin harness gate fixture proves the swept ribbon inner edge does not fold (exit 0).
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-18-SUMMARY.md` when done.
</output>
