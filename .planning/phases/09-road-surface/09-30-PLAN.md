---
phase: 09-road-surface
plan: 30
type: execute
wave: 5
depends_on: ["09-27", "09-28", "09-29"]
files_modified:
  - test/spline-continuity.mjs
autonomous: false
requirements: [SURF-01, SURF-03, SURF-04, SURF-05]
must_haves:
  truths:
    - "A headless seam-grade gate samples grade on both sides of a tile boundary (carve path AND profile path) and asserts |ΔY| < ε"
    - "A headless ribbon-edge-weld gate sweeps a sharp-cornered polyline across a slice seam and asserts ±halfWidth edges are C0 (no gap > ε, no inverted quads)"
    - "A headless camber-across-run gate joins two adjacent runs sharing a boundary mid-curve and asserts |Δcamber| ≤ slew rate across the run boundary"
    - "All existing harness gates stay green; node test/spline-continuity.mjs exits 0"
    - "One combined in-sim human-verify pass confirms BUG-14/12/10 closed (seed 7 + Coarse Amp 150 + switchbacks + sharp corners)"
  artifacts:
    - path: "test/spline-continuity.mjs"
      provides: "seam-grade, ribbon-edge-weld, camber-across-run gate fixtures + runners"
      contains: "seam-grade"
  key_links:
    - from: "new gates"
      to: "the seams BUG-14/12/10 exploited"
      via: "fixtures that bite the actual fragment boundaries, not just spline endpoints"
      pattern: "role: 'gate'"
---

<objective>
Verification. Add three headless gates to `test/spline-continuity.mjs` that
actually bite the seams (the current tile-seam gate only checks spline endpoints),
then run ONE combined in-sim human-verify pass.

- seam-grade gate — sample grade on BOTH sides of a tile boundary via the
  carve-table path AND the profile path; assert |ΔY| < ε across the seam.
- ribbon-edge-weld gate — sweep a sharp-cornered polyline across a slice seam;
  assert the ±halfWidth edge polylines are C0 (no gap > ε, no inverted quads).
- camber-across-run gate — two adjacent runs sharing a boundary mid-curve; assert
  |Δcamber| ≤ slew rate across the run boundary.

All existing gates stay green. Final task is the combined browser checkpoint.

Purpose: prove the refactor closed BUG-14/12/10 and guard against regression.
Output: 3 new role:'gate' fixtures + a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-road-surface/09-CONTINUOUS-PROFILE-DESIGN.md
@.planning/phases/09-road-surface/09-PATTERNS.md

<interfaces>
<!-- The headless harness contract. From test/spline-continuity.mjs. -->

- The harness imports ONLY `signedCurvature, crownProfile, filletMinRadius, circumradiusXZ` from
  ../src/road-carve.js (lines 19). It must NOT import three, simplex-noise, road.js, or terrain.js
  (zero-install constraint, lines 9–10). New gates must reconstruct profile sampling HEADLESSLY using
  the vendored centripetal Catmull-Rom (`catmullRomCurve`, ~69) + a small headless mirror of
  runProfile's arc-walk + binary-search interp (mirror _buildRunProfile/_interpolateCamber logic in
  the harness — do not import road.js).
- FIXTURES array (~238) with `role: 'gate'` | `'demo-expected-fail'`; custom-runner fixtures use a
  mode flag (`physicsMode`, `hairpinMode`, `switchbackMode`, `twoArmsMode`, `camberRateMode`) and a
  dedicated runner block (the FIXTURES loop ~1045 skips them; mode arrays handled separately).
- Existing thresholds: MAX_BOUNDARY_MISMATCH_M=0.05, MAX_PHYSICS_DY_M=0.05, MAX_DCAMBER_DEG_PER_M=2.0,
  CAMBER_STRENGTH=200, CAMBER_CLAMP_RAD=6°, slew = roadCamberRate(1.5)°/m.
- `process.exit(allGatesPassed ? 0 : 1)` (~1410); `allGatesPassed` flips false on any gate metric fail.
- Camber math reference for the headless mirror: signedCurvature(t0x,t0z,t1x,t1z,ds) → raw =
  CAMBER_STRENGTH·kappa, clamp ±6°, then forward-march slew limit ≤ slewRate·Δs (mirror road.js
  _buildCamberProfile exactly, including the cross-run seed from 09-29 for the camber gate).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: seam-grade gate + ribbon-edge-weld gate</name>
  <files>test/spline-continuity.mjs</files>
  <read_first>
    - test/spline-continuity.mjs lines 1–120 (header constraints, thresholds, vendored catmullRomCurve)
    - test/spline-continuity.mjs lines 235–400 (FIXTURES + role/mode pattern; camber-rate custom-runner fixture as a template)
    - test/spline-continuity.mjs lines 1040–1410 (runner dispatch, mode-array handling, allGatesPassed, process.exit)
    - src/road.js _buildRunProfile/runProfile (09-25) and _sampleCarveWorld grade source (09-27) — to mirror the grade-by-arcS read headlessly
    - .planning/phases/09-road-surface/09-CONTINUOUS-PROFILE-DESIGN.md (Verification, lines 73–78)
  </read_first>
  <action>
    Add two new role:'gate' fixtures with dedicated runners (follow the existing custom-mode pattern,
    e.g. add `seamGradeMode` and `ribbonWeldMode` flags + mode arrays + runner blocks, mirroring the
    camberRateMode wiring so the FIXTURES loop skips them and a dedicated block computes their metric
    and flips allGatesPassed on fail).

    (a) seam-grade gate: build a graded polyline that crosses a tile/chunk boundary (a 64 m seam — use
    a polyline whose arc passes a multiple of CHUNK_SIZE=64). Headlessly mirror runProfile's
    arc-indexed gradeY (cumulative XZ arc + per-sample Y). Sample the grade at the seam arcS from
    BOTH sides (the value the carve path would read = grade at sampleArcS just below the seam, and the
    value the profile path would read just above) and assert |ΔY| < MAX_BOUNDARY_MISMATCH_M (reuse the
    0.05 m threshold, or tighten to ε=0.01 and add a named constant). The gate must FAIL on a
    discrete-nearest grade (the old BUG-14 behavior) and PASS on the continuous-profile grade — so the
    fixture data must include a non-trivial vertical step between adjacent control points near the seam.

    (b) ribbon-edge-weld gate: build a sharp-cornered polyline (reuse/adapt the hairpin or a tight
    90°-ish corner) split into two slices at a seam at the corner. Headlessly sweep the ±halfWidth edge
    polylines using the cross-section frame derived from the CONTINUOUS tangent (mirror sweepRibbon's
    runProfile.tx/tz frame, right=(tz,0,−tx), ROAD_HALF_WIDTH=5). Assert: (i) the left-edge and
    right-edge vertices at the shared seam are C0 across the two slices (gap < ε), and (ii) no inverted
    quads — check the signed area / winding of the boundary quad does not flip sign (cross-product of
    the two edge vectors keeps consistent sign). FAIL on per-slice independent tangent frames (old
    BUG-12), PASS on the shared continuous-tangent frame.

    Set both fixtures role:'gate'. Keep zero-install: NO new imports beyond road-carve.js.
  </action>
  <verify>
    <automated>node --check test/spline-continuity.mjs && node test/spline-continuity.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check test/spline-continuity.mjs` exits 0
    - `node test/spline-continuity.mjs` exits 0 with the two new gates printed and PASS, and ALL existing gates still PASS
    - The harness imports NOTHING beyond road-carve.js (verify: no new `import` of three/simplex-noise/road.js/terrain.js)
    - seam-grade gate asserts |ΔY| across a 64 m seam below threshold; ribbon-edge-weld gate asserts edge C0 + no inverted quads
    - Both fixtures have `role: 'gate'`
  </acceptance_criteria>
  <done>seam-grade and ribbon-edge-weld gates added as role:'gate', biting the actual seams; full harness exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: camber-across-run gate</name>
  <files>test/spline-continuity.mjs</files>
  <read_first>
    - test/spline-continuity.mjs lines 388+ (camber-rate gate + camberRateMode runner — the closest template)
    - src/road.js _buildCamberProfile + the 09-29 cross-run seed (mirror its math, including the boundary seed)
    - .planning/phases/09-road-surface/09-CONTINUOUS-PROFILE-DESIGN.md (camber-across-run gate, line 77)
  </read_first>
  <action>
    Add a third role:'gate' fixture (e.g. `camberRunMode`) that builds TWO adjacent runs sharing a
    boundary NODE mid-curve (run A ends, run B starts at the same XZ point, both inside a continuous
    curve so the true banking is non-zero at the boundary). Headlessly mirror _buildCamberProfile for
    each run INCLUDING the 09-29 cross-run seed: run B's start camber seeded from run A's end camber.
    Compute the camber on each side of the shared boundary and assert |Δcamber| ≤ slew rate × Δs
    across the run boundary (use slewRate = roadCamberRate 1.5°/m; the assertion proves banking does
    NOT reset to 0 at the boundary). The gate must FAIL on the old forced-zero run start (Δcamber
    would equal the full mid-curve bank ~ up to 6°, far above one step of slew) and PASS on the
    stitched profile. role:'gate'.
  </action>
  <verify>
    <automated>node --check test/spline-continuity.mjs && node test/spline-continuity.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check test/spline-continuity.mjs` exits 0
    - `node test/spline-continuity.mjs` exits 0 with the camber-across-run gate printed PASS, all existing + the two new (Task 1) gates PASS
    - The gate joins two runs at a shared boundary mid-curve and asserts |Δcamber| ≤ slew rate (not a 0-reset)
    - Fixture has `role: 'gate'`; no new imports beyond road-carve.js
  </acceptance_criteria>
  <done>camber-across-run gate added as role:'gate'; banking continuity across the run boundary asserted; harness exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Combined in-sim verification pass (BUG-14 / BUG-12 / BUG-10)</name>
  <what-built>
    The full continuous-profile refactor: P0 RoadRunProfile + runProfile (09-25), P1 byArc/sampleRoadAt
    (09-26), P2 grade-Y from profile in physics + carve (09-27, BUG-14), P3 ribbon frame from
    continuous tangent (09-28, BUG-12), P4 cross-run camber stitch (09-29, BUG-10), plus three new
    headless gates (this plan). `node test/spline-continuity.mjs` exits 0 with all gates green and
    `git diff --stat src/terrain-worker.js` is empty.
  </what-built>
  <how-to-verify>
    Serve the app locally (e.g. `npx serve .`) and open index.html. Run the three scenarios from the
    design's Verification (lines 79):
    1. BUG-14 (grade seam / launch): set seed 7, Coarse Amp 150 (the BUG-14 custom params), and drive
       across the tile seam behind spawn. EXPECT: no teleport, no launch, the dirt foundation has NO
       vertical step across the seam.
    2. BUG-10 (cross-run camber): drive the switchbacks / a winding N-S climb that spans multiple
       E-W rows. EXPECT: banking EASES across run boundaries (no snap-to-flat at the boundary between
       switchback arms / row bands).
    3. BUG-12 (ribbon tears): drive the sharp corners / hairpins. EXPECT: the ribbon is sealed — no
       gap or overlapping flap at sharp apexes.
    Also confirm: physics height == visible ribbon height on-road (truck rides the surface, no sink /
    no float) — height-agreement.
  </how-to-verify>
  <resume-signal>Type "approved" if all three scenarios pass + height-agreement holds, or describe the residual issues (which bug, where) so a follow-up gap-closure round can be planned.</resume-signal>
</task>

</tasks>

<verification>
- `node --check test/spline-continuity.mjs` exits 0
- `node test/spline-continuity.mjs` exits 0 — existing gates + the 3 new gates (seam-grade, ribbon-edge-weld, camber-across-run) all PASS
- Harness still imports only road-carve.js (zero-install)
- `git diff --stat src/terrain-worker.js` empty
- Human-verify checkpoint: BUG-14/12/10 confirmed closed in-sim + height-agreement holds
</verification>

<success_criteria>
Three seam-biting headless gates guard BUG-14/12/10 and stay green alongside the
existing gates; the combined in-sim pass confirms no seam launch, eased cross-run
banking, sealed sharp-corner ribbon, and height-agreement.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-30-SUMMARY.md` when done.
</output>
