---
phase: 09-road-surface
plan: 27
type: execute
wave: 3
depends_on: ["09-25", "09-26"]
files_modified:
  - src/road.js
  - src/terrain.js
autonomous: true
requirements: [SURF-04, SURF-05]
must_haves:
  truths:
    - "Physics contact height reads grade Y from runProfile(arcS, runKey).gradeY, not nr.point.y"
    - "Carve table grade Y reads from runProfile(sampleArcS[i], runKey).gradeY, not the nearest-discrete sample ny"
    - "Grade Y is C0 across tile/chunk seams — no vertical step, no teleport, no upward-step launch (BUG-14 closed)"
    - "Mesh grade == physics grade == carve grade at the same on-road position (height-agreement)"
  artifacts:
    - path: "src/road.js"
      provides: "_sampleCarveWorld designY from runProfile gradeY"
      contains: "runProfile"
    - path: "src/terrain.js"
      provides: "_buildCarveTable roadY from runProfile gradeY"
      contains: "runProfile"
  key_links:
    - from: "_sampleCarveWorld + _buildCarveTable"
      to: "runProfile gradeY by arcS"
      via: "both read the SAME profile by the SAME arcS → shared seam vertices match"
      pattern: "runProfile"
    # Closes BUG-14; height-agreement by construction
---

<objective>
P2 — BUG-14: grade Y from the continuous profile (physics + carve). This is
literally the camber fix applied to grade Y, and it is the highest-severity /
lowest-risk fix (it mirrors the proven camber path).

- Physics `road.js _sampleCarveWorld`: set `designY = runProfile(nr.arcS,
  runKey).gradeY` instead of `nr.point.y`. `nr.arcS` is continuous across a
  slice-switch (both sides give the boundary arcS), so contact height is C0 → no
  teleport, no upward-step penetration → no launch.
- Carve `terrain.js _buildCarveTable`: set `roadY = runProfile(sampleArcS[i],
  runKey).gradeY` instead of the nearest-discrete-sample `ny = samples[bi+1]`.
  Both chunks read the same profile by `arcS` → shared chunk-boundary vertices
  match → the foundation step is gone, AND mesh grade == physics grade.

Purpose: kill the seam grade-step that launches/teleports the truck.
Output: both grade-Y reads sourced from runProfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-road-surface/09-CONTINUOUS-PROFILE-DESIGN.md
@.planning/phases/09-road-surface/09-PATTERNS.md

<interfaces>
<!-- P0 sampler (09-25) + the two grade-Y read sites to retarget. From src/road.js and src/terrain.js. -->

P0 (09-25): `runProfile(arcS, runKey) → { gradeY, camberRad, tx, tz }`.

Physics read site — src/road.js _sampleCarveWorld (~1634):
- `const nr = this.queryNearest(wx, wz, maxExt)` → `nr.arcS` (run-global), `nr.runKey`, `nr.camberSign`.
- `let designY = nr.point.y`  ← line ~1670, THE BUG-14 physics read to replace.
- Crown/camber fold already uses `nr.arcS` + `this.camberProfile(centerlineArcS, nr.runKey)` (~1684–1694).

Carve read site — src/terrain.js _buildCarveTable (~848+):
- `const { pts: samples, sampleArcS, sampleRunKeys, sampleCamberSign } =
   this._roadSystem.collectChunkSplinePoints(...)` (~898).
- `const ny = samples[bi + 1]`  ← line ~962, THE BUG-14 carve read to replace.
- `const arcS = sampleArcS[biIdx]`, `const runKey = sampleRunKeys[biIdx]` (~988–989) already available.
- `let carveTargetY = ny + crownY + tiltY − clearanceMargin` (~997).
- carveTable stores gradeY PRE-AMPLITUDE: `carveTable layout [blendW, gradeY_preamp]` (terrain.js ~234,
  worker ~184); `gradeY_preamp = designGradeY / terrainAmplitude`. Mind the amplitude convention —
  runProfile.gradeY is the world-space routed centerline Y (post-amplitude, same units as samples[bi+1]).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Physics — _sampleCarveWorld reads gradeY from runProfile by nr.arcS</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js lines 1634–1714 (_sampleCarveWorld — full function; the designY = nr.point.y line and crown/camber fold)
    - src/road.js the runProfile signature (09-25) + camberProfile (~1927) for the cache-coherence pattern
    - .planning/phases/09-road-surface/09-CONTINUOUS-PROFILE-DESIGN.md (P2 physics bullet, line 45)
  </read_first>
  <action>
    In `_sampleCarveWorld`, replace `let designY = nr.point.y` with
    `let designY = this.runProfile(nr.arcS ?? 0, nr.runKey ?? '').gradeY`. Keep the existing crown +
    camber + pothole fold-in below it UNCHANGED — it already keys on `nr.arcS` / `nr.runKey`, so it
    stays consistent. Do NOT re-apply the BUG-13 fill cap (the design forbids capping physics grade).
    Update the comment block at the former `nr.point.y` line to state the new source: the run-global
    continuous profile gradeY (C0 across slice-switches) replaces the per-slice spline-evaluated
    `nr.point.y` whose slice-switch was the BUG-14 launch source. Confirm `nr.arcS` is the run-global
    arc (it is — BUG-10 fix). Allocation note: runProfile allocates one object per call; this is the
    hot physics path — if you added an optional out-object parameter to runProfile in 09-25, pass a
    module-scope reusable object here; otherwise leave the single allocation (acceptable for now,
    flag in SUMMARY as the perf-cache chokepoint per design line 70).
  </action>
  <verify>
    <automated>node --check src/road.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/road.js` exits 0
    - `grep -n "nr.point.y" src/road.js` no longer matches inside _sampleCarveWorld (the designY assignment now uses runProfile gradeY)
    - `grep -n "runProfile" src/road.js` shows the new read inside _sampleCarveWorld
    - The crown/camber/pothole fold below designY is unchanged and still keys on nr.arcS / nr.runKey
    - No BUG-13 fill cap reintroduced (no `Math.min(... fillHeight ...)` clamp added to designY)
    - `git diff --stat src/terrain-worker.js` empty
  </acceptance_criteria>
  <done>Physics contact height comes from runProfile gradeY by run-global arcS — C0 across slice seams.</done>
</task>

<task type="auto">
  <name>Task 2: Carve — _buildCarveTable roadY from runProfile by sampleArcS[i]</name>
  <files>src/terrain.js</files>
  <read_first>
    - src/terrain.js lines 848–1030 (_buildCarveTable — the per-vertex carve loop, the ny = samples[bi+1] read, carveTargetY formula)
    - src/terrain.js lines 184–203 + 234–252 (carveTable layout: blendW + gradeY_preamp; sampleCarve blend)
    - src/road.js the runProfile signature (09-25) — note gradeY is world-space (post-amplitude)
    - .planning/phases/09-road-surface/09-CONTINUOUS-PROFILE-DESIGN.md (P2 carve bullet, line 46; Worker constraint line 83)
  </read_first>
  <action>
    In `_buildCarveTable`, replace `const ny = samples[bi + 1]` (the nearest-discrete grade Y) with a
    read from the profile: `const roadY = this._roadSystem.runProfile(arcS, runKey).gradeY`, using the
    `arcS = sampleArcS[biIdx]` and `runKey = sampleRunKeys[biIdx]` already computed just below the old
    `ny` line — so reorder so `arcS`/`runKey` are available before the grade read, then use `roadY`
    everywhere `ny` was used (the `carveTargetY = ny + crownY + tiltY − clearanceMargin` formula and
    any exterior-sample fallback `enyExt` analog). Apply the SAME substitution to the exterior-sample
    branch (the `extBi`/`extIdx` path ~1013) so both interior and exterior carve grade reads come from
    the profile. CRITICAL amplitude convention: the carveTable stores `gradeY_preamp = designGradeY /
    terrainAmplitude`. `runProfile.gradeY` is world-space (post-amplitude), exactly like the old
    `samples[bi+1]` it replaces, so when writing the carveTable entry keep the SAME pre-amplitude
    division that the old code applied to `ny` — do not double-divide or skip the division. Verify by
    inspection that the value written to `carveTable[... +1]` has identical amplitude handling to
    before. This is MAIN-THREAD only — terrain-worker.js must stay byte-identical (it only reads RAW
    heights + the carveTable; it does not call runProfile).
  </action>
  <verify>
    <automated>node --check src/terrain.js && git diff --stat src/terrain-worker.js | grep -q . && echo "WORKER CHANGED - FAIL" || echo "worker byte-identical OK"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/terrain.js` exits 0
    - `git diff --stat src/terrain-worker.js` produces NO output (byte-identical — the verify command prints "worker byte-identical OK")
    - `grep -n "samples\[bi + 1\]" src/terrain.js` no longer used for the grade read (replaced by runProfile gradeY); same for the exterior `enyExt` analog
    - `grep -n "runProfile" src/terrain.js` shows the carve grade read
    - carveTable gradeY entry retains the SAME pre-amplitude (`/ terrainAmplitude`) handling as the old ny path — verify by inspection
    - Both interior and exterior carve grade reads use runProfile gradeY
  </acceptance_criteria>
  <done>Carve grade Y comes from runProfile gradeY by run-global arcS; chunk-boundary vertices match; worker byte-identical.</done>
</task>

</tasks>

<verification>
- `node --check src/road.js` and `node --check src/terrain.js` exit 0
- `git diff --stat src/terrain-worker.js` shows NO change (RAW heights only; carve+profile are main-thread)
- `node test/spline-continuity.mjs` — existing gates green (the new seam-grade gate lands in 09-30)
- Both grade-Y read sites (physics nr.point.y, carve ny) now read runProfile.gradeY by arcS
</verification>

<success_criteria>
BUG-14 closed: grade Y is C0 across tile/chunk seams from both the physics and
carve paths, sourced from the one continuous profile by run-global arcS. No
seam teleport, no upward-step launch; mesh grade == physics grade == carve grade
(height-agreement). terrain-worker.js byte-identical.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-27-SUMMARY.md` when done.
</output>
