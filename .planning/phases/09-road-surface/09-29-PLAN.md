---
phase: 09-road-surface
plan: 29
type: execute
wave: 4
depends_on: ["09-25", "09-27"]
files_modified:
  - src/road.js
autonomous: true
requirements: [SURF-03]
must_haves:
  truths:
    - "_buildCamberProfile no longer forces camber=0 at every run start"
    - "Each run's start camber is seeded from the adjacent run's end camber across the shared boundary node"
    - "A run beginning mid-curve is not zeroed; banking eases across N-S / winding-climb run boundaries (BUG-10 closed)"
    - "Camber profile is generation-invalidated (D1) like the rest; runProfile.camberRad reflects the stitched profile"
  artifacts:
    - path: "src/road.js"
      provides: "Cross-run camber stitch in _buildCamberProfile (run-adjacency boundary seed)"
      contains: "_buildCamberProfile"
  key_links:
    - from: "_buildCamberProfile run start"
      to: "adjacent run end camber across shared node"
      via: "run-adjacency lookup / carried boundary camber instead of forcing 0"
      pattern: "_buildCamberProfile"
    # Closes BUG-10; generation-invalidated like camberProfile
---

<objective>
P4 — BUG-10: stitch camber across run boundaries. `_buildCamberProfile` forces
`rawCamber[0] = camberRad[0] = 0` at every run start, so banking jumps to 0
wherever `runKey` changes (N-S / winding climbs spanning multiple E-W rows).

Seed each run's start camber from the adjacent run's end value (build
run-adjacency, or carry a boundary camber across the shared node) instead of
forcing 0; do NOT zero a run that begins mid-curve. The profile is
generation-invalidated (D1) like the rest.

Purpose: banking eases across run boundaries instead of resetting to flat.
Output: cross-run camber seed in _buildCamberProfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-road-surface/09-CONTINUOUS-PROFILE-DESIGN.md
@.planning/phases/09-road-surface/09-PATTERNS.md

<interfaces>
<!-- The camber build to stitch + the network it walks. From src/road.js. -->

- `_buildCamberProfile(runKey)` (road.js ~1849): walks `this._network.get(runKey).points`, sets
  `arcPos[0]=0`, `rawCamber[0]=0` (~1866), then forward-marches a slew-rate-limited `camberRad[]`
  with `camberRad[0]=rawCamber[0]` (~1900). THE `=0` at the run start is the BUG-10 reset.
  Slew limiter: `slewRateRadPerM = (roadCamberRate ?? 1.5)·π/180`; `MAX_CAMBER = 6°`.
- `this._network`: Map "<mz>:<runIndex>" → { points: THREE.Vector3[] } (~978/1276). Runs share
  boundary NODES where a winding climb spans multiple E-W rows (different runKey, adjacent endpoints).
- `_segXZ` (road.js ~306, currently a closure / promoted helper) — XZ segment intersection, model for
  detecting shared/adjacent run endpoints if a run-adjacency map is needed.
- D1 generation: `this._generation` (~880); camberProfile cache is generation-keyed (~1934).
- `_buildRunProfile` / runProfile (09-25) reuse `_buildCamberProfile`'s camber — so stitching here
  flows into runProfile.camberRad automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run-adjacency boundary camber seed (replace the forced-zero run start)</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js lines 1849–1913 (_buildCamberProfile — full; the rawCamber[0]=0 and camberRad[0]=seed)
    - src/road.js lines 1915–1947 (camberProfile cache + generation invalidation — the seed must be generation-coherent)
    - src/road.js lines 306–317 + the _network key format ~978 (shared-node detection between runs)
    - .planning/phases/09-road-surface/09-INSIM-VERIFY.md (open issue 1 — the exact symptom: banking resets to 0 at each run boundary)
    - .planning/phases/09-road-surface/09-CONTINUOUS-PROFILE-DESIGN.md (P4 section, lines 52–53)
  </read_first>
  <action>
    Replace the forced `rawCamber[0]=0` / `camberRad[0]=0` run-start in `_buildCamberProfile` with a
    boundary seed. Add a helper (e.g. `_runStartCamber(runKey)` or a `_buildRunAdjacency()` memo
    cleared on re-stream like `_slicedFrom`/`_junctions`) that finds the run whose END node coincides
    (within an XZ epsilon, reuse the `_segXZ`-style endpoint comparison) with THIS run's START node,
    and returns that adjacent run's END camber (the last `camberRad` value of the adjacent run's
    profile). Seed `camberRad[0]` (and the curvature-based `rawCamber[0]`) from that value instead of
    0, so banking carries across the shared boundary node. If a run truly has no predecessor (a free
    run start, not mid-curve), fall back to the curvature-derived value at sample 0 rather than a hard
    0 — "do not zero a run that begins mid-curve" (design line 53). GUARD AGAINST RECURSION: building
    run A's profile to read its end-camber while building run B (which A may also reference) can loop;
    break the cycle (e.g. compute end-camber from the adjacent run's RAW curvature-derived last value
    without recursively seeding it, or memoize partial results, or only seed from already-built cached
    profiles). Keep it pure/deterministic (D-16) and generation-invalidated: clear any adjacency memo
    in the same `invalidateCache`/re-stream site that clears `_camberProfileCache`/`_tiles`, and key it
    on `this._generation`. The slew limiter and ±6° clamp stay unchanged downstream of the new seed.
  </action>
  <verify>
    <automated>node --check src/road.js && node test/spline-continuity.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --check src/road.js` exits 0
    - `node test/spline-continuity.mjs` exits 0 (existing gates green; the camber-across-run gate lands in 09-30)
    - `grep -n "rawCamber\[0\] = 0" src/road.js` no longer forces a hard zero unconditionally (replaced by the boundary seed / curvature fallback)
    - A run-adjacency seed path exists and reads the adjacent run's END camber across the shared node
    - The adjacency memo (if added) is cleared at the SAME re-stream/invalidateCache site as _camberProfileCache and is generation-keyed
    - No infinite recursion when building two mutually-adjacent runs (verify the cycle-break by inspection)
    - `git diff --stat src/terrain-worker.js` empty
  </acceptance_criteria>
  <done>Run-start camber is seeded from the adjacent run's end camber across the shared node; no forced zero mid-curve; generation-invalidated.</done>
</task>

</tasks>

<verification>
- `node --check src/road.js` exits 0
- `node test/spline-continuity.mjs` — existing gates green (camber-across-run gate added in 09-30)
- `_buildCamberProfile` no longer hard-zeros the run start; adjacency seed present + generation-invalidated
- `git diff --stat src/terrain-worker.js` empty
</verification>

<success_criteria>
BUG-10 closed: camber carries across run boundaries — banking eases across the
shared node of a winding/N-S climb instead of resetting to flat. Stitched camber
flows into runProfile.camberRad so physics, carve, and ribbon all read the
continuous banking.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-29-SUMMARY.md` when done.
</output>
