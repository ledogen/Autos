---
phase: 09-road-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/road.js
  - test/test-road-carve.html
  - test/test-road-mesh.html
  - test/road-test-harness.js
autonomous: true
requirements: [SURF-07]
must_haves:
  truths:
    - "The road geometry at a fixed world coordinate is identical across two different streaming-window centers"
    - "test/test-road-carve.html opens and runs a window-invariance assertion that PASSes"
    - "test/test-road-mesh.html opens with the r184 importmap and renders without console error"
  artifacts:
    - path: "src/road.js"
      provides: "Canonical per-run derivation in _streamNetwork; module-scope _segXZ"
      contains: "_segXZ"
    - path: "test/test-road-carve.html"
      provides: "Exit-gate harness scaffold + window-invariance assertion"
      contains: "importmap"
    - path: "test/test-road-mesh.html"
      provides: "Smoke harness scaffold"
      contains: "importmap"
  key_links:
    - from: "test/test-road-carve.html"
      to: "src/road.js RoadSystem"
      via: "import + two-window stream comparison"
      pattern: "_streamNetwork|queryNearest"
  # D-16 — BUG-08 window-invariant splines (junctions/ribbon require stable geometry)
---

<objective>
Fix BUG-08: make the road network a pure function of (seed, world coords, params) — window-invariant — so road geometry does not pop/reshape as the streaming window shifts. This is a prerequisite (Wave 0) for stable junctions (SURF-07) and the ribbon mesh. Also scaffold the two phase test harnesses with the verbatim r184 importmap so all later waves have a known-good harness to extend.

Purpose: Junctions and the ribbon mesh are built from `this._network`; if a fixed world location yields different splines depending on the view center, every downstream mesh rebuilds/shifts under the truck while driving (research Pitfall 4).
Output: Window-invariant `_streamNetwork`, module-scope `_segXZ`, and two scaffolded harnesses (`test-road-carve.html`, `test-road-mesh.html`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-road-surface/09-CONTEXT.md
@.planning/phases/09-road-surface/09-RESEARCH.md
@.planning/phases/09-road-surface/09-PATTERNS.md
@.planning/todos/pending/bug-road-restream-pop.md
@src/road.js
@test/test-road.html
@test/road-test-harness.js

<interfaces>
<!-- Key road.js internals the executor must work with (verified by grep). -->
- `_streamNetwork(center)` — re-streams when center moves > PROTO_REGEN_MOVE = 96 m; clears this._network and rebuilds over [center ± radius]. Root cause: post-passes operate on the windowed polyline.
- `this._network` — Map keyed `"${mz}:${runIndex}"` → { points: Vector3[] }. Single source of truth for slicing.
- `_removeSelfCrossings(pts)` (road.js ~715) — contains a closure `_segXZ(ax,az,bx,bz,cx,cz,dx,dz)` returning {x,z} crossing point or null. PROMOTE to module scope (needed by Wave 3 junction detection).
- `PROTO_COVER_D=36, PROTO_COVER_DOT=0.93, PROTO_COVER_FRAC=0.5` — same-direction overlap suppression, rebuilt per-stream (window-dependent → must operate on canonical runs).
- `_sliceNetwork()` + `this._tiles` + `this._slicedFrom` — memoization identity guard pattern (road.js ~1026): `if (this._slicedFrom === this._network && this._tiles.size > 0) return`.
- Test harness pattern: see test/test-road.html (importmap lines 7-15, assert helper, IIFE test functions) and test/road-test-harness.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote _segXZ to module scope and add canonical per-run derivation in _streamNetwork</name>
  <files>src/road.js</files>
  <read_first>
    - src/road.js (full — focus _streamNetwork ~893-999, _removeSelfCrossings/_segXZ ~715-748, PROTO_* constants ~77-88, this._network keying ~978, _sliceNetwork ~1026)
    - .planning/todos/pending/bug-road-restream-pop.md (root cause + fix directions)
    - .planning/phases/09-road-surface/09-RESEARCH.md (§BUG-08 Window-Invariant Splines)
    - .planning/phases/09-road-surface/09-PATTERNS.md (src/road.js section — _segXZ verbatim, purity comment, _slicedFrom memoization)
  </read_first>
  <action>
    Lift the `_segXZ` closure out of `_removeSelfCrossings` into a module-scope function `_segXZ(ax,az,bx,bz,cx,cz,dx,dz)` (keep `_removeSelfCrossings` calling the same function; no behavior change there). It must keep the exact denom guard `Math.abs(denom) < 1e-10` and the open-interval test `t,u in (1e-6, 1-1e-6)`. This is the shared crossing primitive Wave 3 (junctions) will reuse.

    Make `_streamNetwork` window-invariant per D-16: each macro-row run MUST be built from a canonical, center-INDEPENDENT anchor extent rather than `[center ± radius]`. Derive the canonical band from a stable world-aligned grid: for the row's macro index `mz`, build the anchor chain from `mx_min` to `mx_max` computed as `floor(center.x / PROTO_ANCHOR_SPACING) ± CANONICAL_HALF_WIDTH` where CANONICAL_HALF_WIDTH (add as a module const, ~ the streaming radius rounded up to anchor spacing) is large enough that no rendered road falls outside it. Run loop/curvature/overlap post-passes (`_removeLoops`, `_removeSelfCrossings`, `_limitCurvature`, PROTO_COVER_* suppression) over the FULL canonical run, NOT the window slice. Build PROTO_COVER_* suppression by registering canonical runs in deterministic `mz` order (rebuild the spatial hash from completed canonical runs each stream — never accumulate across re-streams). Add a memo so a re-stream whose center still maps to the same canonical band keyed `mz:mx0:mx1` is a no-op (early return, like the `_slicedFrom` identity guard). Add a purity comment: "Pure function of (worldSeed, world coords, params) — window-invariant (D-16); identical world regions yield identical runs across re-streams."

    Do NOT modify routing cost (D-09 weights), the router's use of pure `coarseHeight`, or `_protoConnect`/`_protoAnchor` (already anchor-key cached). Do NOT bake any carve here.
  </action>
  <verify>
    <automated>grep -n "^function _segXZ\|^const _segXZ" src/road.js && grep -v '^[[:space:]]*//' src/road.js | grep -c "CANONICAL_HALF_WIDTH"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `_segXZ` is declared at module scope in road.js (not only inside `_removeSelfCrossings`); `_removeSelfCrossings` still references it.
    - SOURCE: `_streamNetwork` builds each run over a canonical extent derived from a world-aligned grid (CANONICAL_HALF_WIDTH const present) and runs post-passes over that canonical run, not the window slice.
    - SOURCE: a `mz:mx0:mx1`-keyed memo (or equivalent) short-circuits re-streams within the same canonical band.
    - BEHAVIOR: existing Phase-8 same-center determinism is preserved (no change to D-09 weights, no routing edits).
  </acceptance_criteria>
  <done>_segXZ is module-scope and reused; _streamNetwork produces window-invariant runs keyed by canonical extent; routing untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold test/test-road-carve.html (with window-invariance assertion) and test/test-road-mesh.html</name>
  <files>test/test-road-carve.html, test/test-road-mesh.html, test/road-test-harness.js</files>
  <read_first>
    - test/test-road.html (importmap lines 7-15, style block, assert pattern, IIFE tests)
    - test/road-test-harness.js (assert helper to import or inline)
    - .planning/phases/09-road-surface/09-PATTERNS.md (§Test Harness Pattern — verbatim importmap, assert template)
    - .planning/phases/09-road-surface/09-VALIDATION.md (Wave 0 Requirements)
    - src/road.js (RoadSystem constructor + _streamNetwork + queryNearest for the invariance test)
  </read_first>
  <action>
    Create `test/test-road-carve.html` copying the importmap block from test-road.html VERBATIM (three@0.184.0, three/addons/, simplex-noise@4.0.3 — do NOT change CDN versions), the monospace dark style block, and the "Open DevTools console to see PASS/FAIL" header. Use the `assert(label, cond)` helper (import from `test/road-test-harness.js` or inline the same `console.log((cond?'PASS':'FAIL')+' '+label)` form). Add the BUG-08 window-invariance assertion now (the carve/continuity assertions are added in Plan 02): construct a RoadSystem with a fixed seed; call `_streamNetwork` at center A (e.g. (0,0,0)), record `queryNearest` point+tangent at 5 fixed world XZ positions that lie on a road; call `_streamNetwork` at a far-shifted center B (e.g. (300,0,0), past PROTO_REGEN_MOVE); re-query the SAME 5 world positions; assert each point delta < 0.01 m and tangent delta < 0.001. Label e.g. `BUG-08 window-invariance (x,z): dP=__m < 0.01`.

    Create `test/test-road-mesh.html` as a SMOKE scaffold: same verbatim importmap + style + header. Set up a minimal THREE scene (Scene, PerspectiveCamera, WebGLRenderer, a light) and a placeholder comment block `// SURF-01/02/07 mesh smoke assertions added in Plan 03/04/05`. It must open and render without throwing (no road mesh yet — later plans add the ribbon/junction/material assertions). Assert at least `harness scene constructed` PASS so the file is non-empty and runnable.
  </action>
  <verify>
    <automated>grep -c "three@0.184.0" test/test-road-carve.html && grep -c "three@0.184.0" test/test-road-mesh.html && grep -c "window-invariance" test/test-road-carve.html</automated>
  </verify>
  <acceptance_criteria>
    - HARNESS: `test/test-road-carve.html` exists, contains the verbatim r184 importmap, and includes a window-invariance assertion comparing queryNearest at 5 fixed world positions across two stream centers with tolerance < 0.01 m.
    - HARNESS: `test/test-road-mesh.html` exists with the verbatim importmap and a minimal renderable scene scaffold.
    - HARNESS-OUTPUT: opened in a browser, test-road-carve.html prints `PASS BUG-08 window-invariance ...` for all 5 positions (manual browser open + console read).
    - SOURCE: neither harness modifies CDN versions from test-road.html.
  </acceptance_criteria>
  <done>Both harness files exist with verbatim importmaps; carve harness's window-invariance assertion passes on the Task-1 fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/free-cam input → road stream center | Local-only numeric camera position; no external/untrusted data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | (none applicable) | RoadSystem._streamNetwork | accept | Browser-only, single-origin, static-hosted game. No server, no network I/O, no auth, no persistence, no user-supplied data beyond local keyboard/camera position. No injection/auth/data-exposure vector applies. Honest threat surface: none high-severity. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | This phase installs NO packages (all hand-rolled + existing Three.js r184). RESEARCH Package Legitimacy Audit confirms zero packages. No legitimacy checkpoint required. |
</threat_model>

<verification>
- Open test/test-road-carve.html: console shows PASS for all 5 window-invariance assertions.
- Open test/test-road-mesh.html: scene constructs, no console error.
- In-game (manual): fly the free-cam past a road; the centerline does not visibly reshape as new map streams in.
</verification>

<success_criteria>
- BUG-08 fixed: road geometry at fixed world coords is window-invariant (harness PASS + in-game no reshape).
- `_segXZ` available at module scope for Wave 3 junction detection.
- Both phase harnesses scaffolded with verbatim r184 importmap.
</success_criteria>

<output>
Create `.planning/phases/09-road-surface/09-01-SUMMARY.md` when done.
</output>
