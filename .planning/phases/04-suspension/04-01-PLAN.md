---
phase: 04-suspension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scenarios/m4-02-asymmetric-bump.json
  - scenarios/m4-04-static-vs-braking.json
  - scenarios/m4-05-wheel-lift-ramp.json
  - scenarios/m4-06-bump-response.json
autonomous: true
requirements:
  - M4-02
  - M4-04
  - M4-05
  - M4-06
tags:
  - suspension
  - validation
  - scenarios

must_haves:
  truths:
    - "Four scenarios/m4-*.json files exist and parse as valid JSON"
    - "Each file conforms to scenarios/straight-60kph.json schema (position, velocity, quaternion, angularVelocity)"
    - "Loader (logger.js openInitialCondition) accepts each file without console error"
    - "Each scenario description documents the assertion it supports (M4-02 / M4-04 / M4-05 / M4-06)"
  artifacts:
    - path: "scenarios/m4-02-asymmetric-bump.json"
      provides: "IC for per-wheel independence verification (M4-02)"
    - path: "scenarios/m4-04-static-vs-braking.json"
      provides: "IC for longitudinal load transfer verification (M4-04, M4-07)"
    - path: "scenarios/m4-05-wheel-lift-ramp.json"
      provides: "IC for airborne-wheel verification (M4-05, D-14)"
    - path: "scenarios/m4-06-bump-response.json"
      provides: "IC for unsprung-mass dynamics verification (M4-06)"
  key_links:
    - from: "scenarios/m4-*.json"
      to: "src/logger.js openInitialCondition"
      via: "JSON parse + position/velocity/quaternion/angularVelocity keys"
      pattern: "position.*velocity.*quaternion"
---

<objective>
Author the four Wave 0 validation scenarios referenced by VALIDATION.md before any physics changes ship. These JSON initial-condition files are the assertion vehicle for M4-02 (per-wheel independence), M4-04 (dynamic Fz / load transfer), M4-05 (airborne wheel), and M4-06 (unsprung-mass dynamics). Without them the Phase 4 sampling map (VALIDATION.md §Per-Task Verification Map) has no commands to run.

Purpose: Wave 0 closes the test-infrastructure gap so the core suspension implementation (Plan 02) can be sampled against concrete scenarios per VALIDATION.md sampling rate.

Output: Four JSON scenario files in scenarios/, each loadable via the existing logger.js openInitialCondition path (Ctrl+I).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/04-suspension/04-CONTEXT.md
@.planning/phases/04-suspension/04-RESEARCH.md
@.planning/phases/04-suspension/04-PATTERNS.md
@.planning/phases/04-suspension/04-VALIDATION.md
@scenarios/straight-60kph.json
@src/logger.js

<interfaces>
<!-- Loader contract (logger.js openInitialCondition, lines ~130-160):
     Only these top-level keys are honored; unknown keys are silently ignored.
     Implication: scenarios CANNOT seed hubY/hubVy state — they settle from spawn equilibrium.

     Schema (per scenarios/straight-60kph.json):
     {
       "description": "<string>",
       "position":        { "x": number, "y": number, "z": number },
       "velocity":        { "x": number, "y": number, "z": number },
       "quaternion":      { "x": number, "y": number, "z": number, "w": number },
       "angularVelocity": { "x": number, "y": number, "z": number }
     }

     Spawn Y reference: position.y ≈ 0.55 m (cgHeight) for a settled car.
     Coordinate convention (per CLAUDE.md / GLOSSARY): Y up, forward = -Z, right = +X.
     Wheel index: 0=FL, 1=FR, 2=RL, 3=RR.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author four Wave 0 scenario JSON files</name>
  <files>scenarios/m4-02-asymmetric-bump.json, scenarios/m4-04-static-vs-braking.json, scenarios/m4-05-wheel-lift-ramp.json, scenarios/m4-06-bump-response.json</files>
  <read_first>
    - scenarios/straight-60kph.json (canonical schema)
    - src/logger.js (openInitialCondition — confirm honored keys: position, velocity, quaternion, angularVelocity; unknown keys silently ignored)
    - .planning/phases/04-suspension/04-VALIDATION.md §Wave 0 Requirements (assertions each scenario supports)
    - .planning/phases/04-suspension/04-RESEARCH.md §Validation Architecture / Wave 0 Gaps
  </read_first>
  <action>
    Create four JSON files matching the straight-60kph.json schema exactly (same five top-level keys: description, position, velocity, quaternion, angularVelocity). Add NO new keys — the loader silently drops them and they create false confidence.

    Each scenario seeds the initial driving state that, when combined with manual driver input (or planned future scripted-input support), exercises the named assertion:

    - `m4-02-asymmetric-bump.json` (M4-02 per-wheel hub independence): car at rest at spawn (position.y ≈ 0.55, all velocities zero, identity quaternion). description field documents: "Static start; drive forward at low speed over a single-side bump prop placed by main.js terrain. Asserts hubY[0,2] (left) move while hubY[1,3] (right) stay flat (or vice versa). M4-02."
    - `m4-04-static-vs-braking.json` (M4-04 dynamic Fz / load transfer): car moving forward at 16.667 m/s (60 km/h) in straight line, identity quaternion. description: "Drive forward 1s, full brake 1s. Asserts front fl_fz/fr_fz rise and rear rl_fz/rr_fz fall during brake; sum ≈ mass·g ± 5%. M4-04, M4-07 longitudinal."
    - `m4-05-wheel-lift-ramp.json` (M4-05 airborne wheel): car moving forward at 13.889 m/s (50 km/h) approaching the existing asymmetric ramp prop in main.js. description: "Drive forward into ramp asymmetrically; one wheel leaves ground. Asserts that wheel's fz=0 AND its fy=0 / sa=0 during airborne window per D-14. M4-05."
    - `m4-06-bump-response.json` (M4-06 unsprung-mass dynamics): car at rest. description: "Static start; drive at low speed (~5 m/s) over single bump prop. Asserts hub vertical oscillation settles within 1.5s — characterizes ζ≈0.4 damping target. M4-06."

    All four use position.y in the range 0.50–0.60 (settled spawn height — exact value chosen by Plan 02's static-equilibrium calculation will retune naturally on load). Identity quaternion {0,0,0,1} for all four. angularVelocity all zeros.

    Pretty-print JSON with 2-space indent matching straight-60kph.json formatting. No trailing commas.

    Per D-13 (Phase 2 docs convention) the description string is the only place the assertion intent is recorded — make it precise so a future reader can recover the test purpose without consulting VALIDATION.md.
  </action>
  <verify>
    <automated>for f in scenarios/m4-02-asymmetric-bump.json scenarios/m4-04-static-vs-braking.json scenarios/m4-05-wheel-lift-ramp.json scenarios/m4-06-bump-response.json; do node -e "const d=require('./'+process.argv[1]); for (const k of ['description','position','velocity','quaternion','angularVelocity']) if (!(k in d)) { console.error('MISSING',k,'in',process.argv[1]); process.exit(1) }; if (typeof d.quaternion.w !== 'number') { console.error('NO quaternion.w in',process.argv[1]); process.exit(1) }" "$f" || exit 1; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist at the paths in `files_modified`.
    - Each parses as valid JSON via `node -e "require('./scenarios/m4-XX.json')"` with no SyntaxError.
    - Each contains exactly the five keys: description, position, velocity, quaternion, angularVelocity (no extra keys; see PATTERNS.md §scenarios/m4-*.json Loader contract — extras silently ignored, so they are misleading noise).
    - quaternion.w === 1 in all four (identity orientation).
    - position.y is between 0.50 and 0.60 inclusive (settled spawn band).
    - description field is non-empty and names the requirement ID (M4-02 / M4-04 / M4-05 / M4-06) it supports.
    - angularVelocity is { "x": 0, "y": 0, "z": 0 } in all four.
  </acceptance_criteria>
  <done>Four scenarios/m4-*.json files exist, validate as JSON, contain the canonical 5-key schema, and document their respective M4-XX assertion in the description.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user file load → openInitialCondition | User-supplied JSON crosses into vehicleState via Ctrl+I; existing parser already JSON.parse-wrapped with try/catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (self-inflicted) | scenarios/m4-*.json | accept | No external attack surface — scenarios are author-controlled; loader already gates JSON parse with try/catch (logger.js openInitialCondition). Validation tooling (Task 1 verify command) catches malformed JSON at author time. |
</threat_model>

<verification>
- Verify command in Task 1 passes (all four files valid JSON with required keys).
- Manual sanity: open one scenario via Ctrl+I in the browser; vehicleState.position/velocity/quaternion update without console error. (Not required for plan completion since the spawn IC works the same as straight-60kph.json.)
</verification>

<success_criteria>
- Four `scenarios/m4-*.json` files committed
- All parse and conform to the loader schema documented in PATTERNS.md
- VALIDATION.md sampling map (rows pointing at these filenames) can now execute
- Plan 02 has scenarios to replay during its sampling cadence
</success_criteria>

<output>
After completion, create `.planning/phases/04-suspension/04-01-SUMMARY.md` per the standard summary template.
</output>
