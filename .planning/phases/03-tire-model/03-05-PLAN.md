---
phase: 03-tire-model
plan: 05
type: execute
wave: 4
depends_on: [03-02]
files_modified:
  - src/physics.js
autonomous: true
gap_closure: true
requirements: [M3-01, M3-02, M3-03, M3-05]
must_haves:
  truths:
    - "Throttling from backward roll uses maxReverseTorque (800 N·m), not maxBrakeTorque (3000 N·m)"
    - "wheelOmega is integrated every physics step for every wheel, including airborne wheels"
    - "Lateral tire force opposes lateral hub velocity (resists slip), not amplifies it"
  artifacts:
    - path: "src/physics.js"
      provides: "Corrected getDriveTorque reverse path; omega integrator hoisted out of contacts loop; lateral force sign corrected at application site"
      contains: "params.maxReverseTorque"
  key_links:
    - from: "src/physics.js getDriveTorque"
      to: "params.maxReverseTorque"
      via: "throttle-while-rolling-backward branch returns maxReverseTorque"
      pattern: "throttle \\* params\\.maxReverseTorque"
    - from: "src/physics.js stepPhysics per-wheel loop"
      to: "vehicleState.wheelOmega[i]"
      via: "omega integration runs unconditionally per wheel (outside contacts for-of)"
      pattern: "wheelOmega\\[i\\] ="
---

<objective>
Close three physics correctness issues from REVIEW.md so the Pacejka tire model behaves honestly in all states:

- CR-02: throttling out of a backward roll currently applies `maxBrakeTorque` (3000 N·m) — 3.75× the intended `maxReverseTorque` (800 N·m). Causes a violent jerk.
- CR-03: the omega integrator is nested inside the per-contact `for (const ... of contacts)` loop. Airborne wheels never update `wheelOmega`, producing stale slip ratios on landing and no in-air spin-up from drivetrain torque.
- WR-02: `slipAngle = atan2(latVel, longVelAbs)` is positive when the hub slides toward `+wheelRight`, but `Flat` is then applied via `+wheelRight` — pushing the car further into the slide instead of opposing it. Negate at the application site (option A from REVIEW.md) to preserve tire.js sign convention.

Purpose: Make the friction circle, slip ratio, and lateral-force feedback loop work correctly across grounded, airborne, forward, and reverse states — prerequisite for the human drift/wheelspin tests in VERIFICATION.md.
Output: src/physics.js with three targeted edits and no other behavioral changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-tire-model/03-VERIFICATION.md
@.planning/phases/03-tire-model/03-REVIEW.md
@.planning/phases/03-tire-model/03-02-SUMMARY.md
@src/physics.js
@data/ranger.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CR-02 — reverse-throttle uses maxReverseTorque</name>
  <files>src/physics.js</files>
  <read_first>
    - src/physics.js lines 30-51 (getDriveTorque)
    - data/ranger.js (confirm maxReverseTorque value is 800 and the "Bug 4 fix" comment exists)
    - .planning/phases/03-tire-model/03-REVIEW.md CR-02 (lines 75-89)
  </read_first>
  <action>
    In `src/physics.js` `getDriveTorque`, modify the branch at the current line 43. The condition `if (longVel < -DRIVE_DEAD_ZONE)` inside the `throttle > 0` block currently returns `vehicleState.throttle * params.maxBrakeTorque` for rear wheels. Change it to return `vehicleState.throttle * params.maxReverseTorque` for rear wheels. Front wheels still return 0 in this branch.

    Do not touch the brake branch (lines 46-48) which already uses `maxReverseTorque` correctly for the brake-while-reversing path. Do not change DRIVE_DEAD_ZONE. Do not refactor the function shape — keep the single-line conditional style consistent with the surrounding code.
  </action>
  <verify>
    <automated>grep -n 'throttle \* params\.maxReverseTorque' src/physics.js | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one occurrence of `throttle * params.maxReverseTorque` appears in src/physics.js (in getDriveTorque)
    - The string `throttle * params.maxBrakeTorque` no longer appears anywhere in src/physics.js (`grep -c 'throttle \* params\.maxBrakeTorque' src/physics.js` returns 0)
    - The brake path `-vehicleState.brake * params.maxBrakeTorque` is preserved
  </acceptance_criteria>
  <done>
    Pressing W while the truck is rolling backward at >0.5 m/s applies 800 N·m drive torque to rear wheels (per RANGER_PARAMS.maxReverseTorque), not 3000 N·m.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-03 — hoist omega integrator out of contacts loop</name>
  <files>src/physics.js</files>
  <read_first>
    - src/physics.js lines 110-217 (per-wheel loop and the nested contacts loop)
    - .planning/phases/03-tire-model/03-REVIEW.md CR-03 (lines 93-115)
    - .planning/phases/03-tire-model/03-02-SUMMARY.md (omega integrator design intent)
  </read_first>
  <action>
    Restructure the per-wheel block (currently lines 110-217) so the omega integrator runs once per wheel per step, regardless of contact count. Critical constraints preserved from existing code: the omega integrator must use friction-circle-SCALED `Flong` (not raw) as the road reaction, AND it must use the same `OMEGA_EPSILON = 0.5` free-rolling clamp behavior. When the wheel is airborne the road reaction is zero.

    Concrete shape (apply as careful edits, do not rewrite the whole file):

    1. Inside the per-wheel loop body, BEFORE the `const contacts = queryContacts(...)` call, introduce `let lastScaledFlong = 0` to record the friction-circle-scaled longitudinal force from the last processed contact (initially zero for the airborne case).

    2. Inside the `for (const { normal, depth, contactPoint } of contacts)` loop, KEEP the existing normal-force application, lateral/longitudinal force computation, and friction-circle scaling block. After the friction-circle scaling assigns final `Flat`/`Flong`, set `lastScaledFlong = Flong`. Then REMOVE the omega integrator block (currently lines 187-202 — `OMEGA_EPSILON`, `wheelInertia`, `driveTorque`, `brakeTorque`, `roadReactionTorque`, `vehicleSpd`, `wheelSurfaceSpd`, the free-rolling clamp, and the Euler-integration assignment) from inside the contacts loop. The wheelForce add and wheelDebug writes stay inside the contacts loop unchanged.

    3. AFTER the `for (const ... of contacts)` block closes — still inside the `for (let i = 0; i < 4; i++)` per-wheel loop — re-emit the omega integrator block exactly once per wheel:
       - `const OMEGA_EPSILON = 0.5`
       - `const wheelInertia = params.wheelInertia || 1.22`
       - `const driveTorque = getDriveTorque(i, vehicleState, params)`
       - `const brakeTorque = getBrakeTorque(i, vehicleState, params)`
       - `const roadReactionTorque = lastScaledFlong * params.wheelRadius`
       - `const vehicleSpd = Math.abs(params._longitudinalVelocity || 0)`
       - `const wheelSurfaceSpd = Math.abs((vehicleState.wheelOmega?.[i] ?? 0) * params.wheelRadius)`
       - When `vehicleSpd + wheelSurfaceSpd < OMEGA_EPSILON` AND `contacts.length > 0`: free-rolling clamp `vehicleState.wheelOmega[i] = (params._longitudinalVelocity || 0) / params.wheelRadius` (preserves grounded-rest clamp behavior).
       - Otherwise: Euler-integrate `vehicleState.wheelOmega[i] = (vehicleState.wheelOmega?.[i] ?? 0) + (driveTorque - roadReactionTorque - brakeTorque) / wheelInertia * dt`. This branch now also handles airborne — `lastScaledFlong` is 0 so road reaction is 0 and drive/brake torque spin the wheel freely.

    4. After the omega integrator, update the wheelDebug `omega` field one last time so airborne wheels still log their evolving omega: if `vehicleState.wheelDebug` exists, set `vehicleState.wheelDebug[i].omega = vehicleState.wheelOmega[i]`. (Do not overwrite the other fn/fy/sa/c fields — only omega.)

    Constraint #7 (critical ordering) is preserved: frictionBudget check still precedes the road-reaction usage, since `lastScaledFlong` is only written AFTER friction-circle scaling.
  </action>
  <verify>
    <automated>node -e "
const { stepPhysics } = await import('./src/physics.js');
const RP = (await import('./data/ranger.js')).RANGER_PARAMS;
const THREE = await import('three');
const vs = {
  position: new THREE.Vector3(0, 100, 0),  // airborne
  velocity: new THREE.Vector3(0, 0, -5),
  quaternion: new THREE.Quaternion(),
  angularVelocity: new THREE.Vector3(),
  throttle: 1.0, brake: 0, handbrake: false, steerAngle: 0,
  wheelOmega: [0, 0, 0, 0],
  wheelDebug: [{},{},{},{}],
};
const before = [...vs.wheelOmega];
stepPhysics(vs, RP, 0.01, () => []);  // no contacts = airborne
const after = vs.wheelOmega;
const rearSpunUp = after[2] > before[2] && after[3] > before[3];
if (!rearSpunUp) { console.error('FAIL: rear wheels did not spin under airborne throttle', after); process.exit(1); }
console.log('OK: airborne rear wheels spin up', after);
"</automated>
  </verify>
  <acceptance_criteria>
    - The string `for (const { normal, depth, contactPoint } of contacts)` still exists exactly once in src/physics.js
    - The omega-integrator assignment `vehicleState.wheelOmega[i] =` appears OUTSIDE the contacts for-of (verify by line-numbers: integrator line > line of the contacts loop's closing `}`)
    - `grep -c 'wheelInertia' src/physics.js` returns 1 (no duplicated integrator)
    - The smoke test above prints `OK: airborne rear wheels spin up` and exits 0 — airborne wheels now spin under throttle
    - The friction-circle scaling block remains INSIDE the contacts loop and runs BEFORE `lastScaledFlong = Flong` is set
  </acceptance_criteria>
  <done>
    Airborne wheels spin up under drive torque; on landing, slip ratio is computed from a physically meaningful wheelOmega rather than a stale pre-jump value.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix WR-02 — negate lateral force at application site</name>
  <files>src/physics.js</files>
  <read_first>
    - src/physics.js lines 172-207 (slipAngle, computeLateralForce call, wheelForce assembly)
    - src/tire.js (confirm computeLateralForce return-sign convention from M3-03)
    - .planning/phases/03-tire-model/03-REVIEW.md WR-02 (lines 139-160)
  </read_first>
  <action>
    In src/physics.js, change the line currently reading `wheelForce.addScaledVector(wheelRight, Flat)` (near line 205) to `wheelForce.addScaledVector(wheelRight, -Flat)`. This negates at the application site and preserves the tire.js return-sign convention (positive slipAngle → positive `Flat`).

    Add a one-line comment immediately above the changed line explaining the negation: lateral grip force opposes the lateral hub velocity (resists the slide), so a positive `Flat` from `computeLateralForce(positive slipAngle)` must be applied along `-wheelRight`. Reference WR-02 in the comment so future reviewers find the rationale.

    Do not change the slipAngle calculation, the friction-circle scaling, or the wheelDebug fy write (the debug field still records the raw `Flat` magnitude). Do not change tire.js.
  </action>
  <verify>
    <automated>grep -n 'addScaledVector(wheelRight, -Flat)' src/physics.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'addScaledVector(wheelRight, -Flat)' src/physics.js` returns 1
    - `grep -c 'addScaledVector(wheelRight, Flat)'  src/physics.js` returns 0 (no unnegated form left)
    - A comment referencing WR-02 appears within 3 lines above the negated application
    - tire.js is unchanged (`git diff --name-only src/tire.js` is empty)
  </acceptance_criteria>
  <done>
    Lateral force opposes lateral hub velocity. Sliding right produces a leftward grip force, enabling correct understeer/oversteer feedback in the human drift test.
  </done>
</task>

</tasks>

<verification>
- All three grep/smoke-test gates from the per-task `<verify>` pass
- `node -e "import('./src/physics.js')"` imports without syntax errors
- A second smoke test should confirm grounded behavior is unchanged: at rest with throttle=0 the free-rolling clamp still sets wheelOmega from longitudinal velocity
</verification>

<success_criteria>
- VERIFICATION.md re-verify shows CR-02, CR-03, WR-02 closed
- Human verification steps 1 (drift) and 2 (wheelspin) from VERIFICATION.md can be performed meaningfully
- No regression in critical-ordering constraint (frictionBudget < roadReactionTorque-usage)
</success_criteria>

<output>
After completion, create `.planning/phases/03-tire-model/03-05-SUMMARY.md`.
</output>
