---
phase: 03-tire-model
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/physics.js
  - src/vehicle.js
  - src/main.js
autonomous: true
requirements: [M3-01, M3-02, M3-05, M3-06]
tags: [tire-model, physics, integrator, handbrake]

must_haves:
  truths:
    - "vehicleState.wheelOmega[4] is integrated each physics step using (driveTorque - Flong*r - brakeTorque) / wheelInertia * dt"
    - "Below 0.5 m/s combined speed, wheelOmega[i] is clamped to v_x / wheelRadius (free-rolling) to prevent explicit-Euler oscillation"
    - "Slip ratio is computed as (omegaR - vx) / max(|omegaR|, |vx|, 0.1) per wheel and passed into computeLongitudinalForce"
    - "Friction circle scales Flat and Flong by frictionBudget / combinedForce whenever sqrt(Flat^2 + Flong^2) exceeds (params.frictionCoeff || 0.9) * Fn"
    - "The omega integrator uses the friction-circle-SCALED Flong as its road reaction torque source"
    - "Space key sets vehicleState.handbrake = true; rear wheels (index 2, 3) receive params.maxHandbrakeTorque as brake torque while handbrake is true"
    - "R-key reset zeroes wheelOmega[0..3] and sets handbrake = false"
  artifacts:
    - path: "src/physics.js"
      provides: "slip ratio computation, friction circle, omega integrator, getBrakeTorque helper"
      contains: "getBrakeTorque"
    - path: "src/vehicle.js"
      provides: "Space-key handbrake input wiring; vehicleState.handbrake field"
      contains: "handbrake"
    - path: "src/main.js"
      provides: "wheelOmega and handbrake fields on vehicleState init and reset block"
      contains: "wheelOmega"
  key_links:
    - from: "src/physics.js"
      to: "src/tire.js"
      via: "computeLongitudinalForce called with the per-wheel slipRatio derived from wheelOmega[i] and params._longitudinalVelocity"
      pattern: "computeLongitudinalForce\\(slipRatio"
    - from: "src/physics.js"
      to: "vehicleState.wheelOmega"
      via: "omega integrator writes vehicleState.wheelOmega[i] each step; reads it on the next step"
      pattern: "vehicleState\\.wheelOmega"
    - from: "src/vehicle.js"
      to: "vehicleState.handbrake"
      via: "Space key state copied into vehicleState.handbrake each updateVehicle call"
      pattern: "vehicleState\\.handbrake\\s*="
    - from: "src/physics.js getBrakeTorque"
      to: "params.maxHandbrakeTorque"
      via: "rear-only return of params.maxHandbrakeTorque when vehicleState.handbrake is true"
      pattern: "maxHandbrakeTorque"
---

<objective>
Wire the Pacejka tire functions from Plan 01 into the physics step: compute per-wheel slip ratio, apply friction-circle coupling, integrate wheel angular velocity, and route handbrake torque to the rear wheels. Add the Space-key handbrake input in `src/vehicle.js`. Initialise and reset the new `wheelOmega` and `handbrake` fields in `src/main.js`.

Purpose: Deliver M3-01 (per-wheel omega), M3-02 (slip ratio), M3-05 (friction circle), and M3-06 (handbrake). After this plan, the car will exhibit wheelspin from rest, drift under Space, and combined-slip behavior — without any new UI yet.

Output:
- `src/physics.js` Step 3 per-wheel contact loop extended with slip ratio, friction circle, omega integrator, and `wheelDebug[i].omega` write. New module-private `getBrakeTorque(wheelIndex, vehicleState, params)` helper.
- `src/vehicle.js` keys object includes `' '`, listeners route Space to it, `vehicleState.handbrake` written each step, `SPAWN_STATE.handbrake = false`.
- `src/main.js` vehicleState init declares `wheelOmega: [0,0,0,0]` and `handbrake: false`; R-key reset block zeroes both. `wheelDebug` init objects gain `omega: 0`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tire-model/03-CONTEXT.md
@.planning/phases/03-tire-model/03-RESEARCH.md
@.planning/phases/03-tire-model/03-PATTERNS.md
@.planning/phases/03-tire-model/03-01-PLAN.md
@src/physics.js
@src/vehicle.js
@src/main.js
@src/tire.js
@data/ranger.js

<interfaces>
From Plan 01 (now available on RANGER_PARAMS):
  pacejkaB, pacejkaC, pacejkaD, pacejkaE
  pacejkaBx, pacejkaCx, pacejkaDx, pacejkaEx
  wheelInertia: 1.22
  maxHandbrakeTorque: 2000

Existing locked signatures (unchanged):
  computeLateralForce(slipAngle, Fz, params) -> Number
  computeLongitudinalForce(slipRatio, Fz, params) -> Number
  getDriveTorque(wheelIndex, vehicleState, params) -> Number   // D-03 — signature stable

New module-private helper (this plan adds, NOT exported):
  function getBrakeTorque(wheelIndex, vehicleState, params) -> Number

Existing per-step params augmentation in physics.js (read-only here):
  params._lateralVelocity      = hubVel.dot(wheelRight)
  params._longitudinalVelocity = hubVel.dot(wheelFwd)
  params._driveForce           = driveTorque / wheelRadius   // keep for rolling-resistance use
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Space-key handbrake input and SPAWN_STATE field in src/vehicle.js</name>
  <files>src/vehicle.js</files>
  <read_first>
    - src/vehicle.js (current keys object, key listeners, SPAWN_STATE, throttle/brake assignment in updateVehicle)
    - .planning/phases/03-tire-model/03-RESEARCH.md (Pattern 5; Pitfall 7 — e.key is the space character, not the string 'space')
    - .planning/phases/03-tire-model/03-PATTERNS.md (src/vehicle.js section — exact lines to change)
  </read_first>
  <action>
    Modify `src/vehicle.js` per D-09 and D-10.

    1. Extend the existing `keys` object to include the single space character as a new field with initial value `false`. Existing fields (`w`, `s`, `a`, `d`, `r`) must remain unchanged and unreordered.

    2. Update both `document.addEventListener('keydown', ...)` and `document.addEventListener('keyup', ...)` handlers so that when `e.key === ' '` the lookup hits the space entry. Replace `const k = e.key.toLowerCase()` with `const k = e.key === ' ' ? ' ' : e.key.toLowerCase()` in both listeners. The `if (k in keys) keys[k] = true/false` guard stays the same. Per Pitfall 7 / constraint #6, using the string `'space'` is wrong — the key MUST be the single space character.

    3. In `updateVehicle` (or wherever `vehicleState.throttle` and `vehicleState.brake` are assigned from `keys`), append `vehicleState.handbrake = keys[' '] || false`. Place it immediately after the existing `vehicleState.brake = keys.s ? 1 : 0` line.

    4. Extend `SPAWN_STATE` (the exported initial-state object) to include `handbrake: false`. Append it at the end of the object so existing field order is preserved.

    Do NOT change steering, Ackermann, or wheel-angle update code. Do NOT use `e.code === 'Space'` — stick to `e.key === ' '` per the locked decision.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/vehicle.js','utf8');const checks=[['space-key-equality',(s.match(/e\.key === ' '/g)||[]).length>=2],['handbrake-assign',/vehicleState\.handbrake\s*=\s*keys\[' '\]/.test(s)],['handbrake-spawn',/handbrake\s*:\s*false/.test(s)],['no-space-literal',!/'space'/.test(s)]];for(const[k,v]of checks){if(!v){console.error('FAIL '+k);process.exit(1)}}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The `keys` object literal contains both `w:` and a space-character key (verify by `node -e "import('./src/vehicle.js')"` does not throw)
    - `grep -c "e.key === ' '" src/vehicle.js` returns 2 (one per keydown/keyup listener)
    - `grep -F "vehicleState.handbrake = keys[' ']" src/vehicle.js` returns one match
    - `grep -E "handbrake\s*:\s*false" src/vehicle.js` returns at least one match (inside SPAWN_STATE)
    - `grep -c "'space'" src/vehicle.js` returns 0 (the literal string `'space'` MUST NOT appear — constraint #6)
    - `node --input-type=module -e "import('./src/vehicle.js').then(m => { if (!('handbrake' in m.SPAWN_STATE)) { process.exit(1) } })"` exits 0
    - Existing keys w, s, a, d, r still present (`grep -c "w: false" src/vehicle.js` >= 1; same for s, a, d, r)
  </acceptance_criteria>
  <done>Space key is registered, handbrake boolean is written each step, SPAWN_STATE includes handbrake, no `'space'` literal anywhere.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add slip ratio, friction circle, omega integrator, and getBrakeTorque to src/physics.js</name>
  <files>src/physics.js</files>
  <read_first>
    - src/physics.js (current Step 3 contact loop, getDriveTorque, params augmentation block, wheelDebug write block)
    - src/tire.js (confirms function signatures still locked after Plan 01)
    - .planning/phases/03-tire-model/03-RESEARCH.md (Patterns 2, 3, 4, 5; Pitfalls 2, 3, 4)
    - .planning/phases/03-tire-model/03-PATTERNS.md (src/physics.js section — exact insertion points and code patterns; Critical Ordering Constraints list)
  </read_first>
  <action>
    Modify `src/physics.js` per D-02, D-04, D-05, D-09, D-10. All changes are inside (or adjacent to) the existing per-wheel contact loop in Step 3.

    Apply the changes in the order listed below. Names and constants MUST be exactly as specified — Plan 03 reads `wheelDebug[i].omega` and the logger reads `vehicleState.wheelOmega`.

    A. Add a module-private helper `getBrakeTorque(wheelIndex, vehicleState, params)` next to `getDriveTorque`. It must NOT be exported. Body:
       - `const isRear = wheelIndex === 2 || wheelIndex === 3`
       - If `vehicleState.handbrake && isRear` return `params.maxHandbrakeTorque`
       - Else if `vehicleState.brake > 0` return `vehicleState.brake * params.maxBrakeTorque`
       - Else return `0`
       Use `params.maxHandbrakeTorque` (literal field name) — do NOT introduce a fallback default.

    B. In the contact loop iteration, AFTER `params._longitudinalVelocity` is set and BEFORE calling `computeLateralForce` / `computeLongitudinalForce`, compute the per-wheel slip ratio:
       - `const SLIP_EPSILON = 0.1`  (m/s)
       - `const omegaR = (vehicleState.wheelOmega?.[i] ?? 0) * params.wheelRadius`
       - `const vx = params._longitudinalVelocity`
       - `const slipRatio = (omegaR - vx) / Math.max(Math.abs(omegaR), Math.abs(vx), SLIP_EPSILON)`

    C. Change the existing call `computeLongitudinalForce(0, Fn, params)` to `computeLongitudinalForce(slipRatio, Fn, params)`. The `computeLateralForce(slipAngle, Fn, params)` call is unchanged.

    D. AFTER both tire functions return AND BEFORE the omega integrator AND BEFORE constructing `wheelForce`, apply the friction circle. `Flat` and `Flong` must be declared with `let` (not `const`) so they can be scaled in place:
       - `const frictionBudget = (params.frictionCoeff || 0.9) * Fn`
       - `const combinedForce = Math.sqrt(Flat * Flat + Flong * Flong)`
       - `if (combinedForce > frictionBudget && combinedForce > 0) { const scale = frictionBudget / combinedForce; Flat *= scale; Flong *= scale }`

    E. AFTER the friction circle (so it uses the SCALED `Flong` per Pitfall 2 / constraint #5) and BEFORE `wheelForce` construction, run the omega integrator:
       - `const OMEGA_EPSILON = 0.5`  (m/s combined-speed threshold)
       - `const wheelInertia = params.wheelInertia || 1.22`
       - `const driveTorque = getDriveTorque(i, vehicleState, params)`
       - `const brakeTorque = getBrakeTorque(i, vehicleState, params)`
       - `const roadReactionTorque = Flong * params.wheelRadius`
       - `const vehicleSpd = Math.abs(params._longitudinalVelocity)`
       - `const wheelSurfaceSpd = Math.abs((vehicleState.wheelOmega?.[i] ?? 0) * params.wheelRadius)`
       - If `vehicleSpd + wheelSurfaceSpd < OMEGA_EPSILON`, set `vehicleState.wheelOmega[i] = params._longitudinalVelocity / params.wheelRadius` (free-rolling clamp per Pattern 2).
       - Else, set `vehicleState.wheelOmega[i] = (vehicleState.wheelOmega?.[i] ?? 0) + (driveTorque - roadReactionTorque - brakeTorque) / wheelInertia * dt`

    F. In the existing `wheelDebug[i]` write block (which currently sets `.fn`, `.fy`, `.sa`, `.c`), add `vehicleState.wheelDebug[i].omega = vehicleState.wheelOmega[i]`. Also extend the wheelDebug zero-init (in the per-iteration init block) so the object literal includes `omega: 0`.

    G. The `wheelForce` construction (`wheelFwd.clone().multiplyScalar(Flong).addScaledVector(wheelRight, Flat)`) stays where it is — but it now reads the post-scaled `Flat` and `Flong`.

    Critical ordering (do not violate — Pitfall 2):
    1. params._longitudinalVelocity set (existing)
    2. slip ratio computed
    3. computeLateralForce + computeLongitudinalForce called
    4. friction circle scales Flat, Flong
    5. omega integrator runs using scaled Flong
    6. wheelForce constructed and added to totalForce

    Do NOT change `getDriveTorque` signature. Do NOT remove the existing `params._driveForce` augmentation — rolling-resistance code may still read it. Do NOT export `getBrakeTorque`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/physics.js','utf8');const checks=[['getBrakeTorque-def',/function\s+getBrakeTorque\s*\(\s*wheelIndex/.test(s)],['not-exported',!/export\s+(function|const|\{[^}]*getBrakeTorque)/.test(s.replace(/getDriveTorque/g,''))||!/export[^\n]*getBrakeTorque/.test(s)],['slip-epsilon',/SLIP_EPSILON\s*=\s*0\.1/.test(s)],['omega-epsilon',/OMEGA_EPSILON\s*=\s*0\.5/.test(s)],['wheel-inertia',/params\.wheelInertia\s*\|\|\s*1\.22/.test(s)],['friction-budget',/params\.frictionCoeff\s*\|\|\s*0\.9/.test(s)],['friction-circle',/Math\.sqrt\(Flat\s*\*\s*Flat\s*\+\s*Flong\s*\*\s*Flong\)/.test(s)],['road-reaction',/Flong\s*\*\s*params\.wheelRadius/.test(s)],['slip-ratio-passed',/computeLongitudinalForce\(slipRatio/.test(s)],['no-zero-call',!/computeLongitudinalForce\(0/.test(s)],['wheeldebug-omega',/wheelDebug\[i\]\.omega/.test(s)],['handbrake-torque-ref',/params\.maxHandbrakeTorque/.test(s)]];for(const[k,v]of checks){if(!v){console.error('FAIL '+k);process.exit(1)}}console.log('OK '+checks.length+' checks')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "function\s+getBrakeTorque\s*\(\s*wheelIndex\s*,\s*vehicleState\s*,\s*params\s*\)" src/physics.js` returns one match
    - `grep -E "^export[^\n]*getBrakeTorque" src/physics.js` returns 0 matches (helper is module-private)
    - `grep -F "SLIP_EPSILON = 0.1" src/physics.js` returns one match
    - `grep -F "OMEGA_EPSILON = 0.5" src/physics.js` returns one match
    - `grep -F "params.wheelInertia || 1.22" src/physics.js` returns one match
    - `grep -F "params.maxHandbrakeTorque" src/physics.js` returns at least one match
    - `grep -F "params.frictionCoeff || 0.9" src/physics.js` returns at least one match
    - `grep -F "Math.sqrt(Flat * Flat + Flong * Flong)" src/physics.js` returns one match
    - `grep -F "Flong * params.wheelRadius" src/physics.js` returns one match
    - `grep -c "computeLongitudinalForce(slipRatio" src/physics.js` returns 1
    - `grep -c "computeLongitudinalForce(0" src/physics.js` returns 0
    - `grep -F "vehicleState.wheelDebug[i].omega" src/physics.js` returns at least one match
    - Ordering check (frictionBudget before roadReactionTorque before wheelForce accumulation): `awk '/frictionBudget/ && !f { f=NR } /roadReactionTorque/ && !r { r=NR } /totalForce\.add\(wheelForce\)/ && !t { t=NR } END { exit (f && r && t && f<r && r<t) ? 0 : 1 }' src/physics.js` exits 0
    - `node --input-type=module -e "import('./src/physics.js').then(() => console.log('OK'))"` prints `OK`
  </acceptance_criteria>
  <done>Slip ratio, friction circle, omega integrator, and getBrakeTorque are present in physics.js with the exact constants and ordering above; the file loads as ESM; no stale `computeLongitudinalForce(0, ...)` call remains; wheelDebug carries omega.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Initialise and reset wheelOmega and handbrake fields on vehicleState in src/main.js</name>
  <files>src/main.js</files>
  <read_first>
    - src/main.js (current vehicleState declaration and R-key reset block)
    - src/vehicle.js (confirm SPAWN_STATE field added in Task 1)
    - .planning/phases/03-tire-model/03-RESEARCH.md (Pitfall 6 — wheelOmega MUST be reset on R)
    - .planning/phases/03-tire-model/03-PATTERNS.md (src/main.js section — vehicleState init and reset block patterns)
  </read_first>
  <action>
    Modify `src/main.js` so the new physics state survives both fresh initialisation and R-key reset.

    1. In the `vehicleState` declaration (the object literal at module top), append two new fields after the existing `wheelDebug` array:
       - `wheelOmega: [0, 0, 0, 0]`  (rad/s, per-wheel)
       - `handbrake: false`
       Do NOT reorder existing fields.

    2. Update each `wheelDebug` initialiser object literal (both at first init and in the reset block) so it carries `omega: 0` alongside `fn`, `fy`, `sa`, `c`. The new shape is `{ fn: 0, fy: 0, sa: 0, c: 0, omega: 0 }`.

    3. In the R-key reset block (the branch that re-zeroes position/velocity/angular velocity/wheelAngles), add two new assignments:
       - `vehicleState.wheelOmega = [0, 0, 0, 0]`
       - `vehicleState.handbrake = false`
       These must run on every R-key reset. Without these, the car will perform a burnout immediately after reset (Pitfall 6 / constraint #7).

    Do NOT change any other behavior in `main.js` in this plan — slip angle HUD, throttle/brake HUD spans, the `updatePacejkaCurve` import, and the `updatePacejkaCurve` call are all owned by Plan 03 and must not be added here.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/main.js','utf8');const wheelOmegaCount=(s.match(/wheelOmega/g)||[]).length;const handbrakeCount=(s.match(/handbrake/g)||[]).length;const omegaInit=(s.match(/omega:\s*0/g)||[]).length;if(wheelOmegaCount<2){console.error('FAIL wheelOmega refs='+wheelOmegaCount+' (need >=2: init + reset)');process.exit(1)}if(handbrakeCount<2){console.error('FAIL handbrake refs='+handbrakeCount+' (need >=2: init + reset)');process.exit(1)}if(omegaInit<1){console.error('FAIL wheelDebug initialiser missing omega:0');process.exit(1)}console.log('OK wheelOmega='+wheelOmegaCount+' handbrake='+handbrakeCount+' omega:0='+omegaInit)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "wheelOmega: [0, 0, 0, 0]" src/main.js` returns at least one match (vehicleState init)
    - `grep -F "vehicleState.wheelOmega = [0, 0, 0, 0]" src/main.js` returns at least one match (reset block)
    - `grep -E "handbrake\s*:\s*false" src/main.js` returns at least one match (init)
    - `grep -F "vehicleState.handbrake = false" src/main.js` returns at least one match (reset)
    - `grep -c "omega: 0" src/main.js` returns at least 1 (wheelDebug initialiser includes omega)
    - `node --input-type=module -e "import('./src/main.js').then(() => console.log('OK')).catch(e => { console.error(e.message); process.exit(1) })"` does not throw a syntax error (note: full execution may fail due to no DOM in Node; only check that the syntax parses — alternative: `node --check src/main.js` if ESM-compatible, otherwise rely on the regex checks above)
    - No `updatePacejkaCurve` reference appears in `main.js` after this plan (`grep -c updatePacejkaCurve src/main.js` returns 0 — that call is added in Plan 03)
  </acceptance_criteria>
  <done>vehicleState init and reset both carry wheelOmega=[0,0,0,0] and handbrake=false; wheelDebug initialiser includes omega:0; Plan 03 HUD wiring is NOT present yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard input -> vehicleState | DOM keydown/keyup events read directly into a trusted local `keys` object; the only new field is `' '` (space) which routes to `handbrake` boolean. No external input cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | Omega integrator divergence at low speed (explicit Euler stiffness) | mitigate | OMEGA_EPSILON = 0.5 m/s combined-speed guard clamps wheelOmega to free-rolling (`v_x / wheelRadius`) below the threshold; verified by acceptance criterion `grep "OMEGA_EPSILON = 0.5"` |
| T-03-04 | Denial of Service | Slip-ratio NaN from 0/0 at rest | mitigate | SLIP_EPSILON = 0.1 m/s in `Math.max(..., SLIP_EPSILON)` denominator guarantees denominator >= 0.1; verified in acceptance criteria |
| T-03-05 | Information Disclosure | n/a | accept | No new I/O surface; all state remains in-memory |
| T-03-06 | Tampering | Stale R-key reset leaves wheelOmega spinning (burnout-on-reset bug) | mitigate | Task 3 reset block explicitly zeroes wheelOmega and handbrake; verified by grep |
</threat_model>

<verification>
- All `<verify>` blocks pass
- All acceptance-criteria greps succeed
- Open `index.html` via local HTTP server:
  - From rest, full throttle → rear wheels spin visibly faster than vehicle speed (M3-01, M3-04 smoke)
  - Hold Space while turning at moderate speed → rear axle slides; oversteer develops without instant spin-out (M3-06 smoke)
  - Press R → car resets to spawn with NO immediate burnout (Pitfall 6 verification)
  - Slow approach to zero speed → no NaN, no jitter, vehicle position stays finite (OMEGA_EPSILON, SLIP_EPSILON working)
  - Hard throttle + steer combined → car does not exhibit unbounded lateral force (friction circle firing; M3-05)
</verification>

<success_criteria>
- M3-01: wheelOmega[i] integrates each step and changes visibly under throttle from rest
- M3-02: slip ratio is computed using the documented formula and ε; NaN-free at rest
- M3-05: friction circle scales Flat/Flong before omega integrator and before wheelForce accumulation
- M3-06: Space key applies maxHandbrakeTorque to rear wheels only; drift develops naturally
- R-key reset cleanly returns car to spawn with zero wheelOmega and handbrake = false
</success_criteria>

<output>
After completion, create `.planning/phases/03-tire-model/03-02-SUMMARY.md` following the standard summary template. Note in the summary: the car now exhibits combined-slip physics, but no UI feedback exists yet (slip-angle HUD, throttle/brake HUD, Pacejka plot, slider audit, logger fields, glossary updates all land in Plan 03).
</output>
