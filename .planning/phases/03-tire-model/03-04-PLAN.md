---
phase: 03-tire-model
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/debug.js
autonomous: true
gap_closure: true
requirements: [M3-09]
must_haves:
  truths:
    - "Pressing backtick (`) opens the debug panel AND a 300x200 canvas appears at right:320px"
    - "While driving with the panel open, updatePacejkaCurve runs each render frame without throwing ReferenceError"
    - "Pacejka curve renders (green) and FL/FR operating-point dots update each frame"
  artifacts:
    - path: "src/debug.js"
      provides: "Module-level plotCanvas/plotCtx assigned inside initDebug; updatePacejkaCurve uses them safely"
      contains: "let plotCanvas = null"
  key_links:
    - from: "src/debug.js updatePacejkaCurve"
      to: "module-level plotCanvas/plotCtx"
      via: "module-scope let bindings assigned inside initDebug"
      pattern: "^let plotCanvas"
---

<objective>
Fix the BLOCKER scope bug identified in VERIFICATION.md gap 1 and REVIEW.md CR-01: `plotCanvas` and `plotCtx` are declared `const` inside `initDebug()`, but `updatePacejkaCurve` references them at module scope — every render-frame call throws `ReferenceError: plotCanvas is not defined`.

Purpose: Restore M3-09 (live Pacejka curve plot) without changing any other behavior.
Output: src/debug.js with module-level `let plotCanvas` and `let plotCtx`, assigned inside `initDebug`, with a null-guard at the top of `updatePacejkaCurve`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-tire-model/03-VERIFICATION.md
@.planning/phases/03-tire-model/03-REVIEW.md
@.planning/phases/03-tire-model/03-03-SUMMARY.md
@src/debug.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote plotCanvas/plotCtx to module scope in src/debug.js</name>
  <files>src/debug.js</files>
  <read_first>
    - src/debug.js (entire file — only ~150 lines)
    - .planning/phases/03-tire-model/03-VERIFICATION.md gap 1 (lines 8-17)
    - .planning/phases/03-tire-model/03-REVIEW.md CR-01 (lines 41-72)
  </read_first>
  <action>
    Make three changes to src/debug.js, preserving all other behavior:

    1. Add module-level declarations directly below the `import { GUI } ...` line (around line 19): two `let` bindings named `plotCanvas` and `plotCtx`, both initialized to `null`. Add a one-line comment noting that they are module-level so `updatePacejkaCurve` (defined at module scope) can read them.

    2. Inside `initDebug` change the existing lines that create the canvas (currently `const plotCanvas = document.createElement('canvas')` near line 69 and `const plotCtx = plotCanvas.getContext('2d')` near line 74) to plain assignments (`plotCanvas = document.createElement('canvas')` and `plotCtx = plotCanvas.getContext('2d')`). Do NOT add a second `let`/`const` — that would shadow the module binding and reintroduce the bug.

    3. In `updatePacejkaCurve` (currently starts near line 105), replace the existing early-return check `if (plotCanvas.style.display === 'none') return` with a guarded version: `if (!plotCanvas || !plotCtx || plotCanvas.style.display === 'none') return`. This protects against the case where the function is called before `initDebug` has run.

    Do not touch the backtick listener, the canvas styling, the curve drawing math, the operating-point dot logic, the lateral/longitudinal folders, or any slider definition. The diff should be three precise edits in a single file.
  </action>
  <verify>
    <automated>node -e "import('./src/debug.js').then(m => { if (typeof m.updatePacejkaCurve !== 'function') process.exit(1); m.updatePacejkaCurve({wheelDebug:[{sa:0},{sa:0}]}, {pacejkaB:10,pacejkaC:1.9,pacejkaD:1,pacejkaE:0.97}); console.log('OK: no ReferenceError') })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '^let plotCanvas' src/debug.js` returns exactly one match at module scope
    - `grep -n '^let plotCtx'    src/debug.js` returns exactly one match at module scope
    - `grep -c 'const plotCanvas' src/debug.js` returns 0
    - `grep -c 'const plotCtx'    src/debug.js` returns 0
    - `grep -n 'if (!plotCanvas' src/debug.js` finds the new guard inside `updatePacejkaCurve`
    - The verify command above prints `OK: no ReferenceError` and exits 0 (proves updatePacejkaCurve can be called without DOM/initDebug without throwing)
  </acceptance_criteria>
  <done>
    updatePacejkaCurve no longer throws ReferenceError when called from the render loop. The Pacejka curve canvas appears on backtick toggle and draws without errors. M3-09 wiring is now runtime-correct.
  </done>
</task>

</tasks>

<verification>
- `node -e` smoke test (above) confirms updatePacejkaCurve is callable without ReferenceError
- grep gates confirm module-level `let` bindings and absence of `const` shadowing
- Phase 3 verifier should re-run gap 1 and mark it CLOSED
</verification>

<success_criteria>
- VERIFICATION.md gap 1 (plotCanvas scope) closes on re-verify
- No regression in backtick toggle behavior (panel + canvas still hide/show together)
- Human verification step 3 from VERIFICATION.md (Pacejka Curve Plot) can now proceed
</success_criteria>

<output>
After completion, create `.planning/phases/03-tire-model/03-04-SUMMARY.md`.
</output>
