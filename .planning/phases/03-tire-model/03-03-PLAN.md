---
phase: 03-tire-model
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/debug.js
  - src/main.js
  - src/logger.js
  - index.html
  - docs/GLOSSARY.md
autonomous: true
requirements: [M3-07, M3-08, M3-09, M3-10]
tags: [tire-model, hud, debug-ui, pacejka-plot, logger, glossary]

must_haves:
  truths:
    - "Debug panel exposes lateral Pacejka sliders (B, C [1.0-1.99], D, E) and longitudinal sliders (Bx, Cx [1.0-1.99], Dx, Ex) plus a maxHandbrakeTorque slider"
    - "corneringStiffness and lateralDampingCoeff sliders are removed from the debug panel"
    - "A standalone canvas overlay (300x200 px) displays the Pacejka lateral force curve normalized to D and dots for FL/FR operating points; colored green/orange/red by |f|/D thresholds 0.5/0.8"
    - "The Pacejka canvas overlay shows/hides with the same backtick listener that toggles the lil-gui panel"
    - "HUD shows live speed, front slip angle (deg, colored green <5, orange <10, red >=10), throttle %, brake %"
    - "updatePacejkaCurve(vehicleState, params) is exported from src/debug.js and called once per render frame in src/main.js (outside the fixed-timestep accumulator)"
    - "Logger FIELDS array appends fl_omega, fr_omega, rl_omega, rr_omega at the END (existing field order preserved); captureFrame pushes the matching omega values at the matching positions"
    - "docs/GLOSSARY.md gains entries for longitudinal slip ratio (kappa), wheel angular velocity (wheelOmega), Pacejka B/C/D/E, friction circle, handbrake; logger field list gains the 4 omega entries"
  artifacts:
    - path: "src/debug.js"
      provides: "Pacejka folder sliders, slider audit, Pacejka canvas, updatePacejkaCurve export, synced backtick toggle"
      contains: "updatePacejkaCurve"
    - path: "src/main.js"
      provides: "import of updatePacejkaCurve; slip/throttle/brake HUD update; per-frame updatePacejkaCurve call outside the fixed accumulator"
      contains: "updatePacejkaCurve"
    - path: "src/logger.js"
      provides: "fl_omega / fr_omega / rl_omega / rr_omega FIELDS entries and matching captureFrame values"
      contains: "fl_omega"
    - path: "index.html"
      provides: "HUD spans slipVal, thrVal, brkVal"
      contains: "slipVal"
    - path: "docs/GLOSSARY.md"
      provides: "definitions for slip ratio, wheelOmega, Pacejka B/C/D/E, friction circle, handbrake; logger omega fields"
      contains: "wheelOmega"
  key_links:
    - from: "src/main.js render section"
      to: "src/debug.js updatePacejkaCurve"
      via: "function call inside requestAnimationFrame render branch, OUTSIDE the fixed-timestep accumulator (constraint #10)"
      pattern: "updatePacejkaCurve\\(vehicleState"
    - from: "src/debug.js backtick listener"
      to: "plotCanvas.style.display"
      via: "same listener toggles both gui.domElement.style.display and plotCanvas.style.display (constraint #9)"
      pattern: "plotCanvas\\.style\\.display"
    - from: "src/logger.js captureFrame"
      to: "vehicleState.wheelDebug[i].omega"
      via: "omega values pushed in the order fl, fr, rl, rr at the end of the row (matches FIELDS append-only contract — constraint #8)"
      pattern: "omega"
---

<objective>
Land the user-facing surface of Phase 3: live Pacejka curve plot, slider audit (D-08, D-16), front-slip-angle and throttle/brake HUD indicators, logger omega fields (D-15), GLOSSARY updates (D-15), and the wire that calls `updatePacejkaCurve` from the render loop outside the fixed-timestep accumulator.

Purpose: Deliver M3-07 (slip angle HUD), M3-08 (throttle/brake HUD), M3-09 (live Pacejka plot), M3-10 (tunable via debug menu). Closes Phase 3 end-of-phase housekeeping.

Output:
- `src/debug.js`: remove `corneringStiffness` and `lateralDampingCoeff` sliders; add lateral and longitudinal Pacejka folders with sliders; add `maxHandbrakeTorque` slider; create the Pacejka canvas overlay; extend the backtick toggle to sync canvas visibility with the panel; export `updatePacejkaCurve(vehicleState, params)`.
- `src/main.js`: import `updatePacejkaCurve`; add slip/throttle/brake HUD update block in the render section; call `updatePacejkaCurve(vehicleState, RANGER_PARAMS)` once per render frame, OUTSIDE the fixed-timestep accumulator.
- `src/logger.js`: append `fl_omega`, `fr_omega`, `rl_omega`, `rr_omega` to FIELDS and to the `captureFrame` push array at matching trailing positions.
- `index.html`: extend the `#hud` div with `slipVal`, `thrVal`, `brkVal` span elements.
- `docs/GLOSSARY.md`: add full definitions for kappa, wheelOmega, Pacejka B/C/D/E, friction circle, handbrake; add the 4 omega fields to the Frame Logger Fields section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tire-model/03-CONTEXT.md
@.planning/phases/03-tire-model/03-RESEARCH.md
@.planning/phases/03-tire-model/03-PATTERNS.md
@.planning/phases/03-tire-model/03-02-PLAN.md
@src/debug.js
@src/main.js
@src/logger.js
@index.html
@docs/GLOSSARY.md
@data/ranger.js

<interfaces>
New export from src/debug.js (this plan adds):
  export function updatePacejkaCurve(vehicleState, params): void
  (called once per render frame; early-returns when the plot canvas is hidden)

Existing export (unchanged):
  export function initDebug(params): GUI

State read from vehicleState in the HUD section (set by Plans 01–02):
  vehicleState.wheelDebug[0].sa   (FL slip angle in radians)
  vehicleState.throttle           (0.0–1.0)
  vehicleState.brake              (0.0–1.0)
  vehicleState.wheelDebug[i].omega for the operating-point dots (FL, FR; i = 0, 1)

Logger FIELDS contract (constraint #8): append-only; never reorder existing 33 fields. New length = 37.

HUD element ids in index.html:
  #speedVal (existing), #slipVal (new), #thrVal (new), #brkVal (new)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Debug panel slider audit and Pacejka canvas overlay in src/debug.js</name>
  <files>src/debug.js</files>
  <read_first>
    - src/debug.js (current initDebug body, slider list, backtick listener)
    - data/ranger.js (confirm Plan 01 added pacejkaB/C/D/E, pacejkaBx/Cx/Dx/Ex, maxHandbrakeTorque)
    - .planning/phases/03-tire-model/03-RESEARCH.md (Pattern 6 — canvas overlay; Pattern 7 — HUD note; Pitfall 8 — canvas position)
    - .planning/phases/03-tire-model/03-PATTERNS.md (src/debug.js section — slider lines to remove, folder pattern, canvas creation, updatePacejkaCurve body)
  </read_first>
  <action>
    Modify `src/debug.js` per D-08, D-11, D-12, D-13, D-16.

    1. Slider audit (D-08, D-16). REMOVE these slider lines entirely from `initDebug`:
       - `gui.add(params, 'corneringStiffness', ...)`
       - `gui.add(params, 'lateralDampingCoeff', ...)`
       Do NOT delete the underlying fields from `data/ranger.js`.

    2. Add a "Lateral Tire (Pacejka)" folder via `gui.addFolder('Lateral Tire (Pacejka)')`. Add four sliders inside it (one per line):
       - `pacejkaB`, range 5 to 20, step 0.5, label `B - Stiffness`
       - `pacejkaC`, range 1.0 to 1.99, step 0.01, label `C - Shape [1.0-1.99]`
       - `pacejkaD`, range 0.5 to 2.0, step 0.05, label `D - Peak Factor`
       - `pacejkaE`, range -1.0 to 1.0, step 0.05, label `E - Curvature`

    3. Add a "Longitudinal Tire (Pacejka)" folder. Sliders inside (matching the lateral set):
       - `pacejkaBx` 5 to 20 step 0.5, `pacejkaCx` 1.0 to 1.99 step 0.01, `pacejkaDx` 0.5 to 2.0 step 0.05, `pacejkaEx` -1.0 to 1.0 step 0.05.

    4. Add a top-level slider `gui.add(params, 'maxHandbrakeTorque', 500, 5000, 100).name('Handbrake Torque (Nm)')`.

    5. Create a standalone canvas (D-11) before the backtick listener:
       - `const plotCanvas = document.createElement('canvas')`
       - `plotCanvas.width = 300`
       - `plotCanvas.height = 200`
       - `plotCanvas.style.cssText = 'position:fixed;top:20px;right:320px;background:#111;border:1px solid #444;display:none'`
       - `document.body.appendChild(plotCanvas)`
       - `const plotCtx = plotCanvas.getContext('2d')`

    6. Extend the existing backtick listener so it toggles BOTH `gui.domElement.style.display` AND `plotCanvas.style.display` in lockstep (constraint #9):
       - On each `e.key === '`' ` event, read `const hidden = gui.domElement.style.display === 'none'`
       - Set `gui.domElement.style.display = hidden ? '' : 'none'`
       - Set `plotCanvas.style.display = hidden ? '' : 'none'`

    7. Export a new function `updatePacejkaCurve(vehicleState, params)` at module scope (it must close over `plotCanvas` and `plotCtx`). Body:
       - Early return if `plotCanvas.style.display === 'none'` (performance guard)
       - Clear `plotCtx.clearRect(0, 0, plotCanvas.width, plotCanvas.height)`
       - Read `const B = params.pacejkaB`, `const C = Math.max(1.0, Math.min(1.99, params.pacejkaC))`, `const D = params.pacejkaD`, `const E = params.pacejkaE`
       - Draw the normalized curve over slip angle range [-0.3, +0.3] rad (D-12) at 200 samples. Y is `Math.sin(C * Math.atan(B*sa - E*(B*sa - Math.atan(B*sa))))` (no `D` multiplication, no `Fz` — the plot is normalized). Map `px = (sa + 0.3)/(0.6) * W`, `py = H/2 - fNorm * (H/2 - 10)`. Stroke color `#44ff88`, lineWidth 1.5.
       - Plot operating-point dots for `i in [0, 1]`: read `const sa = vehicleState.wheelDebug?.[i]?.sa || 0`, compute `fNorm` with the same expression, map to `px`/`py`, choose color by `pct = Math.abs(fNorm)`: `< 0.5` → `#00ff88`, `< 0.8` → `#ffaa00`, else `#ff2222`. Fill a 4-radius arc at each `(px, py)`.

    8. `initDebug` continues to return `gui` (unchanged return). `updatePacejkaCurve` is a separate named export.

    Do NOT touch the existing physics-constant sliders (mass, frictionCoeff, etc.) other than the two removals above. Do NOT register a second backtick listener — extend the existing one.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/debug.js','utf8');const checks=[['removed-corneringStiffness',!/gui\.add\(params,\s*'corneringStiffness'/.test(s)],['removed-lateralDamping',!/gui\.add\(params,\s*'lateralDampingCoeff'/.test(s)],['lateral-folder',/addFolder\(['\"]Lateral Tire \(Pacejka\)['\"]\)/.test(s)],['longitudinal-folder',/addFolder\(['\"]Longitudinal Tire \(Pacejka\)['\"]\)/.test(s)],['pacejkaB-slider',/'pacejkaB'\s*,\s*5\s*,\s*20\s*,\s*0\.5/.test(s)],['pacejkaC-slider',/'pacejkaC'\s*,\s*1\.0\s*,\s*1\.99/.test(s)],['pacejkaBx-slider',/'pacejkaBx'\s*,\s*5\s*,\s*20/.test(s)],['handbrake-slider',/'maxHandbrakeTorque'\s*,\s*500\s*,\s*5000/.test(s)],['canvas-create',/document\.createElement\(['\"]canvas['\"]\)/.test(s)],['canvas-width-300',/plotCanvas\.width\s*=\s*300/.test(s)],['canvas-right-320',/right:320px/.test(s)],['backtick-syncs-canvas',/plotCanvas\.style\.display\s*=\s*hidden/.test(s)],['updatePacejkaCurve-export',/export\s+function\s+updatePacejkaCurve\s*\(\s*vehicleState\s*,\s*params\s*\)/.test(s)],['curve-range',/-?0\.3/.test(s)&&/RANGE/.test(s)||/0\.3/.test(s)],['operating-point-loop',/\[0,\s*0?1?\]/.test(s)||/for\s*\(\s*const\s+i\s+of\s+\[0,\s*1\]/.test(s)],['C-clamp-in-curve',/Math\.max\(1\.0,\s*Math\.min\(1\.99/.test(s)]];for(const[k,v]of checks){if(!v){console.error('FAIL '+k);process.exit(1)}}console.log('OK '+checks.length+' checks')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "corneringStiffness" src/debug.js` returns 0
    - `grep -c "lateralDampingCoeff" src/debug.js` returns 0
    - `grep -F "addFolder('Lateral Tire (Pacejka)')" src/debug.js` returns one match
    - `grep -F "addFolder('Longitudinal Tire (Pacejka)')" src/debug.js` returns one match
    - All 8 Pacejka slider field names appear: `for f in pacejkaB pacejkaC pacejkaD pacejkaE pacejkaBx pacejkaCx pacejkaDx pacejkaEx; do grep -q "'$f'" src/debug.js || exit 1; done` returns 0
    - `grep -F "'maxHandbrakeTorque', 500, 5000, 100" src/debug.js` returns one match
    - `grep -F "document.createElement('canvas')" src/debug.js` returns one match
    - `grep -F "plotCanvas.width = 300" src/debug.js` returns one match
    - `grep -F "plotCanvas.height = 200" src/debug.js` returns one match
    - `grep -F "right:320px" src/debug.js` returns one match (per Pitfall 8)
    - `grep -F "plotCanvas.style.display = hidden" src/debug.js` returns one match (canvas sync; constraint #9)
    - `grep -E "export\s+function\s+updatePacejkaCurve\s*\(\s*vehicleState\s*,\s*params" src/debug.js` returns one match
    - `grep -F "Math.max(1.0, Math.min(1.99" src/debug.js` returns at least one match (C clamp inside the curve sampler — constraint #3)
    - Only ONE backtick listener exists: `grep -c "e.key === '\\\`'" src/debug.js` returns 1
    - `node --input-type=module -e "import('./src/debug.js').then(m => { if (typeof m.updatePacejkaCurve !== 'function') process.exit(1); console.log('OK') })"` prints `OK`
  </acceptance_criteria>
  <done>Slider audit complete; Pacejka canvas overlay created; backtick toggle syncs both gui and canvas; updatePacejkaCurve is exported and draws the normalized curve plus FL/FR operating-point dots with color thresholds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire HUD spans, updatePacejkaCurve call, and slip/throttle/brake updates in src/main.js + index.html</name>
  <files>src/main.js, index.html</files>
  <read_first>
    - src/main.js (existing import line for debug.js; existing speedVal HUD update; render loop placement vs fixed-timestep accumulator)
    - index.html (existing #hud div layout)
    - src/debug.js (confirm updatePacejkaCurve is exported — Task 1)
    - .planning/phases/03-tire-model/03-RESEARCH.md (Pattern 7 — HUD additions; D-14 threshold note vs M3-07)
    - .planning/phases/03-tire-model/03-PATTERNS.md (src/main.js section; index.html section)
  </read_first>
  <action>
    Apply two file changes. Use the threshold values from D-14 (CONTEXT.md takes precedence over REQUIREMENTS.md M3-07 per RESEARCH.md note).

    A. `index.html`. Modify the existing `<div id="hud">SPEED: <span id="speedVal">0.0</span> km/h</div>` so it becomes a multi-line HUD containing three new spans with these EXACT ids: `slipVal`, `thrVal`, `brkVal`. Suggested literal markup (other separators are acceptable as long as the ids match exactly):

    ```
    <div id="hud">
      SPEED: <span id="speedVal">0.0</span> km/h<br>
      SLIP: <span id="slipVal">0.0&deg;</span><br>
      THR: <span id="thrVal">0</span>% &nbsp; BRK: <span id="brkVal">0</span>%
    </div>
    ```

    Do NOT change `#hud` CSS or remove the existing speedVal span.

    B. `src/main.js`. Three edits:

    1. Extend the existing import from `./debug.js` to include `updatePacejkaCurve`:
       From: `import { initDebug } from './debug.js'`
       To:   `import { initDebug, updatePacejkaCurve } from './debug.js'`

    2. In the render section (the branch that runs once per `requestAnimationFrame` AFTER the fixed-timestep accumulator while-loop), immediately after the existing speed HUD line, add this block:
       - Compute `const slipDeg = (vehicleState.wheelDebug?.[0]?.sa || 0) * (180 / Math.PI)`
       - `const slipEl = document.getElementById('slipVal')`
       - If `slipEl` truthy: set `slipEl.textContent = slipDeg.toFixed(1) + '°'` and set `slipEl.style.color` to `'#00ff88'` when `Math.abs(slipDeg) < 5`, `'#ffaa00'` when `Math.abs(slipDeg) < 10`, otherwise `'#ff2222'` (D-14 thresholds — NOT M3-07's `15` value).
       - `const thrEl = document.getElementById('thrVal'); if (thrEl) thrEl.textContent = (vehicleState.throttle * 100).toFixed(0)`
       - `const brkEl = document.getElementById('brkVal'); if (brkEl) brkEl.textContent = (vehicleState.brake * 100).toFixed(0)`

    3. After the HUD updates above, still inside the render branch (NOT inside the fixed-timestep while-loop — constraint #10), call `updatePacejkaCurve(vehicleState, RANGER_PARAMS)`. Use whichever local identifier currently holds the Ranger params object in `main.js` (typically `RANGER_PARAMS` imported from `../data/ranger.js`).

    Do NOT place the `updatePacejkaCurve` call inside the `while (accumulator >= dt)` body — it would run zero or many times per frame instead of exactly once per repaint (constraint #10).

    Do NOT use `document.getElementById('thrVal')?.textContent = ...` — optional chaining on the LHS of an assignment is invalid syntax in current ECMAScript. Use the explicit `if (el) el.textContent = ...` pattern shown above.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=fs.readFileSync('src/main.js','utf8');const checks=[['html-slipVal',/id=\"slipVal\"/.test(h)],['html-thrVal',/id=\"thrVal\"/.test(h)],['html-brkVal',/id=\"brkVal\"/.test(h)],['html-speedVal-kept',/id=\"speedVal\"/.test(h)],['main-import',/import\s*\{[^}]*updatePacejkaCurve[^}]*\}\s*from\s*['\"]\.\/debug\.js['\"]/.test(m)],['main-call',/updatePacejkaCurve\s*\(\s*vehicleState/.test(m)],['main-slipDeg',/slipDeg/.test(m)],['main-slipVal-ref',/getElementById\(['\"]slipVal['\"]\)/.test(m)],['main-thrVal-ref',/getElementById\(['\"]thrVal['\"]\)/.test(m)],['main-brkVal-ref',/getElementById\(['\"]brkVal['\"]\)/.test(m)],['main-color-thresholds',/['\"]#00ff88['\"]/.test(m)&&/['\"]#ffaa00['\"]/.test(m)&&/['\"]#ff2222['\"]/.test(m)],['main-d14-threshold-5',/Math\.abs\(slipDeg\)\s*<\s*5/.test(m)],['main-d14-threshold-10',/Math\.abs\(slipDeg\)\s*<\s*10/.test(m)],['main-no-bad-optional-chain',!/getElementById\(['\"][^'\"]+['\"]\)\?\.textContent\s*=/.test(m)]];for(const[k,v]of checks){if(!v){console.error('FAIL '+k);process.exit(1)}}console.log('OK '+checks.length+' checks')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'id="slipVal"' index.html` returns one match
    - `grep -F 'id="thrVal"' index.html` returns one match
    - `grep -F 'id="brkVal"' index.html` returns one match
    - `grep -F 'id="speedVal"' index.html` still returns one match (existing span preserved)
    - `grep -E "import\s*\{[^}]*updatePacejkaCurve[^}]*\}\s*from" src/main.js` returns one match
    - `grep -F "updatePacejkaCurve(vehicleState" src/main.js` returns one match
    - `grep -F "getElementById('slipVal')" src/main.js` returns one match
    - `grep -F "getElementById('thrVal')" src/main.js` returns one match
    - `grep -F "getElementById('brkVal')" src/main.js` returns one match
    - All three color hex strings present: `grep -c "#00ff88" src/main.js` >= 1; `grep -c "#ffaa00" src/main.js` >= 1; `grep -c "#ff2222" src/main.js` >= 1
    - D-14 thresholds present (NOT M3-07's 15): `grep -E "Math.abs\(slipDeg\)\s*<\s*5" src/main.js` matches; `grep -E "Math.abs\(slipDeg\)\s*<\s*10" src/main.js` matches; `grep -c "< 15" src/main.js` does not include any new slip-angle threshold line (this check is informational; only fail if the slipDeg block uses 15 — verify by `! grep -E "Math.abs\(slipDeg\)\s*<\s*15" src/main.js`)
    - No invalid optional-chain assignment: `! grep -E "getElementById\([^)]+\)\?\.textContent\s*=" src/main.js`
    - Ordering: `updatePacejkaCurve` call appears AFTER the speed/slip/throttle/brake HUD updates AND OUTSIDE the fixed-accumulator while-loop. Verify the call sits in the render branch (one call per animation frame): `awk '/while[^\n]*accumulator/ { w=NR } /updatePacejkaCurve\(vehicleState/ { u=NR } /renderer\.render/ { r=NR } END { if (u && r && u < r && (!w || u > w)) print "OK"; else { print "FAIL w="w" u="u" r="r; exit 1 } }' src/main.js`
    - `node --input-type=module -e "import('./src/main.js').then(() => console.log('OK')).catch(e => { console.error(e.message); process.exit(e.message.includes('document') ? 0 : 1) })"` exits 0 (module either loads or fails only because of missing DOM — never because of a syntax error)
  </acceptance_criteria>
  <done>HUD spans exist in index.html; main.js imports and calls updatePacejkaCurve once per render frame outside the accumulator; slip-angle color thresholds use D-14 values (5 / 10); throttle and brake percentages are written each frame.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Append omega fields to src/logger.js and update docs/GLOSSARY.md</name>
  <files>src/logger.js, docs/GLOSSARY.md</files>
  <read_first>
    - src/logger.js (current FIELDS array, captureFrame push array, header comment with field count)
    - docs/GLOSSARY.md (current Term Definitions section and Frame Logger Fields section; "Deferred to Phase 3" table)
    - .planning/phases/03-tire-model/03-RESEARCH.md (Logger FIELDS Extension snippet)
    - .planning/phases/03-tire-model/03-PATTERNS.md (src/logger.js and docs/GLOSSARY.md sections — exact additions)
  </read_first>
  <action>
    A. `src/logger.js`:

    1. Append exactly four new entries to the FIELDS array, AT THE END (constraint #8 — append-only; never reorder existing fields). In this exact order:
       `'fl_omega', 'fr_omega', 'rl_omega', 'rr_omega'`

    2. In `captureFrame` (the function that pushes a row to `_frames`), append four trailing values at the END of the row in the same fl/fr/rl/rr order. Source values come from `vehicleState.wheelDebug[i].omega`:
       `fl.omega ?? 0, fr.omega ?? 0, rl.omega ?? 0, rr.omega ?? 0`
       (Use whatever local aliases the file already uses for `vehicleState.wheelDebug[0..3]`; do NOT rename them.)

    3. Update the file's header comment (the line that documents the field count) from 33 to 37 and add a brief note that `{fl/fr/rl/rr}_omega` are Phase 3 additions.

    Do NOT reorder any existing FIELDS entry. Do NOT change the order of existing values in `captureFrame`. The position-to-name mapping is the public log contract.

    B. `docs/GLOSSARY.md`:

    1. In the "Term Definitions" section, add new headings (`### Heading` style consistent with existing entries) for each of these terms. Each entry must include a one-line definition plus a bullet list with Unit, Sign (where applicable), and a one-line note on where used:
       - `### Longitudinal Slip Ratio (kappa)` — formula `kappa = (omega * r - v_x) / max(|omega*r|, |v_x|, epsilon)` with epsilon = 0.1 m/s; unit dimensionless; range [-1, +1]; positive = wheel spinning faster than ground, negative = wheel rotating slower than ground (braking/locked); used by `computeLongitudinalForce` in `src/tire.js`.
       - `### Wheel Angular Velocity (omega_wheel / wheelOmega)` — unit rad/s; per-wheel field on `vehicleState.wheelOmega[4]`; integrated in `src/physics.js` each step; distinct from the visual `vehicleState.wheelAngles[4]`.
       - `### Pacejka B Coefficient (Stiffness Factor)` — shapes the initial slope of the Pacejka force curve.
       - `### Pacejka C Coefficient (Shape Factor)` — shape factor; hard-clamped to `[1.0, 1.99]` inside `src/tire.js`.
       - `### Pacejka D Coefficient (Peak Factor)` — peak normalized force; effective `peak force = D * Fz`.
       - `### Pacejka E Coefficient (Curvature Factor)` — controls the post-peak falloff shape.
       - `### Friction Circle` — combined Flat/Flong budget; if `sqrt(Flat^2 + Flong^2) > frictionCoeff * Fn`, both components are scaled by `frictionBudget / combinedForce` in `src/physics.js`.
       - `### Handbrake` — Space key; sets `vehicleState.handbrake = true`; rear wheels receive `params.maxHandbrakeTorque` from `getBrakeTorque` while engaged; drift develops naturally via the Pacejka friction circle (not by hard-locking omega).

    2. Remove the moved entries from the "Deferred to Phase 3" table so the table no longer lists Pacejka B/C/D/E, friction circle, longitudinal slip ratio, or wheel angular velocity. If the table becomes empty, leave a single placeholder row noting Phase 4+ items, or delete the table entirely.

    3. In the "Frame Logger Fields" section, append four new field entries (one per line) matching the existing `fl_fn` / `fr_fn` style. Names must be exactly `fl_omega`, `fr_omega`, `rl_omega`, `rr_omega`. Description: `Wheel angular velocity (rad/s) — Phase 3 addition`.

    Do NOT touch other sections of GLOSSARY.md (coordinate system, sign conventions, slip angle definition).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const lg=fs.readFileSync('src/logger.js','utf8');const gl=fs.readFileSync('docs/GLOSSARY.md','utf8');const checks=[['fl_omega-fields',/'fl_omega'/.test(lg)],['fr_omega-fields',/'fr_omega'/.test(lg)],['rl_omega-fields',/'rl_omega'/.test(lg)],['rr_omega-fields',/'rr_omega'/.test(lg)],['fl_omega-push',/fl\.omega/.test(lg)],['fr_omega-push',/fr\.omega/.test(lg)],['rl_omega-push',/rl\.omega/.test(lg)],['rr_omega-push',/rr\.omega/.test(lg)],['order-fields',lg.indexOf(\"'fl_omega'\")<lg.indexOf(\"'fr_omega'\")&&lg.indexOf(\"'fr_omega'\")<lg.indexOf(\"'rl_omega'\")&&lg.indexOf(\"'rl_omega'\")<lg.indexOf(\"'rr_omega'\")],['order-push',lg.indexOf('fl.omega')<lg.indexOf('fr.omega')&&lg.indexOf('fr.omega')<lg.indexOf('rl.omega')&&lg.indexOf('rl.omega')<lg.indexOf('rr.omega')],['glossary-kappa',/Longitudinal Slip Ratio/i.test(gl)],['glossary-wheelOmega',/wheelOmega/.test(gl)],['glossary-pacejkaB',/Pacejka B/.test(gl)],['glossary-pacejkaC',/Pacejka C/.test(gl)],['glossary-pacejkaD',/Pacejka D/.test(gl)],['glossary-pacejkaE',/Pacejka E/.test(gl)],['glossary-friction-circle',/Friction Circle/.test(gl)],['glossary-handbrake',/Handbrake/.test(gl)],['glossary-fl_omega-field',/fl_omega/.test(gl)],['glossary-fr_omega-field',/fr_omega/.test(gl)],['glossary-rl_omega-field',/rl_omega/.test(gl)],['glossary-rr_omega-field',/rr_omega/.test(gl)]];for(const[k,v]of checks){if(!v){console.error('FAIL '+k);process.exit(1)}}console.log('OK '+checks.length+' checks')"</automated>
  </verify>
  <acceptance_criteria>
    - All four logger fields present in FIELDS array IN ORDER fl, fr, rl, rr (verified by index comparison in the automated check)
    - All four omega values present in captureFrame push array IN THE SAME ORDER
    - `node --input-type=module -e "import('./src/logger.js').then(() => console.log('OK'))"` prints `OK`
    - GLOSSARY.md contains the strings `Longitudinal Slip Ratio`, `wheelOmega`, `Pacejka B`, `Pacejka C`, `Pacejka D`, `Pacejka E`, `Friction Circle`, `Handbrake`, plus all four logger field names
    - Existing GLOSSARY sign-convention text for slip angle is unchanged: `grep -c "Slip Angle" docs/GLOSSARY.md` returns at least 1 (existing definition still present)
    - Logger header comment updated: `grep -E "(37|four).*omega|omega.*Phase 3" src/logger.js` matches at least one comment line (informational — manual review sufficient)
    - Existing 33 FIELDS entries still present and unreordered. Quick check: the first existing field (typically `t` or `time`) line number is LESS than the line of `'fl_omega'`. Verify with `awk "/'fl_omega'/ { fo=NR } /FIELDS\s*=/ { fa=NR } END { exit (fa && fo && fa < fo) ? 0 : 1 }" src/logger.js` exits 0
  </acceptance_criteria>
  <done>Logger appends four omega fields and matching values in fl/fr/rl/rr order at the END of the row; GLOSSARY.md gains 8 new term definitions and 4 new logger field entries; existing logger field order is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | All inputs are local DOM (debug GUI sliders mutating an already-trusted in-memory params object); no new I/O surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | Slider sets Pacejka C to 2.0 -> sin(pi) collapse | mitigate | Slider range is [1.0, 1.99]; the C clamp inside `updatePacejkaCurve` AND inside the tire functions (Plan 01) provides defense in depth — the plot stays smooth even at the slider extremum |
| T-03-08 | Repudiation | Reordering FIELDS would silently break downstream log consumers | mitigate | Append-only enforced by acceptance-criterion index comparison; documented as the public log contract per constraint #8 |
| T-03-09 | Denial of Service | Pacejka canvas redrawing each frame even when hidden | mitigate | `updatePacejkaCurve` early-returns when `plotCanvas.style.display === 'none'` |
</threat_model>

<verification>
- All `<verify>` blocks pass
- Open `index.html` via local HTTP server:
  - HUD shows SPEED, SLIP, THR, BRK values updating in real time (M3-07, M3-08)
  - Press backtick -> debug panel AND Pacejka canvas BOTH appear; press again -> BOTH disappear (constraint #9)
  - Drive into a corner -> slip-angle text color transitions green -> orange -> red as cornering intensifies (D-14 thresholds)
  - Open the panel and observe the Pacejka curve with two operating-point dots (FL = index 0, FR = index 1) that move as the car slides (M3-09)
  - Adjust `pacejkaD` slider -> curve peak height changes; adjust `pacejkaB` -> initial slope changes; drift feel changes accordingly (M3-10)
  - Drag `pacejkaC` slider toward its 1.99 maximum -> no curve collapse (clamp is defending)
  - Record a frame log (existing backslash toggle from Phase 2) -> downloaded JSON contains 37 fields including `fl_omega`, `fr_omega`, `rl_omega`, `rr_omega` (constraint #8)
</verification>

<success_criteria>
- M3-07: front slip-angle HUD displays degrees and transitions green/orange/red at 5/10 degree thresholds (D-14)
- M3-08: throttle and brake percentages update each render frame
- M3-09: Pacejka curve plot visible, normalized, with FL+FR operating-point dots colored by |fNorm| thresholds; toggles with backtick alongside the debug panel
- M3-10: tunable via debug menu — all 8 Pacejka sliders + maxHandbrakeTorque mutate behavior live; corneringStiffness and lateralDampingCoeff sliders removed
- Logger contract: 33 existing fields untouched; 4 omega fields appended in fl/fr/rl/rr order at the end; values pushed in matching positions
- GLOSSARY.md: kappa, wheelOmega, Pacejka B/C/D/E, friction circle, handbrake defined; 4 omega logger fields documented
</success_criteria>

<output>
After completion, create `.planning/phases/03-tire-model/03-03-SUMMARY.md` following the standard summary template. End-of-phase housekeeping (D-15, D-16) is COMPLETE with this plan — Phase 3 is ready for `/gsd-verify-work`.
</output>
