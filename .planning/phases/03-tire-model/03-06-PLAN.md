---
phase: 03-tire-model
plan: 06
type: execute
wave: 4
depends_on: []
files_modified:
  - .planning/phases/03-tire-model/03-VERIFICATION.md
autonomous: false
gap_closure: true
requirements: [M3-06]
must_haves:
  truths:
    - "M3-06 requirement deviation (brake-torque vs Pacejka-D) is formally accepted via an overrides: entry"
    - "VERIFICATION.md frontmatter overrides_applied count reflects the new override"
  artifacts:
    - path: ".planning/phases/03-tire-model/03-VERIFICATION.md"
      provides: "Explicit override entry accepting CONTEXT.md D-09 brake-torque approach as satisfying M3-06"
      contains: "overrides:"
  key_links:
    - from: "VERIFICATION.md overrides"
      to: "CONTEXT.md D-09"
      via: "override reason text references D-09 design decision"
      pattern: "D-09"
---

<objective>
Resolve VERIFICATION.md gap 2 (M3-06 requirement deviation) per the gap_closure instructions: the implementation uses brake torque on rear wheels (CONTEXT.md D-09 design decision) instead of literal Pacejka D reduction. CONTEXT.md D-09 is a locked decision — do NOT change the implementation. Instead, add a formal `overrides:` entry to VERIFICATION.md accepting the deviation, as specified at the bottom of 03-VERIFICATION.md (lines 222-231).

Purpose: Document developer acceptance so re-verification reports M3-06 as override-accepted rather than failed.
Output: 03-VERIFICATION.md frontmatter contains an `overrides:` block with one accepted entry; `overrides_applied` count updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-tire-model/03-VERIFICATION.md
@.planning/phases/03-tire-model/03-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm M3-06 override acceptance</name>
  <decision>Accept the brake-torque approach as satisfying M3-06's intent (drift initiation) without modifying CONTEXT.md D-09?</decision>
  <context>
    REQUIREMENTS.md M3-06 literal text says "reduces rear wheel Pacejka D for drift initiation". CONTEXT.md D-09 locked a different mechanism: rear-axle brake torque via `maxHandbrakeTorque`, which builds negative slip ratio and lets the Pacejka model saturate naturally — achieving drift without discontinuous D jumps. The functional outcome (drivable drift) is met. The gap_closure instructions explicitly direct this resolution path: add an override, do NOT change the code.
  </context>
  <options>
    <option id="accept">
      <name>Accept deviation (recommended, matches gap_closure instructions)</name>
      <pros>Honors locked CONTEXT.md D-09 decision; brake-torque approach is mechanistically smoother; no code churn</pros>
      <cons>Requirement literal text and implementation mechanism do not match — must be documented</cons>
    </option>
    <option id="reimplement">
      <name>Implement Pacejka D reduction literally</name>
      <pros>Matches requirement text verbatim</pros>
      <cons>Overrides locked CONTEXT.md D-09 decision; introduces discontinuous D jumps; abandons sounder physics</cons>
    </option>
  </options>
  <resume-signal>Type `accept` to proceed with the override entry (default per gap_closure instructions), or `reimplement` to switch to code change</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Append overrides block to 03-VERIFICATION.md frontmatter</name>
  <files>.planning/phases/03-tire-model/03-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-tire-model/03-VERIFICATION.md (entire frontmatter and gap 2 section lines 211-231)
    - .planning/phases/03-tire-model/03-CONTEXT.md (find D-09 to quote the decision text)
  </read_first>
  <action>
    Edit the YAML frontmatter of `.planning/phases/03-tire-model/03-VERIFICATION.md`:

    1. Update the `overrides_applied: 0` field to `overrides_applied: 1`.

    2. Add a new top-level frontmatter key `overrides:` (sibling of `gaps:`, before the closing `---`). The single list entry must contain:
       - `must_have: "M3-06: Handbrake (Space) reduces rear wheel Pacejka D for drift initiation"`
       - `reason: "CONTEXT.md D-09 uses brake torque to build negative slip ratio naturally — achieves drift via Pacejka saturation without D modification. Mechanistically superior: avoids discontinuous D jumps; drift develops and recovers smoothly."`
       - `accepted_by: "ledogen"`
       - `accepted_at: "2026-05-30T00:00:00Z"`

    3. Update the gap 2 entry (`truth: "M3-06: Handbrake (Space) reduces rear wheel Pacejka D..."`) inside the `gaps:` list: change its `status: failed` to `status: overridden` and append a single new line at the end of that gap entry: `resolution: "See overrides[0]"`.

    Do NOT touch the body markdown of the verification report, the gap 1 plotCanvas entry, the Requirements Coverage table, or any other section. Only the frontmatter is modified.
  </action>
  <verify>
    <automated>node -e "
const fs = await import('fs');
const yaml = (await import('js-yaml')).default;
const raw = fs.readFileSync('.planning/phases/03-tire-model/03-VERIFICATION.md','utf8');
const fm = raw.split('---')[1];
const data = yaml.load(fm);
if (data.overrides_applied !== 1) { console.error('FAIL overrides_applied', data.overrides_applied); process.exit(1); }
if (!Array.isArray(data.overrides) || data.overrides.length !== 1) { console.error('FAIL overrides shape'); process.exit(1); }
const o = data.overrides[0];
if (!o.must_have.includes('M3-06')) { console.error('FAIL must_have'); process.exit(1); }
if (!o.reason.includes('D-09')) { console.error('FAIL reason'); process.exit(1); }
if (o.accepted_by !== 'ledogen') { console.error('FAIL accepted_by'); process.exit(1); }
const m306 = data.gaps.find(g => g.truth.includes('M3-06'));
if (!m306 || m306.status !== 'overridden') { console.error('FAIL gap status'); process.exit(1); }
console.log('OK: override entry valid');
" 2>/dev/null || node -e "
// Fallback parse without js-yaml — string-level checks
const fs = await import('fs');
const raw = fs.readFileSync('.planning/phases/03-tire-model/03-VERIFICATION.md','utf8');
const checks = [
  /overrides_applied: 1/,
  /^overrides:/m,
  /must_have: \"M3-06/,
  /accepted_by: \"ledogen\"/,
  /D-09/,
  /status: overridden/,
];
for (const re of checks) {
  if (!re.test(raw)) { console.error('FAIL', re); process.exit(1); }
}
console.log('OK: override entry valid (string checks)');
"</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md frontmatter contains `overrides_applied: 1`
    - VERIFICATION.md frontmatter contains an `overrides:` list with exactly one entry
    - That entry has fields `must_have`, `reason`, `accepted_by: "ledogen"`, `accepted_at: "2026-05-30T00:00:00Z"`
    - The `reason` value references `D-09`
    - The M3-06 gap entry in `gaps:` now has `status: overridden` (not `failed`)
    - The plotCanvas gap entry is unchanged
    - The verification report body markdown is unchanged
  </acceptance_criteria>
  <done>
    Re-verifier reads the frontmatter and treats M3-06 as override-accepted rather than gap. Gap 2 is formally closed without code churn.
  </done>
</task>

</tasks>

<verification>
- The frontmatter validator above passes
- Gap 1 (plotCanvas) entry is untouched — only gap 2 status flips to `overridden`
- No body content of VERIFICATION.md is altered
</verification>

<success_criteria>
- VERIFICATION.md formally documents the M3-06 deviation as accepted
- Re-verification of phase 03 reports score `9/10 must-haves verified + 1 override` (assuming plan 04 closes gap 1)
</success_criteria>

<output>
After completion, create `.planning/phases/03-tire-model/03-06-SUMMARY.md`.
</output>
