---
phase: 02-scenario-system-debug-menu
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/debug.js
  - src/logger.js
  - src/main.js
  - docs/GLOSSARY.md
autonomous: true
requirements:
  - M2-01
  - M2-02
  - M2-03
  - M2-04
  - M2-05
  - M2-06

must_haves:
  truths:
    - "Every physics parameter that changed in Phase 2 (wheelDebug fields, corneringStiffness, frictionCoeff, bodyContact*, mass, maxDriveTorque, maxBrakeTorque) has a slider in the debug menu or is documented as intentionally fixed (D-09)"
    - "GLOSSARY.md contains entries for all log field names used in the frame logger (fn, fy, sa, c, thr, brk, steer)"
    - "The HUD speed readout and all Phase 1 HUD elements still function correctly after Phase 2 changes"
    - "Opening the debug menu shows no duplicate sliders and no orphaned sliders for removed fields"
  artifacts:
    - path: "docs/GLOSSARY.md"
      provides: "Log field name definitions (fn, fy, sa, c per wheel — aligned with D-07 field list)"
      contains: "fn"
    - path: "src/debug.js"
      provides: "Audited slider set — all D-08 fields present, no D-09/D-10 fields present as sliders"
  key_links:
    - from: "docs/GLOSSARY.md log field definitions"
      to: "src/logger.js FIELDS constant"
      via: "field names must match glossary terms"
      pattern: "fl_fn|fl_fy|fl_sa"
---

<objective>
End-of-phase housekeeping (D-13): audit all debug sliders, HUD displays, and log fields introduced in Phase 2. Ensure complete coverage with no gaps or orphans, and update GLOSSARY.md with Phase 2 log field definitions so future agents and users understand what each field means.

Purpose: Each phase ends with a clean audit so Phase 3 starts from a known, documented baseline.

Output: GLOSSARY.md (log field entries added), src/debug.js (any gaps or orphans fixed), src/main.js (HUD verified), src/logger.js (field names verified against glossary)
</objective>

<execution_context>
@/Users/ledogen/CodeShit/CarGame/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ledogen/CodeShit/CarGame/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ledogen/CodeShit/CarGame/.planning/PROJECT.md
@/Users/ledogen/CodeShit/CarGame/.planning/ROADMAP.md
@/Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-CONTEXT.md
@/Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-02-SUMMARY.md
@/Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Key signatures the executor needs. -->

docs/GLOSSARY.md — existing glossary file. Read before editing. Add a new section
"## Frame Logger Fields" (or append to an appropriate existing section) with entries for:
  t — simulation time in seconds since recording started
  px, py, pz — vehicle CG position in world space [m]
  vx, vy, vz — vehicle linear velocity [m/s]
  qx, qy, qz, qw — vehicle body quaternion (orientation)
  wx, wy, wz — vehicle angular velocity [rad/s]
  steer — front wheel steer angle [rad] (positive = left per sign convention)
  thr — throttle input [0..1]
  brk — brake input [0..1]
  {fl/fr/rl/rr}_fn — normal force at wheel contact [N]
  {fl/fr/rl/rr}_fy — lateral tire force [N]
  {fl/fr/rl/rr}_sa — slip angle [rad] (atan2 of lateral/longitudinal velocity)
  {fl/fr/rl/rr}_c — contact compression depth [m] (0 when airborne)

D-08 expected slider fields in src/debug.js after Plan 02:
  mass, frictionCoeff, maxDriveTorque, maxBrakeTorque, bodyContactStiffness, bodyContactDamping,
  corneringStiffness, lateralDampingCoeff (kept with '(unused)' label), tireStiffness, tireDamping
  (rollingResistanceCoeff was removed in Plan 02 per D-09 — it must NOT appear)

D-09/D-10 fields that MUST NOT be sliders:
  rollingResistanceCoeff, steerRate, steerDecayRate, maxSteerAngle, speedSteerRef, wheelbase,
  trackFront, trackRear, cgHeight, wheelRadius, bodyLength, bodyWidth, bodyHeight, weightFront,
  weightRear, inertiaRoll, inertiaPitch, inertiaYaw

src/logger.js FIELDS constant (33 entries) — verify names match glossary additions.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Audit debug sliders, HUD, and log fields; update GLOSSARY.md</name>
  <files>docs/GLOSSARY.md, src/debug.js, src/logger.js, src/main.js</files>
  <read_first>
    /Users/ledogen/CodeShit/CarGame/docs/GLOSSARY.md
    /Users/ledogen/CodeShit/CarGame/src/debug.js
    /Users/ledogen/CodeShit/CarGame/src/logger.js
    /Users/ledogen/CodeShit/CarGame/src/main.js
    /Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-CONTEXT.md
  </read_first>
  <action>
    Read all four files. Then perform the following audit and fixes:

    SLIDER AUDIT — src/debug.js:
    Confirm these D-08 sliders are present (fix if missing):
      mass (500–3000, step 10)
      frictionCoeff (0.1–1.5, step 0.05)
      maxDriveTorque (100–2000, step 50)
      maxBrakeTorque (500–8000, step 100)
      bodyContactStiffness (50000–500000, step 10000)
      bodyContactDamping (1000–30000, step 500)
      corneringStiffness (5000–200000, step 1000) labeled 'Phase 2 placeholder'
    Confirm these D-09/D-10 fields do NOT appear as gui.add calls:
      rollingResistanceCoeff, steerRate, steerDecayRate, maxSteerAngle, speedSteerRef,
      inertiaRoll, inertiaPitch, inertiaYaw, wheelbase, trackFront, trackRear, cgHeight,
      wheelRadius, bodyLength, bodyWidth, bodyHeight, weightFront, weightRear
    Fix any discrepancy in-place.

    LOG FIELD AUDIT — src/logger.js:
    Confirm FIELDS constant has exactly 33 entries in the specified order.
    Confirm captureFrame pushes exactly 33 values per row.
    Fix any mismatch.

    HUD AUDIT — src/main.js:
    Confirm document.getElementById('speedVal') is still assigned in the render loop.
    Confirm no HUD elements were accidentally removed by Plan 01 edits.
    Fix if needed.

    GLOSSARY UPDATE — docs/GLOSSARY.md:
    Add a section titled "## Frame Logger Fields" near the end of the file (before any trailing
    notes or appendix, if present).
    For each field listed in the interfaces section above, add an entry in the format:
      ### {fieldName}
      {definition} — unit, sign convention, source in codebase

    Write the section in the same prose style as the existing glossary. Do not use tables —
    use the heading-per-entry format. If the glossary already has matching entries, update them
    rather than duplicating.

    After all fixes and additions, verify the file changes are consistent:
    - FIELDS constant names in logger.js match the glossary entries
    - No slider covers a D-09/D-10 fixed field (including rollingResistanceCoeff)
  </action>
  <verify>
    <automated>grep -c "Frame Logger Fields" /Users/ledogen/CodeShit/CarGame/docs/GLOSSARY.md && grep -c "fl_fn" /Users/ledogen/CodeShit/CarGame/docs/GLOSSARY.md && grep -v '^[[:space:]]*//' /Users/ledogen/CodeShit/CarGame/src/debug.js | grep -c "gui.add" && grep -c "getElementById('speedVal')" /Users/ledogen/CodeShit/CarGame/src/main.js</automated>
  </verify>
  <acceptance_criteria>
    - docs/GLOSSARY.md contains a "Frame Logger Fields" section with entries for all 33 log fields
    - docs/GLOSSARY.md entries for {fl/fr/rl/rr}_fn, _fy, _sa, _c include units and sign conventions
    - src/debug.js has all 7 D-08 sliders (mass, frictionCoeff, maxDriveTorque, maxBrakeTorque, bodyContactStiffness, bodyContactDamping, corneringStiffness) confirmed present
    - src/debug.js has no gui.add for any D-09/D-10 field (inertia fields, geometry fields, steer params, rollingResistanceCoeff)
    - src/logger.js FIELDS has exactly 33 entries
    - src/main.js still assigns document.getElementById('speedVal').textContent in the render loop (grep -c "getElementById('speedVal')" returns 1)
    - No console errors when opening the browser
  </acceptance_criteria>
  <done>Phase 2 housekeeping complete: GLOSSARY.md updated with all log field definitions, debug slider set matches D-08 exactly (10 sliders, rollingResistanceCoeff absent), HUD speed readout verified intact, logger FIELDS count confirmed 33.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → codebase | GLOSSARY.md is reference only; no code reads it at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | GLOSSARY.md documents internal field names | accept | Glossary is a dev tool; no security boundary; field names are not secrets |
</threat_model>

<verification>
Manual check:
1. Open docs/GLOSSARY.md in a text editor — confirm "Frame Logger Fields" section exists with entries for t, px, fl_fn, fl_sa etc.
2. Open browser, press backtick — confirm all 7 D-08 sliders appear; no steerRate, no inertia sliders, no Rolling Resistance slider
3. Confirm speed HUD still shows km/h in the top-left corner while driving
4. Open browser console — confirm zero errors on page load
</verification>

<success_criteria>
- GLOSSARY.md has "Frame Logger Fields" section covering all 33 log field names
- Debug panel has exactly the sliders specified by D-08 (no extras from D-09/D-10, rollingResistanceCoeff absent)
- HUD speed readout intact
- Zero console errors on page load
- Phase 2 audit complete — all M2-01 through M2-06 requirements deliverable
</success_criteria>

<output>
After completion, create /Users/ledogen/CodeShit/CarGame/.planning/phases/02-scenario-system-debug-menu/02-03-SUMMARY.md
</output>
